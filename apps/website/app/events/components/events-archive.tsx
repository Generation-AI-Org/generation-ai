// Phase 22.6 Plan 03 — EventsArchive stub.
// Full implementation deferred to Plan 05 (archive section with past events).
// Returns null so the import in events-client.tsx resolves without build errors.

import type { EventEntry } from "@/lib/mdx/events";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function EventsArchive({ events: _events }: { events: EventEntry[] }) {
  return null;
}
