'use client'

// Stub for Plan 04 Task 2 — filled with Infinite Moving Cards + 'Beispiel' badges.
// Do NOT remove the <section> element's data-section attribute.
export function ToolShowcaseSection() {
  return (
    <section
      aria-labelledby="tool-showcase-stub-heading"
      data-section="tool-showcase"
      className="min-h-[60vh] flex items-center justify-center bg-bg border-b border-border"
    >
      <h2 id="tool-showcase-stub-heading" className="font-mono text-text-muted">
        [Tool-Showcase — Plan 04 Task 2]
      </h2>
    </section>
  )
}
