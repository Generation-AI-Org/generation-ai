'use client'

// Stub for Plan 05 Task 3 — filled with Aceternity Lamp Effect + claim + Jetzt-beitreten CTA.
// Do NOT remove the <section> element's data-section attribute.
export function FinalCTASection() {
  return (
    <section
      aria-labelledby="final-cta-stub-heading"
      data-section="final-cta"
      className="min-h-[60vh] flex items-center justify-center bg-bg"
    >
      <h2 id="final-cta-stub-heading" className="font-mono text-text-muted">
        [Final-CTA — Plan 05 Task 3]
      </h2>
    </section>
  )
}
