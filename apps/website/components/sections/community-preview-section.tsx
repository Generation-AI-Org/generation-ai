'use client'

// Stub for Plan 04 Task 3 — filled with two-column preview (Artikel + Events) + 'Beispiel' badges.
// Do NOT remove the <section> element's data-section attribute.
export function CommunityPreviewSection() {
  return (
    <section
      aria-labelledby="community-preview-stub-heading"
      data-section="community-preview"
      className="min-h-[60vh] flex items-center justify-center bg-bg border-b border-border"
    >
      <h2 id="community-preview-stub-heading" className="font-mono text-text-muted">
        [Community-Preview — Plan 04 Task 3]
      </h2>
    </section>
  )
}
