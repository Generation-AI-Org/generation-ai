'use client'

// Stub for Plan 03 Task 2 — filled with Bento-Split + useScroll divergence + Number-Tickers.
// Do NOT remove the <section> element's data-section attribute.
export function DiscrepancySection() {
  return (
    <section
      aria-labelledby="discrepancy-stub-heading"
      data-section="discrepancy"
      className="min-h-[60vh] flex items-center justify-center bg-bg border-b border-border"
    >
      <h2 id="discrepancy-stub-heading" className="font-mono text-text-muted">
        [Discrepancy — Plan 03 Task 2]
      </h2>
    </section>
  )
}
