'use client'

// Stub for Plan 05 Task 1 — filled with Studi block (primary) + B2B strip (dezent).
// Do NOT remove the <section> element's data-section attribute.
export function AudienceSplitSection() {
  return (
    <section
      aria-labelledby="audience-split-stub-heading"
      data-section="audience-split"
      className="min-h-[60vh] flex items-center justify-center bg-bg border-b border-border"
    >
      <h2 id="audience-split-stub-heading" className="font-mono text-text-muted">
        [Audience-Split — Plan 05 Task 1]
      </h2>
    </section>
  )
}
