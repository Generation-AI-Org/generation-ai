---
'@genai/tools-app': patch
---

fix(mobile): Chat im expanded-Modus nimmt jetzt den vollen Screen ein statt unter Header + Filter-Bar eingeengt zu sein. Desktop bleibt 35%-Sidebar. Minimize- und X-Button funktionieren wie gehabt.
