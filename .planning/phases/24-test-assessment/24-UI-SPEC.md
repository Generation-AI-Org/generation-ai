---
phase: 24
slug: test-assessment
status: approved
shadcn_initialized: true
preset: base-nova
created: 2026-04-24
reviewed_at: 2026-04-24
---

# Phase 24 — UI Design Contract: /test AI Literacy Assessment

> Visual and interaction contract for the /test KI-Literacy-Assessment. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn | components.json |
| Preset | base-nova | components.json |
| Style | base-nova, cssVariables: true | components.json |
| Component library | Radix UI (via shadcn) | components.json |
| Icon library | lucide-react | components.json |
| Font sans | Geist Sans (`var(--font-geist-sans)`) | base.css |
| Font mono | Geist Mono (`var(--font-geist-mono)`) | base.css |
| Dark mode | Default dark (`:root`), light via `.light` on `<html>` | base.css |

### Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | button, dropdown-menu, sheet, progress, slider, badge, card, separator | not required — official registry |
| @aceternity | labeled-nodes (already installed — `components/ui/labeled-nodes.tsx`) | already in codebase, no new fetch required |

No new third-party registry blocks are introduced in Phase 24. All required components are either already installed or from the shadcn official registry.

---

## Spacing Scale

Source: `base.css` `--space-*` tokens. 4-base grid.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px (`--space-1`) | Icon gaps, inline badge padding |
| sm | 8px (`--space-2`) | Compact element spacing, chip gaps |
| md | 16px (`--space-4`) | Default element spacing, card padding inner |
| lg | 24px (`--space-6`) | Card padding, section inner padding |
| xl | 32px (`--space-8`) | Layout gaps between widget zones |
| 2xl | 48px (`--space-12`) | Major section breaks within a page |
| 3xl | 64px (`--space-16`) | Page-level vertical rhythm |

**Exceptions for Phase 24:**
- Touch targets (all interactive widget elements): `min-w-[48px] min-h-[48px]` — enforced on all card-pick cards, drag handles, slider thumb, matching targets, spot-click zones. 48px exceeds WCAG 2.5.5 minimum of 44px.
- Progress-bar height: 4px (visual accent bar, not spacing)
- Widget full-screen padding on mobile: `px-4 py-6` (16px / 24px) — intentionally tighter to maximize content area on small screens

---

## Typography

Source: `base.css` `--fs-*` + `--lh-*` tokens, Phase 16 UI-SPEC.

| Role | Size | Weight | Line Height | Font | Letter Spacing | Usage |
|------|------|--------|-------------|------|----------------|-------|
| Display | `var(--fs-display)` = `clamp(40px, 6.5vw, 76px)` | 700 | 1.02 | Geist Mono | -0.03em | /test Hero H1, Results Level-Name |
| Heading | `var(--fs-h2)` = `clamp(24px, 3vw, 32px)` | 700 | 1.2 | Geist Sans | -0.015em | All section headings, Aufgaben-Text H2, Empfehlungs-Card titles, Results section headings |
| Body | `var(--fs-body)` = 16px | 400 | 1.65 | Geist Sans | 0 | Widget body text, answer options, level description, all prose |
| Small | `var(--fs-small)` = 14px | 400 | 1.55 | Geist Sans | 0 | Progress label "Aufgabe 3/10", badge text, meta, buttons (weight 700 + Geist Mono for buttons), code blocks (Geist Mono), micro/tag labels (weight 700 + Geist Mono + tracking +0.08em) |

**Rules:**
- H1 on /test Hero: Geist Mono (matches all subpage hero pattern from AGENTS.md)
- Aufgaben body text (question stem): Geist Sans 16px weight 400, max 2 sentences
- Buttons: Geist Mono 14px weight 700 tracking +0.02em (site-wide rule from base.css `button { font-family: var(--font-mono) }`)
- Code/prompt blocks: Geist Mono 14px with shiki syntax highlighting
- Widget type badges and level tier labels: Geist Mono 14px weight 700 tracking +0.08em
- No italics in body/heading (base.css rule: italic only in blockquotes)
- No ligatures: `font-feature-settings: "liga" 0, "calt" 0` (inherited from body)

---

## Color

Source: `base.css` — dark default, light via `.light`.

### Base Palette (pre-populated from DS)

| Role | Dark Mode Value | Light Mode Value | Usage |
|------|----------------|-----------------|-------|
| Dominant (60%) | `#141414` (`--bg`) | `#F6F6F6` (`--bg`) | Page background, full-screen widget area |
| Secondary (30%) | `#1C1C1C` (`--bg-card`) | `#FFFFFF` (`--bg-card`) | Widget containers, answer cards, result cards |
| Elevated | `#222222` (`--bg-elevated`) | `#EFEFEF` (`--bg-elevated`) | Active/selected card state, drag-drop drag zone |
| Accent (10%) | `#CEFF32` (`--accent`) | `#F5133B` (`--accent`) | See reserved list below |
| Destructive | `#DC2626` (`--status-error`) | `#DC2626` (`--status-error`) | Not applicable in Phase 24 (no destructive actions) |

### Accent Reserved For (Phase 24 specific)

The accent (`--accent`) is reserved ONLY for:
1. Primary CTA button ("Test starten", "Nächste Aufgabe", "Jetzt beitreten")
2. Progress-bar fill (active portion)
3. Selected/confirmed answer card border (`border-[var(--border-accent)]`)
4. Level badge background for the user's earned level
5. Focus ring on keyboard-navigable widget elements (`outline: 2px solid var(--accent)` on active drag handle, selected card)

Accent is NOT used for: hover states on unselected cards, informational badges, secondary navigation, the sparring-slot container.

### Level Color System (5 Levels — Radix Brand Palette)

Each level has a distinct color assignment using existing brand scales. These are used exclusively for the Level-Badge, the Skill-Radar stroke/fill for that user's level, and the level profile card accent border.

| Level | Name | Color Token | Dark Hex | Semantic Justification |
|-------|------|-------------|----------|------------------------|
| 1 | Neugieriger | `--slate-9` | `#696e77` | Neutral — curiosity, not yet committed |
| 2 | Einsteiger | `--blue-7` / `--brand-blue` | `#5454FC` | Blue — learning mode, calm |
| 3 | Fortgeschritten | `--neon-9` / `--accent` (dark) | `#CEFF32` | Neon — energy, momentum, on-brand |
| 4 | Pro | `--pink-9` / `--brand-pink` | `#FC78FE` | Pink — distinguishable, premium feel |
| 5 | Expert | `--brand-red` | `#F5133B` | Red — intensity, mastery |

In light mode: Level 3 uses `--brand-blue` (`#3A3AFF`) as neon is the light-mode accent already. All others identical. The executor must ensure Level 3 in dark uses `--brand-neon` and in light uses `--brand-blue` to avoid accent collision with the light-mode accent (`--accent = --brand-red`).

### Widget State Colors

| State | Color |
|-------|-------|
| Unselected card border | `var(--border)` (rgba(255,255,255,0.08) dark) |
| Hover card border | `var(--slate-7)` |
| Selected card border | `var(--border-accent)` (accent at 60% opacity) |
| Selected card background | `var(--bg-elevated)` |
| Drag-pick-up card | `var(--bg-elevated)` bg + `box-shadow: var(--shadow-glow)` |
| Drop-zone active | `var(--accent-soft)` bg (rgba(206,255,50,0.15) dark) + `var(--border-accent)` border |
| Disabled / not-yet-interacted button | `var(--bg-elevated)` + `var(--text-muted)` text |
| Progress bar track | `var(--border)` |
| Progress bar fill | `var(--accent)` |
| Checkpoint celebration | `var(--accent)` glow pulse |

---

## Layout Contracts

### Route Structure

| Route | Layout Mode | Notes |
|-------|------------|-------|
| `/test` | Hero + CTA section | Matches subpage hero pattern (AGENTS.md blueprint) |
| `/test/aufgabe/[n]` | Full-screen single widget | No nav clutter, progress bar only header element |
| `/test/ergebnis` | Scrollable results page | Level badge → Radar → Cards → Sparring → CTAs |

### /test Hero Layout

**Primary visual anchor: H1 headline — all other elements recede to support it.**

- Inherits subpage hero pattern from AGENTS.md: `<LabeledNodes>` background, `relative isolate` wrapper, `min-h-[calc(100vh-5rem)]` inner height
- Container: `relative z-10 mx-auto max-w-4xl px-6 py-20 text-center`
- H1: `fontSize: var(--fs-display)` + `leading-[1.02]` + `tracking-[-0.03em]` + Geist Mono
- Text shadows on H1: `0 0 18px rgba(var(--bg-rgb), 1), 0 0 8px rgba(var(--bg-rgb), 1)`
- Subline: `var(--fs-h2)` + `var(--lh-headline)` + `max-w-3xl` (centered)
- Intro-Lede (trust/meta line): `text-lg sm:text-xl leading-[1.5] max-w-2xl`
- Badge row ("Kostenlos · Keine Anmeldung · Anonym · Max 15 Min"): flex-wrap gap-2, pill badges, `var(--fs-small)` Geist Mono, `var(--bg-elevated)` bg, `var(--text-muted)` text
- Start button: Primary CTA style (`bg-[var(--accent)] text-[var(--text-on-accent)] rounded-full`), centered, `px-8 py-3`, full-width on mobile max-w-xs
- Trust line below CTA: 14px Small, `var(--text-muted)`, italic-free

### /test/aufgabe/[n] Full-Screen Layout

- Header strip: `h-14` (56px), `bg-[var(--bg)]` with `border-b border-[var(--border)]`
  - Left: Generation AI Logo (small, links to `/test` with back-navigation warning pattern)
  - Center: animated Progress-Bar, `w-full max-w-xs`, `h-1` (4px), accent fill
  - Right: "Aufgabe N/10" label, 14px Geist Sans, `var(--text-muted)`
- Main content area: `min-h-[calc(100vh-56px)]`, flex-col, justify-between
  - Top zone: Aufgaben-Text (question stem), `max-w-2xl mx-auto px-4 pt-8 pb-4`
  - Widget zone: `flex-1 w-full max-w-3xl mx-auto px-4 pb-4` — widget fills available height
  - Bottom zone: Nächste-Aufgabe-Button row, `h-20 flex items-center justify-end px-4 sm:px-8 border-t border-[var(--border)]`
- No back button (D-06 — enforced by router config, no visible back UI)
- Transition between aufgaben: Framer Motion `AnimatePresence` with `mode="wait"` — slide-from-right on enter, fade-out on exit
- Reduced-motion: `useReducedMotion()` gate — cross-fade only (`opacity` transition 200ms, no translate)

### /test/ergebnis Layout

- Full scrollable page, same Client-Wrapper pattern (`MotionConfig` + `Header` + `Footer`)
- `max-w-2xl mx-auto px-4` for reading content, `max-w-4xl mx-auto px-4` for wider elements (Radar, Cards grid)
- Section order (vertical stack):
  1. Level Badge Hero (full-width, centered, `py-16`)
  2. Level Description Text (`max-w-2xl`, `py-8`)
  3. Skill Radar Chart (`max-w-lg mx-auto`, `py-8`)
  4. Empfehlungs-Cards grid (`max-w-4xl`, 2-col desktop / 1-col mobile, `py-8`)
  5. Sparring-Slot Placeholder (`max-w-2xl`, `py-8`)
  6. CTA cluster (Primary + Secondary, `py-12`)
- `<SectionTransition variant="soft-fade" />` between Radar and Empfehlungs-Cards, and between Sparring-Slot and CTA cluster

---

## Widget Specifications

### W1: Szenario-Pick (Card-Grid, Single-Select)

**Layout:** 2×2 grid on desktop, 1-col stack on mobile. Each card: `bg-[var(--bg-card)] rounded-2xl border border-[var(--border)] p-4`, min-height 80px.

**States:**
- Default: `border-[var(--border)]`
- Hover: `border-[var(--slate-7)] bg-[var(--bg-elevated)]` + scale(1.01), transition 150ms
- Selected: `border-[var(--border-accent)] bg-[var(--bg-elevated)]` + checkmark icon (lucide `Check`, 16px, accent color) in top-right corner
- Confirmed (after Nächste Aufgabe): visual lock, no re-select

**Interaction:**
- Click/tap: single-select (clicking another deselects previous)
- Keyboard: Tab to navigate cards, Space/Enter to select
- Touch: full card tap target

**A11y:**
- `role="radiogroup"` on container, `role="radio"` on each card
- `aria-checked` on selected card
- `aria-label="Option: [card text truncated]"` on each card
- Screen reader announces: "Option 1 of 4 selected" on selection

**Nächste-Aufgabe-Button enable condition:** at least 1 card selected

**Mobile:** 1-col stack, cards full-width, min-height 72px

### W2: Prompt-Ranking (Drag-Drop, Levenshtein Scoring)

**Layout:** Vertical stack of 4 draggable cards, full-width container `max-w-xl mx-auto`

**Card design:** `bg-[var(--bg-card)] rounded-xl border border-[var(--border)] p-3` with drag handle icon (lucide `GripVertical`, 20px, `var(--text-muted)`) on left. Number badge (1-4, current position) on right, 24px circle, `var(--bg-elevated)`.

**States:**
- Default: as described
- Dragging: `bg-[var(--bg-elevated)]` + `box-shadow: var(--shadow-glow)` + scale(1.03) + rotate(1deg), z-index 50
- Drop-zone active: `bg-[var(--accent-soft)] border-[var(--border-accent)]` dashed border 2px
- Placed: snap animation (spring: stiffness 400, damping 30)

**Interaction:**
- Drag via `@dnd-kit/core` (A11y-first per CONTEXT.md decision note)
- Tap-alternative on mobile: tap card to select (highlighted), then tap target position to move. Two-step tap flow.
- Keyboard: Tab to focus drag handle, Space to "pick up", Arrow Up/Down to move, Space to drop, Escape to cancel

**A11y:**
- `role="listbox"` on container
- Each card: `role="option"` + `aria-grabbed` (true when dragging)
- `aria-live="polite"` region announcing: "Prompt [N] moved to position [M]"
- Keyboard instructions shown as collapsed `<details>` summary below widget on first render

**Nächste-Aufgabe-Button enable condition:** all 4 cards have been interacted with (any reorder, or explicit "I'm done" — widget shows "Reihenfolge bestätigen" button that enables Nächste Aufgabe)

**Mobile fallback:** Tap-to-select + tap-to-place (no drag needed). Show "Tippe zum Auswählen, dann Zielposition" helper text, 14px, `var(--text-muted)`.

### W3: Prompt-Best-Pick (Card Select with Syntax Highlighting)

**Layout:** 2×2 grid on desktop, 1-col scroll on mobile. Each card contains a code/prompt block.

**Card design:** Same as W1 but with code block inside — `bg-[var(--bg-card)] rounded-2xl border border-[var(--border)] p-4`. Code block: `bg-[var(--slate-2)] rounded-lg p-3 font-mono text-sm leading-relaxed overflow-x-auto max-h-40`. Shiki syntax highlighting theme: `github-dark` in dark mode, `github-light` in light mode.

**States:** Identical to W1 (hover border shift, selected border-accent + checkmark). No hover-expand in this widget (code blocks need stable height for readability).

**Interaction + A11y:** Identical to W1 (radiogroup pattern).

**Nächste-Aufgabe-Button enable condition:** 1 card selected.

### W4: Output-Side-by-Side + Grund-Multi-Select

**Layout:** Two-phase widget.
- Phase A: Side-by-side cards, `grid grid-cols-2 gap-4` on desktop, stacked on mobile. Each card: `bg-[var(--bg-card)] rounded-2xl border border-[var(--border)] p-4`. Label "Output A" / "Output B" in micro badge (14px Geist Mono weight 700 tracking +0.08em). Content scrollable, `max-h-48 overflow-y-auto`.
- Phase B (revealed after A/B pick): Multi-select row of reason chips. Slides in with Framer Motion (`height: 0 → auto`, spring). 4 chips: "Halluzination", "Format", "Verbosität", "Bias". Chip style: `rounded-full px-3 py-1.5 text-sm border border-[var(--border)]`. Selected: `bg-[var(--accent-soft)] border-[var(--border-accent)] text-[var(--text)]`.

**States for A/B cards:**
- Default: neutral border
- Hover: `border-[var(--slate-7)]`
- Selected: `border-[var(--border-accent)] bg-[var(--bg-elevated)]` + "Gewählt" badge replacing "Output A/B" label (same micro style, accent text)

**A11y:**
- Phase A: `role="radiogroup"`, each card `role="radio"`
- Phase B: `role="group" aria-label="Warum ist dieser Output besser?"`, chips as `role="checkbox"`
- `aria-live="polite"` announces "Phase B geöffnet: Wähle Gründe aus" when revealed

**Nächste-Aufgabe-Button enable condition:** A or B selected (Phase A). Phase B chips are optional (omitting = 0 reason points, still valid).

### W5: Output-Fehler-Spot (Click-to-Mark auf Text-Abschnitten)

**Layout:** Single text block, `max-w-xl mx-auto`. Text rendered as 4-6 paragraph spans, each hoverable/clickable.

**Span design:** Each clickable segment: inline `rounded-md px-1` hover highlight. No visible marking in default state.

**States:**
- Default: plain text, no visual distinction between segments
- Hover: `bg-[var(--slate-4)] rounded-md` highlight on hovered segment
- Selected: `bg-[var(--accent-soft)] rounded-md text-[var(--text)]` + underline dotted accent
- Wrong selection (only shown after Nächste Aufgabe): `bg-[var(--red-3)] text-[var(--red-11)]`
- Correct segment (shown after Nächste Aufgabe): `bg-[var(--neon-3)] text-[var(--neon-11)]`

(After-Nächste-Aufgabe feedback: briefly shown 1.5s before transition — enough to confirm, not enough to game next question.)

**Instruction banner:** 16px body text above the passage: "Klicke auf den problematischen Textabschnitt." Persistent until selection made.

**Interaction:**
- Click any span: selects it, deselects previous
- Keyboard: Tab cycles through segments, Space/Enter selects

**A11y:**
- Container: `role="listbox" aria-label="Textabschnitte zum Auswählen"`
- Each span: `role="option" tabIndex={0}` + `aria-selected`
- `aria-live="polite"` announces: "Abschnitt [N] ausgewählt"

**Nächste-Aufgabe-Button enable condition:** 1 span selected.

### W6: Tool-zu-Task-Matching (Drag-Drop or Dropdown Pairing)

**Layout:** Two-column layout. Left: 4 Task cards (fixed). Right: 4 Tool cards to assign. On desktop: `grid grid-cols-2 gap-4`. On mobile: dropdown pairing mode (each task row has a `<select>` dropdown for Tool assignment).

**Desktop drag-drop:**
- Task cards: `bg-[var(--bg-elevated)] rounded-xl border border-[var(--border)] p-3`, non-draggable
- Tool cards: same styling, draggable, with `GripVertical` handle
- Drop zone on each task: dashed border `border-[var(--border-accent)] bg-[var(--accent-soft)]` when dragging over
- Matched: Tool card snaps under its Task card, displays Tool name in success state (`bg-[var(--bg-card)] border-[var(--border-accent)]`)
- Un-match: drag matched tool out, returns to pool

**Mobile dropdown mode (auto-detected via `pointer: coarse`):**
- Each task row: `flex justify-between items-center gap-2`
- Dropdown: `bg-[var(--bg-elevated)] border border-[var(--border)] rounded-xl px-3 py-2 text-sm` (native `<select>`)

**A11y:**
- Desktop: same dnd-kit keyboard pattern as W2 (`aria-grabbed`, `aria-live`)
- Mobile: native `<select>` — full screen-reader support out-of-box
- Progress count: "N von 4 Zuordnungen gemacht" as `aria-live="polite"` update

**Nächste-Aufgabe-Button enable condition:** all 4 tasks have an assigned tool.

### W7: Confidence-Slider (Diskret, 5 Stufen)

**Layout:** Single wide slider, `max-w-sm mx-auto`. 5 discrete steps: 0%, 25%, 50%, 75%, 100%.

**Visual design:** shadcn Slider component. Track: 4px height, `var(--border)` background. Thumb: 24px circle, `var(--accent)` fill. Active fill: `var(--accent)`. Step tick marks: 4px × 8px rectangles at each 25% increment, `var(--slate-6)`.

Labels below each tick: "0%" to "100%", 14px Geist Mono weight 700 tracking +0.08em, `var(--text-muted)`.

Semantic labels above each tick (visible on desktop, tooltip on mobile): "Sicher kein Halluzinat" / "Eher nicht" / "Unsicher" / "Eher ja" / "Sicher ja". 14px Small, `var(--text-muted)`.

**States:**
- Unset: thumb starts at 50% (neutral), track fill to midpoint
- Interacted: thumb position + accent fill to left of thumb
- Hover on thumb: scale(1.15) + `box-shadow: var(--shadow-glow)`

**Interaction:**
- Drag thumb
- Arrow keys Left/Right: move 1 step (25%)
- 1/2/3/4/5 number keys: jump to step 1-5

**A11y:**
- `role="slider"` + `aria-valuemin="0"` + `aria-valuemax="100"` + `aria-valuenow` + `aria-valuetext` (semantic label, e.g. "Eher ja — 75%")
- Live region: "Confidence: Eher ja (75%)" on change

**Nächste-Aufgabe-Button enable condition:** user has moved slider from center (any move counts) OR waited 3 seconds (auto-enable — slider has a default answer, no blocking on this widget).

### W8: Fill-in-Parameter (Inline-Dropdown im Code-Block)

**Layout:** Single code block, max-w-xl, with one or two inline dropdown widgets replacing a `[BLANK]` placeholder.

**Code block:** `bg-[var(--slate-2)] rounded-xl p-4 font-mono text-sm leading-relaxed`. Shiki highlighting for surrounding code.

**Inline dropdown:** Native `<select>` styled to match code context — `bg-[var(--slate-3)] border border-[var(--slate-6)] rounded px-2 py-0.5 text-sm font-mono text-[var(--text)]`. Width: auto, min-width 80px. Focus: `border-[var(--border-accent)]`. Appears inline in the code text flow.

**States:**
- Unset: `bg-[var(--slate-3)]` placeholder text "— wählen —" in `var(--text-muted)`
- Set: shows selected value in `var(--text)`, `border-[var(--slate-7)]`
- Confirmed-correct (post-Nächste Aufgabe): `bg-[var(--neon-3)] border-[var(--neon-7)] text-[var(--neon-11)]`
- Confirmed-wrong (post-Nächste Aufgabe): `bg-[var(--red-3)] border-[var(--red-7)] text-[var(--red-11)]`

**A11y:**
- Native `<select>` — full keyboard + screen-reader support
- `aria-label="Parameter auswählen für [context description]"`
- Result announcement via `aria-live`

**Nächste-Aufgabe-Button enable condition:** all inline dropdowns have a selection (not "— wählen —").

### W9: Fakten-Check-MC (Classic Radio)

**Layout:** Vertical list of 3-4 options. Each option: `flex items-start gap-3 p-3 rounded-xl cursor-pointer`. Custom radio using `role="radio"` card (identical interaction to W1 but single-column).

**Visual:** Same card select pattern as W1. No grid — single column stack for readability of longer option text.

**States:** Identical to W1.

**A11y:** Identical to W1 (radiogroup/radio pattern).

**Nächste-Aufgabe-Button enable condition:** 1 option selected.

---

## Progress Bar Contract

**Component:** `<Progress>` (shadcn), `h-1` (4px), `max-w-xs`, centered in header strip.

**Visual:** track `var(--border)`, fill `var(--accent)`, `rounded-full`.

**Animation:** `transition-all duration-500 ease-out` on fill width change.

**Accessibility:** `role="progressbar"` + `aria-valuenow={n}` + `aria-valuemin="1"` + `aria-valuemax="10"` + `aria-valuetext="Aufgabe 3 von 10"`. `aria-live="polite"` region updates on advance.

---

## Transition Specs

### Aufgabe → Aufgabe Transition

- Framer Motion `AnimatePresence mode="wait"`
- Outgoing: `opacity: 1 → 0, x: 0 → -20px`, duration 200ms, ease-in
- Incoming: `opacity: 0 → 1, x: 30px → 0`, duration 300ms, ease-out
- Reduced-motion: `opacity` only, 150ms cross-fade, no translate

### Hero → /test/aufgabe/1 Transition

- Page navigation (router push) — no cross-route animation
- Brief loading state: progress bar pulse animation (CSS keyframe `opacity: 0.5 → 1`, 600ms loop) until first aufgabe renders

### Aufgabe 5 Checkpoint Micro-Celebration

**Trigger:** After Nächste Aufgabe from Aufgabe 5 confirms (before Aufgabe 6 renders).
**Timing:** Shown for 1.5 seconds in the header strip area, then auto-proceeds.

**Visual:**
- Header strip background transitions from `var(--bg)` to `var(--accent-soft)`, duration 300ms
- Text appears: "Halbzeit! Weiter so." — 14px Geist Sans, `var(--text)`, centered
- Confetti effect: 8 small circles (4-8px) with `var(--accent)` color burst from center, scatter, fade. CSS keyframe animation — not Framer Motion (simpler, reduced-motion gate).
- Reduced-motion: only the text, no confetti, no background color shift

---

## Results Page Contracts

### Level Badge Hero

**Container:** `py-16 text-center`

**Badge anatomy:**
- Large badge pill: `rounded-2xl px-6 py-3 inline-flex items-center gap-3`
- Background: Level color (e.g. `#CEFF32` for Level 3 in dark) at 15% opacity as bg, full color as border at 60% opacity
- Level number: 48px Geist Mono weight 700, level color
- Level name: `var(--fs-h2)` Geist Sans weight 400, `var(--text)`
- Icon: lucide icon per level below:

| Level | Icon |
|-------|------|
| 1 Neugieriger | `Sparkle` |
| 2 Einsteiger | `BookOpen` |
| 3 Fortgeschritten | `Rocket` |
| 4 Pro | `Zap` |
| 5 Expert | `Star` |

- Icon size: 28px, level color

**Below badge:** Level name large — `var(--fs-display)`, Geist Mono, weight 700, level color. Example: "Level 3: Fortgeschritten"

**Entry animation:** Badge scales in from 0.8→1 + fade, duration 600ms, delay 100ms. Level name fades in at delay 300ms. Reduced-motion: fade only.

### Skill Radar Chart

**Library:** `recharts` (D-recharts from CONTEXT.md). `ResponsiveContainer` wrapper, `RadarChart` with `PolarGrid`, `PolarAngleAxis`, `Radar`.

**Dimensions:** 5 axes — "Tools", "Prompting", "Agents", "Anwendung", "Critical Literacy"

**Visual spec:**
- Chart container: `max-w-lg mx-auto` (512px max), aspect-ratio `1/1`
- Background: transparent (inherits page bg)
- Grid lines: `var(--border)` (rgba(255,255,255,0.08)), `strokeWidth: 1`
- Axis labels: 14px Geist Sans, `var(--text-muted)`
- User's radar fill: level color at 20% opacity, stroke level color, `strokeWidth: 2`
- Animated: `recharts` built-in animation, `animationDuration: 800`, `animationEasing: ease-out`. Reduced-motion: `isAnimationActive={false}`

**A11y:** Radar chart wrapped in `<figure>` with `<figcaption>` showing a textual summary of scores. Example: "Deine Stärken: Tools (80%), Prompting (60%). Ausbaufähig: Agents (40%)." Screen readers read the caption.

### Empfehlungs-Cards

**Layout:** `grid grid-cols-1 sm:grid-cols-2 gap-4 max-w-4xl mx-auto`

**Card anatomy:** `bg-[var(--bg-card)] rounded-2xl border border-[var(--border)] p-5 flex flex-col gap-3`

**Card content:**
- Type badge (top): micro pill — "Workshop", "Tool", "Artikel", "Community" — `bg-[var(--bg-elevated)] text-[var(--text-muted)] rounded-full px-2.5 py-1 font-mono text-sm font-bold tracking-[0.08em]`
- Title: `var(--fs-h2)`, Geist Sans, weight 700
- Description: 14px Small, Geist Sans, weight 400, `var(--text-muted)`, max 2 lines
- CTA link: 14px Small Geist Mono, `var(--accent)`, `hover:text-[var(--accent-hover)]`, arrow icon `ArrowRight` 14px

**Hover state:** `border-[var(--slate-7)] bg-[var(--bg-elevated)]`, transition 150ms

### Sparring-Slot Placeholder (V1)

**Props Interface (for later Live-Swap):**
```ts
interface SparringSlotProps {
  level: 1 | 2 | 3 | 4 | 5
  skills: { tools: number; prompting: number; agents: number; application: number; literacy: number }
  mode: 'placeholder' | 'live'  // V1 = 'placeholder', later swap to 'live'
  className?: string
}
```

**Container:**
- `bg-[var(--bg-card)] rounded-2xl border border-[var(--border)] max-w-2xl mx-auto overflow-hidden`

**Header strip:**
- `bg-[var(--bg-elevated)] px-4 py-3 flex items-center gap-2 border-b border-[var(--border)]`
- PRISMA avatar: 28px circle, gradient `from-[var(--brand-blue)] to-[var(--brand-pink)]`
- Name: "PRISMA" — 14px Geist Mono weight 700, `var(--text)`
- Status badge: "Kommt bald" — pill, `var(--bg-elevated)]` bg, `var(--text-muted)` text, 14px Geist Mono weight 700 tracking +0.08em

**Message area:**
- `p-4 flex flex-col gap-3 min-h-[160px] max-h-[240px]`
- Single "bot" message bubble: `bg-[var(--bg-elevated)] rounded-xl rounded-tl-none px-4 py-3 max-w-[85%] self-start`
- Message text: 14px Small Geist Sans, `var(--text)]`, line-height 1.65
- Static opening message copy: "PRISMA ist deine KI-Sparring-Partner:in auf deinem Lernweg — sie kommt bald. Schau dir bis dahin die Empfehlungen an, oder tritt bei und sei dabei, wenn sie live geht."
- Small CTA below bubble (not a button, a text link): "Jetzt beitreten →" — 14px Small Geist Mono, `var(--accent)`

**Input area (disabled in V1):**
- `border-t border-[var(--border)] px-4 py-3 flex items-center gap-2`
- Input field: placeholder "Chat mit PRISMA kommt bald…" — `opacity-50 cursor-not-allowed`
- Send button: disabled state — `opacity-40`
- Tooltip on hover over input: "Verfügbar nach Live-Schaltung von PRISMA"

**No animation** on the sparring slot in V1 (it's a static teaser, not an interactive element).

### CTA Cluster (Results Page)

**Container:** `flex flex-col sm:flex-row gap-4 justify-center items-center py-12`

**Primary CTA:** "Jetzt beitreten & loslegen" → `/join?pre={levelSlug}&source=test&skills={encodedSkills}`
- Style: Primary button — `bg-[var(--accent)] text-[var(--text-on-accent)] rounded-full px-8 py-3 text-sm font-mono font-bold tracking-[0.02em]`
- Hover: `hover:shadow-[0_0_20px_var(--accent-glow)] hover:scale-[1.03]`

**Secondary CTA:** "Test nochmal machen" → `/test` (router.push, resets state)
- Style: `border border-[var(--border)] text-[var(--text-muted)] rounded-full px-6 py-2.5 text-sm font-mono hover:border-[var(--slate-7)] hover:text-[var(--text)]`

**levelSlug mapping:** `1=neugieriger`, `2=einsteiger`, `3=fortgeschritten`, `4=pro`, `5=expert`

---

## Copywriting Contract

| Element | Copy | Notes |
|---------|------|-------|
| Page `<title>` | "AI Literacy Test — Wo stehst du wirklich mit KI?" | EN keyword + DE subtitles per D-10 |
| Meta description | "Take the AI Literacy Test — 10 interactive tasks, 15 minutes, honest results. Free, anonymous, no signup required." | EN for SEO keyword match |
| Hero H1 | "Wo stehst du wirklich mit KI?" | Geist Mono, `--fs-display` |
| Hero Subline | "15 Minuten. 10 Aufgaben. Ehrliches Ergebnis." | `--fs-h2` |
| Hero Lede | "Kein Selbsteinschätzungs-Quiz — wir fragen ab, was du kannst." | 16px body |
| Badge row | "Kostenlos · Keine Anmeldung · Anonym · Max 15 Min" | Pill badges, see layout spec |
| Primary CTA (Hero) | "Test starten" | Geist Mono button |
| Progress label | "Aufgabe [N] von 10" | Live-updated, aria-live |
| Nächste-Aufgabe button (active) | "Nächste Aufgabe" | Enabled when widget interaction complete |
| Nächste-Aufgabe button (disabled) | "Nächste Aufgabe" | Visually disabled, `aria-disabled="true"`, tooltip "Beantworte zuerst diese Aufgabe" on hover |
| No-back tooltip (if user tries browser back) | — | Handled by router — no custom copy needed, native browser behavior |
| Checkpoint (Aufgabe 5) | "Halbzeit! Weiter so." | Micro-celebration header text, 1.5s display |
| Results H1 | "Dein Ergebnis" | Subpage H1, `--fs-display` |
| Level badge aria-label | "Level [N]: [Name]" | e.g. "Level 3: Fortgeschritten" |
| Radar chart figcaption | "Deine Stärken: [top 2 dims]. Ausbaufähig: [bottom dim]." | Programmatically generated from scores |
| Empfehlungs-Cards heading | "Das empfehlen wir für Level [N]" | H2, `--fs-h2` |
| Sparring-Slot opening message | "PRISMA ist deine KI-Sparring-Partner:in auf deinem Lernweg — sie kommt bald. Schau dir bis dahin die Empfehlungen an, oder tritt bei und sei dabei, wenn sie live geht." | Static, no truncation |
| Sparring-Slot CTA | "Jetzt beitreten →" | Text link in message area |
| Primary CTA (Results) | "Jetzt beitreten & loslegen" | Main conversion CTA |
| Secondary CTA (Results) | "Test nochmal machen" | Reset + return to /test |
| Share result link text (V2) | — | Deferred |

**Error states:**

| Scenario | Copy |
|----------|------|
| Drag-Drop widget: item not placed after 30s | Helper text appears: "Ziehe die Karten in die gewünschte Reihenfolge." (14px, `var(--text-muted)`) |
| Questions.json load failure | "Test konnte nicht geladen werden. Bitte Seite neu laden." + Reload-Button |
| Result page: state missing (direct URL access to /test/ergebnis without completing test) | "Kein Ergebnis vorhanden. Starte den Test, um dein Ergebnis zu sehen." + "Test starten" CTA → `/test` |

**No destructive actions in Phase 24.** (The "Test nochmal machen" is a reset, not destructive in the data-loss sense — no persistent data is written in V1. No confirmation dialog required.)

---

## Mobile Responsive Contracts

**Breakpoint strategy:** `sm` = 640px (Tailwind default).

| Element | Mobile (<640px) | Desktop (≥640px) |
|---------|----------------|-----------------|
| Hero layout | Single column, `px-4`, CTA full-width max-w-xs centered | As specified in Layout Contracts |
| Aufgabe header | Logo hidden, progress bar full-width, "N/10" label right | Full header with logo |
| Widget area | `px-4`, full viewport width | `max-w-3xl mx-auto px-4` |
| W1 Card-Grid | 1 column, cards full-width | 2×2 grid |
| W2 Drag-Drop | Tap-to-select mode (auto on `pointer: coarse`) | Drag handles |
| W3 Prompt-Best-Pick | 1 column scroll | 2×2 grid |
| W4 Side-by-Side | Stacked (A then B) | Side-by-side cols |
| W6 Tool-zu-Task | Dropdown pairing mode | Drag-drop columns |
| Results: Empfehlungs-Cards | 1 column | 2 columns |
| Results: CTA cluster | Stacked (primary full-width, secondary below) | Row |
| Sparring-Slot | `max-h-[200px]` message area | `max-h-[240px]` |

**All touch targets:** minimum `48px × 48px` enforced via `min-w-[48px] min-h-[48px]` on:
- All card-pick cards (via min-height: 72px)
- Drag handles (explicit min-w/h)
- Slider thumb (via CSS `--slider-thumb-size: 24px` + `touch-action: none`)
- Matching drop targets
- Nächste-Aufgabe button (`py-3 px-8` → approximately 48px height)
- Spot-click spans (min-height 48px via line-height + padding)

---

## Accessibility Contracts

### Keyboard Navigation Per Widget

| Widget | Tab stops | Action keys | Escape |
|--------|-----------|-------------|--------|
| W1 Szenario-Pick | Each card | Space/Enter = select | No-op (no modal) |
| W2 Prompt-Ranking | Drag handles | Space = pick up, Arrow = move, Space = drop | Escape = cancel drag |
| W3 Prompt-Best-Pick | Each card | Space/Enter = select | No-op |
| W4 Side-by-Side | A card, B card, each chip | Space/Enter = select/toggle | No-op |
| W5 Fehler-Spot | Each text span | Space/Enter = mark | No-op |
| W6 Tool-Matching (desktop) | Drag handles | Space = pick up, Arrow = move, Space = drop | Escape = cancel |
| W6 Tool-Matching (mobile) | Each select | Arrow keys (native) | No-op |
| W7 Confidence-Slider | Slider thumb | Left/Right arrows = move 1 step, 1-5 keys = jump | No-op |
| W8 Fill-in-Param | Each select | Arrow keys (native) | No-op |
| W9 MC | Each option | Space/Enter = select | No-op |
| Nächste-Aufgabe button | Tab from widget | Enter = submit | No-op |

### Focus Management Between Aufgaben

On Aufgabe transition complete (new aufgabe rendered): focus moves to the question stem `<h2>` (via `ref.focus()`). This ensures screen readers read the new question immediately.

### Reduced-Motion Policy

Apply `useReducedMotion()` from Framer Motion as a hook. If true:
- All Framer Motion translates: disabled (opacity only)
- CSS `@keyframes` with `translate/scale`: paused via `prefers-reduced-motion: reduce` media query
- Checkpoint confetti: hidden entirely
- Radar chart: `isAnimationActive={false}`
- Widget pick-up scale/rotate: `transform: none`
- Transition between aufgaben: 150ms opacity cross-fade only

### Screen Reader Announcements (aria-live regions)

One global `aria-live="polite"` region at the bottom of the aufgabe layout (`sr-only` class, visually hidden). Updated for:
- Widget selection events ("Option 2 von 4 ausgewählt")
- Drag-drop moves ("Element 2 auf Position 3 verschoben")
- Matching completions ("Tool X Aufgabe Y zugeordnet — 3 von 4")
- Confidence slider changes ("Confidence: 75% — Eher ja")
- Transition to next aufgabe ("Aufgabe 4 von 10")
- Checkpoint: "Halbzeit erreicht! Weiter so."

---

## SEO Contract

| Meta | Value |
|------|-------|
| `<title>` | "AI Literacy Test — Wo stehst du wirklich mit KI?" |
| `description` | "Take the AI Literacy Test — 10 interactive tasks, 15 minutes, honest results. Free, anonymous, no signup required." (EN) |
| `og:title` | "AI Literacy Test" |
| `og:description` | "Free AI literacy assessment — find out your real AI skill level in 15 minutes." (EN) |
| `og:type` | "website" |
| `<h1>` on /test | "Wo stehst du wirklich mit KI?" (DE — target audience) |
| Canonical | `https://generation-ai.org/test` |
| `noindex` on aufgabe routes | `/test/aufgabe/[n]` routes: `<meta name="robots" content="noindex, nofollow">` — no value in indexing individual question pages |
| `/test/ergebnis` | `noindex, nofollow` — personalized result, no SEO value |

---

## Component Inventory

New components to build (Phase 24):

| Component | Path | Reuses |
|-----------|------|--------|
| `<TestHeroSection>` | `components/test/test-hero-section.tsx` | `<LabeledNodes>` background |
| `<AufgabeLayout>` | `components/test/aufgabe-layout.tsx` | `<Progress>` (shadcn) |
| `<CardPickWidget>` | `components/test/widgets/card-pick-widget.tsx` | — |
| `<DragRankWidget>` | `components/test/widgets/drag-rank-widget.tsx` | `@dnd-kit/core` |
| `<PromptBestPickWidget>` | `components/test/widgets/prompt-best-pick-widget.tsx` | shiki |
| `<SideBySideWidget>` | `components/test/widgets/side-by-side-widget.tsx` | — |
| `<FehlerspotWidget>` | `components/test/widgets/fehlerspot-widget.tsx` | — |
| `<MatchingWidget>` | `components/test/widgets/matching-widget.tsx` | `@dnd-kit/core` |
| `<ConfidenceSliderWidget>` | `components/test/widgets/confidence-slider-widget.tsx` | shadcn Slider |
| `<FillInWidget>` | `components/test/widgets/fill-in-widget.tsx` | shiki |
| `<MCWidget>` | `components/test/widgets/mc-widget.tsx` | Reuses CardPickWidget pattern |
| `<CheckpointCelebration>` | `components/test/checkpoint-celebration.tsx` | Framer Motion |
| `<LevelBadge>` | `components/test/level-badge.tsx` | lucide icons |
| `<SkillRadarChart>` | `components/test/skill-radar-chart.tsx` | recharts |
| `<EmpfehlungsCard>` | `components/test/empfehlungs-card.tsx` | — |
| `<SparringSlot>` | `components/test/sparring-slot.tsx` | Props-interface for later live-swap |
| `<TestResultsClient>` | `components/test-results-client.tsx` | MotionConfig + Header + Footer |
| `<AufgabeClient>` | `components/aufgabe-client.tsx` | MotionConfig |

Existing components consumed (no modification):
- `<LabeledNodes>` — hero background
- `<SectionTransition variant="soft-fade">` — results page
- `<Progress>` (shadcn) — progress bar
- Slider (shadcn) — W7 confidence slider
- `<Header>` + `<Footer>` — results page wrapper

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
