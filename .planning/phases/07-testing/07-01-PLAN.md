---
phase: 07-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/auth/vitest.config.mts
  - packages/auth/package.json
  - packages/auth/__tests__/helpers.test.ts
  - apps/tools-app/vitest.config.mts
  - apps/tools-app/vitest.setup.ts
  - apps/tools-app/package.json
  - apps/tools-app/__tests__/components/ChatInput.test.tsx
autonomous: true
requirements: [R4.1, R4.3]

must_haves:
  truths:
    - "Vitest laeuft in packages/auth"
    - "Vitest laeuft in apps/tools-app"
    - "Auth helpers haben Unit Tests"
    - "ChatInput Component hat Unit Test"
  artifacts:
    - path: "packages/auth/vitest.config.mts"
      provides: "Vitest config fuer auth package"
    - path: "packages/auth/__tests__/helpers.test.ts"
      provides: "Unit tests fuer getUser, getSession"
    - path: "apps/tools-app/vitest.config.mts"
      provides: "Vitest config mit jsdom + RTL"
    - path: "apps/tools-app/vitest.setup.ts"
      provides: "RTL setup mit jest-dom matchers"
    - path: "apps/tools-app/__tests__/components/ChatInput.test.tsx"
      provides: "Component test fuer Chat input"
  key_links:
    - from: "packages/auth/package.json"
      to: "vitest.config.mts"
      via: "test script"
      pattern: "vitest run"
    - from: "apps/tools-app/vitest.config.mts"
      to: "vitest.setup.ts"
      via: "setupFiles config"
      pattern: "setupFiles.*vitest.setup"
---

<objective>
Vitest + React Testing Library Setup in packages/auth und apps/tools-app.

Purpose: Test-Infrastruktur fuer die kritischsten Packages schaffen — Auth-Logik und Tool-App-Komponenten.
Output: Lauffaehige Vitest-Konfiguration mit ersten Unit/Component Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/testing.md

<interfaces>
<!-- packages/auth exports -->
From packages/auth/package.json:
```json
{
  "exports": {
    ".": "./src/index.ts",
    "./browser": "./src/browser.ts",
    "./server": "./src/server.ts",
    "./admin": "./src/admin.ts",
    "./helpers": "./src/helpers.ts"
  }
}
```

From packages/auth/src/helpers.ts:
```typescript
export async function getUser() // returns User | null
export async function getSession() // returns Session | null
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vitest Setup in packages/auth</name>
  <files>
    packages/auth/vitest.config.mts
    packages/auth/package.json
    packages/auth/__tests__/helpers.test.ts
  </files>
  <behavior>
    - Test: getUser returns null when supabase returns error
    - Test: getUser returns user object when supabase returns valid user
    - Test: getSession returns null when supabase returns error
    - Test: getSession returns session when supabase returns valid session
  </behavior>
  <action>
1. Dependencies installieren:
   ```bash
   pnpm add -D vitest --filter @genai/auth
   ```

2. vitest.config.mts erstellen (packages/auth/):
   ```typescript
   import { defineConfig } from 'vitest/config'

   export default defineConfig({
     test: {
       environment: 'node',
       globals: true,
     },
   })
   ```

3. package.json scripts ergaenzen:
   ```json
   "scripts": {
     "test": "vitest run",
     "test:watch": "vitest"
   }
   ```

4. __tests__/helpers.test.ts erstellen mit vi.mock fuer @supabase/ssr:
   - Mock createServerClient mit fake supabase client
   - Test getUser/getSession mit success + error cases
   - Wichtig: createClient aus ./server wird gemockt
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && pnpm --filter @genai/auth test</automated>
  </verify>
  <done>
    - vitest.config.mts existiert in packages/auth/
    - 4 Tests in helpers.test.ts (2x getUser, 2x getSession)
    - `pnpm --filter @genai/auth test` zeigt 4 passing tests
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vitest + RTL Setup in apps/tools-app</name>
  <files>
    apps/tools-app/vitest.config.mts
    apps/tools-app/vitest.setup.ts
    apps/tools-app/package.json
    apps/tools-app/__tests__/components/ChatInput.test.tsx
  </files>
  <behavior>
    - Test: ChatInput rendert ohne Fehler
    - Test: ChatInput akzeptiert Eingabe
    - Test: onSend wird bei Enter aufgerufen (oder Submit-Button)
  </behavior>
  <action>
1. Dependencies installieren:
   ```bash
   pnpm add -D vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/dom @testing-library/jest-dom @testing-library/user-event vite-tsconfig-paths --filter @genai/tools-app
   ```

2. vitest.config.mts erstellen (apps/tools-app/):
   ```typescript
   import { defineConfig } from 'vitest/config'
   import react from '@vitejs/plugin-react'
   import tsconfigPaths from 'vite-tsconfig-paths'

   export default defineConfig({
     plugins: [tsconfigPaths(), react()],
     test: {
       environment: 'jsdom',
       globals: true,
       setupFiles: ['./vitest.setup.ts'],
       include: ['__tests__/**/*.test.{ts,tsx}'],
     },
   })
   ```

3. vitest.setup.ts erstellen:
   ```typescript
   import '@testing-library/jest-dom/vitest'
   ```

4. package.json scripts ergaenzen:
   ```json
   "scripts": {
     "test": "vitest run",
     "test:watch": "vitest"
   }
   ```

5. __tests__/components/ Ordner erstellen, ChatInput.test.tsx:
   - Finde zuerst die ChatInput Komponente (oder aehnliche Input-Komponente im Chat)
   - Wenn keine separate ChatInput existiert, erstelle einen einfachen Smoke-Test fuer eine existierende Komponente
   - render(), screen.getByRole(), userEvent fuer Interaktion
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && pnpm --filter @genai/tools-app test</automated>
  </verify>
  <done>
    - vitest.config.mts + vitest.setup.ts existieren in apps/tools-app/
    - Mindestens 1 Component Test laeuft durch
    - `pnpm --filter @genai/tools-app test` zeigt passing tests
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test environment | Tests laufen in isolierter Umgebung, keine echten Credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-07-01 | I (Information Disclosure) | Test files | accept | Tests committen keine Secrets, .env.test nicht committed |
</threat_model>

<verification>
```bash
# Auth tests
pnpm --filter @genai/auth test

# Tools-app tests
pnpm --filter @genai/tools-app test
```
</verification>

<success_criteria>
- packages/auth hat Vitest mit 4 passing tests fuer helpers
- apps/tools-app hat Vitest + RTL mit mindestens 1 Component test
- Beide packages haben `test` und `test:watch` scripts
</success_criteria>

<output>
After completion, create `.planning/phases/07-testing/07-01-SUMMARY.md`
</output>
