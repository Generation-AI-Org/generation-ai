---
phase: 07-testing
plan: 03
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - apps/tools-app/__tests__/api/chat.test.ts
  - apps/tools-app/__tests__/api/health.test.ts
  - packages/e2e-tools/tests/auth.spec.ts
  - packages/e2e-tools/tests/chat.spec.ts
autonomous: true
requirements: [R4.4, R4.5]

must_haves:
  truths:
    - "API Route /api/chat hat Unit Tests"
    - "API Route /api/health hat Unit Tests"
    - "E2E Auth Flow Test existiert"
    - "E2E Chat Flow Test existiert"
  artifacts:
    - path: "apps/tools-app/__tests__/api/chat.test.ts"
      provides: "Tests fuer Chat API (Rate Limit, Auth, Success)"
    - path: "apps/tools-app/__tests__/api/health.test.ts"
      provides: "Tests fuer Health Endpoint"
    - path: "packages/e2e-tools/tests/auth.spec.ts"
      provides: "E2E Login/Logout Flow"
    - path: "packages/e2e-tools/tests/chat.spec.ts"
      provides: "E2E Chat Message Flow"
  key_links:
    - from: "apps/tools-app/__tests__/api/chat.test.ts"
      to: "app/api/chat/route.ts"
      via: "testApiHandler import"
      pattern: "testApiHandler.*chatHandler"
    - from: "packages/e2e-tools/tests/auth.spec.ts"
      to: "https://tools.generation-ai.org"
      via: "page.goto"
      pattern: "page.goto.*login"
---

<objective>
API Route Tests mit next-test-api-route-handler + E2E Tests fuer Auth und Chat Flows.

Purpose: Kritische API-Endpunkte unit-testen, wichtigste User Flows E2E absichern.
Output: Chat/Health API Tests, Auth + Chat E2E Specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/testing.md
@.planning/phases/07-testing/07-01-SUMMARY.md
@.planning/phases/07-testing/07-02-SUMMARY.md

<interfaces>
<!-- Chat API Route -->
From apps/tools-app/app/api/chat/route.ts:
```typescript
export async function POST(req: Request)
// Body: { message: string, history?: ChatMessage[], sessionId?: string, mode?: 'public' | 'member' }
// Returns: 200 with { response, sessionId, sources? }
// Returns: 400 if message empty
// Returns: 429 if rate limited (with Retry-After header)
```

From apps/tools-app/app/api/health/route.ts:
```typescript
export async function GET(req: Request)
// Returns: 200 with { status: 'ok', timestamp: string }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API Route Tests mit next-test-api-route-handler</name>
  <files>
    apps/tools-app/__tests__/api/chat.test.ts
    apps/tools-app/__tests__/api/health.test.ts
    apps/tools-app/vitest.config.mts
  </files>
  <behavior>
    - Test: GET /api/health returns 200 with status ok
    - Test: POST /api/chat returns 400 when message is empty
    - Test: POST /api/chat returns 200 with valid message (mocked dependencies)
    - Test: POST /api/chat returns 429 when rate limited (mocked)
  </behavior>
  <action>
1. Dependency installieren:
   ```bash
   pnpm add -D next-test-api-route-handler --filter @genai/tools-app
   ```

2. vitest.config.mts erweitern fuer API tests (environment node):
   ```typescript
   // Ergaenze environmentMatchGlobs
   test: {
     environment: 'jsdom',
     globals: true,
     setupFiles: ['./vitest.setup.ts'],
     include: ['__tests__/**/*.test.{ts,tsx}'],
     environmentMatchGlobs: [
       ['**/*.test.tsx', 'jsdom'],
       ['**/api/**/*.test.ts', 'node'],
     ],
   }
   ```

3. __tests__/api/health.test.ts erstellen:
   ```typescript
   import { testApiHandler } from 'next-test-api-route-handler'
   import * as healthHandler from '@/app/api/health/route'

   describe('/api/health', () => {
     it('returns 200 with status ok', async () => {
       await testApiHandler({
         appHandler: healthHandler,
         test: async ({ fetch }) => {
           const res = await fetch({ method: 'GET' })
           expect(res.status).toBe(200)
           const data = await res.json()
           expect(data.status).toBe('ok')
         }
       })
     })
   })
   ```

4. __tests__/api/chat.test.ts erstellen:
   - WICHTIG: next-test-api-route-handler muss ERSTER Import sein
   - Mock @/lib/supabase, @/lib/ratelimit, @/lib/llm, @/lib/agent mit vi.mock()
   - Test: 400 wenn message leer
   - Test: 200 mit gemocktem Success-Response
   - Test: 429 wenn checkRateLimit { success: false } returnt
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && pnpm --filter @genai/tools-app test -- --run __tests__/api/</automated>
  </verify>
  <done>
    - health.test.ts hat 1 passing test
    - chat.test.ts hat 3+ passing tests (400, 200, 429 cases)
    - API Tests laufen mit environment: node
  </done>
</task>

<task type="auto">
  <name>Task 2: Playwright E2E Tests fuer Auth und Chat</name>
  <files>
    packages/e2e-tools/tests/auth.spec.ts
    packages/e2e-tools/tests/chat.spec.ts
    packages/e2e-tools/.env.example
  </files>
  <action>
1. .env.example erstellen fuer E2E Test-Credentials:
   ```
   # E2E Test Configuration
   BASE_URL=http://localhost:3001
   # Optional: Test user credentials for authenticated flows
   TEST_USER_EMAIL=
   TEST_USER_PASSWORD=
   ```

2. tests/auth.spec.ts erstellen (Smoke Tests):
   ```typescript
   import { test, expect } from '@playwright/test'

   test.describe('Auth Flow', () => {
     test('homepage loads', async ({ page }) => {
       await page.goto('/')
       await expect(page).toHaveTitle(/Generation AI/i)
     })

     test('login page accessible', async ({ page }) => {
       await page.goto('/login')
       await expect(page.getByRole('heading', { name: /anmelden|login/i })).toBeVisible()
     })

     test('signup page accessible', async ({ page }) => {
       await page.goto('/signup')
       await expect(page.getByRole('heading', { name: /registrieren|signup/i })).toBeVisible()
     })

     // TODO: Full login flow with test credentials
     test.skip('can login with valid credentials', async ({ page }) => {
       const email = process.env.TEST_USER_EMAIL
       const password = process.env.TEST_USER_PASSWORD
       if (!email || !password) {
         test.skip()
         return
       }
       await page.goto('/login')
       await page.getByLabel(/email/i).fill(email)
       await page.getByLabel(/password/i).fill(password)
       await page.getByRole('button', { name: /anmelden|login/i }).click()
       await expect(page).toHaveURL(/chat|dashboard/i)
     })
   })
   ```

3. tests/chat.spec.ts erstellen (Smoke Tests):
   ```typescript
   import { test, expect } from '@playwright/test'

   test.describe('Chat Flow', () => {
     test('chat page loads', async ({ page }) => {
       await page.goto('/chat')
       // Should redirect to login or show chat
       await expect(page.locator('body')).toBeVisible()
     })

     // Public chat test (wenn verfuegbar)
     test('can see chat interface', async ({ page }) => {
       await page.goto('/chat')
       // Check for chat input or login redirect
       const chatInput = page.getByRole('textbox')
       const loginButton = page.getByRole('link', { name: /login|anmelden/i })
       await expect(chatInput.or(loginButton)).toBeVisible()
     })

     // TODO: Full chat flow with authenticated user
     test.skip('can send message in chat', async ({ page }) => {
       // Requires authenticated session
       await page.goto('/chat')
       const input = page.getByRole('textbox')
       await input.fill('Hallo, das ist ein Test')
       await page.getByRole('button', { name: /senden|send/i }).click()
       await expect(page.getByText('Hallo, das ist ein Test')).toBeVisible()
     })
   })
   ```

4. Smoke-Test lokal ausfuehren (erfordert laufende App):
   - Info: Diese Tests sind so designed dass sie auch ohne Auth funktionieren
   - Skipped tests fuer volle Auth flows (brauchen Test-Credentials)
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai/packages/e2e-tools && ls tests/*.spec.ts | wc -l</automated>
  </verify>
  <done>
    - auth.spec.ts existiert mit 3+ tests (homepage, login page, signup page)
    - chat.spec.ts existiert mit 2+ tests (chat page load, interface check)
    - .env.example dokumentiert benoetigte Environment Variables
    - Tests sind so designed dass Smoke Tests ohne Auth laufen
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API test mocks | Mocks verhindern echte DB/LLM Calls in Unit Tests |
| E2E test environment | Tests gegen localhost, nicht production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-07-03 | I (Information Disclosure) | Test credentials | mitigate | Test credentials in .env.local, .env.example hat nur Platzhalter |
| T-07-04 | S (Spoofing) | E2E auth tests | accept | Tests laufen nur lokal/CI, Test-User hat eingeschraenkte Rechte |
</threat_model>

<verification>
```bash
# API tests
pnpm --filter @genai/tools-app test

# E2E test files exist
ls packages/e2e-tools/tests/
```
</verification>

<success_criteria>
- API Tests fuer /api/health und /api/chat existieren und laufen durch
- E2E Specs fuer auth.spec.ts und chat.spec.ts existieren
- Smoke Tests koennen ohne Auth laufen
- Volle Auth-Tests sind als .skip() markiert fuer spaetere Aktivierung
</success_criteria>

<output>
After completion, create `.planning/phases/07-testing/07-03-SUMMARY.md`
</output>
