---
phase: 07-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/vitest.config.mts
  - apps/website/vitest.setup.ts
  - apps/website/package.json
  - apps/website/__tests__/components/Button.test.tsx
  - packages/e2e-tools/package.json
  - packages/e2e-tools/playwright.config.ts
  - packages/e2e-tools/tsconfig.json
autonomous: true
requirements: [R4.2, R4.5]

must_haves:
  truths:
    - "Vitest laeuft in apps/website"
    - "Playwright E2E Package existiert"
    - "Website hat mindestens einen Component Test"
  artifacts:
    - path: "apps/website/vitest.config.mts"
      provides: "Vitest config fuer website"
    - path: "apps/website/__tests__/components/Button.test.tsx"
      provides: "Beispiel Component Test"
    - path: "packages/e2e-tools/package.json"
      provides: "E2E package definition"
    - path: "packages/e2e-tools/playwright.config.ts"
      provides: "Playwright Konfiguration"
  key_links:
    - from: "apps/website/package.json"
      to: "vitest.config.mts"
      via: "test script"
      pattern: "vitest run"
    - from: "packages/e2e-tools/package.json"
      to: "playwright.config.ts"
      via: "e2e script"
      pattern: "playwright test"
---

<objective>
Vitest in apps/website + Playwright E2E Package Setup.

Purpose: Website bekommt Unit-Test-Infrastruktur, E2E Package fuer kritische Flow-Tests wird vorbereitet.
Output: Website mit Vitest, neues packages/e2e-tools mit Playwright ready.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/testing.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vitest Setup in apps/website</name>
  <files>
    apps/website/vitest.config.mts
    apps/website/vitest.setup.ts
    apps/website/package.json
    apps/website/__tests__/components/Button.test.tsx
  </files>
  <behavior>
    - Test: Button rendert korrekt
    - Test: Button zeigt children text
    - Test: onClick wird aufgerufen bei click
  </behavior>
  <action>
1. Dependencies installieren:
   ```bash
   pnpm add -D vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/dom @testing-library/jest-dom @testing-library/user-event vite-tsconfig-paths --filter @genai/website
   ```

2. vitest.config.mts erstellen (apps/website/):
   ```typescript
   import { defineConfig } from 'vitest/config'
   import react from '@vitejs/plugin-react'
   import tsconfigPaths from 'vite-tsconfig-paths'

   export default defineConfig({
     plugins: [tsconfigPaths(), react()],
     test: {
       environment: 'jsdom',
       globals: true,
       setupFiles: ['./vitest.setup.ts'],
       include: ['__tests__/**/*.test.{ts,tsx}'],
     },
   })
   ```

3. vitest.setup.ts erstellen:
   ```typescript
   import '@testing-library/jest-dom/vitest'
   ```

4. package.json scripts ergaenzen:
   ```json
   "scripts": {
     "test": "vitest run",
     "test:watch": "vitest"
   }
   ```

5. __tests__/components/Button.test.tsx erstellen:
   - Finde eine existierende Button/UI-Komponente in components/
   - Falls keine vorhanden, erstelle einfachen Test fuer beliebige kleine Komponente
   - render, screen.getByRole('button'), userEvent.click()
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && pnpm --filter @genai/website test</automated>
  </verify>
  <done>
    - vitest.config.mts + vitest.setup.ts existieren in apps/website/
    - Mindestens 1 Component Test laeuft durch
    - `pnpm --filter @genai/website test` zeigt passing tests
  </done>
</task>

<task type="auto">
  <name>Task 2: Playwright E2E Package erstellen</name>
  <files>
    packages/e2e-tools/package.json
    packages/e2e-tools/playwright.config.ts
    packages/e2e-tools/tsconfig.json
    packages/e2e-tools/tests/.gitkeep
  </files>
  <action>
1. Ordner erstellen:
   ```bash
   mkdir -p packages/e2e-tools/tests
   ```

2. package.json erstellen (packages/e2e-tools/):
   ```json
   {
     "name": "@genai/e2e-tools",
     "version": "0.0.1",
     "private": true,
     "scripts": {
       "e2e": "playwright test",
       "e2e:ui": "playwright test --ui",
       "e2e:headed": "playwright test --headed"
     },
     "devDependencies": {
       "@playwright/test": "^1.52.0",
       "typescript": "^5"
     }
   }
   ```

3. tsconfig.json erstellen:
   ```json
   {
     "compilerOptions": {
       "target": "ES2022",
       "module": "ESNext",
       "moduleResolution": "bundler",
       "strict": true,
       "esModuleInterop": true,
       "skipLibCheck": true,
       "resolveJsonModule": true
     },
     "include": ["**/*.ts"]
   }
   ```

4. playwright.config.ts erstellen:
   ```typescript
   import { defineConfig, devices } from '@playwright/test'

   export default defineConfig({
     testDir: './tests',
     fullyParallel: true,
     forbidOnly: !!process.env.CI,
     retries: process.env.CI ? 2 : 0,
     workers: process.env.CI ? 1 : undefined,
     reporter: 'html',
     use: {
       baseURL: process.env.BASE_URL || 'http://localhost:3001',
       trace: 'on-first-retry',
     },
     projects: [
       {
         name: 'chromium',
         use: { ...devices['Desktop Chrome'] },
       },
     ],
   })
   ```

5. tests/.gitkeep erstellen (leerer Ordner fuer Tests)

6. Dependencies installieren:
   ```bash
   pnpm install --filter @genai/e2e-tools
   ```

7. Playwright browsers installieren:
   ```bash
   pnpm --filter @genai/e2e-tools exec playwright install chromium
   ```
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && ls packages/e2e-tools/playwright.config.ts && pnpm --filter @genai/e2e-tools exec playwright --version</automated>
  </verify>
  <done>
    - packages/e2e-tools/ existiert mit package.json, playwright.config.ts, tsconfig.json
    - Playwright ist installiert (playwright --version funktioniert)
    - tests/ Ordner ready fuer Test-Files
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E Test environment | Tests laufen gegen localhost oder staging, nie production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-07-02 | I (Information Disclosure) | E2E tests | mitigate | BASE_URL nur auf localhost/staging, keine prod credentials in tests |
</threat_model>

<verification>
```bash
# Website tests
pnpm --filter @genai/website test

# Playwright version check
pnpm --filter @genai/e2e-tools exec playwright --version
```
</verification>

<success_criteria>
- apps/website hat Vitest mit mindestens 1 passing Component test
- packages/e2e-tools existiert mit Playwright config
- Playwright browsers sind installiert
</success_criteria>

<output>
After completion, create `.planning/phases/07-testing/07-02-SUMMARY.md`
</output>
