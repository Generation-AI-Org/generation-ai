---
phase: 04-dsgvo-legal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/tools-app/app/api/account/delete/route.ts
  - apps/tools-app/app/settings/page.tsx
  - apps/tools-app/app/settings/DeleteAccountButton.tsx
  - packages/auth/src/admin.ts
autonomous: true
requirements: [R2.7]

must_haves:
  truths:
    - "User kann Account ueber Settings-Seite loeschen"
    - "Account-Loeschung loescht auth.users Eintrag"
    - "Account-Loeschung loescht alle chat_sessions des Users"
    - "Account-Loeschung loescht alle chat_messages des Users"
    - "Loeschung erfordert Bestaetigung (kein versehentliches Loeschen)"
  artifacts:
    - path: "apps/tools-app/app/api/account/delete/route.ts"
      provides: "DELETE Endpoint fuer Account-Loeschung"
      exports: ["DELETE"]
    - path: "apps/tools-app/app/settings/page.tsx"
      provides: "Settings-Seite mit Delete-Option"
      contains: "DeleteAccountButton"
    - path: "apps/tools-app/app/settings/DeleteAccountButton.tsx"
      provides: "Client Component fuer Loeschung mit Bestaetigung"
      contains: "use client"
  key_links:
    - from: "DeleteAccountButton"
      to: "/api/account/delete"
      via: "fetch DELETE"
      pattern: "fetch.*api/account/delete.*DELETE"
    - from: "API route"
      to: "Supabase Admin Client"
      via: "deleteUser"
      pattern: "auth.admin.deleteUser"
---

<objective>
Self-Service Account-Delete-Funktion in tools-app implementieren (Art. 17 DSGVO).

Purpose: User muessen jederzeit ihre Daten loeschen koennen. Self-Service ist besser als manuelle E-Mail-Anfragen.

Output: Settings-Seite mit "Account loeschen"-Button, der Auth-User und alle Custom-Daten entfernt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/dsgvo-privacy.md

<interfaces>
<!-- Aus packages/auth/src/admin.ts -->
```typescript
import { createClient } from '@supabase/supabase-js'

export function createAdminClient() {
  return createClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    {
      auth: {
        autoRefreshToken: false,
        persistSession: false,
      },
    }
  )
}
```

<!-- Aus packages/auth/src/server.ts -->
```typescript
export async function createClient() {
  // Returns authenticated server client
}
```

<!-- Aus packages/auth/src/helpers.ts -->
```typescript
export async function getUser() {
  // Returns current user or null
}
```

<!-- Aus apps/tools-app/supabase/schema.sql -->
```sql
-- chat_sessions hat user_id REFERENCES auth.users(id)
-- chat_messages hat user_id REFERENCES auth.users(id)
-- chat_messages hat ON DELETE CASCADE auf session_id
-- ABER: chat_sessions hat KEIN ON DELETE CASCADE auf user_id
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API Route fuer Account-Loeschung erstellen</name>
  <files>apps/tools-app/app/api/account/delete/route.ts</files>
  <read_first>
    - packages/auth/src/admin.ts
    - packages/auth/src/server.ts
    - packages/auth/src/helpers.ts
    - apps/tools-app/supabase/schema.sql (RLS Policies und Foreign Keys)
    - apps/tools-app/app/api/chat/route.ts (als Beispiel fuer API-Struktur)
  </read_first>
  <action>
    Erstelle `apps/tools-app/app/api/account/delete/route.ts`:
    
    ```typescript
    import { NextResponse } from 'next/server'
    import { createServerClient, createAdminClient } from '@genai/auth'
    
    export async function DELETE() {
      // 1. Get current user via server client
      const supabase = await createServerClient()
      const { data: { user }, error: userError } = await supabase.auth.getUser()
      
      if (userError || !user) {
        return NextResponse.json({ error: 'Nicht authentifiziert' }, { status: 401 })
      }
      
      const userId = user.id
      
      // 2. Delete user data in order (cascade not set on user_id):
      // - chat_messages (via session cascade, but also direct for safety)
      // - chat_sessions
      const admin = createAdminClient()
      
      // Delete chat_messages for this user
      const { error: messagesError } = await admin
        .from('chat_messages')
        .delete()
        .eq('user_id', userId)
      
      if (messagesError) {
        console.error('Failed to delete chat_messages:', messagesError)
        return NextResponse.json({ error: 'Fehler beim Loeschen der Nachrichten' }, { status: 500 })
      }
      
      // Delete chat_sessions for this user
      const { error: sessionsError } = await admin
        .from('chat_sessions')
        .delete()
        .eq('user_id', userId)
      
      if (sessionsError) {
        console.error('Failed to delete chat_sessions:', sessionsError)
        return NextResponse.json({ error: 'Fehler beim Loeschen der Sessions' }, { status: 500 })
      }
      
      // 3. Delete auth user (requires admin client)
      const { error: authError } = await admin.auth.admin.deleteUser(userId)
      
      if (authError) {
        console.error('Failed to delete auth user:', authError)
        return NextResponse.json({ error: 'Fehler beim Loeschen des Accounts' }, { status: 500 })
      }
      
      return NextResponse.json({ success: true })
    }
    ```
    
    WICHTIG:
    - Zuerst Custom-Daten loeschen, dann Auth-User (sonst Foreign Key Violations)
    - Admin Client braucht SUPABASE_SERVICE_ROLE_KEY (in .env.local vorhanden)
    - User-Identifikation ueber Server Client (Session-basiert), nicht ueber Request Body
  </action>
  <verify>
    <automated>ls -la apps/tools-app/app/api/account/delete/route.ts && grep -c "deleteUser" apps/tools-app/app/api/account/delete/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/tools-app/app/api/account/delete/route.ts existiert
    - Datei exportiert DELETE function
    - Datei verwendet createAdminClient von @genai/auth
    - Datei ruft auth.admin.deleteUser auf
    - Datei loescht chat_messages VOR chat_sessions
    - Datei loescht chat_sessions VOR auth user
    - pnpm build --filter=tools-app succeeds
  </acceptance_criteria>
  <done>API Route fuer Account-Loeschung ist implementiert mit korrekter Reihenfolge</done>
</task>

<task type="auto">
  <name>Task 2: Settings-Seite mit Delete-Button erstellen</name>
  <files>apps/tools-app/app/settings/page.tsx, apps/tools-app/app/settings/DeleteAccountButton.tsx</files>
  <read_first>
    - apps/tools-app/app/layout.tsx (fuer Styling-Konventionen)
    - apps/tools-app/components/AuthProvider.tsx (fuer Auth-Context)
    - packages/auth/src/helpers.ts (fuer getUser)
  </read_first>
  <action>
    **Erstelle apps/tools-app/app/settings/DeleteAccountButton.tsx (Client Component):**
    
    ```tsx
    'use client'
    
    import { useState } from 'react'
    import { useRouter } from 'next/navigation'
    
    export function DeleteAccountButton() {
      const [isConfirming, setIsConfirming] = useState(false)
      const [isDeleting, setIsDeleting] = useState(false)
      const [error, setError] = useState<string | null>(null)
      const router = useRouter()
      
      async function handleDelete() {
        setIsDeleting(true)
        setError(null)
        
        try {
          const res = await fetch('/api/account/delete', { method: 'DELETE' })
          const data = await res.json()
          
          if (!res.ok) {
            throw new Error(data.error || 'Loeschung fehlgeschlagen')
          }
          
          // Redirect to homepage after successful deletion
          router.push('/')
          router.refresh()
        } catch (err) {
          setError(err instanceof Error ? err.message : 'Unbekannter Fehler')
          setIsDeleting(false)
        }
      }
      
      if (!isConfirming) {
        return (
          <button
            onClick={() => setIsConfirming(true)}
            className="px-4 py-2 text-red-500 border border-red-500 rounded-lg hover:bg-red-500/10 transition-colors"
          >
            Account loeschen
          </button>
        )
      }
      
      return (
        <div className="space-y-4 p-4 border border-red-500/30 rounded-lg bg-red-500/5">
          <p className="text-sm text-red-400">
            Bist du sicher? Diese Aktion loescht deinen Account und alle deine Daten unwiderruflich.
          </p>
          {error && <p className="text-sm text-red-500">{error}</p>}
          <div className="flex gap-3">
            <button
              onClick={() => setIsConfirming(false)}
              disabled={isDeleting}
              className="px-4 py-2 text-text-muted border border-border rounded-lg hover:bg-surface transition-colors disabled:opacity-50"
            >
              Abbrechen
            </button>
            <button
              onClick={handleDelete}
              disabled={isDeleting}
              className="px-4 py-2 bg-red-500 text-white rounded-lg hover:bg-red-600 transition-colors disabled:opacity-50"
            >
              {isDeleting ? 'Wird geloescht...' : 'Endgueltig loeschen'}
            </button>
          </div>
        </div>
      )
    }
    ```
    
    **Erstelle apps/tools-app/app/settings/page.tsx (Server Component):**
    
    ```tsx
    import { Metadata } from 'next'
    import { redirect } from 'next/navigation'
    import Link from 'next/link'
    import { getUser } from '@genai/auth'
    import { DeleteAccountButton } from './DeleteAccountButton'
    
    export const metadata: Metadata = {
      title: 'Einstellungen | Generation AI Tools',
    }
    
    export default async function SettingsPage() {
      const user = await getUser()
      
      if (!user) {
        redirect('/login')
      }
      
      return (
        <main className="min-h-screen bg-bg pt-24 pb-16 px-4">
          <div className="max-w-2xl mx-auto">
            <Link
              href="/"
              className="inline-flex items-center gap-2 text-text-muted hover:text-text mb-8 transition-colors"
            >
              <svg className="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M15 19l-7-7 7-7" />
              </svg>
              Zurueck
            </Link>
            
            <h1 className="text-3xl font-bold text-text mb-8">Einstellungen</h1>
            
            <section className="mb-12">
              <h2 className="text-xl font-semibold text-text mb-4">Account</h2>
              <div className="p-4 bg-surface rounded-lg border border-border">
                <p className="text-text-secondary mb-2">Angemeldet als:</p>
                <p className="text-text font-medium">{user.email}</p>
              </div>
            </section>
            
            <section>
              <h2 className="text-xl font-semibold text-red-400 mb-4">Gefahrenzone</h2>
              <div className="p-4 bg-surface rounded-lg border border-red-500/30">
                <h3 className="text-lg font-medium text-text mb-2">Account loeschen</h3>
                <p className="text-text-secondary mb-4">
                  Loescht deinen Account und alle zugehoerigen Daten (Chat-Verlauf, Sessions).
                  Diese Aktion kann nicht rueckgaengig gemacht werden.
                </p>
                <DeleteAccountButton />
              </div>
            </section>
          </div>
        </main>
      )
    }
    ```
    
    WICHTIG:
    - Settings-Seite erfordert Auth (redirect zu /login wenn nicht eingeloggt)
    - DeleteAccountButton ist Client Component wegen useState/onClick
    - Zwei-Schritt-Bestaetigung verhindert versehentliches Loeschen
  </action>
  <verify>
    <automated>ls -la apps/tools-app/app/settings/ && grep -c "DeleteAccountButton" apps/tools-app/app/settings/page.tsx && pnpm build --filter=tools-app</automated>
  </verify>
  <acceptance_criteria>
    - apps/tools-app/app/settings/page.tsx existiert
    - apps/tools-app/app/settings/DeleteAccountButton.tsx existiert
    - DeleteAccountButton.tsx hat 'use client' Direktive
    - page.tsx importiert und verwendet DeleteAccountButton
    - page.tsx prueft Auth und redirected zu /login wenn nicht eingeloggt
    - DeleteAccountButton hat Zwei-Schritt-Bestaetigung (isConfirming state)
    - pnpm build --filter=tools-app succeeds
  </acceptance_criteria>
  <done>Settings-Seite mit Account-Delete-Funktion ist implementiert mit Bestaetigung</done>
</task>

<task type="auto">
  <name>Task 3: Navigation zur Settings-Seite hinzufuegen</name>
  <files>apps/tools-app/components/AppShell.tsx</files>
  <read_first>
    - apps/tools-app/components/AppShell.tsx
    - apps/tools-app/components/AuthProvider.tsx
  </read_first>
  <action>
    Pruefe AppShell.tsx und fuege Link zu /settings hinzu:
    
    1. Finde den Bereich wo User-bezogene Links sind (Login/Logout Button)
    2. Fuege fuer eingeloggte User einen Settings-Link hinzu:
       - Icon: Zahnrad (Cog) SVG
       - Text: "Einstellungen" (oder nur Icon im Header)
       - Link zu "/settings"
    
    Falls User-Menu existiert (Dropdown):
    - Fuege "Einstellungen" als Menu-Item vor "Abmelden" hinzu
    
    Falls kein User-Menu existiert:
    - Fuege kleinen Zahnrad-Icon-Link neben User-Email/Avatar hinzu
    
    Beispiel fuer Icon-Link:
    ```tsx
    <Link href="/settings" className="p-2 hover:bg-surface rounded-lg transition-colors" title="Einstellungen">
      <svg className="w-5 h-5 text-text-muted" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} 
          d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
        <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
      </svg>
    </Link>
    ```
    
    NUR sichtbar wenn User eingeloggt ist.
  </action>
  <verify>
    <automated>grep -c "settings" apps/tools-app/components/AppShell.tsx && pnpm build --filter=tools-app</automated>
  </verify>
  <acceptance_criteria>
    - AppShell.tsx enthaelt Link zu "/settings"
    - Settings-Link ist nur fuer eingeloggte User sichtbar
    - pnpm build --filter=tools-app succeeds
    - Navigation zur Settings-Seite ist intuitiv auffindbar
  </acceptance_criteria>
  <done>Settings-Link ist in der Navigation fuer eingeloggte User sichtbar</done>
</task>

</tasks>

<verification>
Nach Abschluss aller Tasks:
1. `pnpm build --filter=tools-app` - Build erfolgreich
2. Manueller Test:
   - Einloggen in tools-app
   - Zu /settings navigieren
   - "Account loeschen" klicken
   - Bestaetigung wird angezeigt
   - (Nicht tatsaechlich loeschen in Prod!)
3. `grep -r "deleteUser" apps/tools-app/` - API nutzt Supabase Admin deleteUser
</verification>

<success_criteria>
- [ ] API Route DELETE /api/account/delete existiert
- [ ] API loescht chat_messages, chat_sessions, dann auth.users
- [ ] Settings-Seite unter /settings erreichbar (nur fuer eingeloggte User)
- [ ] DeleteAccountButton hat Zwei-Schritt-Bestaetigung
- [ ] Navigation zur Settings-Seite existiert
- [ ] `pnpm build --filter=tools-app` erfolgreich
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/04-dsgvo-legal/04-02-SUMMARY.md` erstellen
</output>
