---
phase: 19-password-flow-and-test-baseline
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/e2e-tools/playwright.config.ts
  - packages/e2e-tools/tests/chat.spec.ts
  - packages/e2e-tools/tests/auth.spec.ts
  - turbo.json
autonomous: true
requirements: [D-07, D-08]
must_haves:
  truths:
    - "playwright.config.ts hat https://tools.generation-ai.org als Default-baseURL, überschreibbar via E2E_BASE_URL"
    - "chat.spec.ts läuft gegen Prod via baseURL, nicht localhost"
    - "auth.spec.ts liest baseURL aus playwright-config statt aus eigener BASE_URL-env (konsistent mit smoke.spec.ts)"
    - "turbo.json e2e-Task leitet E2E_BASE_URL via passThroughEnv durch (sonst wird der Override von turbo gefiltert)"
    - "pnpm e2e gegen Prod grün (mit gesetztem TEST_USER_EMAIL/TEST_USER_PASSWORD in .env.test.local oder CI-Secrets)"
  artifacts:
    - path: "packages/e2e-tools/playwright.config.ts"
      provides: "Prod-Default-baseURL mit E2E_BASE_URL-Override"
      contains: "tools.generation-ai.org"
    - path: "packages/e2e-tools/tests/chat.spec.ts"
      provides: "Chat-Smoke-Tests gegen Prod (Login-Redirect-Check statt internal state)"
      contains: "/chat"
    - path: "turbo.json"
      provides: "passThroughEnv inkl. E2E_BASE_URL für e2e-Task"
      contains: "E2E_BASE_URL"
  key_links:
    - from: "packages/e2e-tools/playwright.config.ts"
      to: "process.env.E2E_BASE_URL"
      via: "baseURL: process.env.E2E_BASE_URL || 'https://tools.generation-ai.org'"
      pattern: "E2E_BASE_URL"
    - from: "turbo.json tasks.e2e.passThroughEnv"
      to: "process.env.E2E_BASE_URL in playwright.config.ts"
      via: "turbo reicht E2E_BASE_URL unverändert durch an pnpm e2e"
      pattern: "\"E2E_BASE_URL\""
---

<objective>
E2E-Baseline reparieren (D-07, D-08): `playwright.config.ts` bekommt Prod-Default-baseURL mit `E2E_BASE_URL`-Override (statt localhost-Default + `BASE_URL`-env). `chat.spec.ts` wird auf Prod-tauglich angepasst — keine Annahme über internen State, nur Loading + Login-Redirect-Check (Test-User ist nicht eingeloggt, Server-Component `/chat` redirected zu `/login`). `auth.spec.ts` nutzt baseURL aus Config statt eigenem `BASE_URL`-Fallback. `turbo.json` e2e-Task wird um `E2E_BASE_URL` in `passThroughEnv` erweitert, sonst filtert Turbo den Env-Var heraus und Config nimmt immer Prod-Default — Override via `pnpm e2e` würde sonst wirkungslos sein (D-08 Override).

Purpose: Grüne E2E-Baseline gegen Prod (D-07). Konsistent mit smoke.spec.ts. D-08-Override funktioniert sowohl via `pnpm exec playwright test` als auch via `pnpm e2e` (turbo).
Output: 4 angepasste Files, `pnpm e2e` grün gegen Prod mit Test-User-Secrets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-password-flow-and-test-baseline/CONTEXT.md
@packages/e2e-tools/playwright.config.ts
@packages/e2e-tools/tests/smoke.spec.ts
@packages/e2e-tools/tests/auth.spec.ts
@packages/e2e-tools/tests/chat.spec.ts
@packages/e2e-tools/fixtures/test-user.ts
@turbo.json

<interfaces>
<!-- Aktuell: config hat localhost:3001 default, auth.spec.ts hängt an BASE_URL-env. -->

Current playwright.config.ts:
```ts
import "dotenv/config"
import * as dotenv from "dotenv"
import { defineConfig, devices } from "@playwright/test"

dotenv.config({ path: ".env.test.local" })

export default defineConfig({
  // ...
  use: {
    baseURL: process.env.BASE_URL || "http://localhost:3001",
    // ...
  },
})
```

Current auth.spec.ts (line 10):
```ts
const TOOLS_URL = process.env.BASE_URL || "https://tools.generation-ai.org"
const WEBSITE_URL = process.env.WEBSITE_URL || "https://generation-ai.org"
```

Current chat.spec.ts: nutzt relative Pfade (`page.goto('/chat')`), greift also via `baseURL` aus config. Testet: Chat-Page lädt, body visible, `getByRole('textbox')` OR Login-Redirect sichtbar. Auf Prod ohne Login → Server redirected zu `/login`, Page zeigt Login-Form.

test-user fixture (`packages/e2e-tools/fixtures/test-user.ts`):
```ts
// Liest TEST_USER_EMAIL + TEST_USER_PASSWORD aus process.env
// requireTestUser() wirft wenn fehlend; getTestUser() returned null
```
→ .env.test.local (lokal) oder CI-Secrets (Plan 05) füllen diese.

Current turbo.json tasks.e2e:
```json
"e2e": {
  "dependsOn": ["^build"],
  "passThroughEnv": ["PLAYWRIGHT_*", "BASE_URL", "TEST_USER_EMAIL", "TEST_USER_PASSWORD"],
  "outputs": ["playwright-report/**", "test-results/**"]
}
```
→ `E2E_BASE_URL` fehlt. `pnpm e2e` (turbo) filtert diese Env-Var heraus — der D-08-Override funktioniert damit nur wenn Tests direkt via `pnpm exec playwright test` ohne turbo-Wrapper laufen. Für CI und lokalen `pnpm e2e`-Run muss `E2E_BASE_URL` explizit in `passThroughEnv` stehen.

Hinweis: `dotenv.config({ path: ".env.test.local" })` liest Env vor Playwright-Run. Wenn Config auf `E2E_BASE_URL` umschwenkt, muss `.env.test.local.example` das dokumentieren — aber das ist Dokumentation (Plan 05), nicht hier.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: playwright.config.ts auf Prod-Default + E2E_BASE_URL-Override</name>
  <files>packages/e2e-tools/playwright.config.ts</files>
  <read_first>
    - packages/e2e-tools/playwright.config.ts (aktuell 30 Zeilen)
    - packages/e2e-tools/tests/smoke.spec.ts (Referenz für Prod-Pattern)
    - .planning/phases/19-password-flow-and-test-baseline/CONTEXT.md §D-08
  </read_first>
  <behavior>
    - Default baseURL: `https://tools.generation-ai.org`
    - Overridable via `E2E_BASE_URL` env var (D-08 im CONTEXT nennt explizit E2E_BASE_URL)
    - Backward-Compat für `BASE_URL`: als Fallback akzeptieren, damit existierende CI-Configs nicht brechen (Plan 05 updated CI auf E2E_BASE_URL, aber Übergang smooth)
    - `.env.test.local`-Loading bleibt
  </behavior>
  <action>
Ersetze `packages/e2e-tools/playwright.config.ts` komplett durch:

```ts
import "dotenv/config"
import * as dotenv from "dotenv"
import { defineConfig, devices } from "@playwright/test"

dotenv.config({ path: ".env.test.local" })

// D-08: Default gegen Prod. Override via E2E_BASE_URL (oder legacy BASE_URL).
// Lokale Dev gegen Dev-Server: E2E_BASE_URL=http://localhost:3001 pnpm exec playwright test
const BASE_URL =
  process.env.E2E_BASE_URL ||
  process.env.BASE_URL ||
  "https://tools.generation-ai.org"

export default defineConfig({
  testDir: "./tests",
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: "html",
  timeout: 30_000,
  expect: {
    timeout: 10_000,
  },
  use: {
    baseURL: BASE_URL,
    trace: "on-first-retry",
  },
  projects: [
    {
      name: "chromium",
      use: { ...devices["Desktop Chrome"] },
    },
  ],
})
```
  </action>
  <verify>
    <automated>pnpm --filter @genai/e2e-tools exec playwright test --list | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/e2e-tools/playwright.config.ts` contains `E2E_BASE_URL`
    - File contains literal string `https://tools.generation-ai.org`
    - File contains `baseURL: BASE_URL` (derived constant)
    - File still contains `dotenv.config({ path: ".env.test.local" })`
    - `pnpm --filter @genai/e2e-tools exec playwright test --list` exits 0 (config parses)
    - grep for `http://localhost:3001` in playwright.config.ts returns 0 matches (no hardcoded localhost)
  </acceptance_criteria>
  <done>Config verwendet Prod als Default, E2E_BASE_URL als primärer Override, BASE_URL als Legacy-Fallback.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: auth.spec.ts — TOOLS_URL aus baseURL ableiten</name>
  <files>packages/e2e-tools/tests/auth.spec.ts</files>
  <read_first>
    - packages/e2e-tools/tests/auth.spec.ts (227 Zeilen, viele TOOLS_URL-Calls)
    - packages/e2e-tools/tests/smoke.spec.ts (Referenz: nutzt hardcoded URLs statt TOOLS_URL-Konstante)
    - packages/e2e-tools/playwright.config.ts (nach Task 1 — jetzt Prod-Default)
  </read_first>
  <behavior>
    - `TOOLS_URL` und `WEBSITE_URL` lesen neue Env-Vars mit gleichen Defaults wie Config
    - Bestehende Test-Logik unverändert
  </behavior>
  <action>
In `packages/e2e-tools/tests/auth.spec.ts`, ändere Zeile 10-11 von:

```ts
const TOOLS_URL = process.env.BASE_URL || "https://tools.generation-ai.org"
const WEBSITE_URL = process.env.WEBSITE_URL || "https://generation-ai.org"
```

zu:

```ts
// Aligned mit playwright.config.ts (Phase 19 D-08): E2E_BASE_URL primary, BASE_URL legacy-fallback.
const TOOLS_URL =
  process.env.E2E_BASE_URL ||
  process.env.BASE_URL ||
  "https://tools.generation-ai.org"
const WEBSITE_URL = process.env.WEBSITE_URL || "https://generation-ai.org"
```

Alle anderen Zeilen (Test-Bodies, `loginWithPassword`-Helper, alle describe-Blocks) unverändert lassen. Existierende Phase-13-E2E-Tests dürfen NICHT brechen.
  </action>
  <verify>
    <automated>pnpm --filter @genai/e2e-tools exec playwright test auth.spec.ts --list</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/e2e-tools/tests/auth.spec.ts` contains `process.env.E2E_BASE_URL`
    - File still contains `process.env.BASE_URL` (legacy-fallback)
    - File still contains `test.describe("Auth Path 1: Password Login"` (Phase-13-Tests intakt)
    - File still contains `test.describe("Auth Path 5: Password Reset End-to-End"` (Plan 02 darf diese nicht brechen — greift `#password`/`#confirmPassword`, die Plan 02 bewusst NICHT umbenennt)
    - `pnpm --filter @genai/e2e-tools exec playwright test auth.spec.ts --list` exits 0
    - grep -c "process.env.E2E_BASE_URL" packages/e2e-tools/tests/auth.spec.ts >= 1
  </acceptance_criteria>
  <done>auth.spec.ts verwendet gleiches Env-Pattern wie Config, keine Test-Body-Änderung.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: chat.spec.ts — Prod-Reality (unauthenticated redirect to login)</name>
  <files>packages/e2e-tools/tests/chat.spec.ts</files>
  <read_first>
    - packages/e2e-tools/tests/chat.spec.ts (aktuell 47 Zeilen, 3 active + 2 skipped)
    - packages/e2e-tools/tests/smoke.spec.ts (Pattern für Prod-Smoke)
    - packages/e2e-tools/playwright.config.ts (nach Task 1)
    - .planning/phases/19-password-flow-and-test-baseline/CONTEXT.md §E2E-Test-Strategie
  </read_first>
  <behavior>
    - Test 1 ("chat page loads") bleibt: body visible, baseURL greift via config → Prod
    - Test 2 ("can see chat interface or login prompt") bleibt: erkennt beide Möglichkeiten (input OR login)
    - Test 3 ("chat interface has expected elements when accessible") ist fragil weil es IMMER `main` erwartet — auf Prod unauthenticated wird zu `/login` redirected und da IST ein `<main>` oder `<body>`. Robuster: check URL is `/login` OR `/chat` und nicht 5xx.
    - Skipped-Tests („can send message", „receives AI response") bleiben skipped (unchanged) — D-07 nennt nur Password-Login-Test als Ziel.
  </behavior>
  <action>
Ersetze `packages/e2e-tools/tests/chat.spec.ts` komplett durch:

```ts
import { test, expect } from '@playwright/test'

/**
 * Chat-Smoke-Tests gegen Prod (Phase 19 D-08).
 * Test-Runner ist unauthenticated → Server redirected /chat zu /login.
 * Diese Tests prüfen nur: Page lädt ohne 5xx, eine bekannte UI (Chat oder Login) ist sichtbar.
 *
 * Volle Chat-Flows mit echtem User sind im BACKLOG (Mail-Inbox-Scraping nötig für Magic-Link-Flow).
 */

test.describe('Chat Flow (prod smoke)', () => {
  test('chat page loads', async ({ page }) => {
    const response = await page.goto('/chat', { waitUntil: 'networkidle' })
    expect(response?.status(), 'HTTP status < 400').toBeLessThan(400)
    await expect(page.locator('body')).toBeVisible()
  })

  test('unauthenticated /chat shows login prompt or chat interface', async ({ page }) => {
    await page.goto('/chat', { waitUntil: 'networkidle' })
    // Unauthenticated: server redirects to /login → email input visible.
    // Authenticated (rare in CI): chat textbox visible.
    const loginEmail = page.locator('#email')
    const chatInput = page.getByRole('textbox')
    await expect(loginEmail.or(chatInput)).toBeVisible({ timeout: 10_000 })
  })

  test('/chat is not a 5xx page', async ({ page }) => {
    const response = await page.goto('/chat')
    expect(response?.status(), '/chat must not 5xx').toBeLessThan(500)
    // Final URL is either /chat (authenticated) or /login (redirect).
    const finalPath = new URL(page.url()).pathname
    expect(['/chat', '/login']).toContain(finalPath)
  })

  // TODO: Full chat flow with authenticated user (BACKLOG — needs mail-inbox-scraping for magic link)
  test.skip('can send message in chat', async ({ page }) => {
    await page.goto('/chat')
    const input = page.getByRole('textbox')
    await input.fill('Hallo, das ist ein Test')
    await page.getByRole('button', { name: /senden|send/i }).click()
    await expect(page.getByText('Hallo, das ist ein Test')).toBeVisible()
  })

  test.skip('receives AI response after sending message', async ({ page }) => {
    await page.goto('/chat')
    const input = page.getByRole('textbox')
    await input.fill('Was sind KI-Tools?')
    await page.getByRole('button', { name: /senden|send/i }).click()
    await expect(page.locator('[data-testid="ai-message"]')).toBeVisible({ timeout: 30000 })
  })
})
```
  </action>
  <verify>
    <automated>pnpm --filter @genai/e2e-tools exec playwright test chat.spec.ts --list</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/e2e-tools/tests/chat.spec.ts` contains `test.describe('Chat Flow (prod smoke)'`
    - File contains `/chat must not 5xx`
    - File contains `['/chat', '/login']` (URL-whitelist)
    - File contains `loginEmail.or(chatInput)` (flexible locator)
    - File still contains 2 `test.skip(` (chat-send tests remain skipped)
    - `pnpm --filter @genai/e2e-tools exec playwright test chat.spec.ts --list` exits 0
    - grep -c "test.skip" packages/e2e-tools/tests/chat.spec.ts == 2
  </acceptance_criteria>
  <done>chat.spec.ts ist prod-tauglich, 3 aktive Tests verlangen nur baseURL + Response-Status. Skipped-Tests unverändert.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: turbo.json e2e-Task — E2E_BASE_URL in passThroughEnv</name>
  <files>turbo.json</files>
  <read_first>
    - turbo.json (root, 49 Zeilen — tasks.e2e.passThroughEnv aktuell ["PLAYWRIGHT_*", "BASE_URL", "TEST_USER_EMAIL", "TEST_USER_PASSWORD"])
    - .planning/phases/19-password-flow-and-test-baseline/CONTEXT.md §D-08 (Override-Semantik)
  </read_first>
  <behavior>
    - `tasks.e2e.passThroughEnv` Array wird um `"E2E_BASE_URL"` erweitert
    - Reihenfolge der anderen Einträge bleibt unverändert
    - Rest von turbo.json (globalPassThroughEnv, build, dev, lint, clean, test, test:watch) NICHT anfassen
  </behavior>
  <action>
Modifiziere `turbo.json` im Projekt-Root. Finde den `e2e`-Task und ersetze den `passThroughEnv`-Array:

Von (aktuell):
```json
"e2e": {
  "dependsOn": ["^build"],
  "passThroughEnv": ["PLAYWRIGHT_*", "BASE_URL", "TEST_USER_EMAIL", "TEST_USER_PASSWORD"],
  "outputs": ["playwright-report/**", "test-results/**"]
}
```

Zu (neu):
```json
"e2e": {
  "dependsOn": ["^build"],
  "passThroughEnv": ["PLAYWRIGHT_*", "BASE_URL", "E2E_BASE_URL", "TEST_USER_EMAIL", "TEST_USER_PASSWORD"],
  "outputs": ["playwright-report/**", "test-results/**"]
}
```

Nur diese eine Zeile ändern. Reihenfolge: `E2E_BASE_URL` direkt nach `BASE_URL` einfügen (gruppiert die baseURL-Vars). Rest der Datei (alle anderen Tasks, globalPassThroughEnv, $schema) unverändert lassen.

Rationale: Ohne diesen Eintrag filtert Turbo die Env-Var heraus und `pnpm e2e` greift immer den Config-Default (Prod). Der D-08-Override via `E2E_BASE_URL=... pnpm e2e` würde sonst wirkungslos sein — würde nur via `pnpm exec playwright test` direkt funktionieren. Mit diesem Fix funktioniert beides.
  </action>
  <verify>
    <automated>grep -q '"E2E_BASE_URL"' turbo.json && node -e "JSON.parse(require('fs').readFileSync('turbo.json','utf8'))" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `turbo.json` contains the string `"E2E_BASE_URL"` (exact JSON-quoted form)
    - `grep -q '"E2E_BASE_URL"' turbo.json` exits 0
    - `node -e "JSON.parse(require('fs').readFileSync('turbo.json','utf8'))"` exits 0 (valid JSON)
    - File still contains `"TEST_USER_EMAIL"` AND `"TEST_USER_PASSWORD"` (not accidentally removed)
    - File still contains `"globalPassThroughEnv"` (top-level block intact)
    - File still contains all original tasks: `"build"`, `"dev"`, `"lint"`, `"clean"`, `"test"`, `"test:watch"`, `"e2e"`
  </acceptance_criteria>
  <done>turbo e2e-Task leitet E2E_BASE_URL durch. `pnpm e2e` respektiert den D-08-Override.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @genai/e2e-tools exec playwright test --list` grün (alle Tests parsen)
- playwright.config.ts hat Prod-Default
- Tests referenzieren E2E_BASE_URL wo nötig
- turbo.json leitet E2E_BASE_URL durch — `E2E_BASE_URL=http://localhost:3001 pnpm e2e` schwenkt tatsächlich um
- Phase-13-Auth-Tests unverändert (keine regressionen in Test-Bodies)
</verification>

<success_criteria>
- [ ] `playwright.config.ts` default baseURL = `https://tools.generation-ai.org`
- [ ] `E2E_BASE_URL=http://localhost:3001 pnpm exec playwright test` wechselt auf local
- [ ] `E2E_BASE_URL=http://localhost:3001 pnpm e2e` (via turbo) wechselt ebenfalls auf local
- [ ] `chat.spec.ts` 3 aktive Tests grün gegen Prod
- [ ] `auth.spec.ts` (gegen Prod mit Test-User-Secrets gesetzt) grün
- [ ] Keine hardcoded `http://localhost:3001` mehr
- [ ] `turbo.json` tasks.e2e.passThroughEnv enthält `E2E_BASE_URL`
</success_criteria>

<output>
After completion, create `.planning/phases/19-password-flow-and-test-baseline/19-04-SUMMARY.md`
</output>
