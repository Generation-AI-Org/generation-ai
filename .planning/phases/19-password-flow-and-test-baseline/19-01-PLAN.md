---
phase: 19-password-flow-and-test-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/tools-app/app/auth/confirm/route.ts
autonomous: true
requirements: [D-01, D-02, D-06]
must_haves:
  truths:
    - "Magic-Link-Login für User ohne has_password=true landet auf /auth/set-password?first=1"
    - "Magic-Link-Login für User mit has_password=true landet auf / (kein Re-Prompt)"
    - "Magic-Link-Login für User mit has_password=false landet auf / (Skip-Entscheidung respektiert)"
    - "Recovery-Flow bleibt unverändert: type=recovery → /auth/set-password (ohne first-Param)"
    - "packages/emails/src/templates/recovery.tsx bleibt unverändert (D-06)"
  artifacts:
    - path: "apps/tools-app/app/auth/confirm/route.ts"
      provides: "has_password-Check nach verifyOtp für magiclink/email-Types"
      contains: "has_password"
  key_links:
    - from: "apps/tools-app/app/auth/confirm/route.ts"
      to: "apps/tools-app/app/auth/set-password/page.tsx"
      via: "NextResponse.redirect mit ?first=1 bei has_password !== true"
      pattern: "set-password\\?first=1"
---

<objective>
Magic-Link-Handler (`/auth/confirm`) erweitern: Nach erfolgreichem `verifyOtp` für Non-Recovery-Types prüfen, ob `user_metadata.has_password === true`. Wenn nicht, auf `/auth/set-password?first=1` redirecten statt auf `next`/`/`. User mit `has_password=false` (explizit skipped) oder `has_password=true` gehen weiter zur App ohne Re-Prompt.

Purpose: First-Login-Prompt für Password-Setup. Respektiert Skip-Entscheidung (D-02). Recovery-Mail-Template bleibt unverändert (D-06).
Output: Erweiterte confirm-Route, existing verifyOtp-Pattern bleibt intakt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/19-password-flow-and-test-baseline/CONTEXT.md
@docs/AUTH-FLOW.md
@apps/tools-app/app/auth/confirm/route.ts

<interfaces>
<!-- Aus aktueller confirm/route.ts (35 Zeilen) — Pattern darf nicht kaputt gemacht werden. -->

Current flow:
```ts
const { error } = await supabase.auth.verifyOtp({ token_hash, type })
if (error) → redirect /login?error=...
if (type === 'recovery') → redirect /auth/set-password
else → redirect `${origin}${next}` (default '/')
```

User object from verifyOtp returns `{ user, session }` — aber aktueller Code nutzt nur `error`. Für has_password-Check brauchen wir `data.user` aus dem verifyOtp-Response ODER einen separaten `supabase.auth.getUser()`-Call nach erfolgreichem OTP. `verifyOtp` returned `{ data: { user, session }, error }` — direkter Zugriff spart Round-Trip.

Supabase user_metadata shape:
```ts
user.user_metadata: Record<string, unknown>
// Lesen: user.user_metadata?.has_password
// Werte: true | false | undefined
```

Types:
```ts
import { type EmailOtpType } from '@supabase/supabase-js'
// EmailOtpType = 'signup' | 'invite' | 'magiclink' | 'recovery' | 'email_change' | 'email'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: confirm-Route mit has_password-Check + First-Login-Redirect</name>
  <files>apps/tools-app/app/auth/confirm/route.ts</files>
  <read_first>
    - apps/tools-app/app/auth/confirm/route.ts (aktuelle Route, 35 Zeilen — verifyOtp-Pattern verstehen)
    - docs/AUTH-FLOW.md Abschnitt "Path 2: Magic Link" + "Path 5: Password-Reset" (canonical flow)
    - .planning/phases/19-password-flow-and-test-baseline/CONTEXT.md §D-01, §D-02, §D-06 (metadata-Semantik + recovery-Template)
    - LEARNINGS.md (CSP-Constraints: Route Handler, kein Layout → force-dynamic nicht nötig)
  </read_first>
  <behavior>
    - verifyOtp error → unverändert: redirect /login?error=...
    - type === 'recovery' → unverändert: redirect /auth/set-password (OHNE ?first=1, weil Password-Reset-Flow)
    - type !== 'recovery' UND data.user.user_metadata.has_password === true → redirect ${origin}${next}
    - type !== 'recovery' UND data.user.user_metadata.has_password === false → redirect ${origin}${next} (Skip respektiert, D-02)
    - type !== 'recovery' UND data.user.user_metadata.has_password === undefined (Alt-User, noch nie prompted) → redirect /auth/set-password?first=1
    - Wenn `data.user` fehlt (theoretisch unmöglich nach !error, aber defensiv): fallthrough zu next-Redirect
    - D-06: Recovery-Mail-Template (packages/emails/src/templates/recovery.tsx) wird NICHT angefasst
  </behavior>
  <action>
Modifiziere `apps/tools-app/app/auth/confirm/route.ts` wie folgt (komplettes File ersetzen, keine Teil-Edits):

```ts
import { type EmailOtpType } from '@supabase/supabase-js'
import { NextRequest, NextResponse } from 'next/server'
import { createClient as createServerClient } from '@genai/auth/server'

export async function GET(request: NextRequest) {
  const { searchParams } = new URL(request.url)
  const token_hash = searchParams.get('token_hash')
  const type = searchParams.get('type') as EmailOtpType | null
  const next = searchParams.get('next') ?? '/'

  const origin = new URL(request.url).origin

  if (!token_hash || !type) {
    console.error('Auth confirm: missing token_hash or type')
    return NextResponse.redirect(`${origin}/login?error=missing_params`)
  }

  const supabase = await createServerClient()
  const { data, error } = await supabase.auth.verifyOtp({ token_hash, type })

  if (error) {
    console.error('OTP verification error:', error.message)
    return NextResponse.redirect(
      `${origin}/login?error=${encodeURIComponent(error.message)}`
    )
  }

  // Recovery flow: always to /auth/set-password (password reset UI), D-06 — template stays
  if (type === 'recovery') {
    return NextResponse.redirect(`${origin}/auth/set-password`)
  }

  // First-Login-Prompt (D-01, D-02): Wenn has_password weder true noch false, First-Login-Screen zeigen.
  // has_password=true → hat Passwort, kein Prompt.
  // has_password=false → hat Skip gewählt, kein Re-Prompt.
  // has_password=undefined → Alt-User oder neuer User, First-Login-Screen mit Skip-Option.
  const hasPassword = data?.user?.user_metadata?.has_password
  if (hasPassword !== true && hasPassword !== false) {
    return NextResponse.redirect(`${origin}/auth/set-password?first=1`)
  }

  const redirectPath = next.startsWith('/') ? next : '/'
  return NextResponse.redirect(`${origin}${redirectPath}`)
}
```

Wichtige Notes:
- `data` aus `verifyOtp` destrukturiert (vorher nur `error`) — spart getUser()-Round-Trip.
- `hasPassword !== true && hasPassword !== false` — explizit beide Werte prüfen, nicht `!hasPassword` (false würde fälschlich prompten).
- Comments referenzieren D-01/D-02/D-06 für Traceability.
- `type === 'recovery'` bleibt exakt wie vorher: redirect zu `/auth/set-password` ohne `?first=1` — Recovery-Flow ist NICHT First-Login (D-06 sagt Mail-Template bleibt, Handling bleibt).
- `createClient as createServerClient` Import-Pattern unverändert (@genai/auth canonical, Phase 12).
- D-06 Guard: packages/emails/src/templates/recovery.tsx NICHT anfassen, nicht importieren, kein Export-Change. Dieser Plan touched ausschließlich `apps/tools-app/app/auth/confirm/route.ts`.
  </action>
  <verify>
    <automated>pnpm --filter tools-app build</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/tools-app/app/auth/confirm/route.ts` contains the string `has_password`
    - File contains `set-password?first=1`
    - File contains `user_metadata?.has_password`
    - File still contains `type === 'recovery'` AND `/auth/set-password` (recovery branch intact)
    - File still contains `supabase.auth.verifyOtp({ token_hash, type })` (canonical pattern intact)
    - File still imports from `@genai/auth/server` (not `@supabase/ssr`)
    - `pnpm --filter tools-app build` exits 0
    - grep -c "hasPassword" apps/tools-app/app/auth/confirm/route.ts >= 2
    - D-06 guard — recovery-Template unverändert: `git diff HEAD -- packages/emails/src/templates/recovery.tsx | wc -l` returns `0`
    - D-06 guard — confirm-Route importiert NICHT aus emails-Package: `grep -c "@genai/emails\\|packages/emails" apps/tools-app/app/auth/confirm/route.ts` returns `0`
  </acceptance_criteria>
  <done>Confirm-Route redirected First-Login-User zu /auth/set-password?first=1, Alt-User mit has_password=true oder explicit false gehen direkt zu next/'/'. Recovery-Flow unverändert. recovery.tsx unverändert. Build grün.</done>
</task>

</tasks>

<verification>
- `pnpm --filter tools-app build` grün
- grep confirms all acceptance criteria strings
- Kein verifyOtp-Pattern-Bruch (confirm-Route funktioniert für alle 5 EmailOtpTypes)
- D-06: `git diff HEAD -- packages/emails/src/templates/recovery.tsx` zeigt 0 Zeilen Diff
</verification>

<success_criteria>
- [ ] Magic-Link-Login für User mit `user_metadata.has_password === undefined` redirected zu `/auth/set-password?first=1`
- [ ] Magic-Link-Login für User mit `has_password === true` redirected zu `next`/`/`
- [ ] Magic-Link-Login für User mit `has_password === false` redirected zu `next`/`/` (Skip respektiert)
- [ ] Recovery-Flow (`type=recovery`) redirected zu `/auth/set-password` (OHNE `?first=1`)
- [ ] `packages/emails/src/templates/recovery.tsx` unverändert (D-06)
- [ ] `pnpm --filter tools-app build` grün
</success_criteria>

<output>
After completion, create `.planning/phases/19-password-flow-and-test-baseline/19-01-SUMMARY.md`
</output>
