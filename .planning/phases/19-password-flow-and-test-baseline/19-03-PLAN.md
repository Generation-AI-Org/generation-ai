---
phase: 19-password-flow-and-test-baseline
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - apps/tools-app/app/settings/page.tsx
  - apps/tools-app/app/settings/PasswordSection.tsx
autonomous: true
requirements: [D-03, D-04, D-05]
must_haves:
  truths:
    - "User ohne has_password sieht in /settings einen Section-Block 'Passwort setzen' mit 2-Feld-Form (Neues Passwort + Bestätigen)"
    - "User mit has_password=true sieht in /settings 'Passwort ändern' mit 3-Feld-Form (Aktuelles + Neues + Bestätigen)"
    - "Change-Mode: falsches aktuelles Passwort zeigt klaren Error, kein updateUser-Call"
    - "Erfolgreiches Setzen/Ändern: Success-Feedback + has_password=true wird (falls nicht schon true) geschrieben"
    - "Kein Redirect nach Success — User bleibt auf /settings"
  artifacts:
    - path: "apps/tools-app/app/settings/PasswordSection.tsx"
      provides: "Client-Component mit Inline-Form, 2 Modi basierend auf has_password-Prop"
      contains: "has_password"
      min_lines: 80
    - path: "apps/tools-app/app/settings/page.tsx"
      provides: "Server-Component mit PasswordSection-Mount inkl. user-Prop"
      contains: "PasswordSection"
  key_links:
    - from: "apps/tools-app/app/settings/page.tsx"
      to: "apps/tools-app/app/settings/PasswordSection.tsx"
      via: "<PasswordSection hasPassword={...} email={user.email} />"
      pattern: "PasswordSection"
    - from: "apps/tools-app/app/settings/PasswordSection.tsx"
      to: "supabase.auth.signInWithPassword"
      via: "Re-Auth-Check bei Change-Mode vor updateUser({password})"
      pattern: "signInWithPassword"
---

<objective>
In `/settings` einen neuen Passwort-Section einbauen. Server-Component liest `user.user_metadata.has_password` und übergibt an neue Client-Component `PasswordSection`, die als Inline-Form zwei Modi rendert:
- **Set-Mode** (`hasPassword !== true`): 2-Feld-Form (Neues Passwort + Bestätigen)
- **Change-Mode** (`hasPassword === true`): 3-Feld-Form (Aktuelles + Neues + Bestätigen) mit Re-Auth via `signInWithPassword` vor dem eigentlichen `updateUser({ password })`

Industrie-Standard à la GitHub/Google (D-03). Kein Mail-Loop, kein Redirect nach Success (D-05).

Purpose: Re-Entry-Point für Password-Setup aus der App heraus, ohne Magic-Link-Dependency.
Output: Neuer Passwort-Block in /settings, funktional und separiert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-password-flow-and-test-baseline/CONTEXT.md
@docs/AUTH-FLOW.md
@apps/tools-app/app/settings/page.tsx
@apps/tools-app/app/auth/set-password/page.tsx

<interfaces>
<!-- Settings ist Server-Component (force-dynamic), User-Daten via getUser(). -->

Current settings/page.tsx (70 Zeilen):
```ts
import { getUser } from '@/lib/auth'
export const dynamic = 'force-dynamic'
export default async function SettingsPage() {
  const user = await getUser()
  if (!user) redirect('/login')
  // ...sections: Account, Rechtliches, Gefahrenzone
}
```

`user` shape (from @genai/auth/server → Supabase User):
```ts
{
  id: string
  email: string | undefined
  user_metadata: Record<string, unknown>  // { has_password?: boolean }
}
```

Re-Auth-Pattern (D-04):
```ts
// 1) Verify current password (client-side, via signInWithPassword on same browser client):
const { error: reauthError } = await supabase.auth.signInWithPassword({
  email: userEmail,
  password: currentPassword,
})
if (reauthError) → show "Aktuelles Passwort stimmt nicht" error, return

// 2) Update password (reuses active session established by re-auth):
const { error } = await supabase.auth.updateUser({
  password: newPassword,
  data: { has_password: true },
})
```

Hinweis aus CONTEXT "Risks": Re-Auth via `signInWithPassword` *ersetzt* die aktive Session. Das ist OK — der User bleibt eingeloggt (gleicher User, frische Tokens), und `updateUser({ password })` ist im selben Client-Context verfügbar.

Existing `DeleteAccountButton` ist bereits ein lokaler Client-Component-Pattern (im settings/-Ordner). PasswordSection folgt demselben Muster.

Brand-Tokens (Phase 16):
- Surface card: `bg-[var(--surface)] rounded-lg border border-[var(--border)]`
- Heading: `text-xl font-semibold text-[var(--text)] mb-4` (matches existing sections)
- Inputs: `bg-[var(--bg-elevated)] border border-[var(--border)] rounded-xl` (aus set-password-page)
- Primary button: `bg-[var(--accent)] text-bg rounded-full py-2.5 px-6` (aus set-password-page)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Neue PasswordSection Client-Component</name>
  <files>apps/tools-app/app/settings/PasswordSection.tsx</files>
  <read_first>
    - apps/tools-app/app/settings/page.tsx (bestehendes Section-Muster)
    - apps/tools-app/app/settings/DeleteAccountButton.tsx (Pattern für lokale Client-Component im settings-Ordner)
    - apps/tools-app/app/auth/set-password/page.tsx (Form-Styling, Validation-Pattern, Brand-Tokens — nach Plan 02)
    - .planning/phases/19-password-flow-and-test-baseline/CONTEXT.md §D-04, §D-05
  </read_first>
  <behavior>
    - Props: `{ hasPassword: boolean; email: string }`
    - Set-Mode (hasPassword=false): 2 Inputs (newPassword, confirmPassword), Button "Passwort setzen"
    - Change-Mode (hasPassword=true): 3 Inputs (currentPassword, newPassword, confirmPassword), Button "Passwort ändern"
    - Validation vor Supabase-Call: newPassword min 8, newPassword === confirmPassword
    - Change-Mode: erst `signInWithPassword({ email, password: currentPassword })`, bei error → Message "Aktuelles Passwort stimmt nicht" und return (kein updateUser)
    - Bei success: `updateUser({ password: newPassword, data: { has_password: true } })`
    - Success-Feedback als Inline-Message (grün), Form-Felder clearen, KEIN Redirect
    - Nach Success: Component bleibt im Change-Mode (weil nach Success hasPassword effektiv true ist) — einfach durch State `hasPasswordState` gesteuert, initialisiert aus Prop, nach Success auf `true` gesetzt
    - Error-Feedback als Inline-Message (rot)
  </behavior>
  <action>
Erstelle neue Datei `apps/tools-app/app/settings/PasswordSection.tsx`:

```tsx
'use client'

import { useState } from 'react'
import { createBrowserClient } from '@genai/auth'

type Props = {
  hasPassword: boolean
  email: string
}

export function PasswordSection({ hasPassword: initialHasPassword, email }: Props) {
  const [hasPasswordState, setHasPasswordState] = useState(initialHasPassword)
  const [currentPassword, setCurrentPassword] = useState('')
  const [newPassword, setNewPassword] = useState('')
  const [confirmPassword, setConfirmPassword] = useState('')
  const [loading, setLoading] = useState(false)
  const [message, setMessage] = useState<{ type: 'success' | 'error'; text: string } | null>(null)

  const isChangeMode = hasPasswordState

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault()
    setMessage(null)

    if (newPassword.length < 8) {
      setMessage({ type: 'error', text: 'Passwort muss mindestens 8 Zeichen haben.' })
      return
    }
    if (newPassword !== confirmPassword) {
      setMessage({ type: 'error', text: 'Passwörter stimmen nicht überein.' })
      return
    }

    setLoading(true)
    const supabase = createBrowserClient()

    // D-04: Change-Mode — Re-Auth via signInWithPassword als Current-Password-Check.
    if (isChangeMode) {
      const { error: reauthError } = await supabase.auth.signInWithPassword({
        email,
        password: currentPassword,
      })
      if (reauthError) {
        setLoading(false)
        setMessage({ type: 'error', text: 'Aktuelles Passwort stimmt nicht.' })
        return
      }
    }

    // D-05: Passwort setzen + has_password=true flagen (idempotent).
    const { error } = await supabase.auth.updateUser({
      password: newPassword,
      data: { has_password: true },
    })
    setLoading(false)

    if (error) {
      setMessage({ type: 'error', text: error.message })
      return
    }

    setMessage({
      type: 'success',
      text: isChangeMode ? 'Passwort erfolgreich geändert.' : 'Passwort erfolgreich gesetzt.',
    })
    setHasPasswordState(true)
    setCurrentPassword('')
    setNewPassword('')
    setConfirmPassword('')
  }

  return (
    <form onSubmit={handleSubmit} className="space-y-4">
      {isChangeMode && (
        <div>
          <label htmlFor="currentPassword" className="block text-sm font-medium text-[var(--text-secondary)] mb-1.5">
            Aktuelles Passwort
          </label>
          <input
            id="currentPassword"
            type="password"
            value={currentPassword}
            onChange={(e) => setCurrentPassword(e.target.value)}
            required
            minLength={8}
            autoComplete="current-password"
            className="w-full px-3 py-2.5 rounded-xl bg-[var(--bg-elevated)] border border-[var(--border)] text-[var(--text)] placeholder:text-[var(--text-muted)] focus:outline-none focus:ring-2 focus:ring-[var(--accent)]/50 focus:border-[var(--accent)]"
          />
        </div>
      )}

      <div>
        <label htmlFor="newPassword" className="block text-sm font-medium text-[var(--text-secondary)] mb-1.5">
          Neues Passwort
        </label>
        <input
          id="newPassword"
          type="password"
          value={newPassword}
          onChange={(e) => setNewPassword(e.target.value)}
          placeholder="Mindestens 8 Zeichen"
          required
          minLength={8}
          autoComplete="new-password"
          className="w-full px-3 py-2.5 rounded-xl bg-[var(--bg-elevated)] border border-[var(--border)] text-[var(--text)] placeholder:text-[var(--text-muted)] focus:outline-none focus:ring-2 focus:ring-[var(--accent)]/50 focus:border-[var(--accent)]"
        />
      </div>

      <div>
        <label htmlFor="confirmNewPassword" className="block text-sm font-medium text-[var(--text-secondary)] mb-1.5">
          Passwort bestätigen
        </label>
        <input
          id="confirmNewPassword"
          type="password"
          value={confirmPassword}
          onChange={(e) => setConfirmPassword(e.target.value)}
          placeholder="Passwort wiederholen"
          required
          minLength={8}
          autoComplete="new-password"
          className="w-full px-3 py-2.5 rounded-xl bg-[var(--bg-elevated)] border border-[var(--border)] text-[var(--text)] placeholder:text-[var(--text-muted)] focus:outline-none focus:ring-2 focus:ring-[var(--accent)]/50 focus:border-[var(--accent)]"
        />
      </div>

      {message && (
        <div
          className={`p-3 rounded-xl text-sm ${
            message.type === 'success'
              ? 'bg-green-500/10 text-green-400 border border-green-500/20'
              : 'bg-red-500/10 text-red-400 border border-red-500/20'
          }`}
        >
          {message.text}
        </div>
      )}

      <button
        type="submit"
        disabled={loading || !newPassword || !confirmPassword || (isChangeMode && !currentPassword)}
        className="py-2.5 px-6 rounded-full bg-[var(--accent)] text-bg font-medium shadow-[0_0_12px_var(--accent-glow)] hover:opacity-90 focus:outline-none focus:ring-2 focus:ring-[var(--accent)]/50 disabled:opacity-50 disabled:cursor-not-allowed transition-all"
      >
        {loading
          ? 'Wird gespeichert...'
          : isChangeMode
            ? 'Passwort ändern'
            : 'Passwort setzen'}
      </button>
    </form>
  )
}
```

Wichtig:
- Umlaute: echte `ä` in „Passwörter stimmen nicht überein", „Passwort ändern" (CLAUDE.md: keine ae/oe/ue/ss).
- `autoComplete`-Attribute: `current-password` / `new-password` (Browser-Password-Manager-Integration).
- IDs `currentPassword`, `newPassword`, `confirmNewPassword` — bewusst andere IDs als set-password-page (`password`, `confirmPassword`), damit E2E-Tests in auth.spec.ts (Phase 13, Path 5) NICHT versehentlich diese Inputs greifen.
- Kein Redirect, kein Toast-System — Inline-Message ist Claude's Discretion laut CONTEXT.
  </action>
  <verify>
    <automated>test -f apps/tools-app/app/settings/PasswordSection.tsx && wc -l apps/tools-app/app/settings/PasswordSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/tools-app/app/settings/PasswordSection.tsx` exists
    - File contains `'use client'`
    - File contains `signInWithPassword` (Re-Auth-Check, D-04)
    - File contains `data: { has_password: true }` (D-05)
    - File contains `isChangeMode` (2-Modi-Logic)
    - File contains exact string `Aktuelles Passwort stimmt nicht` (error-message for failed re-auth)
    - File contains both `Passwort setzen` and `Passwort ändern` (button labels, echte Umlaute)
    - File contains `autoComplete="current-password"` AND `autoComplete="new-password"`
    - File count lines: `wc -l apps/tools-app/app/settings/PasswordSection.tsx` >= 80
    - File exports `PasswordSection` (`grep -q "export function PasswordSection"` or `export const PasswordSection`)
  </acceptance_criteria>
  <done>PasswordSection.tsx erstellt mit 2-Modi-Logic, Re-Auth-Pattern, Brand-Tokens und sauberem Export. Build-Grün-Check gehört zu Task 2 (integration).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: PasswordSection in /settings mounten</name>
  <files>apps/tools-app/app/settings/page.tsx</files>
  <read_first>
    - apps/tools-app/app/settings/page.tsx (aktuelles Layout, 70 Zeilen)
    - apps/tools-app/app/settings/PasswordSection.tsx (aus Task 1)
    - .planning/phases/19-password-flow-and-test-baseline/CONTEXT.md §D-03
  </read_first>
  <behavior>
    - Neue `<section>` zwischen "Account" und "Rechtliches" mit Heading "Passwort"
    - Section rendert `<PasswordSection hasPassword={...} email={user.email ?? ''} />`
    - `hasPassword` aus `user.user_metadata?.has_password === true` abgeleitet
    - Server-Component bleibt `async`, `force-dynamic` bleibt
  </behavior>
  <action>
Modifiziere `apps/tools-app/app/settings/page.tsx`:

1. Import ergänzen (oberhalb `DeleteAccountButton`-Import):
```ts
import { PasswordSection } from './PasswordSection'
```

2. Nach der "Account"-Section und VOR der "Rechtliches"-Section einfügen:
```tsx
<section className="mb-12">
  <h2 className="text-xl font-semibold text-[var(--text)] mb-4">Passwort</h2>
  <div className="p-4 bg-[var(--surface)] rounded-lg border border-[var(--border)]">
    <p className="text-[var(--text-secondary)] mb-4">
      {user.user_metadata?.has_password === true
        ? 'Ändere dein Passwort. Du brauchst dein aktuelles Passwort zur Bestätigung.'
        : 'Setze ein Passwort, damit du dich auch ohne Magic-Link anmelden kannst.'}
    </p>
    <PasswordSection
      hasPassword={user.user_metadata?.has_password === true}
      email={user.email ?? ''}
    />
  </div>
</section>
```

3. Alles andere unverändert lassen.

Begründung für Position (Claude's Discretion laut CONTEXT): zwischen "Account" (identity) und "Rechtliches" (meta/docs) — natürliche User-Lesereihenfolge: Identity → Credentials → Legal → Danger-Zone.
  </action>
  <verify>
    <automated>pnpm --filter tools-app build</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/tools-app/app/settings/page.tsx` contains `import { PasswordSection } from './PasswordSection'`
    - File contains `<PasswordSection`
    - File contains `user.user_metadata?.has_password === true`
    - File contains `email={user.email ?? ''}`
    - File contains heading `Passwort` in a new `<h2>` (separate from "Account" heading)
    - File still contains `redirect('/login')` (auth guard intact)
    - File still contains `DeleteAccountButton` (Danger-Zone intact)
    - File still contains `export const dynamic = 'force-dynamic'`
    - `pnpm --filter tools-app build` exits 0
    - grep -c "PasswordSection" apps/tools-app/app/settings/page.tsx >= 2
  </acceptance_criteria>
  <done>Settings-Page rendert neuen Passwort-Block zwischen Account und Rechtliches. Build grün, andere Sections intakt.</done>
</task>

</tasks>

<verification>
- `pnpm --filter tools-app build` grün
- grep confirms mount + Re-Auth-Logic
- Kein E2E-Selektor-Konflikt mit set-password-page (`#password` vs. `#newPassword`)
- force-dynamic bleibt (CSP-Constraint aus LEARNINGS.md)
</verification>

<success_criteria>
- [ ] User ohne `has_password`: `/settings` zeigt 2-Feld-Form „Passwort setzen"
- [ ] User mit `has_password=true`: `/settings` zeigt 3-Feld-Form „Passwort ändern"
- [ ] Falsches aktuelles Passwort → Error „Aktuelles Passwort stimmt nicht", kein Server-State-Change
- [ ] Korrekter Flow → Success-Message + Form cleared + Mode wechselt auf „ändern"
- [ ] Kein Redirect nach Success
- [ ] Build grün, Delete-Account-Button + andere Sections funktional
</success_criteria>

<output>
After completion, create `.planning/phases/19-password-flow-and-test-baseline/19-03-SUMMARY.md`
</output>
