---
phase: 19-password-flow-and-test-baseline
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - .github/workflows/ci.yml
  - .planning/phases/19-password-flow-and-test-baseline/MANUAL-STEPS.md
  - .changeset/phase-19-password-flow.md
autonomous: false
requirements: [D-07, D-09]
user_setup:
  - service: supabase
    why: "E2E-Tests brauchen realen Test-User für Password-Login, Magic-Link-Generation, Recovery-Flow"
    dashboard_config:
      - task: "Test-User anlegen (z.B. e2e-tester@generation-ai.test mit sicherem Passwort)"
        location: "Supabase Dashboard → Authentication → Users → Add user → Create new user"
      - task: "Test-User bestätigen (Email-Verifikation im Dashboard auf 'confirmed' setzen)"
        location: "Supabase Dashboard → Users → edit user → 'Email Confirmed' = true"
  - service: github
    why: "E2E-CI braucht Secrets für Test-User-Login gegen Prod"
    dashboard_config:
      - task: "TEST_USER_EMAIL als Repo-Secret setzen"
        location: "GitHub → Repo → Settings → Secrets and variables → Actions → New repository secret"
      - task: "TEST_USER_PASSWORD als Repo-Secret setzen"
        location: "GitHub → Repo → Settings → Secrets and variables → Actions → New repository secret"
    env_vars:
      - name: TEST_USER_EMAIL
        source: "GitHub Repo Secret (Setup durch Luca)"
      - name: TEST_USER_PASSWORD
        source: "GitHub Repo Secret (Setup durch Luca)"
must_haves:
  truths:
    - "CI-Workflow injiziert TEST_USER_EMAIL + TEST_USER_PASSWORD in E2E-Job"
    - "CI-Workflow nutzt E2E_BASE_URL statt BASE_URL (optional — Prod-Default reicht)"
    - "MANUAL-STEPS.md dokumentiert Test-User-Setup + GH-Secrets-Setup"
    - "Changeset-Datei existiert für v4.4.0 minor release (Phase 19 neues Feature)"
    - "Luca hat Test-User + Secrets gesetzt und Build-Run grün (Checkpoint)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "E2E-Job mit Test-User-Secret-Binding"
      contains: "TEST_USER_EMAIL"
    - path: ".planning/phases/19-password-flow-and-test-baseline/MANUAL-STEPS.md"
      provides: "Dokumentation Manual-Setup für Luca"
      min_lines: 30
    - path: ".changeset/phase-19-password-flow.md"
      provides: "Changeset für minor release v4.4.0"
      contains: "@genai/tools-app"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "packages/e2e-tools/fixtures/test-user.ts"
      via: "env.TEST_USER_EMAIL + env.TEST_USER_PASSWORD werden via dotenv/playwright.config in requireTestUser() verfügbar"
      pattern: "TEST_USER_(EMAIL|PASSWORD)"
---

<objective>
Phase-Closure: CI-Workflow auf E2E-Secrets + Prod-URL updaten. MANUAL-STEPS.md im Phase-Ordner anlegen (Supabase-Test-User + GitHub-Repo-Secrets). Changeset für v4.4.0 erzeugen. Final Build + Playwright-Checkpoint mit Luca: Test-User gesetzt → `pnpm e2e` grün gegen Prod.

Purpose: Closure-Gate für Phase 19. Ohne Manual-Setup ist der Password-Login-E2E-Test nicht lauffähig.
Output: CI grün, MANUAL-STEPS dokumentiert, Changeset da, Luca-Verifikation abgehakt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-password-flow-and-test-baseline/CONTEXT.md
@.planning/phases/17-auth-extensions/MANUAL-STEPS.md
@.github/workflows/ci.yml
@packages/e2e-tools/fixtures/test-user.ts
@packages/e2e-tools/playwright.config.ts

<interfaces>
<!-- CI e2e-Job läuft aktuell nur wenn STAGING_URL-var gesetzt ist, übergibt BASE_URL. -->

Current .github/workflows/ci.yml e2e-Job (Zeilen 48-92):
```yaml
e2e:
  runs-on: ubuntu-latest
  needs: build-and-test
  if: ${{ vars.STAGING_URL != '' }}
  steps:
    # checkout, pnpm setup, Node 20, install, cache playwright, install chromium
    - name: E2E Tests
      run: pnpm e2e
      env:
        BASE_URL: ${{ vars.STAGING_URL }}
    - name: Upload Playwright Report
      # ...
```

Gewünschter Zustand nach Plan 05:
- E2E-Job läuft IMMER auf main-push + PR (Prod ist Default-baseURL, kein STAGING_URL mehr nötig)
- Secrets für Test-User durchgereicht
- Optional: `E2E_BASE_URL` als repo-var setzen lassen (Luca-Checkpoint), fällt auf Prod-Default zurück

Phase 17 MANUAL-STEPS.md als Referenz (Struktur: H1, Kontext, H2 pro Task, Copy-Paste-Blöcke).

Changeset-Schema (aus CLAUDE.md, projekt verwendet @changesets/changelog-github):
- Dateiname: `.changeset/{slug}.md`
- Frontmatter YAML Block zwischen `---` Markern mit Paket + Bump-Type
- Body: 1-2 Sätze Beschreibung

Linked packages: `website, tools-app` werden zusammen versioniert. Phase 19 touched tools-app → Changeset trägt `@genai/tools-app: minor` (linked mit @genai/website). Package namen checken (from apps/tools-app/package.json = @genai/tools-app).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CI-Workflow mit Test-User-Secrets + Prod-Default</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (aktuell 92 Zeilen)
    - .planning/phases/19-password-flow-and-test-baseline/CONTEXT.md §D-07
    - packages/e2e-tools/fixtures/test-user.ts (Env-Var-Namen: TEST_USER_EMAIL, TEST_USER_PASSWORD)
  </read_first>
  <behavior>
    - E2E-Job bekommt `TEST_USER_EMAIL` und `TEST_USER_PASSWORD` als env aus Repo-Secrets
    - `E2E_BASE_URL` wird gesetzt aus Repo-Var (fallback: leer → Config nimmt Prod-Default)
    - `BASE_URL`-env-Verwendung entfernen (Prod-Default aus playwright.config.ts greift)
    - `if: ${{ vars.STAGING_URL != '' }}` entfernen — E2E läuft jetzt immer (gegen Prod)
  </behavior>
  <action>
In `.github/workflows/ci.yml`, ersetze den kompletten `e2e:`-Job (aktuell Zeilen 48-92) durch den folgenden Block. Den `build-and-test`-Job und den env-Block am File-Top NICHT anfassen.

```yaml
  e2e:
    runs-on: ubuntu-latest
    needs: build-and-test

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('packages/e2e-tools/package.json') }}

      - name: Install Playwright browsers
        run: pnpm --filter @genai/e2e-tools exec playwright install --with-deps chromium

      - name: E2E Tests
        run: pnpm e2e
        env:
          # D-08: E2E_BASE_URL optional override. Leer → playwright.config.ts nimmt Prod-Default.
          E2E_BASE_URL: ${{ vars.E2E_BASE_URL }}
          # D-07: Test-User-Secrets für Password-Login-Test. Muss in Repo-Secrets gesetzt sein (siehe MANUAL-STEPS.md).
          TEST_USER_EMAIL: ${{ secrets.TEST_USER_EMAIL }}
          TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD }}
          # Admin-Key weiterhin für Magic-Link/Recovery-Generation nötig (Phase 13 helper).
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

      - name: Upload Playwright Report
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: packages/e2e-tools/playwright-report/
          retention-days: 7
```

Hinweis: Wenn `TEST_USER_EMAIL`/`TEST_USER_PASSWORD` nicht gesetzt sind, wirft `requireTestUser()` und die betroffenen Tests failen. Das ist gewollt — zwingt Luca zum Setup-Schritt (MANUAL-STEPS.md Task 2).
  </action>
  <verify>
    <automated>grep -c "TEST_USER_EMAIL" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - File `.github/workflows/ci.yml` contains `TEST_USER_EMAIL: ${{ secrets.TEST_USER_EMAIL }}`
    - File contains `TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD }}`
    - File contains `E2E_BASE_URL: ${{ vars.E2E_BASE_URL }}`
    - File does NOT contain `BASE_URL: ${{ vars.STAGING_URL }}` (removed, legacy)
    - File does NOT contain `if: ${{ vars.STAGING_URL != '' }}` (removed — E2E now always runs)
    - File still contains the build-and-test job intact (`Build`, `Lint`, `Unit Tests` steps)
    - grep -c "TEST_USER_EMAIL" .github/workflows/ci.yml >= 1
  </acceptance_criteria>
  <done>CI-Workflow geballt: E2E-Job greift Secrets, läuft immer, nutzt Prod-Default.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: MANUAL-STEPS.md im Phase-Ordner anlegen</name>
  <files>.planning/phases/19-password-flow-and-test-baseline/MANUAL-STEPS.md</files>
  <read_first>
    - .planning/phases/17-auth-extensions/MANUAL-STEPS.md (Struktur-Vorbild, falls existiert — sonst Format selbst wählen)
    - .planning/phases/19-password-flow-and-test-baseline/CONTEXT.md §D-09 + Env-Vars-Abschnitt
    - packages/e2e-tools/fixtures/test-user.ts (Env-Var-Namen, exakter Error-String)
  </read_first>
  <behavior>
    - Markdown-File mit 2 Hauptabschnitten: Supabase-Test-User + GitHub-Secrets
    - Pro Abschnitt: Why, Steps (nummeriert), Verify
    - Warnhinweise: Test-Mail-Adresse sollte keine Forwarding (ImprovMX) nutzen, damit die Inbox nicht Lucas Alltag verschmutzt
    - Rollback-Hinweise: wie User/Secrets wieder entfernen
    - Inhaltliche Sections: (1) Supabase Test-User anlegen, (2) GitHub Repo Secrets setzen, (3) Optional E2E_BASE_URL Repo-Variable, (4) Local .env.test.local, (5) Rollback
  </behavior>
  <action>
Erstelle neue Datei `.planning/phases/19-password-flow-and-test-baseline/MANUAL-STEPS.md`. Content:

Header: `# Phase 19 — Manual Steps` + kurze Intro dass Luca diese Setup-Schritte einmalig durchführt damit E2E-Baseline grün läuft. Kontext-Hinweis auf Phase 19 D-07/D-09.

Section 1 — **Supabase Test-User anlegen**:
- Warum: `packages/e2e-tools/tests/auth.spec.ts` Path 1 loggt sich mit realen Credentials ein. Ohne User failed `requireTestUser()` in `packages/e2e-tools/fixtures/test-user.ts`.
- Schritte:
  1. Supabase Dashboard öffnen → Projekt `wbohulnuwqrhystaamjc` → Authentication → Users
  2. „Add user" → „Create new user"
  3. Email: z.B. `e2e-tester@generation-ai.test` — ACHTUNG Fake-Mail die NICHT via ImprovMX geforwarded wird (vermeide `admin@generation-ai.org` o.ä.)
  4. Password: sicheres Passwort via `openssl rand -base64 24` generieren, kopieren
  5. „Auto Confirm User" = ON
  6. „Create user" klicken
  7. Verifizieren: User erscheint mit Status „Confirmed"
- Wichtig-Kasten: Der Test-User darf kein `user_metadata.has_password=true` haben, sonst greift der First-Login-Prompt (Phase 19 Plan 01) und Login-Test macht anderen Flow. Supabase `Add user` setzt kein Metadata default → passt.
- Recovery-Hinweis: Falls Password-Reset-E2E-Test (Path 5) den User nicht sauber zurücksetzt → Dashboard → User → „Reset Password" manuell.

Section 2 — **GitHub Repo Secrets setzen**:
- Warum: CI injiziert Credentials als env in E2E-Job.
- Schritte:
  1. GitHub → Repo `Generation-AI-Org/generation-ai` → Settings → Secrets and variables → Actions
  2. „New repository secret" → Name `TEST_USER_EMAIL` → Value aus Schritt 1.3
  3. „New repository secret" → Name `TEST_USER_PASSWORD` → Value aus Schritt 1.4
- Verify: Secrets-Liste zeigt beide Namen (Values nicht sichtbar nach Anlage).

Section 3 — **(Optional) Repo-Variable E2E_BASE_URL**:
- Default ohne Setup: `https://tools.generation-ai.org` (via `playwright.config.ts`).
- Bei Bedarf (Preview-URL testen): GitHub → Settings → Secrets and variables → Actions → Variables-Tab → New variable → Name `E2E_BASE_URL` → z.B. `https://tools-preview-xxx.vercel.app`.
- Entfernen: gleiche Stelle, „Remove variable".

Section 4 — **Local .env.test.local (für Dev-Runs)**:
- Copy-Paste-Block mit Erklärung (keine nested code fences — nutze einrückung oder prose):
  - `TEST_USER_EMAIL=<email aus Schritt 1.3>`
  - `TEST_USER_PASSWORD=<password aus Schritt 1.4>`
  - Optional: `E2E_BASE_URL=http://localhost:3001` für Local-Dev-Server-Tests
  - `NEXT_PUBLIC_SUPABASE_URL=https://wbohulnuwqrhystaamjc.supabase.co`
  - `SUPABASE_SERVICE_ROLE_KEY=<aus Supabase Dashboard → Settings → API → service_role key>`
- Hinweis: `.env.test.local` muss in `.gitignore` stehen, verifizieren — NIE committen.
- Ausführen: `pnpm --filter @genai/e2e-tools exec playwright test`

Section 5 — **Rollback**:
- Test-User löschen: Supabase Dashboard → Users → Delete user
- Repo-Secrets löschen: Settings → Secrets → Delete
- CI wird dann betroffene Tests failen, markiert Status quo.

Footer: `*Phase: 19-password-flow-and-test-baseline · erstellt: 2026-04-19*`

Formatierung: nutze gewöhnliches Markdown (H1, H2, nummerierte Listen, inline-code `code`). Vermeide triple-backtick Code-Fences in diesem Plan-File (wir wollen keine nested fences). Im resultierenden MANUAL-STEPS.md sind Triple-Backticks explizit OK — nur nicht in den Instructions hier drin.

Falls Phase 17 MANUAL-STEPS.md (unter .planning/phases/17-auth-extensions/MANUAL-STEPS.md) existiert: dessen H1/H2-Struktur übernehmen für Konsistenz.
  </action>
  <verify>
    <automated>test -f .planning/phases/19-password-flow-and-test-baseline/MANUAL-STEPS.md && wc -l .planning/phases/19-password-flow-and-test-baseline/MANUAL-STEPS.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/19-password-flow-and-test-baseline/MANUAL-STEPS.md` exists
    - File contains `TEST_USER_EMAIL`
    - File contains `TEST_USER_PASSWORD`
    - File contains `Supabase Dashboard`
    - File contains `Auto Confirm User` (kritischer Setup-Hinweis)
    - File contains `.env.test.local` (Dev-Setup)
    - File contains `ImprovMX` (Warning — Memory-Eintrag)
    - File contains `E2E_BASE_URL` (optional repo-var)
    - `wc -l` >= 30
  </acceptance_criteria>
  <done>MANUAL-STEPS.md dokumentiert alle Manual-Schritte für Luca's einmaliges Setup.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Changeset für Phase 19 v4.4.0 minor</name>
  <files>.changeset/phase-19-password-flow.md</files>
  <read_first>
    - ./CLAUDE.md (Changeset-Regeln: Pflicht bei jeder Änderung, linked tools-app+website, minor für User-facing Feature)
    - .changeset/config.json (falls existiert — Schema-Verify)
    - apps/tools-app/package.json (für exakten Package-Namen)
    - .changeset/README.md (falls existiert — Standard-Changeset-Format)
  </read_first>
  <behavior>
    - Datei im `.changeset/`-Ordner anlegen
    - YAML-Frontmatter mit einem Eintrag `"@genai/tools-app": minor` (linked → bumpt auch @genai/website)
    - Body: 2-3 Sätze deutsch, was neu ist + dass E2E-Baseline repariert ist
  </behavior>
  <action>
Erstelle neue Datei `.changeset/phase-19-password-flow.md`.

Struktur:
- Zeile 1: Frontmatter-Start-Marker (drei Dashes)
- Zeile 2: `"@genai/tools-app": minor`
- Zeile 3: Frontmatter-End-Marker (drei Dashes)
- Zeile 4: Leerzeile
- Zeile 5-7: Body-Text (Deutsch):

Body-Text exakt so (mit echten Umlauten, kein ae/oe/ue/ss):

Absatz 1: „Password-Flow für eingeloggte User: First-Login-Prompt beim Magic-Link-Login mit Skip-Option, und nachträgliches Setzen/Ändern des Passworts inline in /settings (Re-Auth via aktuelles Passwort bei Change). Recovery-Mail-Flow bleibt unverändert."

Absatz 2: „E2E-Baseline repariert: Tests laufen jetzt default gegen Prod (https://tools.generation-ai.org) mit realem Test-User aus GitHub-Secrets. chat.spec.ts angepasst für unauthenticated Prod-Reality (redirect to /login)."

**Wichtig:** Vorab prüfen: wenn tools-app in `apps/tools-app/package.json` "name" anders heißt als `@genai/tools-app`, den exakten Namen aus package.json übernehmen. Primäre Erwartung aus CLAUDE.md (`linked: [website, tools-app]`) → Package-Namen sind `@genai/website` und `@genai/tools-app`.

Wenn `.changeset/config.json` `linked: [["@genai/website", "@genai/tools-app"]]` hat, reicht ein Eintrag — die linked-Konfiguration bumpt beide. Falls nicht linked, zweiten YAML-Eintrag für `"@genai/website": minor` in selber Frontmatter-Zeile nach Komma hinzufügen.
  </action>
  <verify>
    <automated>test -f .changeset/phase-19-password-flow.md && head -5 .changeset/phase-19-password-flow.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.changeset/phase-19-password-flow.md` exists
    - File starts with YAML frontmatter delimiter
    - File contains `@genai/tools-app` (or whatever the actual package name in package.json is)
    - File contains `minor`
    - File body mentions `Password` and `E2E`
  </acceptance_criteria>
  <done>Changeset für v4.4.0 minor release erzeugt, linked packages werden beim nächsten `pnpm version`-Run bumped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Luca-Checkpoint — Manual Setup + Full Build + E2E Smoke-Verify</name>
  <files>N/A (checkpoint)</files>
  <action>Human verification — see what-built + how-to-verify below.</action>
  <what-built>
    - Plan 01: confirm-Route redirected First-Login-User zu /auth/set-password?first=1
    - Plan 02: Set-Password-Page hat Skip-Button + metadata-Writes
    - Plan 03: /settings hat neuen Passwort-Section (Set/Change mit Re-Auth)
    - Plan 04: Playwright-Config gegen Prod, chat.spec aufgeräumt
    - Plan 05: CI mit Secrets, MANUAL-STEPS.md, Changeset
  </what-built>
  <how-to-verify>
    1. **MANUAL-STEPS.md durchführen:**
       - Supabase Dashboard: Test-User `e2e-tester@generation-ai.test` anlegen (Auto Confirm ON)
       - GitHub Repo Secrets: `TEST_USER_EMAIL` + `TEST_USER_PASSWORD` setzen
       - Bestätigen: "Setup done"
    2. **Lokal Build beider Apps:**
       - `pnpm install`
       - `pnpm build`
       - Erwartet: Beide Apps grün. Wenn Build fehlschlägt → STOP, Fix erforderlich.
    3. **Lokal E2E gegen Prod (Smoke-Subset):**
       - `cd packages/e2e-tools`
       - `.env.test.local` mit TEST_USER_* + SUPABASE_SERVICE_ROLE_KEY füllen (siehe MANUAL-STEPS.md §4)
       - `pnpm exec playwright test smoke.spec.ts chat.spec.ts --reporter=list`
       - Erwartet: Alle Smoke + Chat-Tests grün.
    4. **Optional — Full Auth-Suite lokal:**
       - `pnpm exec playwright test auth.spec.ts --reporter=list`
       - Erwartet: 9 passed, 2 skipped. Falls Path 5 (Password Reset) fehlschlägt → Test-User-Password-Cleanup am Ende prüfen, ggf. manuell in Supabase zurücksetzen.
    5. **Manual UX-Verifikation gegen Prod (mit Test-User):**
       - a) `https://tools.generation-ai.org/login` → Magic-Link anfordern für Test-User → Link klicken → erwartet: Redirect zu `/auth/set-password?first=1` mit sichtbarem „Später setzen"-Button
       - b) „Später setzen" klicken → erwartet: Redirect auf `/`, in Supabase Dashboard prüfen dass `user_metadata.has_password === false`
       - c) `/settings` öffnen → erwartet: „Passwort"-Section mit 2-Feld-Form (weil has_password !== true)
       - d) Passwort setzen → erwartet: Success-Message, Mode wechselt zu Change (3 Felder)
       - e) Zweiten Magic-Link anfordern → erwartet: KEIN Re-Prompt, direkt auf `/`
    6. **CI-Run checken:** Nach Push auf main oder PR → GitHub Actions → `e2e`-Job muss grün sein.
  </how-to-verify>
  <verify>
    <automated>echo "manual checkpoint — see how-to-verify"</automated>
  </verify>
  <acceptance_criteria>
    - Luca antwortet mit "approved" (alle Schritte grün)
  </acceptance_criteria>
  <done>Luca hat "approved" signalisiert — Phase 19 ist done.</done>
  <resume-signal>
    Antworte mit einem der folgenden:
    - "approved" — alle Schritte grün, Phase 19 ist done
    - "build failed: {Logs}" — Build-Fehler
    - "e2e failed: {Logs}" — Test-Fehler (Details: welcher Test, welche Zeile)
    - "ux issue: {Beschreibung}" — UX-Verifikation hat Bug gezeigt
    - "supabase setup incomplete" — Test-User/Secrets nicht angelegt
  </resume-signal>
</task>

</tasks>

<verification>
- Alle 4 Tasks grün
- Luca's approved-Signal empfangen
- CI e2e-Job grün im nächsten Run
</verification>

<success_criteria>
- [ ] `.github/workflows/ci.yml` enthält Test-User-Secret-Bindings
- [ ] MANUAL-STEPS.md existiert, dokumentiert komplett
- [ ] Changeset-File existiert
- [ ] Build beider Apps grün
- [ ] E2E lokal + CI grün gegen Prod (nach Manual-Setup)
- [ ] Manual UX-Flow funktioniert end-to-end (First-Login → Skip → Settings → Change)
</success_criteria>

<output>
After completion, create `.planning/phases/19-password-flow-and-test-baseline/19-05-SUMMARY.md`
</output>
