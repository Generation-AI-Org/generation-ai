---
phase: 19-password-flow-and-test-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/tools-app/app/auth/set-password/page.tsx
autonomous: true
requirements: [D-01, D-02, D-05]
must_haves:
  truths:
    - "Set-Password-Page erkennt ?first=1 und zeigt Skip-Button"
    - "Skip-Button setzt user_metadata.has_password = false und redirected auf /"
    - "Erfolgreiches Passwort-Setzen setzt user_metadata.has_password = true"
    - "Page ohne ?first=1 (Recovery-Flow) zeigt keinen Skip-Button (unverändertes Verhalten)"
  artifacts:
    - path: "apps/tools-app/app/auth/set-password/page.tsx"
      provides: "First-Login-Mode mit Skip-Button + metadata-Write bei Success/Skip"
      contains: "has_password"
  key_links:
    - from: "apps/tools-app/app/auth/set-password/page.tsx"
      to: "supabase.auth.updateUser"
      via: "updateUser({ data: { has_password: true|false } }) + updateUser({ password }) auf Success"
      pattern: "updateUser\\(\\{\\s*data:\\s*\\{\\s*has_password"
---

<objective>
Set-Password-Page (`/auth/set-password`) erweitern: Query-Param `?first=1` aktiviert First-Login-Mode — zeigt Skip-Button („Später setzen"), setzt bei Klick `user_metadata.has_password = false` (D-02) und redirected zu `/`. Erfolgreiches Passwort-Setzen setzt `user_metadata.has_password = true` (D-05). Recovery-Flow (ohne `?first=1`) bleibt unverändert sichtbar (kein Skip-Button).

Purpose: First-Login-Screen mit expliziter Opt-out-Möglichkeit.
Output: Erweiterte Set-Password-Page, bestehende Password-Reset-UX intakt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-password-flow-and-test-baseline/CONTEXT.md
@docs/AUTH-FLOW.md
@apps/tools-app/app/auth/set-password/page.tsx

<interfaces>
<!-- Aus aktueller set-password/page.tsx (139 Zeilen, Client-Component). -->

Current handleSubmit:
```ts
const supabase = createBrowserClient()
const { error } = await supabase.auth.updateUser({ password })
if (!error) {
  setMessage({ type: 'success', text: 'Passwort erfolgreich gesetzt!' })
  setTimeout(() => window.location.href = '/', 1500)
}
```

Supabase metadata-write-API (verwendet im Client, Phase 12 canonical):
```ts
// Single call setzt beide (password + metadata):
await supabase.auth.updateUser({ password, data: { has_password: true } })

// Metadata-only (für Skip):
await supabase.auth.updateUser({ data: { has_password: false } })
```

Next.js 15 App Router — searchParams in Client Components via `useSearchParams`:
```ts
'use client'
import { useSearchParams } from 'next/navigation'
// ...
const searchParams = useSearchParams()
const isFirstLogin = searchParams.get('first') === '1'
```

Brand-Tokens (Phase 16):
- Primary: `bg-[var(--accent)] text-bg` (neon)
- Secondary/Text-Button: `text-[var(--text-muted)] hover:text-[var(--text-secondary)]`
- Surface: `bg-[var(--bg-elevated)] border-[var(--border)]`
- Spacing & Rounded: `rounded-xl`, `py-2.5 px-6` (matches existing submit button)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: useSearchParams + Skip-Button + has_password-metadata bei Success/Skip</name>
  <files>apps/tools-app/app/auth/set-password/page.tsx</files>
  <read_first>
    - apps/tools-app/app/auth/set-password/page.tsx (139 Zeilen, bestehende Form & Styling)
    - .planning/phases/19-password-flow-and-test-baseline/CONTEXT.md §D-01, §D-02, §D-05
    - packages/auth/ (exploration — `createBrowserClient` export, matches @genai/auth)
    - LEARNINGS.md (Phase ist Client-Component → CSP-Nonce irrelevant, aber keine inline scripts)
  </read_first>
  <behavior>
    - `useSearchParams()` liest `first` Query-Param; `isFirstLogin = searchParams.get('first') === '1'`
    - Heading bleibt "Neues Passwort setzen" (passend für beide Modi)
    - Bei `isFirstLogin=true`: unter Submit-Button ein weiterer Button "Später setzen" (Text-Button-Style, kein Primary)
    - Click "Später setzen" → `updateUser({ data: { has_password: false } })` → `window.location.href = '/'` (auch bei error → trotzdem redirect, damit User nicht hängt; error wird nur geloggt)
    - Submit (Passwort setzen): `updateUser({ password, data: { has_password: true } })` in einem Call
    - Bei `isFirstLogin=false`: alter Back-Link "Zurück zur App" bleibt sichtbar (unverändertes Recovery-Verhalten)
    - Bei `isFirstLogin=true`: Back-Link "Zurück zur App" wird ausgeblendet (User soll entweder setzen oder skippen, nicht zurück zur App ohne Entscheidung)
  </behavior>
  <action>
Modifiziere `apps/tools-app/app/auth/set-password/page.tsx`. Konkrete Änderungen:

1. Imports ergänzen:
```ts
'use client'

import { useState } from 'react'
import { useSearchParams } from 'next/navigation'
import { createBrowserClient } from '@genai/auth'
import Link from 'next/link'
```

2. In der Component vor `useState`-Calls:
```ts
const searchParams = useSearchParams()
const isFirstLogin = searchParams.get('first') === '1'
```

3. Neuer State für Skip-Loading:
```ts
const [skipping, setSkipping] = useState(false)
```

4. `handleSubmit` — `updateUser`-Call ändern zu (setzt password UND has_password in einem Call, D-05):
```ts
const { error } = await supabase.auth.updateUser({
  password,
  data: { has_password: true },
})
```
(Kommentar drüber: `// Set password + mark account as having password (D-05, D-01)`)

5. Neuer `handleSkip`-Handler (zwischen handleSubmit und return):
```ts
async function handleSkip() {
  setSkipping(true)
  const supabase = createBrowserClient()
  // D-02: Skip setzt has_password=false explizit, damit confirm-route beim nächsten Magic-Link nicht re-prompt'et.
  const { error } = await supabase.auth.updateUser({
    data: { has_password: false },
  })
  if (error) {
    console.error('Skip metadata write failed (redirecting anyway):', error.message)
  }
  window.location.href = '/'
}
```

6. Im JSX nach dem Submit-Button (innerhalb des `<form>` NICHT weil `type="button"` sonst form-submit triggert → ausserhalb `<form>`, direkt darunter):
```tsx
{isFirstLogin && (
  <button
    type="button"
    onClick={handleSkip}
    disabled={loading || skipping}
    className="mx-auto block mt-3 py-2 px-6 text-sm text-[var(--text-muted)] hover:text-[var(--text-secondary)] disabled:opacity-50 transition-colors"
  >
    {skipping ? 'Wird gespeichert...' : 'Später setzen'}
  </button>
)}
```

7. Back-Link ("Zurück zur App") conditional rendern — nur wenn NICHT isFirstLogin:
```tsx
{!isFirstLogin && (
  <div className="mt-8 text-center">
    <Link href="/" className="text-sm text-[var(--text-muted)] hover:text-[var(--text-secondary)] transition-colors">
      &larr; Zurück zur App
    </Link>
  </div>
)}
```

Alles andere unverändert lassen — Validation, Error-States, Success-Toast, Styling, Header-Struktur.

Hinweis: `useSearchParams` in Next.js 15 Client Components — Page ist bereits `'use client'` ✓. Wenn der Linter eine Suspense-Boundary verlangt, wrap die Component in einer inner-Function und export mit Suspense aus next/react. Zunächst direkt probieren, falls Build-Error → Suspense-Wrapper ergänzen:
```tsx
import { Suspense } from 'react'
function SetPasswordInner() { /* existing component body */ }
export default function SetPasswordPage() {
  return <Suspense fallback={null}><SetPasswordInner /></Suspense>
}
```
  </action>
  <verify>
    <automated>pnpm --filter tools-app build</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/tools-app/app/auth/set-password/page.tsx` contains `useSearchParams`
    - File contains `searchParams.get('first')`
    - File contains `isFirstLogin`
    - File contains `data: { has_password: true }` (password + metadata write on submit)
    - File contains `data: { has_password: false }` (skip metadata write)
    - File contains `Später setzen` (skip button label, echte Umlaute)
    - File still contains `#password` and `#confirmPassword` (existing form IDs intact — E2E-Tests in auth.spec.ts nutzen diese)
    - File still contains `Passwort erfolgreich gesetzt` (existing success-toast intact — E2E in auth.spec.ts nutzt den String)
    - File still contains `passwort speichern` (case-insensitive, submit button aria — E2E nutzt `{ name: /passwort speichern/i }`)
    - `pnpm --filter tools-app build` exits 0
    - grep -c "has_password" apps/tools-app/app/auth/set-password/page.tsx >= 2
  </acceptance_criteria>
  <done>Set-Password-Page hat First-Login-Mode mit Skip-Button, beide Paths schreiben korrekt has_password-Metadata. Recovery-Flow (kein ?first=1) verhält sich identisch zur pre-Plan-Version. Build grün, existierende E2E-Strings (#password, #confirmPassword, "Passwort erfolgreich gesetzt", "passwort speichern") bleiben.</done>
</task>

</tasks>

<verification>
- `pnpm --filter tools-app build` grün
- grep confirms metadata writes for both success + skip paths
- Bestehende E2E-Test-Selektoren (`#password`, `#confirmPassword`, success-toast, submit-button-name) unverändert → Phase-13-Regression-Guard
</verification>

<success_criteria>
- [ ] `/auth/set-password?first=1` zeigt Skip-Button "Später setzen"
- [ ] Skip setzt `user_metadata.has_password = false` und redirected `/`
- [ ] Submit setzt Passwort + `user_metadata.has_password = true` in einem Call
- [ ] `/auth/set-password` (ohne `?first=1`) zeigt KEINEN Skip-Button, "Zurück zur App" bleibt
- [ ] Build grün, bestehender Recovery-E2E-Test (Phase 13 Path 5) bricht nicht
</success_criteria>

<output>
After completion, create `.planning/phases/19-password-flow-and-test-baseline/19-02-SUMMARY.md`
</output>
