---
phase: 22.6
plan: 09
type: execute
wave: 7
branch: feature/phase-22.6-pre-launch-polish
depends_on:
  - 22.6-07
  - 22.6-08
files_modified:
  - apps/tools-app/components/layout/GlobalLayout.tsx
  - packages/e2e-tools/tests/tools-app.spec.ts
autonomous: false
requirements:
  - B-req-4
  - B-req-5
must_haves:
  truths:
    - "tools-app header contains 5 nav-items: Events · Tools · Community · Für Partner · Über uns"
    - "Each nav-item links to the correct destination (Events/Community/Partner/About → generation-ai.org/*, Tools → tools.generation-ai.org or current domain)"
    - "Tools item is marked as active (aria-current='page' + visual emphasis)"
    - "Cross-domain links use <a href> NOT Next.js <Link> (full page-load, B-05)"
    - "Mobile burger menu contains the same nav items + Primary CTA prominently"
    - "Logged-in user menu (Settings/Signout) still functional — Phase 12/19 regression check"
    - "Sticky-Nav behavior verified to match website nav (no drift, B-03 already verified no drift exists per RESEARCH §9)"
  artifacts:
    - path: "apps/tools-app/components/layout/GlobalLayout.tsx"
      provides: "Header block with full nav-items mirror, mobile burger menu, sync-anchor comment intact from Plan 07"
      contains: "Über uns"
  key_links:
    - from: "apps/tools-app/components/layout/GlobalLayout.tsx"
      to: "https://generation-ai.org/events|community|partner|about"
      via: "5 anchor tags with cross-domain hrefs"
      pattern: "generation-ai.org/(events|community|partner|about)"
---

<objective>
Track B closer: complete the nav-item mirror per B-08 (5 cross-domain links + Tools as active), add mobile burger menu, and run the full regression sweep (logged-in flow, sticky-nav side-by-side).

This plan finishes Track B and the entire Phase 22.6.

Purpose: tools-app header now feels structurally identical to the website nav. Visitors moving between domains experience continuity. Track A's `/events` is the new nav target so it must be live (Track A done — guaranteed by depends_on chain).
Output: Full nav rendered, mobile burger functional, all 5 Track B Playwright tests green, Lucas verifies side-by-side parity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/22.6-pre-launch-polish-bundle/22.6-CONTEXT.md
@.planning/phases/22.6-pre-launch-polish-bundle/22.6-RESEARCH.md
@.planning/phases/22.6-pre-launch-polish-bundle/22.6-UI-SPEC.md
@apps/tools-app/components/layout/GlobalLayout.tsx
@apps/website/components/layout/header.tsx

<interfaces>
<!-- Website nav source-of-truth (verified header.tsx) -->

The website header has these 5 nav items (some with dropdown):
1. Events → /events  (NEW — appeared after Track A; tools-app must mirror)
2. Tools → https://tools.generation-ai.org  (external from website's perspective)
3. Community → /community
4. Für Partner → /partner  (has dropdown for sub-items in website; tools-app simplifies to direct link per UI-SPEC)
5. Über uns → /about

For tools-app (currently sitting on tools.generation-ai.org), we mirror the SAME 5 items but flip which one is "internal" vs "external":

| Nav item | Website href | tools-app href | Tag type |
|----------|--------------|----------------|----------|
| Events | /events | https://generation-ai.org/events | <a> |
| Tools | https://tools.generation-ai.org | / (or https://tools.generation-ai.org) — ACTIVE | Next.js <Link> or <a> |
| Community | /community | https://generation-ai.org/community | <a> |
| Für Partner | /partner | https://generation-ai.org/partner | <a> |
| Über uns | /about | https://generation-ai.org/about | <a> |

<!-- Active state pattern -->
From website header.tsx — active item gets:
- `text-text font-bold` (instead of `text-text-muted font-medium`)
- `border-b-2 border-[var(--accent)]` underline OR `text-[var(--accent)]` color
- `aria-current="page"`

<!-- Mobile burger pattern -->
Look at website header.tsx mobile-sheet: uses Sheet component (`@base-ui/react/dialog`). For tools-app, a simpler burger button + overlay is acceptable since the existing tools-app header has limited space. Pattern from website is the reference.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add 5 nav-items + Tools active state + mobile burger menu</name>
  <files>apps/tools-app/components/layout/GlobalLayout.tsx</files>
  <read_first>
    - apps/tools-app/components/layout/GlobalLayout.tsx (FULL current file — Plan 07 has already added Primary CTA + Secondary link + sync-anchor comment)
    - apps/website/components/layout/header.tsx (FULL — mirror source for nav items + active state + mobile sheet pattern)
    - .planning/phases/22.6-pre-launch-polish-bundle/22.6-UI-SPEC.md (Track B nav contracts — desktop + mobile)
    - .planning/phases/22.6-pre-launch-polish-bundle/22.6-CONTEXT.md (B-08 duplicate, B-05 cross-domain page-load)
    - apps/website/components/ui/sheet.tsx (if you need a Sheet pattern for the burger)
  </read_first>
  <action>
**Step 1:** Verify Plan 07's sync-anchor comment is still at the top of the file. If yes, leave it. If somehow removed, restore it.

**Step 2:** Insert the 5 nav-items in the header. Place them in the LEFT or CENTER cluster of the header — between the Logo (which keeps its current position) and the right-side cluster (Theme Toggle + CTAs). Adapt to the existing flex layout.

Desktop nav structure (insert after Logo + KI-Tools label, before right-side cluster):

```tsx
{/* Desktop nav — hidden on mobile */}
<nav
  aria-label="Hauptnavigation"
  className="hidden md:flex items-center gap-6"
  data-tools-nav="desktop"
>
  <a
    href="https://generation-ai.org/events"
    data-nav-item="events"
    className="font-mono text-[13px] font-medium text-text-muted hover:text-text transition-colors"
  >
    Events
  </a>
  <a
    href="https://tools.generation-ai.org"
    data-nav-item="tools"
    aria-current="page"
    className="font-mono text-[13px] font-bold text-text border-b-2 border-[var(--accent)] pb-0.5"
  >
    Tools
  </a>
  <a
    href="https://generation-ai.org/community"
    data-nav-item="community"
    className="font-mono text-[13px] font-medium text-text-muted hover:text-text transition-colors"
  >
    Community
  </a>
  <a
    href="https://generation-ai.org/partner"
    data-nav-item="partner"
    className="font-mono text-[13px] font-medium text-text-muted hover:text-text transition-colors"
  >
    Für Partner
  </a>
  <a
    href="https://generation-ai.org/about"
    data-nav-item="about"
    className="font-mono text-[13px] font-medium text-text-muted hover:text-text transition-colors"
  >
    Über uns
  </a>
</nav>
```

**Step 3:** Mobile burger menu. Add a burger button visible only on mobile (`md:hidden`), and a panel/sheet that opens with the 5 nav-items + Primary CTA + Secondary link.

Use a simple `useState` toggle for V1 (no need to pull in Dialog primitive — keeps bundle lean). Pattern:

```tsx
"use client";  // top of file if not already
import { useState } from "react";
import { Menu, X } from "lucide-react";

// inside component:
const [mobileNavOpen, setMobileNavOpen] = useState(false);

// inside JSX, in the right-side cluster (mobile only):
<button
  type="button"
  onClick={() => setMobileNavOpen(!mobileNavOpen)}
  aria-label={mobileNavOpen ? "Menü schließen" : "Menü öffnen"}
  aria-expanded={mobileNavOpen}
  data-tools-burger
  className="md:hidden inline-flex items-center justify-center h-10 w-10 rounded-md text-text-muted hover:text-text transition-colors"
>
  {mobileNavOpen ? <X className="h-5 w-5" aria-hidden="true" /> : <Menu className="h-5 w-5" aria-hidden="true" />}
</button>

{/* Mobile nav panel */}
{mobileNavOpen && (
  <div
    data-tools-nav="mobile"
    className="md:hidden absolute top-full left-0 right-0 bg-bg-elevated border-b border-border shadow-lg z-50"
  >
    <nav aria-label="Hauptnavigation mobil" className="flex flex-col p-4 gap-3">
      <a href="https://generation-ai.org/events" className="font-mono text-sm text-text-muted hover:text-text">Events</a>
      <a href="https://tools.generation-ai.org" aria-current="page" className="font-mono text-sm text-text font-bold">Tools</a>
      <a href="https://generation-ai.org/community" className="font-mono text-sm text-text-muted hover:text-text">Community</a>
      <a href="https://generation-ai.org/partner" className="font-mono text-sm text-text-muted hover:text-text">Für Partner</a>
      <a href="https://generation-ai.org/about" className="font-mono text-sm text-text-muted hover:text-text">Über uns</a>

      {/* Logged-out CTAs in mobile menu */}
      {!user && (
        <div className="flex flex-col gap-2 pt-3 mt-2 border-t border-border">
          <a
            href="https://generation-ai.org/join?utm_source=tools"
            className="bg-[var(--accent)] text-[var(--text-on-accent)] font-mono font-bold text-sm rounded-full px-4 py-2.5 text-center"
          >
            Kostenlos registrieren
          </a>
          <Link
            href="/login"
            className="font-mono text-xs text-text-muted hover:text-text text-center"
          >
            Bereits Mitglied? Einloggen
          </Link>
        </div>
      )}
    </nav>
  </div>
)}
```

The header element wrapper needs `relative` so the absolute-positioned mobile panel anchors to it. Check the current `<header>` opening tag — add `relative` if missing.

**Step 4:** Hide the Plan-07-added Secondary login link on mobile (we already use `hidden sm:inline-block` on it). Verify it stays hidden when burger menu is the primary mobile nav.

**Step 5:** REGRESSION GUARD: Verify logged-in branch unchanged:
- Settings icon-button still present
- Signout icon-button still present
- No nav-items hide the user menu
- Mobile burger menu does NOT show CTAs when user is logged in (`{!user && (...)}` guard)

**CRITICAL constraint reminders:**
- All 4 cross-domain nav items use `<a href>`, NOT `<Link>` (B-05 full page load, no SPA routing)
- Tools item can use `<a href="https://tools.generation-ai.org">` OR `<Link href="/">` — but Tools is the active page, so a same-domain link is fine
- aria-current="page" on Tools ONLY
- "Über uns" with proper Umlaut (not "Ueber uns")
- "Für Partner" with proper Umlaut (not "Fuer Partner")
  </action>
  <verify>
    <automated>pnpm --filter @genai/tools-app build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/tools-app/components/layout/GlobalLayout.tsx` contains all 5 nav text strings: `Events`, `Tools`, `Community`, `Für Partner`, `Über uns`
    - File contains `href="https://generation-ai.org/events"`
    - File contains `href="https://generation-ai.org/community"`
    - File contains `href="https://generation-ai.org/partner"`
    - File contains `href="https://generation-ai.org/about"`
    - File contains `aria-current="page"` (on the Tools item)
    - File contains `data-nav-item="events"` (test selector)
    - File contains `data-tools-nav="desktop"` and `data-tools-nav="mobile"`
    - File contains `data-tools-burger` (burger toggle test selector)
    - File still contains all Plan 07 elements: `data-cta="primary-register"`, `data-cta="secondary-login"`, sync-anchor comment, https://generation-ai.org/join?utm_source=tools
    - File still contains LOGGED-IN branch (Settings + Signout)
    - File still contains Logo with href to https://generation-ai.org
    - Build exits 0
  </acceptance_criteria>
  <done>5 nav items rendered, Tools marked active, mobile burger functional, logged-in flow intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fill final 2 Track B Playwright tests (nav + eingeloggt)</name>
  <files>packages/e2e-tools/tests/tools-app.spec.ts</files>
  <read_first>
    - packages/e2e-tools/tests/tools-app.spec.ts (current: 3 active + 2 fixme)
    - packages/e2e-tools/tests/auth.spec.ts (Phase 13/19 — login helper pattern for tools-app, may use storageState or programmatic login)
    - apps/tools-app/components/layout/GlobalLayout.tsx (Task 1 — DOM contract)
  </read_first>
  <action>
Replace the final 2 `test.fixme` blocks:

```ts
test('nav: alle Items sichtbar (Events, Tools active, Community, Für Partner, Über uns) (B-req-4)', async ({ page }) => {
  await page.goto(TOOLS_URL);

  const desktopNav = page.locator('[data-tools-nav="desktop"]');
  await expect(desktopNav).toBeVisible();

  // All 5 items present
  await expect(desktopNav.locator('[data-nav-item="events"]')).toHaveText('Events');
  await expect(desktopNav.locator('[data-nav-item="tools"]')).toHaveText('Tools');
  await expect(desktopNav.locator('[data-nav-item="community"]')).toHaveText('Community');
  await expect(desktopNav.locator('[data-nav-item="partner"]')).toHaveText(/Für Partner/);
  await expect(desktopNav.locator('[data-nav-item="about"]')).toHaveText(/Über uns/);

  // Tools is active
  const tools = desktopNav.locator('[data-nav-item="tools"]');
  await expect(tools).toHaveAttribute('aria-current', 'page');

  // Cross-domain hrefs hardcoded
  await expect(desktopNav.locator('[data-nav-item="events"]')).toHaveAttribute('href', 'https://generation-ai.org/events');
  await expect(desktopNav.locator('[data-nav-item="community"]')).toHaveAttribute('href', 'https://generation-ai.org/community');
  await expect(desktopNav.locator('[data-nav-item="partner"]')).toHaveAttribute('href', 'https://generation-ai.org/partner');
  await expect(desktopNav.locator('[data-nav-item="about"]')).toHaveAttribute('href', 'https://generation-ai.org/about');
});

test('eingeloggt: User-Menu statt CTA-Buttons sichtbar — kein Regression (B-req-5)', async ({ page, context }) => {
  // Use programmatic login via Supabase admin OR storageState fixture.
  // If existing auth.spec.ts has a login helper, reuse it. Otherwise, this test
  // is best run with E2E_TEST_USER_EMAIL + E2E_TEST_USER_PASSWORD env vars
  // (set in CI per Phase 19 setup) using a magic-link or password flow.

  const email = process.env.TEST_USER_EMAIL;
  const password = process.env.TEST_USER_PASSWORD;

  if (!email || !password) {
    test.skip(true, 'TEST_USER_EMAIL + TEST_USER_PASSWORD env vars required for logged-in regression test');
  }

  // Navigate to login, sign in
  await page.goto(`${TOOLS_URL}/login`);
  await page.fill('input[type="email"]', email!);
  await page.fill('input[type="password"]', password!);
  await page.click('button[type="submit"]');
  await page.waitForURL(new RegExp(`${TOOLS_URL.replace(/^https?:\/\//, '')}/?$`), { timeout: 10000 });

  // Now on home, verify CTAs are HIDDEN and user-menu elements PRESENT
  await expect(page.locator('[data-cta="primary-register"]')).toHaveCount(0);
  await expect(page.locator('[data-cta="secondary-login"]')).toHaveCount(0);

  // User-menu hallmarks (Settings link or Signout button — adapt to actual selectors)
  // These selectors come from the existing GlobalLayout.tsx logged-in branch.
  // If exact selectors aren't known, assert by accessible name:
  await expect(page.getByRole('link', { name: /Settings/i }).or(page.getByRole('button', { name: /Settings/i }))).toBeVisible();
});
```

The eingeloggt test gracefully `test.skip`s when credentials are missing — won't fail in dev environments without test-user setup, but runs in CI where Phase 19 already provisioned the env vars.

After this, the spec file has 5 active tests + 0 fixme.
  </action>
  <verify>
    <automated>pnpm --filter @genai/e2e-tools exec playwright test tests/tools-app.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/e2e-tools/tests/tools-app.spec.ts` contains 0 `test.fixme(`
    - File contains 5 active `test(` definitions
    - Nav test passes when run against tools-app dev
    - Eingeloggt test either passes (with env vars) or skips gracefully (without env vars) — no failure either way
    - Exit code 0
  </acceptance_criteria>
  <done>All 5 Track B Playwright tests active; suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (checkpoint): Human verification gate</name>
  <files>(human verification only — no file modifications)</files>
  <action>See &lt;what-built&gt; and &lt;how-to-verify&gt; below.</action>
  <what-built>
Track B is feature-complete: tools-app nav now mirrors the website (5 cross-domain items + Tools-active), mobile burger menu works, ToolsHero lives, Primary+Secondary CTAs from Plan 07 still in place. All 5 Track B Playwright tests green.

What needs human eyes:
1. Side-by-side sticky-nav comparison (B.3 from CONTEXT — RESEARCH already concluded no drift exists, but Lucas confirms visually)
2. Cross-domain navigation actually works (clicks really land on generation-ai.org/* with full page load)
3. Lighthouse on tools-app stays ≥ 90 (no regression from new components)
4. Logged-in regression smoke (Lite/Pro toggling, Chat works, Settings+Signout work)
  </what-built>
  <how-to-verify>
1. Build prod locally + serve both apps:
   ```
   pnpm build
   pnpm --filter @genai/tools-app start &  # localhost:3001 (or whatever port tools serves)
   pnpm --filter @genai/website start &     # localhost:3000
   ```

2. **Side-by-side sticky-nav comparison (B.3):**
   - Open localhost:3000 in one browser window, localhost:3001 in another
   - Place windows side-by-side
   - Scroll both pages
   - Verify: nav stays sticky in both, same height, same background, same Geist Mono font, same item spacing
   - Confirm: any visual drift → note + Lucas decides if fix needed in this phase or backlog

3. **Cross-domain nav from tools-app (B.4 + B.5):**
   - On localhost:3001, click "Events" in nav → should navigate to https://generation-ai.org/events (or http://localhost:3000/events in local). Same tab, full page load (no SPA flicker)
   - Same for "Community", "Für Partner", "Über uns"
   - Click "Tools" → stays on tools-app
   - Click "Kostenlos registrieren" → goes to /join with `?utm_source=tools` in URL bar

4. **Mobile burger:**
   - Resize browser to ≤ 767px or use DevTools mobile emulation
   - Logged-out: burger icon visible, click → panel opens with 5 nav-items + Primary CTA + Secondary link
   - Logged-in: burger icon visible, click → panel opens with 5 nav-items but NO CTA buttons (just user-menu items)
   - Click X → panel closes

5. **Logged-in regression smoke (Phase 12/19 protection):**
   - Log in with admin@generation-ai.org or test user
   - Verify: Settings icon visible, Signout icon visible, CTAs gone
   - Click Settings → /settings page loads, password section works
   - Click a tool card → tool detail page loads, Chat works
   - Logout (POST signout) → returns to logged-out state, CTAs reappear

6. **Lighthouse tools-app:**
   ```
   pnpm exec lighthouse http://localhost:3001 \
     --output=json --output-path=./lighthouse-tools.json \
     --chrome-flags="--headless"
   ```
   Verify all 4 scores ≥ 0.90 (Perf may be flaky — re-run if first score < 0.90; investigate only if consistently low)

7. **Umlaute sanity:**
   - "Für Partner" displays as "Für Partner" not "F�r Partner" or "Fuer Partner" — same for "Über uns"
   - Hero displays "KI-Tools, kuratiert für dich." with proper ü
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 verifications pass + Lighthouse ≥ 90. Otherwise describe issues — Plan 09 stays open for fixes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ tools-app server | Public read OK, auth-gated for /chat, /settings (existing) |
| tools-app nav ↔ generation-ai.org | Cross-domain hrefs, full-page-load — no token leakage (no Authorization header propagation, browser handles cookies per-domain) |
| Logged-in user-menu ↔ Supabase auth flow | Existing Phase 12/19 flow, NOT touched in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22.6-B-SESSION | Tampering | Logged-in user-menu unchanged | mitigate | Acceptance criteria explicitly grep for Settings + Signout to ensure preservation. Manual checkpoint includes logged-in regression test (Settings + Chat + Signout) |
| T-22.6-B-NAV-INJECTION | Tampering | All 5 nav hrefs are hardcoded literals | accept | No user input, no template interpolation, no dynamic URL construction. Cannot be tampered |
| T-22.6-B-CROSS-DOMAIN-COOKIE | Information Disclosure | Cross-domain links propagate cookies via browser SOP | accept | Browser per-domain cookie policy enforces isolation. tools.generation-ai.org and generation-ai.org are SAME-eTLD+1 (cookie-domain `.generation-ai.org` per Phase 12 setup) — auth session shared by design (Soft SSO). No new exposure |
| T-22.6-B-MOBILE-A11Y | a11y barrier | Mobile burger button without aria-expanded | mitigate | Burger button has `aria-expanded` toggling true/false + `aria-label` toggling between "Menü öffnen"/"Menü schließen" |
</threat_model>

<verification>
- `pnpm --filter @genai/tools-app build` exits 0
- All 5 Track B Playwright tests pass (or skip with documented reason)
- Manual checkpoint: side-by-side nav parity + cross-domain navigation + mobile burger + logged-in regression + Lighthouse ≥ 90
</verification>

<success_criteria>
- Full nav-mirror in place
- Mobile burger functional
- Logged-in flow zero regression
- All 5 Track B tests green
- Track A and Track B both verified — Phase 22.6 DONE
- Ready for merge into develop branch
</success_criteria>

<output>
Create `.planning/phases/22.6-pre-launch-polish-bundle/22.6-09-SUMMARY.md` documenting nav-items + burger menu + Lucas Lighthouse + side-by-side parity result + final test counts.

Then create `.planning/phases/22.6-pre-launch-polish-bundle/22.6-PHASE-SUMMARY.md` for the whole phase: 9 plans done, Track A + Track B both complete, link to all per-plan SUMMARYs, decision coverage matrix (D-A-01..A-14 + D-B-01..B-11), test totals (12 Playwright + 7 Vitest), readiness for develop merge.
</output>
