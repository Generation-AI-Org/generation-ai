---
phase: 22.6
slug: pre-launch-polish-bundle
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-25
---

# Phase 22.6 — UI Design Contract
# Pre-Launch Polish Bundle (Track A /events + Track B tools-app)

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> Two tracks, two apps. Track A builds `/events` on `apps/website`. Track B polishes `apps/tools-app`. Both consume the same DS token layer — no divergence.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (no shadcn — project uses custom DS) |
| Preset | not applicable |
| Component library | @base-ui/react (v1.4.0, installed — Dialog/Sheet/Dropdown) |
| Icon library | lucide-react (^1.8.0, installed in both apps) |
| Font — Display/Mono | Geist Mono (700) — H1, eyebrow, button, tag, label |
| Font — Body/Sans | Geist Sans (400/500/600) — body, section heads, subheads |
| Token source | `packages/config/tailwind/base.css` + `brand/Generation AI Design System/` |

**Source:** RESEARCH.md Standard Stack + brand/DESIGN.md + base.css verified.

---

## Spacing Scale

All spacing is driven by `--space-*` tokens from `packages/config/tailwind/base.css`. Use Tailwind's `p-*`/`gap-*`/`m-*` utilities which map to these.

| Token | Value | Usage |
|-------|-------|-------|
| --space-1 | 4px | Icon gaps, dot separators, micro-padding |
| --space-2 | 8px | Compact internal padding (tag inner, badge inner) |
| --space-3 | 12px | Dense list gaps, secondary button padding |
| --space-4 | 16px | Default element spacing, card inner padding (mobile) |
| --space-6 | 24px | Section inner padding, card inner padding (desktop) |
| --space-8 | 32px | Gap between cards in grid |
| --space-12 | 48px | Major section top/bottom padding |
| --space-16 | 64px | Hero vertical padding (`py-20` = 80px, acceptable exception per established subpage pattern) |
| --space-24 | 96px | Section-to-section rhythm on large screens |

Exceptions:
- Hero `py-20` (80px) is the established subpage pattern from `/community` — kept for consistency, not changed.
- Modal `p-4` on mobile, `p-6` on desktop — matches card inner rhythm.
- Touch targets: minimum 44px height for all interactive elements (A11y WCAG 2.1 AA).

---

## Typography

All sizes reference DS tokens. No inline `clamp()` in component code — use tokens.

| Role | Size | Weight | Line Height | Font | Letter Spacing |
|------|------|--------|-------------|------|----------------|
| Hero H1 (events, tools hero) | `var(--fs-display)` = clamp(40px, 6.5vw, 76px) | 700 | 1.02 | Geist Mono | -0.03em |
| Section H2 | `var(--fs-h2)` = clamp(24px, 3vw, 32px) | 700 | 1.2 | Geist Sans | -0.015em |
| Card title / Modal H2 | `var(--fs-h3)` = 20px | 600 | 1.3 | Geist Sans | 0 |
| Body / Card body / Modal description | 16px | 400 | 1.65 | Geist Sans | 0 |
| Lede / subhead under H1 | 18px (text-lg sm:text-xl) | 500 | 1.5 | Geist Sans | 0 |
| Eyebrow / label ("// generation ai · events") | 11px | 700 | 1.3 | Geist Mono | +0.2em (tracking-[0.2em]) |
| Tag (Format-Tag, Level-Tag) | 11px | 700 | 1.3 | Geist Mono | +0.08em |
| Button | 14px | 700 | 1 | Geist Mono | +0.02em |
| Caption / meta (date, location) | 13px | 400 | 1.5 | Geist Sans | 0 |
| Body Small | 14px | 400 | 1.55 | Geist Sans | 0 |

**Tools-app hero specifics (B.2):**
- Label `// deine ki-tool-bibliothek`: Geist Mono, 11px, weight 700, uppercase, tracking-[0.2em], color text-text-muted
- H1 `KI-Tools, kuratiert für dich.`: `var(--fs-display)` or `var(--fs-h1)` — use `--fs-h1` (clamp 32px→48px) for the tools hero since it sits inline (not full-viewport), `font-mono font-bold leading-[1.02] tracking-[-0.03em]`
- Body: Geist Sans 16px/1.65, color text-text-secondary

**Source:** brand/DESIGN.md §B, base.css verified, AGENTS.md Hero-Pattern.

---

## Color

Dual-theme system. All values are theme-aware CSS variables — do not hardcode hex in components.

| Role | Dark Token / Value | Light Token / Value | Usage |
|------|--------------------|---------------------|-------|
| Dominant surface (60%) | `--bg: #141414` | `--bg: #F6F6F6` | Page background, modal backdrop base |
| Secondary surface (30%) | `--bg-elevated` (slate-2 dark) | `--bg-elevated` (slate-2 light) | Cards, event grid cards, format cards, archive cards, tools-app header bg |
| Accent (10%) | `--accent: #CEFF32` (neon) | `--accent: #F5133B` (red) | Eyebrow dot, active node glow, primary CTA background, "Anmelden" button, format tag border-on-hover |
| Border | `--border: rgba(255,255,255,0.08)` | `--border: rgba(0,0,0,0.1)` | Card borders, modal border, input border |
| Header band | `--bg-header` (blue #3A3AFF dark / pink #FC78FE light) | same | Sticky nav background (both apps) |
| Destructive | `#DC2626` (semantic, not brand) | same | Non-Member "Anmelden" redirect warning if needed |

**Accent reserved for:**
- Primary CTA button fill (`bg-[var(--accent)]`)
- Eyebrow accent dot in hero sections
- Format-Tag / Level-Tag accent border on hover
- `BeispielBadge` fill (theme-aware: brand-neon dark / brand-red light — already implemented)
- Members-Only-Hinweis left-border stripe
- SignalGrid activated node color (canvas only, no Tailwind class)
- "Kostenlos registrieren" primary button fill in tools-app nav

**Accent NOT used for:** body text, card backgrounds, section backgrounds, borders (except accent-border variant), archive card imagery frames.

**Text tokens:**
- `text-text` — primary text (headings, body)
- `text-text-muted` — secondary/meta text (date, location, eyebrow label)
- `text-text-secondary` — midtone body text (tools hero body, subhead below H1)
- `text-on-accent` — text on accent-filled buttons (`#141414` dark / `#FFFFFF` light)

**Source:** brand/DESIGN.md §C, base.css lines 99-257, signal-grid.tsx color system.

---

## Component Inventory

### Track A — /events (apps/website)

#### Hero (`events-hero.tsx`)
- Pattern: **exact copy of `community-hero.tsx`** with updated copy
- Background: `<LabeledNodes>` (constellation canvas, identical to community/about heroes)
- Wrapper: `<section aria-labelledby="events-hero-heading" data-section="events-hero" className="relative isolate">`
- Container: `relative z-10 mx-auto max-w-4xl px-6 py-20 text-center`
- Eyebrow: `font-mono text-[11px] font-bold uppercase tracking-[0.2em] text-text-muted` with accent dot
- H1: `fontSize: var(--fs-display)` + `font-mono font-bold leading-[1.02] tracking-[-0.03em]` + large textShadow
- Subhead: `text-lg sm:text-xl leading-[1.5] text-text-muted max-w-2xl mx-auto mt-6`
- Motion: `initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }} duration: 0.7` with `useReducedMotion` guard — if reduced: no animation
- Text-shadows on H1: `0 0 18px rgba(var(--bg-rgb), 1), 0 0 8px rgba(var(--bg-rgb), 1)`
- Text-shadows on eyebrow/subhead: `0 0 10px rgba(var(--bg-rgb), 1), 0 0 4px rgba(var(--bg-rgb), 1)`

#### Event Card (`event-card.tsx`)
- Container: `bg-bg-elevated border border-border rounded-2xl overflow-hidden cursor-pointer hover:border-border-accent transition-colors duration-[var(--dur-normal)]`
- Header image area: fixed height `h-40` or `h-48`, `object-fit: cover`. If no `imageUrl` in frontmatter → decorative brand-pattern background (Signal-Grid-Echo: `<SignalGrid>` with low node count, no interaction, aria-hidden)
- `BeispielBadge` rendered top-right on image if `frontmatter.example === true` (import from `components/ui/beispiel-badge.tsx`)
- Tag row: Format-Tag + Level-Tag — `font-mono text-[11px] font-bold uppercase tracking-[0.08em] bg-bg border border-border rounded-full px-2 py-0.5`
- Title: `var(--fs-h3)` (20px), weight 600, Geist Sans, `leading-[1.3]`, `mt-2`
- Meta row (date + location): 13px Geist Sans, `text-text-muted`, lucide icons `CalendarDays` and `MapPin` at 14px
- Partner badge (Masterclasses only): small pill with partner name, `text-[11px] font-mono`
- CTA button: "Anmelden" — primary button style, full-width at bottom of card, `bg-[var(--accent)] text-[var(--text-on-accent)] font-mono font-bold text-sm rounded-full px-4 py-2.5`
- Card click: opens EventModal (entire card is clickable, button click also opens modal)
- Keyboard: card receives `role="button"` with `tabIndex={0}` and `onKeyDown` for Enter/Space

#### Event Grid (`events-grid.tsx`)
- Layout: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8`
- Initial render: first 3 cards
- "Mehr anzeigen ↓" button: appears when `upcomingEvents.length > 3`, client state `showAll`
  - Style: secondary button — `bg-bg-elevated border border-border text-text font-mono text-sm rounded-full px-6 py-2.5 hover:border-border-accent`
  - Centered below grid, `mt-8`
- Empty state (0 upcoming events): centered illustration-free text message (see Copywriting Contract)

#### Event Modal (`event-modal.tsx`)
- Library: `@base-ui/react/dialog` — `Dialog.Root`, `Dialog.Portal`, `Dialog.Backdrop`, `Dialog.Popup`, `Dialog.Close`
- Backdrop: `fixed inset-0 z-50 bg-black/60 backdrop-blur-sm` with `data-starting-style:opacity-0 data-ending-style:opacity-0 transition-opacity duration-200`
- Popup: `fixed inset-0 z-50 flex items-center justify-center p-4` on desktop; mobile: `fixed inset-0 z-50 flex items-end` (sheet-from-bottom)
- Modal content box desktop: `bg-bg-elevated rounded-2xl border border-border shadow-2xl max-w-2xl w-full max-h-[90vh] overflow-y-auto`
- Modal content box mobile: `bg-bg-elevated rounded-t-2xl w-full max-h-[95vh] overflow-y-auto` (full-width, sheet style)
- Responsive breakpoint: `< 768px` = sheet, `>= 768px` = centered dialog
- Close button: top-right, `Dialog.Close`, lucide `X` icon, 44px touch target minimum
- Focus-trap: native via @base-ui (no external library needed)
- ESC-close: native via @base-ui
- aria-modal: native via `Dialog.Popup`
- Modal sections: header-image (same as card, h-48), title (H2 20px), description (body 16px/1.65), agenda (bulleted, body-small 14px), speaker cards (avatar + name + role), "Anmelden" CTA button (primary, full-width)
- Speaker cards: `flex items-center gap-3` — avatar `w-10 h-10 rounded-full bg-bg-elevated` + name `font-sans font-600 text-sm` + role `font-sans text-[13px] text-text-muted`
- "Anmelden" button in modal: primary style, triggers auth-check (useAuth → if no user → router.push('/join?redirect_after=/events/'+slug), if user → window.open(ctaUrl, '_blank', 'noopener,noreferrer'))

#### Format Section (`events-formats.tsx`)
- 3 cards in `grid grid-cols-1 md:grid-cols-3 gap-6`
- Each card: `bg-bg-elevated border border-border rounded-2xl p-6`
- Icon: lucide icon at 24px, `text-accent` color, `mb-4`
- Format name: H3 (20px, Geist Sans, weight 600)
- Description: body 16px/1.65
- No CTA on these cards — purely informational

#### Members-Only Hinweis (`events-members-only.tsx`)
- Container: `bg-bg-elevated border-l-2 border-[var(--accent)] rounded-r-xl p-6 max-w-2xl mx-auto`
- Icon: lucide `Lock` at 20px, `text-accent`
- Heading: H3 (20px, Geist Sans, weight 600)
- Body: 16px, emphasizes "Kostenlos, 2 Minuten"
- CTA link: inline text-link to `/join` — `text-[var(--accent)] font-mono font-bold text-sm`

#### Archive Section (`events-archive.tsx`)
- Section heading: H2 (`var(--fs-h2)`, Geist Sans, weight 700)
- Layout: `grid grid-cols-1 md:grid-cols-2 gap-6`
- Archive card: simpler than upcoming — `bg-bg-elevated border border-border rounded-xl overflow-hidden`
  - Image: `h-32 object-cover` (no fallback brand-pattern, single muted color is fine)
  - Title: 16px weight 600 (not 20px — smaller than upcoming cards, signals "past")
  - Date: 13px `text-text-muted`
  - Recap: 14px Geist Sans, max 2 sentences
  - NO CTA button, NO modal trigger
  - `BeispielBadge` if `frontmatter.example === true`

#### Final CTA (`events-final-cta.tsx`)
- Pattern: analog `community-final-cta.tsx` — use `SectionTransition variant="signal-echo"` before this section
- Centered, `max-w-2xl mx-auto text-center py-24`
- Heading: H2 (`var(--fs-h2)`, Geist Sans, weight 700) — "Keine Events verpassen."
- Body: 18px/1.5, `text-text-muted`
- CTA: primary button → `/join` — "Jetzt beitreten"

#### Section Transitions
- Between hero and upcoming-events grid: no transition (hero has its own landing zone)
- Between upcoming-events and formats: `<SectionTransition variant="soft-fade" />`
- Between formats and members-only-hinweis: `<SectionTransition variant="soft-fade" />`
- Between members-only-hinweis and archive: `<SectionTransition variant="soft-fade" />`
- Before final CTA: `<SectionTransition variant="signal-echo" />`

### Track B — tools-app Polish (apps/tools-app)

#### Tools Hero (`tools-hero.tsx`)
- Placement: between `<GlobalLayout>` header and `<FilterBar>` in `HomeLayout.tsx`
- Container: `py-12 px-6 text-center max-w-3xl mx-auto` — deliberately compact, not full-viewport-height
- Optional background: `<SignalGrid>` with `nodeCountX={18} nodeCountY={6}` (low density, shorter height) — Decision B-09 says optional. Use it if implementation is clean. If complex → plain `bg-transparent` is acceptable.
- Label: `// deine ki-tool-bibliothek` — `font-mono text-[11px] font-bold uppercase tracking-[0.2em] text-text-muted`
- H1: `KI-Tools, kuratiert für dich.` — `font-mono font-bold leading-[1.02] tracking-[-0.03em] text-text text-balance` + `style={{ fontSize: "var(--fs-h1)" }}`
- Body: `mt-3 text-base leading-[1.65] text-text-secondary` — Geist Sans
- Motion: `initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} duration: 0.5` with `useReducedMotion` guard
- Mobile: `py-8 px-4` (reduced padding), text remains same size (tokens handle responsiveness)
- No CTA button in hero itself (primary CTA is in the Nav)

#### Nav (tools-app GlobalLayout.tsx — header block rewrite)
- Structure identical to `apps/website/components/layout/header.tsx` — mirror per Decision B-08
- Code comment at top of section: `// Mirror with apps/website/components/layout/header.tsx — sync manually`
- Sticky behavior: `shrink-0 bg-[var(--bg-header)]` (existing pattern — no scroll-transition needed, Decision B-03 Sticky-Nav-Sync confirmed no drift)
- Nav items (desktop): `Events · Tools · Community · Für Partner · Über uns`
  - "Events" → `<a href="https://generation-ai.org/events">` (full page load, same tab)
  - "Tools" → active state (bold, accent underline or accent text) — `<a href="https://tools.generation-ai.org">` (stays on current domain, or `<Link href="/">`)
  - "Community" → `<a href="https://generation-ai.org/community">`
  - "Für Partner" → `<a href="https://generation-ai.org/partner">`
  - "Über uns" → `<a href="https://generation-ai.org/about">`
  - All cross-domain links: `<a>` tag, NOT Next.js `<Link>`, same tab (no `target="_blank"`)
- Nav item style: `font-mono text-[13px] font-medium text-text-muted hover:text-text transition-colors`
- Active "Tools" item: `text-text font-bold` + `border-b-2 border-[var(--accent)]` (or `text-[var(--accent)]` — match website header pattern)
- Desktop right side (logged-out): 
  - Primary CTA: "Kostenlos registrieren" → `<a href="https://generation-ai.org/join?utm_source=tools">` — `bg-[var(--accent)] text-[var(--text-on-accent)] font-mono font-bold text-sm rounded-full px-4 py-2.5 hover:shadow-[0_0_20px_var(--accent-glow)] hover:scale-[1.03] transition-all`
  - Secondary link: "Bereits Mitglied? Einloggen" → `<Link href="/login">` — `font-mono text-[12px] text-text-muted hover:text-text transition-colors ml-3`
- Desktop right side (logged-in): existing user-menu unchanged (no regression)
- Mobile (logged-out):
  - Burger menu (existing pattern): Primary CTA "Kostenlos registrieren" prominently in burger sheet, full-width primary button style
  - Secondary link "Bereits Mitglied? → Einloggen" as text-link below primary button
- Mobile (logged-in): existing user-menu unchanged

---

## Copywriting Contract

All copy uses correct German Umlaute (ö/ä/ü/ß). Never ae/oe/ue/ss. Final wording iteration is Phase 27 — this contract defines V1 copy that must be parseable and functional.

| Element | Copy |
|---------|------|
| /events page `<title>` | "Events — Generation AI" |
| /events hero eyebrow | "// generation ai · events" |
| /events hero H1 | "Events, die dich weiterbringen." |
| /events hero subhead | "Workshops, Speaker Sessions und Masterclasses — exklusiv für unsere Community." |
| /events upcoming section heading | "Kommende Events" |
| "Mehr anzeigen" button | "Mehr anzeigen ↓" |
| Event card CTA button | "Anmelden" |
| Modal "Anmelden" button (logged-in) | "Jetzt anmelden →" |
| Modal "Anmelden" button (logged-out) | "Kostenlos registrieren und anmelden →" |
| Format section heading | "Unsere Formate" |
| Workshop format name | "Workshops" |
| Speaker Session format name | "Speaker Sessions" |
| Masterclass format name | "Masterclasses" |
| Members-Only heading | "Events nur für Mitglieder" |
| Members-Only body | "Unsere Events sind exklusiv für die Generation AI Community. Die Mitgliedschaft ist kostenlos — in 2 Minuten dabei." |
| Members-Only CTA | "Jetzt kostenlos registrieren →" |
| Archive section heading | "Vergangene Events" |
| Final CTA heading | "Keine Events verpassen." |
| Final CTA body | "Werde Teil der Community und sei bei jedem Event dabei." |
| Final CTA button | "Jetzt beitreten" |
| Empty state (0 upcoming) heading | "Bald neue Events" |
| Empty state (0 upcoming) body | "Gerade keine Events in Planung — aber das ändert sich bald. Trag dich ein und bleib informiert." |
| Error state (event load fails) | "Events konnten nicht geladen werden. Bitte Seite neu laden." |
| BeispielBadge label | "Beispiel" (existing component, no change) |
| tools-app hero label | "// deine ki-tool-bibliothek" |
| tools-app hero H1 | "KI-Tools, kuratiert für dich." |
| tools-app hero body | "Über 100 Tools, sortiert nach Anwendungsfall. Brauchst du Hilfe? Frag unseren Agenten." |
| tools-app nav primary CTA | "Kostenlos registrieren" |
| tools-app nav secondary link | "Bereits Mitglied? Einloggen" |

**Destructive actions in this phase:** None. The "Anmelden" redirect for non-members is a navigation action, not a destructive action. No confirmation dialogs needed.

**Source:** CONTEXT.md §Track A Scope §5.x, CONTEXT.md §Track B §B.2/B.3, brand/VOICE.md (German, proper Umlaute).

---

## Interaction States

### Event Card
| State | Treatment |
|-------|-----------|
| Default | `border border-border` |
| Hover | `border-border-accent` (accent-colored border), `transition-colors dur-normal` |
| Focus-visible | `outline-2 outline-offset-2 outline-[var(--accent)]` — WCAG 2.1 AA |
| Active (pressed) | `scale-[0.99]` |

### Primary Button ("Anmelden", "Kostenlos registrieren", "Jetzt beitreten")
| State | Treatment |
|-------|-----------|
| Default | `bg-[var(--accent)] text-[var(--text-on-accent)]` |
| Hover | `shadow-[0_0_20px_var(--accent-glow)] scale-[1.03]` |
| Focus-visible | `outline-2 outline-offset-2 outline-[var(--accent)]` |
| Disabled | `opacity-50 cursor-not-allowed` |

### Secondary / Ghost Button ("Mehr anzeigen", "Bereits Mitglied? Einloggen")
| State | Treatment |
|-------|-----------|
| Default | `border border-border text-text-muted` |
| Hover | `border-border-accent text-text` |
| Focus-visible | `outline-2 outline-offset-2 outline-[var(--accent)]` |

### Event Modal
| State | Treatment |
|-------|-----------|
| Open | Backdrop fades in (`opacity-0 → opacity-1`, `dur-normal`), Popup slides up (`y: 16 → y: 0`) |
| Close | Reverse animation via `data-ending-style` |
| Reduced-motion | No animation — instant show/hide |
| ESC key | Closes modal (native @base-ui behavior) |
| Click backdrop | Closes modal (native @base-ui behavior) |
| Focus-trap | First focusable element in modal receives focus on open; trap cycles within modal |
| Scroll | Modal content scrolls internally (`overflow-y-auto`); page behind does not scroll (`overflow-hidden` on body while open — @base-ui handles this) |

---

## Animation Contract

All animations require `useReducedMotion` guard. If `prefersReducedMotion === true`: skip all `initial`/`animate` props entirely (pass `false` to `initial`).

| Element | Animation | Duration | Easing |
|---------|-----------|----------|--------|
| Hero H1 + content | `opacity: 0→1, y: 16→0` | 0.7s | `easeOut` (matches community-hero) |
| Event cards (on "Mehr anzeigen" expand) | `opacity: 0→1, y: 8→0` | 0.3s | `var(--ease-out)` |
| Modal backdrop | `opacity: 0→1` | `var(--dur-normal)` = 300ms | linear |
| Modal popup | `opacity: 0→1, y: 16→0` | `var(--dur-normal)` = 300ms | `var(--ease-out)` |
| Tools-app hero | `opacity: 0→1, y: 12→0` | 0.5s | `easeOut` |
| Nav hover transitions | `color`, `border-color` | `var(--dur-fast)` = 150ms | linear |
| Button hover (`scale`, `shadow`) | transform + shadow | `var(--dur-fast)` = 150ms | linear |
| SignalGrid background | Canvas RAF loop — reduced-motion: frozen static nodes | — | — |

CSS `@media (prefers-reduced-motion: reduce)` fallback must be applied to any CSS `animation` or `transition` outside of React components (e.g., button hover via Tailwind class).

---

## Accessibility Contract

WCAG 2.1 AA mandatory throughout.

| Requirement | Implementation |
|-------------|----------------|
| Skip link | Existing site-wide skip link to `#main-content` (already implemented) |
| Focus-visible | All interactive elements: `outline-2 outline-offset-2 outline-[var(--accent)]` |
| Event card keyboard | `role="button"` + `tabIndex={0}` + `onKeyDown` Enter/Space → opens modal |
| Modal aria | `Dialog.Popup` has `aria-modal="true"` (native @base-ui), `Dialog.Title` required |
| Modal focus | Focus moves to modal on open; returns to trigger on close (native @base-ui) |
| Modal ESC | Closes modal (native @base-ui) |
| Archive cards | `role="article"` or `<article>` semantic element; no interactive elements = no keyboard trap concern |
| Images | Event header images: `alt="[Event title]"` — descriptive. AI-generated images: `alt="[Event title] — Vorschaubild"`. Brand-pattern fallback: `aria-hidden="true"` (decorative) |
| SignalGrid canvas | `aria-hidden="true"` (decorative canvas, not conveying information) |
| Color contrast | Accent on dark bg `#CEFF32` on `#141414`: 12.6:1 (AAA). Accent on light bg `#F5133B` on `#F6F6F6`: 4.7:1 (AA). `text-text-muted` must maintain ≥ 4.5:1 against bg |
| Form elements | None in this phase (modal has no form — Anmelden is a navigation action) |
| Reduced-motion | All Motion animations guarded with `useReducedMotion()` |
| Heading hierarchy | Each page has one H1 in hero; section headings use H2; card titles use H3/H4 |
| Events page landmarks | `<header>`, `<main id="main-content">`, `<footer>` via existing client-wrapper pattern |
| tools-app nav | Existing nav + new items: `<nav aria-label="Hauptnavigation">`, active item `aria-current="page"` |

---

## SEO Contract

| Element | Value |
|---------|-------|
| `/events` canonical | `https://generation-ai.org/events` |
| `/events` title | "Events — Generation AI" |
| `/events` description | "Workshops, Speaker Sessions und Masterclasses — exklusiv für die Generation AI Community." |
| `/events/[slug]` canonical | `https://generation-ai.org/events/[slug]` |
| `/events/[slug]` title | `[Event title] — Generation AI Events` |
| `/events/[slug]` OG type | "article" |
| Sitemap priority `/events` | 0.9 |
| Sitemap priority `/events/[slug]` | 0.8, `changeFrequency: "weekly"` |
| Structured data | Not required for V1 (deferred to Phase 27) |
| Dynamic rendering | `/events` and `/events/[slug]` must show `ƒ` in build output (CSP-nonce constraint, `await headers()` enforces this) |

---

## Registry Safety

No shadcn registry. No third-party component registries used in this phase.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| @base-ui/react | dialog (v1.4.0, already installed + already used in sheet.tsx) | verified — existing codebase usage is the trust anchor; no new install |
| lucide-react | CalendarDays, MapPin, Lock, X, ChevronDown, Menu | verified — already installed, no new registry needed |

---

## Platform Constraints (LEARNINGS.md)

These are hard rules derived from production incidents.

| Constraint | Rule |
|------------|------|
| CSP Nonce | `app/events/page.tsx` MUST call `await headers()` to get the nonce — even if not using it directly, this makes the route dynamic |
| Force-dynamic check | After `pnpm build`, `/events` and `/events/[slug]` must show `ƒ` (dynamic), NOT `○` (static). Static = no nonce = CSP blocks all scripts = black page |
| Nonce on request headers | `proxy.ts` sets nonce on request headers, not response — inherited by Root Layout. No changes to proxy.ts needed in this phase |
| @base-ui Dialog | Use `Dialog.Portal` + `Dialog.Backdrop` + `Dialog.Popup` — NOT native `<dialog>` HTML element. Pattern verified in `sheet.tsx` |
| tools-app h-screen layout | Hero-Sektion must be inside the `overflow-y-auto` scroll container in HomeLayout.tsx, not outside it — otherwise content is clipped |
| Cross-domain links | All links from tools-app to generation-ai.org must be `<a href="...">` NOT `<Link>` — full page load, same tab, no `target="_blank"` |
| Reduced-motion | `useReducedMotion()` guard on ALL motion/react animations. CSS-only animations need `@media (prefers-reduced-motion: reduce)` fallback |
| Umlaute | All user-facing copy: ö/ä/ü/ß (never oe/ae/ue/ss). MDX files, component strings, copy constants — all must use correct characters |

---

## File Creation Map

### Track A — new files, correct paths

| File | Type |
|------|------|
| `apps/website/app/events/page.tsx` | Server Component |
| `apps/website/app/events/events-client.tsx` | Client Wrapper |
| `apps/website/app/events/[slug]/page.tsx` | Server Component (standalone page) |
| `apps/website/app/events/components/events-hero.tsx` | Component |
| `apps/website/app/events/components/events-grid.tsx` | Component (client, "Mehr anzeigen" state) |
| `apps/website/app/events/components/event-card.tsx` | Component (client, modal trigger) |
| `apps/website/app/events/components/event-modal.tsx` | Component (client, @base-ui Dialog) |
| `apps/website/app/events/components/events-formats.tsx` | Component |
| `apps/website/app/events/components/events-members-only.tsx` | Component |
| `apps/website/app/events/components/events-archive.tsx` | Component |
| `apps/website/app/events/components/events-final-cta.tsx` | Component |
| `apps/website/lib/mdx/events.ts` | MDX adapter |
| `apps/website/content/events/2026-05-15-prompting-masterclass.mdx` | Placeholder |
| `apps/website/content/events/2026-05-28-agents-workshop.mdx` | Placeholder |
| `apps/website/content/events/2026-06-10-ki-strategie-session.mdx` | Placeholder |
| `apps/website/content/events/past/2026-03-10-launch-event.mdx` | Placeholder |
| `apps/website/content/events/past/2026-04-01-prompt-workshop.mdx` | Placeholder |

### Track A — edits

| File | Change |
|------|--------|
| `apps/website/app/sitemap.ts` | +15 LOC — add eventEntries (static /events + dynamic /events/[slug]) |

### Track B — new files

| File | Type |
|------|------|
| `apps/tools-app/components/tools-hero.tsx` | Component (new) |

### Track B — edits

| File | Change |
|------|--------|
| `apps/tools-app/components/layout/GlobalLayout.tsx` | Replace header block (~150 LOC edit) |
| `apps/tools-app/components/HomeLayout.tsx` | Insert `<ToolsHero />` before `<FilterBar>` (~5 LOC edit) |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
