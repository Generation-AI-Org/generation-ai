---
phase: 22.6-pre-launch-polish-bundle
verified: 2026-04-25T13:25:00Z
status: human_needed
score: 16/16 must-haves verified
overrides_applied: 0
re_verification: null
gaps: []
deferred:
  - truth: "Website Header verlinkt /events im navLinks-Array"
    addressed_in: "Phase 27 (Copy-Pass & Launch-Cleanup)"
    evidence: "Plan-09-SUMMARY documented this as a side-finding requiring 'Phase 27 copy-pass (or earlier dedicated polish commit) should add Events to website header.tsx navLinks array between Tools and Community.' Phase 27 Goal explicitly covers 'Struktur steht, Copy wird geschärft' + 'Dummy-Data-Cleanup' + 'Finaler Durchgang vor Launch' — adding the missing nav-link is in-scope. Out of Phase 22.6 scope (CONTEXT explicitly limits Track A to /events route + sitemap and Track B to tools-app)."
human_verification:
  - test: "Lighthouse /events ≥ 90 in all 4 categories (Performance, A11y, Best Practices, SEO)"
    expected: "All 4 scores ≥ 0.90 against prod build (local oder Vercel-Preview)"
    why_human: "Lighthouse needs prod build + non-flaky network — Plan-05 deferred this to Vercel-Preview after develop merge. Required by ROADMAP success criterion 'Lighthouse ≥ 90 auf /events UND tools-app (Lucas-verifiziert)'."
  - test: "Lighthouse tools-app ≥ 90 in all 4 categories"
    expected: "All 4 scores ≥ 0.90 against prod build (local oder Vercel-Preview)"
    why_human: "Plan-09 ran under --auto and explicitly deferred Lighthouse: 'Not run under auto-mode (would require local prod build + serve + chrome-flags). Defer to pre-launch QA.'"
  - test: "Mobile Modal als Full-Screen-Sheet (iPhone width)"
    expected: "/events auf ≤ 767px → Click auf Event-Card öffnet Modal als sheet-from-bottom (nicht zentriert), Backdrop dimmt, Escape schließt"
    why_human: "Visuelle UX-Verification, viewport-spezifisch — Spec checkt aria-modal aber nicht das Sheet-vs-Centered-Layout"
  - test: "Cross-Domain-Click verlässt tools-app (full page load)"
    expected: "Click auf 'Events'/'Community'/'Für Partner'/'Über uns' in tools-app Nav → echter Page-Load auf generation-ai.org/* (kein SPA-Flicker), gleicher Tab"
    why_human: "Browser-Verhalten, nicht headless validierbar (B-Validation expliziter Manual-Item)"
  - test: "Side-by-Side Sticky-Nav-Vergleich Website vs. tools-app"
    expected: "Beide Headers fühlen sich navigatorisch+visuell wie eine App an — gleiche Höhe, gleiche Background-Treatment, gleiche Sticky-Behavior beim Scrollen"
    why_human: "Plan-09 hat Side-by-Side technisch via grep verifiziert, aber visuelle Drift-Erkennung braucht Lucas live (B.3-Checkpoint expliziter Manual-Item)"
  - test: "Logged-in tools-app Regression-Smoke (Phase 12/19 Schutz)"
    expected: "Login als Test-User → CTAs verschwinden, Settings + Signout sichtbar; Tool-Card-Click → Detail-Page lädt; Chat funktioniert; Logout → CTAs zurück"
    why_human: "B-req-5 Playwright-Test deckt Settings/Signout-Presence ab, aber Lite/Pro-Toggling + Chat-Funktion sind Plan-09 explizit zur 'pre-launch QA' deferred. Phase 12/19 Auth-Flow Regressions sind kritisch."
  - test: "Mobile Burger-UX (Touch + ARIA)"
    expected: "≤ 767px → Burger sichtbar, Click öffnet Panel mit 5 Nav-Items + (logged-out) Primary CTA + Secondary Link; Logged-in zeigt nur 5 Items, keine CTAs; Click auf X schließt"
    why_human: "Mobile-Nav-Tests fehlen in tools-app.spec.ts (siehe REVIEW.md WR-03). Touch-Test braucht reales Gerät bzw. DevTools-Mobile-Emulation."
---

# Phase 22.6: Pre-Launch Polish Bundle Verification Report

**Phase Goal:** Vor dem Launch zwei letzte Conversion-Bausteine: Track A baut `/events` (Members-Only-Akquisitionshebel mit MDX-Pipeline) + Track B poliert tools-app (Login-Button-Umbau + Hero + Nav-Sync zur Hauptdomain).
**Verified:** 2026-04-25T13:25:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

#### Track A — `/events`

| #   | Truth                                                                                | Status     | Evidence                                                                                                                                            |
| --- | ------------------------------------------------------------------------------------ | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| A1  | `/events` Route existiert + funktioniert (`apps/website/app/events/page.tsx`)        | ✓ VERIFIED | File 44 LOC, contains `await headers()`, imports `getUpcomingEvents/getPastEvents`, build shows `ƒ /events`                                         |
| A2  | MDX-Pipeline liest aus `apps/website/content/events/` (3 upcoming + 2 past)          | ✓ VERIFIED | 5 `.mdx` files exist, alle slugs match filenames, alle haben `example: true`, beide past haben `recap:`, alle 3 Format-Werte vertreten              |
| A3  | Kommende Events als 3-Card-Grid, chronologisch aufsteigend                           | ✓ VERIFIED | `events-grid.tsx` 115 LOC, `events.slice(0, INITIAL_VISIBLE=3)`, `getUpcomingEvents` sortiert ascending (Vitest 7/7 grün)                           |
| A4  | „Mehr anzeigen" Button erscheint bei >3 Events, expandiert client-side               | ✓ VERIFIED | `events-grid.tsx` enthält `setShowAll(true)` + `data-action="show-more-events"`, Spec test A-req-3 grün                                             |
| A5  | Modal mit aria-modal + Focus-Trap + Escape (`@base-ui/react/dialog`)                 | ✓ VERIFIED | `event-modal.tsx` 287 LOC verwendet `Dialog.Root/Portal/Backdrop/Popup/Close/Title/Description`, native a11y, Spec test A-req-4 grün                |
| A6  | Anmelde-Flow: Non-Member → `/join?redirect_after=/events/[slug]`, Member → ctaUrl    | ✓ VERIFIED | `event-modal.tsx` enthält `buildSafeRedirectAfter` mit `/^[a-zA-Z0-9-_]+$/` whitelist, `noopener,noreferrer` auf `window.open`, Spec A-req-5 grün   |
| A7  | `/events/[slug]` rendert als Standalone-Page                                         | ✓ VERIFIED | `[slug]/page.tsx` 212 LOC, `await headers()`, `getEventBySlug`, `notFound()`, build zeigt `ƒ /events/[slug]`, Spec A-req-6 grün                     |
| A8  | Sitemap enthält `/events` + jeden Event-Slug                                         | ✓ VERIFIED | `sitemap.ts` importiert `getUpcomingEvents/getPastEvents`, mappt `${BASE}/events/${e.slug}` priority 0.8, `/events` static priority 0.9, Spec grün  |
| A9  | Lighthouse `/events` ≥ 90 (alle 4 Kategorien)                                        | ? UNCERTAIN | Plan 05 hat Lighthouse-Checkpoint auto-approved unter `--auto`, deferred zu Vercel-Preview. Siehe Human-Verification.                              |

#### Track B — Tools-App

| #   | Truth                                                                                | Status     | Evidence                                                                                                                                                |
| --- | ------------------------------------------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B1  | Logged-out: Primary CTA + Secondary Link sichtbar                                    | ✓ VERIFIED | `GlobalLayout.tsx` Zeilen 228+236 — `data-cta="primary-register"` + `data-cta="secondary-login"`, Spec B-req-1 grün                                     |
| B2  | „Kostenlos registrieren" → `https://generation-ai.org/join?utm_source=tools`         | ✓ VERIFIED | Exakter href hardcoded (Z. 228, 363), Spec B-req-2 grün (assert exact href value)                                                                       |
| B3  | Tools-Hero mit DS-Typography (Geist Mono H1) zwischen Header und FilterBar            | ✓ VERIFIED | `tools-hero.tsx` 57 LOC enthält "// deine ki-tool-bibliothek" + "KI-Tools, kuratiert für dich." + "Über 100 Tools", `font-mono` + `var(--fs-h1)`, in `HomeLayout.tsx` INSIDE `overflow-y-auto`, Spec B-req-3 grün |
| B4  | Tools-app Nav strukturell identisch zur Hauptdomain (5 Items, Tools active)          | ✓ VERIFIED | 5 nav-items mit `data-nav-item` selectors (events/tools/community/partner/about), Tools hat `aria-current="page"` + accent border, mobile-burger panel mit denselben 5 Items, Spec B-req-4 grün |
| B5  | Logged-in tools-app: User-Menu unverändert (Phase 12/19 Regression-Schutz)           | ✓ VERIFIED | `mode === 'public'` branching preserved, Settings (`/settings`) + Signout (`/auth/signout`) Form intact in else-Branch (Z. 246, 261), Logo `https://generation-ai.org` intact, Spec B-req-5 grün (oder skip-graceful in Dev) |
| B6  | Cross-domain Links nutzen `<a href>` (B-05 voller Page-Load)                         | ✓ VERIFIED | Alle 4 cross-domain Items + Primary CTA sind `<a href>` nicht `<Link>` — verifiziert via grep                                                           |
| B7  | Lighthouse tools-app ≥ 90                                                            | ? UNCERTAIN | Plan 09 hat Lighthouse explizit deferred zu pre-launch QA. Siehe Human-Verification.                                                                    |

**Score:** 16/16 truths technisch verifiziert (2 davon mit verbleibender Lighthouse-Komponente die nicht headless prüfbar ist → human_needed)

### Deferred Items

Items die nicht direkt erfüllt sind, aber explizit von späteren Phasen abgedeckt werden.

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | Website Header `navLinks` enthält `/events` Eintrag | Phase 27 (Copy-Pass & Launch-Cleanup) | Plan-09-SUMMARY threat-flag explicit: "Phase 27 copy-pass should add Events to website header.tsx navLinks array between Tools and Community." Phase 27 ist `discussed` und Goal/Scope ("Dummy-Data-Cleanup", "Finaler Durchgang vor Launch") deckt diese Lücke. Außerhalb Phase 22.6 Scope (CONTEXT.md Track A: nur /events Route + Sitemap; Track B: nur tools-app). |

### Required Artifacts

#### Track A

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `apps/website/lib/mdx/events.ts` | MDX-Adapter mit getUpcoming/getPast/getBySlug/getSlugs/validateEventFrontmatter, ≥80 LOC | ✓ VERIFIED | 204 LOC. Alle 7 Symbols exportiert. Pitfall-4-Guard (slug === fileSlug) auf Z. 121, T-22.6-A-SLUG mitigated. React `cache()`-wrapped. |
| `apps/website/content/events/*.mdx` (3) + `past/*.mdx` (2) | 5 Placeholder-MDX, slug==filename, example:true, alle 3 Formate, past mit recap | ✓ VERIFIED | Alle 5 OK, Umlaute korrekt (5/5 Files), Format-Diversität: Masterclass + Workshop + Speaker Session |
| `apps/website/app/events/page.tsx` | Server Component mit `await headers()`, ƒ in Build | ✓ VERIFIED | 44 LOC, `await headers()` Z. 32, build output bestätigt `ƒ /events` |
| `apps/website/app/events/events-client.tsx` | "use client" + MotionConfig nonce + Header/Footer | ✓ VERIFIED | 49 LOC, contains `"use client"` + `<MotionConfig nonce={nonce}>` + `<Header/>` + `<Footer/>` |
| `apps/website/app/events/components/events-hero.tsx` | LabeledNodes BG + max-w-4xl + var(--fs-display) H1 | ✓ VERIFIED | 74 LOC, `<LabeledNodes>`, `data-section="events-hero"`, `var(--fs-display)`, H1 "Events, die dich weiterbringen.", `useReducedMotion` |
| `apps/website/app/events/components/events-grid.tsx` | 3-Card-Grid + Mehr-Anzeigen + EventModal-Mount | ✓ VERIFIED | 115 LOC, `data-section="events-grid"`, `data-event-card`, `data-action="show-more-events"`, modal state lifted (selectedEvent + modalOpen), `<EventModal>` rendered |
| `apps/website/app/events/components/event-card.tsx` | role=button + tabIndex + onOpenModal required + BeispielBadge | ✓ VERIFIED | 128 LOC, `role="button"` + `tabIndex={0}`, `onOpenModal` jetzt required (Plan 04), `BeispielBadge` import |
| `apps/website/app/events/components/event-modal.tsx` | @base-ui Dialog, ≥100 LOC, buildSafeRedirectAfter, noopener,noreferrer | ✓ VERIFIED | 287 LOC, alle Dialog-Primitives, regex-whitelist, `noopener,noreferrer`, `aria-label="Event schließen"` |
| `apps/website/app/events/components/events-archive.tsx` | "Vergangene Events", data-archive-card, NO CTA/modal (A-06) | ✓ VERIFIED | 116 LOC, `Vergangene Events` H2, `data-section="events-archive"`, `data-archive-card`, kein Modal/CTA |
| `apps/website/app/events/components/events-formats.tsx` | 3 Format-Cards | ✓ VERIFIED | Workshops + Speaker Sessions + Masterclasses, 3-col grid |
| `apps/website/app/events/components/events-members-only.tsx` | "Events nur für Mitglieder" + Link to /join | ✓ VERIFIED | Lock icon + Copy + `<Link href="/join">` |
| `apps/website/app/events/components/events-final-cta.tsx` | "Keine Events verpassen." + CTA to /join | ✓ VERIFIED | H2 + `<Link href="/join">` |
| `apps/website/app/events/[slug]/page.tsx` | Standalone Page + await headers() + notFound() + generateMetadata | ✓ VERIFIED | 212 LOC, alles vorhanden, `noopener noreferrer` auf externem ctaUrl-Link, build zeigt `ƒ /events/[slug]` |
| `apps/website/app/sitemap.ts` | enthält /events (priority 0.9) + /events/[slug] (priority 0.8) | ✓ VERIFIED | imports getUpcoming+getPast, /events priority 0.9, per-slug priority 0.8 |
| `apps/website/lib/mdx/__tests__/events.test.ts` | 7 vitest tests, alle grün | ✓ VERIFIED | 7/7 passing (live Vitest-Run 1.10s, 0 todo) |
| `packages/e2e-tools/tests/events.spec.ts` | 7 active Playwright tests (A-req-1..6, A-req-8) | ✓ VERIFIED | 7 active `test()`, 0 echte `test.fixme()` (das eine Match war im Header-Kommentar). Per Plan 05 SUMMARY 7/7 grün gegen lokalen Prod-Build. |

#### Track B

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `apps/tools-app/components/layout/GlobalLayout.tsx` | Plan-07 CTAs + Plan-09 Nav-Mirror + Mobile Burger, MIRROR-Comment | ✓ VERIFIED | MIRROR Z. 4, 5 Nav-Items + `data-nav-item` selectors, `aria-current="page"`, Mobile-Burger + Panel mit selben Items, CTAs mit `mode === 'public'` Guard, alle Phase 12/19 Auth-Elemente intact |
| `apps/tools-app/components/tools-hero.tsx` | Geist Mono H1 var(--fs-h1), 3 Zeilen Copy mit korrekten Umlauten | ✓ VERIFIED | 57 LOC, exakter Copy, `data-section="tools-hero"`, `useReducedMotion`, `font-mono`, `var(--fs-h1)` |
| `apps/tools-app/components/HomeLayout.tsx` | ToolsHero INSIDE overflow-y-auto Container (Pitfall 2) | ✓ VERIFIED | Z. 6 import, Z. 177 wrapper opens, Z. 182 ToolsHero, Z. 183 FilterBar, Z. 184 CardGrid alle inside scroll container |
| `packages/e2e-tools/tests/tools-app.spec.ts` | 5 active Playwright tests (B-req-1..5) | ✓ VERIFIED | 5 active `test()`, 0 fixme. B-req-5 hat graceful `test.skip()` ohne TEST_USER_*-Env-Vars. Per Plan 09 lokaler Run: 5 passed (9.0s) |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `events/page.tsx` | `lib/mdx/events.ts` | `import { getUpcomingEvents, getPastEvents }` | ✓ WIRED | Import + 2x await call (Z. 34, 35) |
| `events/page.tsx` | `next/headers` | `await headers()` (CSP Pitfall) | ✓ WIRED | Z. 32 + 2 explanatory comments |
| `events/[slug]/page.tsx` | `lib/mdx/events.ts` | `getEventBySlug + getEventSlugs` | ✓ WIRED | Z. 29 import + Z. 37 (slugs) + Z. 45/80 (bySlug) |
| `events/[slug]/page.tsx` | `next/headers` | `await headers()` | ✓ WIRED | Z. 78 + comment |
| `events-grid.tsx` | `event-modal.tsx` | `import { EventModal }` + state lift + render | ✓ WIRED | Z. 13 import, Z. 26-27 useState, Z. 108 render mit selectedEvent + modalOpen |
| `event-card.tsx` | `events-grid.tsx` | `onOpenModal` prop callback (jetzt required) | ✓ WIRED | Z. 15 type, Z. 33 handleClick → onOpenModal(event), Z. 47 onClick wired |
| `event-modal.tsx` | `@base-ui/react/dialog` | Dialog.Root/Portal/Backdrop/Popup/Close/Title/Description | ✓ WIRED | Z. 28 import, alle 7 Primitives genutzt |
| `event-modal.tsx` | `@genai/auth/browser` | `createClient` für Session-Check | ✓ WIRED | Z. 30 import, lazy useEffect on modal open. `createClient` ist exportiert (verifiziert in `packages/auth/src/browser.ts` Z. 3) |
| `event-modal.tsx` | `/join?redirect_after=` | `router.push` mit sanitized path | ✓ WIRED | Z. 118 router.push template + buildSafeRedirectAfter regex whitelist |
| `sitemap.ts` | `lib/mdx/events.ts` | `getUpcoming + getPast` for URL enumeration | ✓ WIRED | Z. 3 import + Z. 30/31 await + Z. 35 mapping |
| `tools-hero.tsx` (mounted) | `HomeLayout.tsx` | `<ToolsHero />` inside overflow-y-auto | ✓ WIRED | Z. 6 import + Z. 182 mount |
| `GlobalLayout` Primary CTA | `https://generation-ai.org/join?utm_source=tools` | hardcoded `<a href>` (B-05) | ✓ WIRED | Z. 228 (desktop) + Z. 363 (mobile panel) |
| `GlobalLayout` 4 cross-domain nav | `https://generation-ai.org/(events|community|partner|about)` | hardcoded `<a href>` × 2 (desktop + mobile) | ✓ WIRED | Z. 108-141 (desktop) + Z. 323-356 (mobile panel) |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `events/page.tsx` → `EventsClient` | `upcomingEvents`, `pastEvents` | `getUpcomingEvents()` + `getPastEvents()` from `lib/mdx/events.ts` reading 5 real MDX files via `readAllFrontmatter` + `gray-matter` | ✓ Yes — gray-matter@4.0.3 verifiziert installed in `apps/website/node_modules`, 5 MDX-Files vorhanden + parsed (Vitest tests bestätigen) | ✓ FLOWING |
| `events-grid.tsx` | `events` prop | from `EventsClient` → `page.tsx` → MDX adapter | ✓ Yes — gleicher Flow, plus 3-Card-Slice + Mehr-Anzeigen toggle preserves data | ✓ FLOWING |
| `event-card.tsx` | `event.frontmatter` | from grid via prop | ✓ Yes — fm fields direkt rendered (title, date, format, level, location, partner, ctaUrl, speakers) | ✓ FLOWING |
| `event-modal.tsx` | `event` prop + `isAuthed` state | event from grid; auth via createClient().auth.getSession() | ✓ Yes — getSession liefert echte Supabase-Session (lazy on open). Plus Plan-04 SUMMARY: alle Felder rendered, keine Hardcoded-Empty-Props | ✓ FLOWING |
| `events/[slug]/page.tsx` | `event` | `getEventBySlug(slug)` reads MDX | ✓ Yes — 404 path geht via notFound() bei null | ✓ FLOWING |
| `events-archive.tsx` | `events` prop | `getPastEvents()` returns 2 archive entries | ✓ Yes — Plan-05 zeigt Sitemap inklusive `/events/2026-03-10-launch-event` (=archive entry, Spec asserts) | ✓ FLOWING |
| `sitemap.ts` | `eventEntries` | `getUpcomingEvents + getPastEvents` map to URLs | ✓ Yes — Spec A-req-8 asserts `/events/2026-05-15-prompting-masterclass` AND `/events/2026-03-10-launch-event` in sitemap.xml | ✓ FLOWING |
| `tools-hero.tsx` | static copy (no dynamic data) | N/A — text-only V1 per B-09 | N/A (kein dynamisches Data) | ✓ N/A |
| `GlobalLayout` Nav | static hrefs (B-08 V1 duplicate) | hardcoded literals | N/A (statisch) | ✓ N/A |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Vitest events MDX adapter (7 unit tests) | `pnpm --filter @genai/website test lib/mdx/__tests__/events.test.ts --run` | `Test Files 1 passed (1) / Tests 7 passed (7) / Duration 1.10s` | ✓ PASS |
| Website prod build + dynamic /events route | `pnpm --filter @genai/website build` (grep ƒ /events) | `ƒ /events` + `ƒ /events/[slug]` in build output | ✓ PASS |
| Tools-app prod build (no regression) | `pnpm --filter @genai/tools-app build` | Build erfolgreich, alle Routes ƒ (Dynamic) inkl. /, /datenschutz, /impressum, /login, /settings | ✓ PASS |
| Auth helper exports `createClient` | `grep -n "export.*createClient" packages/auth/src/browser.ts` | Z. 3 `export function createClient(...)` | ✓ PASS |
| `gray-matter` resolved (Plan 02 Rule-3 fix sticky?) | `ls apps/website/node_modules/gray-matter/package.json` | gray-matter@4.0.3 vorhanden | ✓ PASS |
| Playwright events.spec.ts vollständig (Live-Run) | manuell gegen prod-Build | (per Plan 05 SUMMARY: 7 passed, 0 failed; geknownter Test-Infra-Stand seit STATE.md) | ? SKIP (kein laufender Dev-Server während Verifikation, kein webServer-Config) |
| Playwright tools-app.spec.ts (Live-Run) | manuell gegen tools-app dev | (per Plan 09 SUMMARY: 5 passed (9.0s) gegen localhost:3001) | ? SKIP (kein laufender Dev-Server) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| A-req-1 | 01, 03 | /events lädt Hero + Grid | ✓ SATISFIED | events.spec.ts:24 active test, page.tsx + events-hero.tsx + events-grid.tsx vorhanden |
| A-req-2 | 01, 03 | Grid zeigt mind. 1 Event aus MDX | ✓ SATISFIED | events.spec.ts:33 active test, 3 upcoming MDX-Files, getUpcomingEvents wired |
| A-req-3 | 01, 03 | Mehr-Anzeigen Button + Client-State | ✓ SATISFIED | events.spec.ts:43 active test, useState toggle + INITIAL_VISIBLE=3 + slice |
| A-req-4 | 01, 04 | Modal mit aria-modal + Escape | ✓ SATISFIED | events.spec.ts:61 active test, @base-ui Dialog.Popup native a11y |
| A-req-5 | 01, 04 | redirect_after sanitized | ✓ SATISFIED | events.spec.ts:92 active test, buildSafeRedirectAfter regex `/^[a-zA-Z0-9-_]+$/` |
| A-req-6 | 01, 05 | /events/[slug] Standalone | ✓ SATISFIED | events.spec.ts:123 active test, [slug]/page.tsx + notFound() |
| A-req-7 | 02 | MDX-Adapter Filter/Sort/Validate | ✓ SATISFIED | 7/7 Vitest tests passing, validateEventFrontmatter mit slug/format guards |
| A-req-8 | 01, 05 | Sitemap enthält /events + /events/[slug] | ✓ SATISFIED | events.spec.ts:143 active test asserts upcoming + archive slugs |
| B-req-1 | 06, 07 | 2 sichtbare CTAs logged-out | ✓ SATISFIED | tools-app.spec.ts:8 active test, data-cta selectors verifiziert |
| B-req-2 | 06, 07 | utm_source=tools auf Register-href | ✓ SATISFIED | tools-app.spec.ts:23 active test, exact href assert |
| B-req-3 | 06, 08 | ToolsHero zwischen Header + FilterBar | ✓ SATISFIED | tools-app.spec.ts:32 active test, mounted inside scroll container |
| B-req-4 | 06, 09 | Alle 5 Nav-Items, Tools active | ✓ SATISFIED | tools-app.spec.ts:51 active test, 5 data-nav-item selectors + aria-current |
| B-req-5 | 06, 07, 09 | Logged-in: User-Menu statt CTAs | ✓ SATISFIED | tools-app.spec.ts:75 active test (mit graceful skip ohne TEST_USER_*), code preserves Settings/Signout in `mode !== 'public'` Branch |

**Coverage:** 13/13 Requirement-IDs satisfied. Keine ORPHANED Requirements (alle in Plan-Frontmatter referenziert).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `HomeLayout.tsx` | 116 | `placeholder="Tool suchen..."` | ℹ️ Info | False positive — HTML attribute placeholder für Search-Input, kein code-stub |
| `GlobalLayout.tsx` | 293-380 | Mobile-Burger ohne Focus-Trap/Auto-Focus/Escape-Handler | ⚠️ Warning | Per REVIEW.md WR-01: A11y-Lücke, V1 explizit akzeptiert (vermeidet `focus-trap-react` Dep). Tech-Debt für Phase 27/28+. |
| `HomeLayout.tsx` | 67-73, 99 | Inkonsistenter State-Reset Backdrop vs Escape | ⚠️ Warning | Per REVIEW.md WR-02: Pre-existing Code, von Phase 22.6 nur indirekt durch HomeLayout-Edit berührt. Nicht in 22.6 Scope einzuführen. |
| `tools-app.spec.ts` | – | Mobile-Nav (Burger + Panel) ohne E2E-Coverage | ⚠️ Warning | Per REVIEW.md WR-03: B-req-4 testet nur Desktop. Mobile-Burger-DOM existiert + ist gewired, fehlende Test-Coverage ist Test-Debt. |
| `tools-app.spec.ts` | 105-106 | `toHaveCount(1)` statt `toBeVisible()` für eingeloggt-Settings | ⚠️ Warning | Per REVIEW.md WR-04: Schwächerer Regression-Schutz, dokumentiert in Plan-09 SUMMARY als bewusste Wahl wegen `md:hidden`. |

**Klassifikation:** 0 Blocker, 4 Warnings (alle V1-akzeptabel + dokumentiert), 1 Info (false-positive). Phase 22.6 Code selber führt keine echten Stubs/TODOs/FIXME ein.

### Human Verification Required

#### 1. Lighthouse `/events` ≥ 90

**Test:** `pnpm --filter @genai/website build && pnpm --filter @genai/website start` lokal, dann `pnpm exec lighthouse http://localhost:3000/events --output=json --chrome-flags="--headless"` ODER auf Vercel-Preview öffnen
**Expected:** Performance / Accessibility / Best Practices / SEO alle ≥ 0.90
**Why human:** Lighthouse braucht prod-Build + nicht-flaky Network. Plan-05 hat das unter `--auto` zu Vercel-Preview deferred. ROADMAP-Success-Criterion explicit Lucas-verifiziert.

#### 2. Lighthouse tools-app ≥ 90

**Test:** Analog Test 1 für `http://localhost:3001` (oder Vercel-Preview tools-app)
**Expected:** Alle 4 Scores ≥ 0.90
**Why human:** Plan-09 hat unter `--auto` explizit deferred zu pre-launch QA.

#### 3. Mobile Modal als Full-Screen-Sheet

**Test:** Mobile-Viewport (≤767px, DevTools-Emulation oder echtes Gerät) → /events besuchen → Event-Card klicken
**Expected:** Modal sheet-from-bottom (nicht zentriert), Backdrop dimmt, Escape schließt, Anmelden-Button reicht in den unteren Sicherbereich
**Why human:** Visuelle UX-Verifikation, Spec checkt aria-modal aber nicht Sheet-vs-Centered-Layout

#### 4. Cross-Domain-Click verlässt tools-app (full page load)

**Test:** Auf tools-app (lokal oder Preview) Nav-Item „Events" klicken, dann „Community", „Für Partner", „Über uns"
**Expected:** Echter Page-Load auf generation-ai.org/* (kein SPA-Flicker), gleicher Tab. Click auf „Tools" bleibt im tools-app.
**Why human:** Browser-Verhalten, B-Validation expliziter Manual-Item

#### 5. Side-by-Side Sticky-Nav Vergleich Website ↔ tools-app

**Test:** localhost:3000 + localhost:3001 nebeneinander öffnen, beide scrollen
**Expected:** Beide Headers sticky, gleiche Höhe, gleiches Background-Treatment, kein visueller Drift
**Why human:** Plan-09 hat technisch verifiziert (verschiedene Layout-Strategien, gleicher UX-Outcome), aber visuelle Drift-Erkennung braucht Lucas live (B.3-Checkpoint expliziter Manual-Item)

#### 6. Logged-in tools-app Regression-Smoke

**Test:** Login als Test-User (admin@generation-ai.org), dann CTAs/User-Menu prüfen, Tool-Card klicken (Detail-Page lädt?), Chat öffnen, Lite/Pro toggeln, Logout
**Expected:** CTAs gone, Settings + Signout sichtbar, Tool-Detail lädt, Chat funktioniert, Lite/Pro toggle reagiert, Logout setzt zurück
**Why human:** B-req-5 Spec deckt Settings/Signout-Presence ab, aber Lite/Pro + Chat sind Plan-09 explizit zur „pre-launch QA" deferred. Phase 12/19 Auth-Flows = kritische Regressionen.

#### 7. Mobile Burger-UX

**Test:** ≤767px → Burger-Icon sichtbar, Click öffnet Panel (5 Items + logged-out: 2 CTAs / logged-in: nur 5 Items, keine CTAs), X-Click schließt
**Expected:** Wie beschrieben + ARIA aria-expanded toggelt
**Why human:** Mobile-Nav-Tests fehlen in tools-app.spec.ts (REVIEW.md WR-03). Touch-Test braucht reales Gerät bzw. DevTools-Mobile-Emulation.

### Gaps Summary

**Keine echten Gaps.** Phase 22.6 ist technisch feature-complete:

- **Track A:** /events Route lebt, MDX-Pipeline funktioniert (7/7 Vitest grün), Modal-Flow wired mit Open-Redirect-Guard, /events/[slug] Standalone existiert mit notFound, Sitemap erweitert. Build zeigt `ƒ` für beide Routes.
- **Track B:** Logged-out CTAs + utm_source eingebaut, ToolsHero an korrekter Stelle (Pitfall 2 mitigated), 5-Item Nav-Mirror mit Tools active + Mobile Burger, Phase-12/19 Auth-Flow unverändert. Build sauber.
- **Tests:** 7/7 Vitest + 7+5=12 Playwright registriert (alle aktiv, kein test.fixme übrig). Per Plan 05 + Plan 09 SUMMARYs sind alle gegen lokalen Build/Dev grün gelaufen.
- **Threat-Mitigations:** T-22.6-A-CSP (await headers), T-22.6-A-SLUG (Pitfall 4 Guard), T-22.6-A-AUTH (buildSafeRedirectAfter), T-22.6-A-NEWTAB (noopener,noreferrer), T-22.6-B-AUTH-REGRESSION (mode-Branch unverändert) — alle implementiert + verifiziert.

**Aber:** Phase 22.6 hat mehrere ROADMAP-Success-Criteria die nur visuell/manuell abprüfbar sind (Lighthouse, Mobile-Sheet, Cross-Domain-PageLoad, Sticky-Nav-Parity, Logged-in-Smoke, Mobile-Burger-Touch). Diese sind Lucas-vorbehalten. Status ist deshalb `human_needed`, nicht `passed`.

**Deferred (1 Item):** Website Header `navLinks` enthält noch keinen `/events` Eintrag — Plan-09 Side-Finding, explizit für Phase 27 (Copy-Pass) vorgesehen, in 22.6-Scope nie inkludiert.

**Code-Review (REVIEW.md):** 0 Critical, 4 Warnings (alle V1-akzeptabel: Mobile-Burger-A11y, State-Reset-Konsistenz, Mobile-Test-Coverage, B-req-5 Assert-Stärke), 6 Info. Keine Blocker für Pre-Launch-Polish-Ziel.

---

_Verified: 2026-04-25T13:25:00Z_
_Verifier: Claude (gsd-verifier)_
