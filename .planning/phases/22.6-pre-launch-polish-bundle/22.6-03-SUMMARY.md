---
phase: 22.6-pre-launch-polish-bundle
plan: "03"
subsystem: events-page-shell
tags: [events, hero, grid, mehr-anzeigen, csp-nonce, playwright, track-a]

requires:
  - phase: 22.6-02
    provides: "events.ts MDX adapter: getUpcomingEvents() + getPastEvents() + EventEntry type"
  - phase: 26-community-page-and-subdomain-integration
    provides: "community-hero.tsx pattern (LabeledNodes + max-w-4xl + --fs-display)"

provides:
  - "/events page.tsx: Server Component with mandatory await headers() → ƒ in build"
  - "events-client.tsx: Client wrapper with MotionConfig nonce + Header/Footer + all section composition"
  - "events-hero.tsx: LabeledNodes BG + max-w-4xl + --fs-display H1 + useReducedMotion guard"
  - "events-grid.tsx: 3-card grid + showAll useState toggle (Mehr-anzeigen) + empty state"
  - "event-card.tsx: BeispielBadge + brand-gradient fallback + role=button + keyboard support"
  - "events-formats.tsx: 3 format cards (Workshops/Speaker Sessions/Masterclasses)"
  - "events-members-only.tsx: accent left-border stripe + Lock icon + CTA to /join"
  - "events-final-cta.tsx: 'Keine Events verpassen.' + 'Jetzt beitreten' CTA"
  - "events-archive.tsx: stub returning null (Plan 05 fills it)"
  - "3 of 7 Playwright tests active and passing (lädt/grid/mehr-anzeigen)"

affects:
  - 22.6-04 (event-modal.tsx — wires onOpenModal prop in event-card.tsx, Plan 04 adds auth flow)
  - 22.6-05 (events-archive.tsx — stub replaced with full archive section)

tech-stack:
  added: []
  patterns:
    - "community-hero.tsx mirror: LabeledNodes + max-w-4xl + --fs-display + useReducedMotion guard"
    - "community-client.tsx mirror: 'use client' + MotionConfig nonce + Header + main + Footer"
    - "await headers() CSP-Pitfall guard — mandatory on all subpage Server Components (LEARNINGS.md)"
    - "onOpenModal?: optional prop pattern — Plan 03 no-op, Plan 04 wires real modal"
    - "brand-gradient fallback for missing imageUrl (Decision A-12 — no stock photos)"

key-files:
  created:
    - apps/website/app/events/page.tsx
    - apps/website/app/events/events-client.tsx
    - apps/website/app/events/components/events-hero.tsx
    - apps/website/app/events/components/events-grid.tsx
    - apps/website/app/events/components/event-card.tsx
    - apps/website/app/events/components/events-formats.tsx
    - apps/website/app/events/components/events-members-only.tsx
    - apps/website/app/events/components/events-final-cta.tsx
    - apps/website/app/events/components/events-archive.tsx
  modified:
    - packages/e2e-tools/tests/events.spec.ts (3 test.fixme → test, 4 fixme stubs remain)

key-decisions:
  - "CSP-guard via await headers() in page.tsx — enforces ƒ dynamic rendering (LEARNINGS.md)"
  - "EventsArchive stub returns null — Plan 05 fills; import resolves now, no breaking change"
  - "Event card fallback: linear-gradient brand-pattern (not SignalGrid canvas) — simpler, correct for Plan 03 scope"
  - "onOpenModal optional prop on EventCard — clean extension point, Plan 04 wires without touching event-card.tsx internals"

metrics:
  duration: "~5 min"
  completed: "2026-04-25"
  tasks: 3
  files_modified: 9
---

# Phase 22.6 Plan 03: Events Page Shell Summary

**Server Component (await headers() CSP-guard) + Client Wrapper + Hero (LabeledNodes) + Grid (Mehr-anzeigen useState) + Event Card (BeispielBadge + brand fallback) + Formats + Members-Only + Final-CTA sections — 3 Playwright tests active and passing, build shows ƒ /events**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-25T10:13:00Z
- **Completed:** 2026-04-25T10:17:54Z
- **Tasks:** 3
- **Files modified:** 9

## Accomplishments

### Task 1: Server Component + Client Wrapper + Hero

- `apps/website/app/events/page.tsx` — Server Component with `await headers()` CSP-guard (LEARNINGS.md mandatory). Calls `getUpcomingEvents()` + `getPastEvents()` from Plan 02 adapter. Passes nonce + events to `EventsClient`.
- `apps/website/app/events/events-client.tsx` — `"use client"` wrapper: `<MotionConfig nonce={nonce}>` + `<Header />` + `<main id="main-content">` + all section components + `<Footer />`. Matches community-client.tsx pattern exactly.
- `apps/website/app/events/components/events-hero.tsx` — Exact mirror of community-hero.tsx with events copy: eyebrow `// generation ai · events`, H1 `Events, die dich weiterbringen.`, subhead. LabeledNodes BG, `max-w-4xl`, `var(--fs-display)`, `useReducedMotion` guard, textShadow on all text layers.
- `apps/website/app/events/components/events-archive.tsx` — Stub returning `null` (Plan 05 fills).
- **Build verified:** `ƒ /events` in build output (NOT `○ /events`)

### Task 2: EventsGrid + EventCard

- `apps/website/app/events/components/events-grid.tsx` — `"use client"`, `useState(false)` showAll toggle, `INITIAL_VISIBLE = 3` slice, `data-section="events-grid"`, `data-event-card` on each card wrapper, `data-action="show-more-events"` on Mehr-anzeigen button (only when `events.length > 3`). Empty state (`data-section="events-empty"`) with `Bald neue Events` copy when no events.
- `apps/website/app/events/components/event-card.tsx` — `role="button"` + `tabIndex={0}` + `onKeyDown` Enter/Space. `BeispielBadge` when `frontmatter.example === true`. Brand-gradient fallback when no `imageUrl` (Decision A-12). `onOpenModal?` optional prop — Plan 04 wires without touching this file.
- **TypeScript clean, build still `ƒ /events`**

### Task 3: Formats + Members-Only + Final-CTA + Playwright tests

- `apps/website/app/events/components/events-formats.tsx` — 3 format cards (Wrench/Workshops, Mic/Speaker Sessions, GraduationCap/Masterclasses), `grid-cols-1 md:grid-cols-3`, `bg-bg-elevated border rounded-2xl p-6`, correct German Umlaute throughout.
- `apps/website/app/events/components/events-members-only.tsx` — Accent left-border stripe (`border-l-2 border-[var(--accent)]`), Lock icon, copy `Events nur für Mitglieder`, `Link href="/join"` → `Jetzt kostenlos registrieren →`.
- `apps/website/app/events/components/events-final-cta.tsx` — H2 `Keine Events verpassen.`, `Link href="/join"` → `Jetzt beitreten` (primary button style).
- `packages/e2e-tools/tests/events.spec.ts` — 3 `test.fixme` replaced with `test(`:
  - `lädt`: H1 text match + `data-section="events-hero"` + `data-section="events-grid"` visible
  - `grid`: `[data-event-card]` first card visible + count ≥ 1
  - `mehr anzeigen`: conditional check (≤3 events → button absent, >3 → button visible + click expands)
  - 4 remaining `test.fixme` stubs untouched (Plans 04+05)
- **Playwright 3/3 passing** against local prod build (`localhost:3000`)

## Task Commits

1. **Task 1: page.tsx + events-client.tsx + events-hero.tsx** — `98242ed` (feat)
2. **Task 2: EventsGrid + EventCard** — `fc03be6` (feat)
3. **Task 3: Formats + Members-Only + Final-CTA + Playwright tests** — `8a2027b` (feat)

## Deviations from Plan

### Auto-fixed Issues

None. Plan executed exactly as written. All components match UI-SPEC and plan code verbatim.

## Known Stubs

1. **events-archive.tsx** — Returns `null`. Intentional (Plan 05 fills it). Import resolves cleanly. The archive section is visually absent from /events until Plan 05. This does not prevent Plan 03's goal (events-grid + hero + formats + members-only + final-cta).

2. **EventCard.onOpenModal** — The prop is declared but all click handlers are no-ops in Plan 03. Visually: clicking a card or CTA button does nothing. Plan 04 wires the real modal + auth flow by passing `onOpenModal` from EventsGrid → EventCard.

Both stubs are intentional, scoped to future plans, and documented in the plan frontmatter.

## Threat Flags

None — no new network endpoints or auth paths introduced. `/events` is a public read-only route. T-22.6-A-CSP mitigated by `await headers()` confirmed in build output (`ƒ /events`). T-22.6-A-XSS-IMG: imageUrl from maintainer MDX only, React escapes attribute values.

## Self-Check: PASSED

- `apps/website/app/events/page.tsx` — FOUND (contains `await headers()`)
- `apps/website/app/events/events-client.tsx` — FOUND (contains `"use client"` + `<MotionConfig`)
- `apps/website/app/events/components/events-hero.tsx` — FOUND (contains `<LabeledNodes>` + `data-section="events-hero"`)
- `apps/website/app/events/components/events-grid.tsx` — FOUND (contains `Mehr anzeigen` + `data-action="show-more-events"`)
- `apps/website/app/events/components/event-card.tsx` — FOUND (contains `BeispielBadge` + `role="button"`)
- `apps/website/app/events/components/events-formats.tsx` — FOUND (Workshops/Speaker Sessions/Masterclasses)
- `apps/website/app/events/components/events-members-only.tsx` — FOUND (contains `Events nur für Mitglieder`)
- `apps/website/app/events/components/events-final-cta.tsx` — FOUND (contains `Keine Events verpassen.`)
- `apps/website/app/events/components/events-archive.tsx` — FOUND (stub, returns null)
- `packages/e2e-tools/tests/events.spec.ts` — FOUND (3 active test(), 4 test.fixme() remaining)
- Commit `98242ed` — FOUND
- Commit `fc03be6` — FOUND
- Commit `8a2027b` — FOUND
- Build shows `ƒ /events` (NOT `○ /events`) — CONFIRMED
- Playwright 3/3 passing — CONFIRMED
