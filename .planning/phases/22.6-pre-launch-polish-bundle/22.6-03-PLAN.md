---
phase: 22.6
plan: 03
type: execute
wave: 2
branch: feature/phase-22.6-pre-launch-polish
depends_on:
  - 22.6-02
files_modified:
  - apps/website/app/events/page.tsx
  - apps/website/app/events/events-client.tsx
  - apps/website/app/events/components/events-hero.tsx
  - apps/website/app/events/components/events-grid.tsx
  - apps/website/app/events/components/event-card.tsx
  - apps/website/app/events/components/events-formats.tsx
  - apps/website/app/events/components/events-members-only.tsx
  - apps/website/app/events/components/events-final-cta.tsx
  - packages/e2e-tools/tests/events.spec.ts
autonomous: true
requirements:
  - A-req-1
  - A-req-2
  - A-req-3
must_haves:
  truths:
    - "/events route is reachable at http://localhost:3000/events"
    - "/events page renders Hero (LabeledNodes BG + H1) + Grid + Formats + Members-Only + Final-CTA"
    - "Build output for /events shows ƒ (dynamic), NOT ○ (static) — CSP-Nonce safety"
    - "Grid renders first 3 upcoming events from MDX"
    - "Mehr-Anzeigen button appears when upcoming events count > 3 and expands grid client-side"
    - "Empty state displays when getUpcomingEvents() returns []"
  artifacts:
    - path: "apps/website/app/events/page.tsx"
      provides: "Server Component: await headers() + getUpcomingEvents() + render EventsClient"
      contains: "await headers()"
    - path: "apps/website/app/events/events-client.tsx"
      provides: "'use client' wrapper: MotionConfig nonce + Header + main + Footer + section composition"
      contains: "use client"
    - path: "apps/website/app/events/components/events-hero.tsx"
      provides: "Hero per UI-SPEC: LabeledNodes BG, max-w-4xl, --fs-display H1, eyebrow + subhead"
      contains: "LabeledNodes"
    - path: "apps/website/app/events/components/events-grid.tsx"
      provides: "3-Card grid + Mehr-Anzeigen button (client state)"
      contains: "Mehr anzeigen"
    - path: "apps/website/app/events/components/event-card.tsx"
      provides: "Card with header image / SignalGrid-Echo fallback + tags + meta + CTA-button (modal trigger placeholder for Plan 04)"
      contains: "BeispielBadge"
  key_links:
    - from: "apps/website/app/events/page.tsx"
      to: "apps/website/lib/mdx/events.ts"
      via: "import { getUpcomingEvents } from '@/lib/mdx/events'"
      pattern: "from ['\"]@/lib/mdx/events['\"]"
    - from: "apps/website/app/events/page.tsx"
      to: "next/headers"
      via: "await headers() to enforce dynamic rendering (CSP nonce)"
      pattern: "await headers\\(\\)"
    - from: "apps/website/app/events/events-client.tsx"
      to: "apps/website/components/layout/header.tsx + footer.tsx"
      via: "shared site-wide chrome"
      pattern: "<Header|<Footer"
---

<objective>
Build the /events page shell: Server-Component (with mandatory `await headers()` CSP-guard), Client-Wrapper, Hero, Grid (with Mehr-Anzeigen client state), Event-Card (modal trigger placeholder), Format-Section, Members-Only-Hinweis, and Final-CTA.

Modal interaction + auth-flow are Plan 04. This plan ends with cards that visually look right and have a placeholder onClick (`console.log` or simple state toggle) — Plan 04 wires the modal.

Purpose: Make the page exist and pass the "lädt + grid" Playwright assertions.
Output: Working /events route renders all sections, Mehr-Anzeigen toggles, build shows `ƒ /events`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/22.6-pre-launch-polish-bundle/22.6-CONTEXT.md
@.planning/phases/22.6-pre-launch-polish-bundle/22.6-RESEARCH.md
@.planning/phases/22.6-pre-launch-polish-bundle/22.6-UI-SPEC.md
@apps/website/app/community/page.tsx
@apps/website/app/community/community-client.tsx
@apps/website/app/community/components/community-hero.tsx
@apps/website/app/community/components/community-final-cta.tsx
@apps/website/components/ui/beispiel-badge.tsx
@LEARNINGS.md
@apps/website/AGENTS.md

<interfaces>
<!-- Data layer (Plan 02 output) -->

```typescript
// apps/website/lib/mdx/events.ts
export const getUpcomingEvents: () => Promise<EventEntry[]>;
export const getPastEvents: () => Promise<EventEntry[]>;
export interface EventFrontmatter {
  title: string; date: string; format: "Workshop" | "Speaker Session" | "Masterclass";
  level: "Einsteiger" | "Fortgeschritten" | "Expert"; location: string;
  partner: string | null; ctaUrl: string; slug: string;
  speakers: Speaker[]; recap?: string; imageUrl?: string; example?: boolean;
  description?: string;
}
```

<!-- Site chrome -->
- `<Header />` from `apps/website/components/layout/header.tsx` — sticky nav
- `<Footer />` from `apps/website/components/layout/footer.tsx` — site footer
- `<MotionConfig nonce={nonce}>` wrapper required (CSP)
- `<LabeledNodes>` from `apps/website/components/ui/labeled-nodes.tsx` — Hero BG canvas
- `<SignalGrid>` from `apps/website/components/ui/signal-grid.tsx` — card BG fallback (low density)
- `<SectionTransition variant="soft-fade" | "signal-echo" />` from `apps/website/components/ui/section-transition.tsx`

<!-- BeispielBadge -->

```typescript
// apps/website/components/ui/beispiel-badge.tsx
export function BeispielBadge(props: { className?: string }): JSX.Element;
// Theme-aware: light → brand-red, dark → brand-neon
```

<!-- Hero pattern (CANONICAL — copy verbatim with copy-swap) -->
See community-hero.tsx structure. Section uses `<LabeledNodes>` BG, container `max-w-4xl px-6 py-20 text-center`, eyebrow uses `// generation ai · events`, H1 uses `var(--fs-display)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create Server-Component page.tsx + Client-Wrapper events-client.tsx + Hero</name>
  <files>apps/website/app/events/page.tsx, apps/website/app/events/events-client.tsx, apps/website/app/events/components/events-hero.tsx</files>
  <read_first>
    - apps/website/app/community/page.tsx (Server-Component reference — copy + adapt)
    - apps/website/app/community/community-client.tsx (Client-Wrapper reference)
    - apps/website/app/community/components/community-hero.tsx (Hero pattern reference — mirror exactly)
    - apps/website/AGENTS.md (Subpage-Hero-Pattern + force-dynamic guidance)
    - LEARNINGS.md (CSP-Pitfall — `await headers()` mandatory)
    - .planning/phases/22.6-pre-launch-polish-bundle/22.6-UI-SPEC.md (Hero copy + animation contract)
  </read_first>
  <action>
**File 1: `apps/website/app/events/page.tsx`** (Server Component, MUST call `await headers()`):

```tsx
import { headers } from "next/headers";
import { Metadata } from "next";
import { getUpcomingEvents, getPastEvents } from "@/lib/mdx/events";
import { EventsClient } from "./events-client";

export const metadata: Metadata = {
  title: "Events — Generation AI",
  description:
    "Workshops, Speaker Sessions und Masterclasses — exklusiv für die Generation AI Community.",
  alternates: { canonical: "https://generation-ai.org/events" },
};

export default async function EventsPage() {
  // CRITICAL (LEARNINGS.md CSP-Pitfall):
  // await headers() forces this route to render dynamically (ƒ in build output).
  // Without this, route renders static (○) → CSP-Nonce middleware never runs → black page in prod.
  const nonce = (await headers()).get("x-nonce") ?? "";

  const upcomingEvents = await getUpcomingEvents();
  const pastEvents = await getPastEvents();

  return (
    <EventsClient
      nonce={nonce}
      upcomingEvents={upcomingEvents}
      pastEvents={pastEvents}
    />
  );
}
```

**File 2: `apps/website/app/events/events-client.tsx`** (Client wrapper):

```tsx
"use client";

import { MotionConfig } from "motion/react";
import type { EventEntry } from "@/lib/mdx/events";
import { Header } from "@/components/layout/header";
import { Footer } from "@/components/layout/footer";
import { SectionTransition } from "@/components/ui/section-transition";
import { EventsHero } from "./components/events-hero";
import { EventsGrid } from "./components/events-grid";
import { EventsFormats } from "./components/events-formats";
import { EventsMembersOnly } from "./components/events-members-only";
import { EventsArchive } from "./components/events-archive";
import { EventsFinalCta } from "./components/events-final-cta";

interface EventsClientProps {
  nonce: string;
  upcomingEvents: EventEntry[];
  pastEvents: EventEntry[];
}

export function EventsClient({
  nonce,
  upcomingEvents,
  pastEvents,
}: EventsClientProps) {
  return (
    <MotionConfig nonce={nonce}>
      <Header />
      <main id="main-content">
        <EventsHero />
        <EventsGrid events={upcomingEvents} />
        <SectionTransition variant="soft-fade" />
        <EventsFormats />
        <SectionTransition variant="soft-fade" />
        <EventsMembersOnly />
        <SectionTransition variant="soft-fade" />
        <EventsArchive events={pastEvents} />
        <SectionTransition variant="signal-echo" />
        <EventsFinalCta />
      </main>
      <Footer />
    </MotionConfig>
  );
}
```

NOTE on `EventsArchive`: declared here but full implementation is Plan 05. For now create a thin stub `apps/website/app/events/components/events-archive.tsx` that exports `export function EventsArchive({ events }: { events: EventEntry[] }) { return null; }` so the import resolves. Plan 05 fills it.

**File 3: `apps/website/app/events/components/events-hero.tsx`** (mirror community-hero.tsx structure verbatim, swap copy from UI-SPEC):

```tsx
"use client";

import { motion, useReducedMotion } from "motion/react";
import { LabeledNodes } from "@/components/ui/labeled-nodes";

export function EventsHero() {
  const prefersReducedMotion = useReducedMotion();

  return (
    <section
      aria-labelledby="events-hero-heading"
      data-section="events-hero"
      className="relative isolate"
    >
      <LabeledNodes className="flex min-h-[calc(100vh-5rem)] flex-col items-center justify-center">
        <motion.div
          initial={prefersReducedMotion ? false : { opacity: 0, y: 16 }}
          animate={prefersReducedMotion ? {} : { opacity: 1, y: 0 }}
          transition={{ duration: 0.7, ease: "easeOut" }}
          className="relative z-10 mx-auto max-w-4xl px-6 py-20 text-center"
        >
          <div
            className="inline-flex items-center gap-2 font-mono text-[11px] font-bold uppercase tracking-[0.2em] text-text-muted"
            style={{
              textShadow:
                "0 0 10px rgba(var(--bg-rgb), 1), 0 0 4px rgba(var(--bg-rgb), 1)",
            }}
          >
            <span
              className="inline-block h-1.5 w-1.5 rounded-full"
              style={{ background: "var(--accent)" }}
            />
            // generation ai · events
          </div>

          <h1
            id="events-hero-heading"
            style={{
              fontSize: "var(--fs-display)",
              textShadow:
                "0 0 18px rgba(var(--bg-rgb), 1), 0 0 8px rgba(var(--bg-rgb), 1)",
            }}
            className="mt-6 font-mono font-bold leading-[1.02] tracking-[-0.03em] text-text text-balance"
          >
            Events, die dich weiterbringen.
          </h1>

          <p
            className="mt-6 text-lg sm:text-xl leading-[1.5] text-text-muted max-w-2xl mx-auto"
            style={{
              textShadow:
                "0 0 10px rgba(var(--bg-rgb), 1), 0 0 4px rgba(var(--bg-rgb), 1)",
            }}
          >
            Workshops, Speaker Sessions und Masterclasses — exklusiv für unsere Community.
          </p>
        </motion.div>
      </LabeledNodes>
    </section>
  );
}
```

Also create the stub `apps/website/app/events/components/events-archive.tsx` to satisfy the import:
```tsx
import type { EventEntry } from "@/lib/mdx/events";

// Stub — implemented in Plan 05
export function EventsArchive({ events: _events }: { events: EventEntry[] }) {
  return null;
}
```
  </action>
  <verify>
    <automated>pnpm --filter @genai/website build 2>&1 | grep -E "ƒ /events|○ /events" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/website/app/events/page.tsx` exists
    - File contains `await headers()` (CSP-Pitfall guard, MANDATORY)
    - File contains `import { getUpcomingEvents, getPastEvents } from "@/lib/mdx/events"`
    - File `apps/website/app/events/events-client.tsx` exists, contains `"use client"` directive
    - File contains `<MotionConfig nonce={nonce}>`
    - File `apps/website/app/events/components/events-hero.tsx` exists
    - File contains `<LabeledNodes>` and `data-section="events-hero"`
    - File contains H1 text `Events, die dich weiterbringen.`
    - File contains `useReducedMotion`
    - `pnpm --filter @genai/website build` output shows `ƒ /events` (NOT `○ /events`)
  </acceptance_criteria>
  <done>/events route exists, builds dynamically, hero renders with LabeledNodes BG + correct copy.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build EventsGrid (3-Card grid + Mehr-Anzeigen) + EventCard (placeholder modal trigger)</name>
  <files>apps/website/app/events/components/events-grid.tsx, apps/website/app/events/components/event-card.tsx</files>
  <read_first>
    - apps/website/app/events/components/events-hero.tsx (Task 1 output — pattern reference)
    - apps/website/components/ui/beispiel-badge.tsx (BeispielBadge component)
    - apps/website/components/ui/signal-grid.tsx (SignalGrid for card-BG fallback)
    - .planning/phases/22.6-pre-launch-polish-bundle/22.6-UI-SPEC.md (Event Card + Event Grid contract)
    - apps/website/lib/mdx/events.ts (EventEntry + EventFrontmatter shape)
  </read_first>
  <action>
**File 1: `apps/website/app/events/components/events-grid.tsx`** (client component, holds Mehr-Anzeigen state):

```tsx
"use client";

import { useState } from "react";
import { motion, useReducedMotion } from "motion/react";
import type { EventEntry } from "@/lib/mdx/events";
import { EventCard } from "./event-card";

interface EventsGridProps {
  events: EventEntry[];
}

const INITIAL_VISIBLE = 3;

export function EventsGrid({ events }: EventsGridProps) {
  const prefersReducedMotion = useReducedMotion();
  const [showAll, setShowAll] = useState(false);

  const visibleEvents = showAll ? events : events.slice(0, INITIAL_VISIBLE);
  const hasMore = events.length > INITIAL_VISIBLE && !showAll;

  if (events.length === 0) {
    return (
      <section
        aria-labelledby="events-empty-heading"
        data-section="events-empty"
        className="mx-auto max-w-4xl px-6 py-16 text-center"
      >
        <h2
          id="events-empty-heading"
          className="font-sans font-bold text-text"
          style={{ fontSize: "var(--fs-h2)" }}
        >
          Bald neue Events
        </h2>
        <p className="mt-4 text-base leading-[1.65] text-text-muted">
          Gerade keine Events in Planung — aber das ändert sich bald. Trag dich ein und bleib informiert.
        </p>
      </section>
    );
  }

  return (
    <section
      aria-labelledby="events-grid-heading"
      data-section="events-grid"
      className="mx-auto max-w-6xl px-6 py-16"
    >
      <h2
        id="events-grid-heading"
        className="font-sans font-bold text-text mb-8"
        style={{ fontSize: "var(--fs-h2)" }}
      >
        Kommende Events
      </h2>

      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {visibleEvents.map((event, idx) => (
          <motion.div
            key={event.slug}
            initial={prefersReducedMotion ? false : { opacity: 0, y: 8 }}
            animate={prefersReducedMotion ? {} : { opacity: 1, y: 0 }}
            transition={{ duration: 0.3, delay: idx * 0.05 }}
            data-event-card
          >
            <EventCard event={event} />
          </motion.div>
        ))}
      </div>

      {hasMore && (
        <div className="mt-8 flex justify-center">
          <button
            type="button"
            onClick={() => setShowAll(true)}
            data-action="show-more-events"
            className="bg-bg-elevated border border-border text-text font-mono text-sm rounded-full px-6 py-2.5 hover:border-border-accent transition-colors duration-[var(--dur-normal)] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[var(--accent)]"
          >
            Mehr anzeigen ↓
          </button>
        </div>
      )}
    </section>
  );
}
```

**File 2: `apps/website/app/events/components/event-card.tsx`** (placeholder modal trigger — Plan 04 wires real modal):

```tsx
"use client";

import { CalendarDays, MapPin } from "lucide-react";
import type { EventEntry } from "@/lib/mdx/events";
import { BeispielBadge } from "@/components/ui/beispiel-badge";

interface EventCardProps {
  event: EventEntry;
  // onOpenModal optional — Plan 04 wires this. Until then, click is a no-op.
  onOpenModal?: (event: EventEntry) => void;
}

function formatDate(iso: string): string {
  const d = new Date(iso);
  return new Intl.DateTimeFormat("de-DE", {
    day: "2-digit",
    month: "long",
    year: "numeric",
    hour: "2-digit",
    minute: "2-digit",
  }).format(d);
}

export function EventCard({ event, onOpenModal }: EventCardProps) {
  const fm = event.frontmatter;

  const handleClick = () => {
    if (onOpenModal) onOpenModal(event);
    // If no handler provided (Plan 03), do nothing. Plan 04 wires this.
  };

  const handleKeyDown = (e: React.KeyboardEvent) => {
    if (e.key === "Enter" || e.key === " ") {
      e.preventDefault();
      handleClick();
    }
  };

  return (
    <article
      role="button"
      tabIndex={0}
      onClick={handleClick}
      onKeyDown={handleKeyDown}
      aria-label={`Event-Details öffnen: ${fm.title}`}
      className="group bg-bg-elevated border border-border rounded-2xl overflow-hidden cursor-pointer hover:border-border-accent transition-colors duration-[var(--dur-normal)] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[var(--accent)] flex flex-col"
    >
      {/* Header image / fallback */}
      <div className="relative h-40 bg-bg overflow-hidden">
        {fm.imageUrl ? (
          <img
            src={fm.imageUrl}
            alt={`${fm.title} — Vorschaubild`}
            className="h-full w-full object-cover"
          />
        ) : (
          <div
            aria-hidden="true"
            className="h-full w-full"
            style={{
              background:
                "linear-gradient(135deg, var(--bg-elevated) 0%, var(--bg) 60%, var(--bg-elevated) 100%)",
            }}
          />
        )}
        {fm.example && (
          <div className="absolute top-3 right-3">
            <BeispielBadge />
          </div>
        )}
      </div>

      {/* Body */}
      <div className="flex flex-col gap-3 p-5 flex-1">
        {/* Tag row */}
        <div className="flex flex-wrap gap-2">
          <span className="font-mono text-[11px] font-bold uppercase tracking-[0.08em] bg-bg border border-border rounded-full px-2 py-0.5 text-text-muted">
            {fm.format}
          </span>
          <span className="font-mono text-[11px] font-bold uppercase tracking-[0.08em] bg-bg border border-border rounded-full px-2 py-0.5 text-text-muted">
            {fm.level}
          </span>
          {fm.partner && (
            <span className="font-mono text-[11px] font-bold uppercase tracking-[0.08em] rounded-full px-2 py-0.5 text-[var(--accent)] border border-[var(--accent)]/40">
              mit {fm.partner}
            </span>
          )}
        </div>

        {/* Title */}
        <h3
          className="font-sans font-semibold text-text leading-[1.3] mt-1"
          style={{ fontSize: "var(--fs-h3)" }}
        >
          {fm.title}
        </h3>

        {/* Meta */}
        <div className="flex flex-col gap-1.5 mt-2 text-[13px] text-text-muted">
          <span className="flex items-center gap-1.5">
            <CalendarDays className="h-3.5 w-3.5" aria-hidden="true" />
            {formatDate(fm.date)}
          </span>
          <span className="flex items-center gap-1.5">
            <MapPin className="h-3.5 w-3.5" aria-hidden="true" />
            {fm.location}
          </span>
        </div>

        {/* CTA */}
        <button
          type="button"
          onClick={(e) => {
            e.stopPropagation();
            handleClick();
          }}
          className="mt-auto bg-[var(--accent)] text-[var(--text-on-accent)] font-mono font-bold text-sm rounded-full px-4 py-2.5 hover:shadow-[0_0_20px_var(--accent-glow)] hover:scale-[1.03] transition-all duration-[var(--dur-fast)] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[var(--accent)]"
        >
          Zum Event anmelden
        </button>
      </div>
    </article>
  );
}
```
  </action>
  <verify>
    <automated>pnpm --filter @genai/website tsc --noEmit && pnpm --filter @genai/website build 2>&1 | grep "ƒ /events"</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/website/app/events/components/events-grid.tsx` exists
    - File contains `data-section="events-grid"`
    - File contains `Mehr anzeigen` text
    - File contains `data-action="show-more-events"` on the button
    - File contains useState-based showAll toggle
    - File handles `events.length === 0` empty state with `data-section="events-empty"`
    - File `apps/website/app/events/components/event-card.tsx` exists
    - File contains `import { BeispielBadge }`
    - File contains `data-event-card` (set on parent in grid via motion.div) — confirmed in events-grid.tsx
    - File contains `role="button"` and `tabIndex={0}` on the article
    - File contains keyboard handler for Enter and Space
    - `pnpm --filter @genai/website tsc --noEmit` exits 0
    - Build still shows `ƒ /events`
  </acceptance_criteria>
  <done>Grid renders 3 cards initially, Mehr-Anzeigen toggles to show all, cards have keyboard support, Beispiel badge shows on placeholder content.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build Formats + Members-Only + Final-CTA sections + fill 3 Playwright tests</name>
  <files>apps/website/app/events/components/events-formats.tsx, apps/website/app/events/components/events-members-only.tsx, apps/website/app/events/components/events-final-cta.tsx, packages/e2e-tools/tests/events.spec.ts</files>
  <read_first>
    - apps/website/app/community/components/community-final-cta.tsx (pattern for Final CTA)
    - .planning/phases/22.6-pre-launch-polish-bundle/22.6-UI-SPEC.md (Format-Section + Members-Only + Final-CTA contracts)
    - .planning/phases/22.6-pre-launch-polish-bundle/22.6-CONTEXT.md (Track A scope §5.5/§5.6/§5.8)
    - packages/e2e-tools/tests/events.spec.ts (Plan 01 stubs — fill 3 of 7 fixme tests)
  </read_first>
  <action>
**File 1: `apps/website/app/events/components/events-formats.tsx`**:

```tsx
"use client";

import { Wrench, Mic, GraduationCap } from "lucide-react";

const FORMATS = [
  {
    icon: Wrench,
    name: "Workshops",
    description:
      "Praktische Hands-on-Sessions in kleinen Gruppen. Du verlässt jeden Workshop mit konkreten Skills, die du sofort anwenden kannst.",
  },
  {
    icon: Mic,
    name: "Speaker Sessions",
    description:
      "Inputs von Praktiker:innen und Forschenden. Strategische Perspektive statt Tool-Hopping — gefolgt von echtem Q&A.",
  },
  {
    icon: GraduationCap,
    name: "Masterclasses",
    description:
      "Tiefe Einblicke von Expert:innen aus Industrie und Wissenschaft. 90-120 Minuten konzentrierte Lerneinheit.",
  },
];

export function EventsFormats() {
  return (
    <section
      aria-labelledby="events-formats-heading"
      data-section="events-formats"
      className="mx-auto max-w-6xl px-6 py-16"
    >
      <h2
        id="events-formats-heading"
        className="font-sans font-bold text-text mb-8 text-center"
        style={{ fontSize: "var(--fs-h2)" }}
      >
        Unsere Formate
      </h2>

      <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
        {FORMATS.map((f) => {
          const Icon = f.icon;
          return (
            <div
              key={f.name}
              className="bg-bg-elevated border border-border rounded-2xl p-6"
            >
              <Icon
                className="h-6 w-6 text-[var(--accent)] mb-4"
                aria-hidden="true"
              />
              <h3
                className="font-sans font-semibold text-text leading-[1.3]"
                style={{ fontSize: "var(--fs-h3)" }}
              >
                {f.name}
              </h3>
              <p className="mt-3 text-base leading-[1.65] text-text-muted">
                {f.description}
              </p>
            </div>
          );
        })}
      </div>
    </section>
  );
}
```

**File 2: `apps/website/app/events/components/events-members-only.tsx`**:

```tsx
"use client";

import Link from "next/link";
import { Lock } from "lucide-react";

export function EventsMembersOnly() {
  return (
    <section
      aria-labelledby="events-members-only-heading"
      data-section="events-members-only"
      className="mx-auto max-w-4xl px-6 py-12"
    >
      <div className="bg-bg-elevated border-l-2 border-[var(--accent)] rounded-r-xl p-6 max-w-2xl mx-auto">
        <div className="flex items-start gap-3">
          <Lock
            className="h-5 w-5 text-[var(--accent)] mt-1 shrink-0"
            aria-hidden="true"
          />
          <div>
            <h2
              id="events-members-only-heading"
              className="font-sans font-semibold text-text leading-[1.3]"
              style={{ fontSize: "var(--fs-h3)" }}
            >
              Events nur für Mitglieder
            </h2>
            <p className="mt-2 text-base leading-[1.65] text-text-muted">
              Unsere Events sind exklusiv für die Generation AI Community. Die Mitgliedschaft ist kostenlos — in 2 Minuten dabei.
            </p>
            <Link
              href="/join"
              className="inline-block mt-3 text-[var(--accent)] font-mono font-bold text-sm hover:underline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[var(--accent)]"
            >
              Jetzt kostenlos registrieren →
            </Link>
          </div>
        </div>
      </div>
    </section>
  );
}
```

**File 3: `apps/website/app/events/components/events-final-cta.tsx`**:

```tsx
"use client";

import Link from "next/link";

export function EventsFinalCta() {
  return (
    <section
      aria-labelledby="events-final-cta-heading"
      data-section="events-final-cta"
      className="mx-auto max-w-2xl px-6 py-24 text-center"
    >
      <h2
        id="events-final-cta-heading"
        className="font-sans font-bold text-text"
        style={{ fontSize: "var(--fs-h2)" }}
      >
        Keine Events verpassen.
      </h2>
      <p className="mt-4 text-lg leading-[1.5] text-text-muted">
        Werde Teil der Community und sei bei jedem Event dabei.
      </p>
      <Link
        href="/join"
        className="inline-block mt-8 bg-[var(--accent)] text-[var(--text-on-accent)] font-mono font-bold text-sm rounded-full px-6 py-3 hover:shadow-[0_0_20px_var(--accent-glow)] hover:scale-[1.03] transition-all duration-[var(--dur-fast)] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[var(--accent)]"
      >
        Jetzt beitreten
      </Link>
    </section>
  );
}
```

**File 4: Fill 3 of 7 tests in `packages/e2e-tools/tests/events.spec.ts`** — replace `test.fixme` with `test` for the 3 covered today:

Replace these 3 stubs:
```ts
test('lädt: /events rendert Hero + Grid (A-req-1, A-req-2)', async ({ page }) => {
  await page.goto('/events');
  await expect(page.getByRole('heading', { level: 1, name: /Events, die dich weiterbringen/i })).toBeVisible();
  await expect(page.locator('[data-section="events-hero"]')).toBeVisible();
  await expect(page.locator('[data-section="events-grid"]')).toBeVisible();
});

test('grid: zeigt mindestens 1 kommendes Event aus MDX (A-req-2)', async ({ page }) => {
  await page.goto('/events');
  const cards = page.locator('[data-event-card]');
  await expect(cards.first()).toBeVisible();
  // We seeded 3 placeholders → at least 3 cards before "mehr anzeigen"
  expect(await cards.count()).toBeGreaterThanOrEqual(1);
});

test('mehr anzeigen: Button erscheint bei >3 Events und expandiert client-side (A-req-3)', async ({ page }) => {
  await page.goto('/events');
  const button = page.locator('[data-action="show-more-events"]');
  // We have only 3 placeholder upcoming events at launch → button should NOT appear.
  // Test contract: if there ARE > 3 events, button is visible AND clicking expands. Otherwise: button absent.
  const total = await page.locator('[data-event-card]').count();
  if (total > 3) {
    await expect(button).toBeVisible();
    await button.click();
    expect(await page.locator('[data-event-card]').count()).toBeGreaterThan(3);
  } else {
    await expect(button).toHaveCount(0);
  }
});
```

Leave the other 4 fixme stubs in place — they get filled by Plans 04 + 05.
  </action>
  <verify>
    <automated>pnpm --filter @genai/website build 2>&1 | grep "ƒ /events" && pnpm --filter @genai/e2e-tools exec playwright test tests/events.spec.ts -g "lädt|grid|mehr anzeigen" --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - File `events-formats.tsx` exists, contains 3 format cards (Workshops/Speaker Sessions/Masterclasses)
    - File `events-members-only.tsx` exists, contains string `Events nur für Mitglieder`
    - File `events-members-only.tsx` contains `<Link href="/join">`
    - File `events-final-cta.tsx` exists, contains H2 `Keine Events verpassen.`
    - File `events-final-cta.tsx` contains `<Link href="/join">`
    - `events.spec.ts` now contains 3 `test(` (not `test.fixme(`) for lädt/grid/mehr-anzeigen
    - `events.spec.ts` still has 4 remaining `test.fixme(` for Plans 04+05
    - Playwright run for those 3 tests: 3 passed, 0 failed
    - Build still shows `ƒ /events`
  </acceptance_criteria>
  <done>All 3 lower sections render, 3 of 7 Playwright tests now active and passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ events-page server | Public route, no auth required. Renders public MDX content. |
| Static MDX ↔ React render | Frontmatter parsed at build/request time; body NOT rendered in this plan (only metadata) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22.6-A-CSP | Information Disclosure | apps/website/app/events/page.tsx | mitigate | MUST call `await headers()` to force dynamic rendering. Acceptance criterion grep ensures presence. Build-output check for `ƒ /events` confirms. Without this: static render → no nonce → CSP blocks scripts → black page (LEARNINGS.md incident 2026-XX) |
| T-22.6-A-XSS-IMG | XSS | event-card.tsx imageUrl | accept | imageUrl comes from maintainer-authored MDX (not UGC). Used in `<img src={fm.imageUrl}>`. React escapes attribute values; not interpreted as JS. If UGC opens: switch to allow-list of hosts (Phase 28+) |
| T-22.6-A-CLICKJACK | Tampering | EventCard click target | accept | role=button on outer article + nested button. e.stopPropagation() on inner button prevents double-trigger. No iframe embedding allowed (CSP frame-ancestors policy already enforced site-wide) |
</threat_model>

<verification>
- `pnpm --filter @genai/website build` shows `ƒ /events` (NOT `○`)
- `pnpm --filter @genai/website tsc --noEmit` exits 0
- Manual smoke: `pnpm dev:website` then visit `localhost:3000/events` — Hero + 3 Grid Cards + Formats + Members-Only + Final-CTA all visible
- Playwright tests for lädt/grid/mehr-anzeigen pass
- 4 test.fixme stubs remain for Plans 04 + 05
</verification>

<success_criteria>
- /events route lives, renders all 5 sections (Hero/Grid/Formats/Members-Only/Final-CTA)
- Build shows `ƒ /events` (CSP-Pitfall guarded)
- Mehr-Anzeigen state functional (or absent when ≤3 events)
- Beispiel badge appears on each placeholder card
- 3 of 7 Playwright tests now passing
</success_criteria>

<output>
Create `.planning/phases/22.6-pre-launch-polish-bundle/22.6-03-SUMMARY.md` documenting all sections built + build output line proving `ƒ /events`.
</output>
