---
phase: 22.6
plan: 01
type: execute
wave: 0
branch: feature/phase-22.6-pre-launch-polish
depends_on: []
files_modified:
  - packages/e2e-tools/tests/events.spec.ts
  - apps/website/lib/mdx/__tests__/events.test.ts
autonomous: true
requirements:
  - A-req-1
  - A-req-2
  - A-req-3
  - A-req-4
  - A-req-5
  - A-req-6
  - A-req-7
  - A-req-8
must_haves:
  truths:
    - "Track A test stub files exist and parse"
    - "Stubs use test.fixme so they don't fail builds while later plans fill them"
    - "Vitest can discover apps/website/lib/mdx/__tests__/events.test.ts"
    - "Playwright can discover packages/e2e-tools/tests/events.spec.ts"
  artifacts:
    - path: "packages/e2e-tools/tests/events.spec.ts"
      provides: "Track A E2E suite skeleton with fixme placeholders for A-req-1..A-req-8"
      min_lines: 60
    - path: "apps/website/lib/mdx/__tests__/events.test.ts"
      provides: "Vitest skeleton for getUpcomingEvents/getPastEvents (A-req-7)"
      min_lines: 30
  key_links:
    - from: "packages/e2e-tools/tests/events.spec.ts"
      to: "playwright.config.ts"
      via: "test.describe + test.fixme registered with default project"
      pattern: "test\\.describe|test\\.fixme"
    - from: "apps/website/lib/mdx/__tests__/events.test.ts"
      to: "apps/website/vitest.config.ts"
      via: "describe/it imported from vitest"
      pattern: "from ['\"]vitest['\"]"
---

<objective>
Wave 0: Create Track A test scaffolding so subsequent plans can ADD assertions instead of inventing test files mid-stream.

Purpose: Nyquist-compliant feedback loop. Plans 02-05 reference these files in their `<verify>` blocks; without them every later task would be "MISSING test file".
Output: Two stub files (Playwright spec + Vitest spec), all tests marked `test.fixme()` so suite is green from minute one.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/22.6-pre-launch-polish-bundle/22.6-CONTEXT.md
@.planning/phases/22.6-pre-launch-polish-bundle/22.6-VALIDATION.md
@.planning/phases/22.6-pre-launch-polish-bundle/22.6-RESEARCH.md

<interfaces>
<!-- Existing test infra exemplars to mirror -->
From packages/e2e-tools/tests/join.spec.ts (Phase 23): test.describe block + smoke patterns.
From packages/e2e-tools/playwright.config.ts: baseURL = process.env.E2E_BASE_URL ?? 'http://localhost:3000'.
From apps/website/vitest.config.ts: standard vitest setup, picks up __tests__ folders.
From apps/website/lib/mdx/__tests__/community.test.ts (Phase 26): existing example for MDX-reader unit tests.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Playwright spec scaffold for /events (Track A)</name>
  <files>packages/e2e-tools/tests/events.spec.ts</files>
  <read_first>
    - packages/e2e-tools/tests/join.spec.ts (Phase 23 spec — copy describe + import pattern)
    - packages/e2e-tools/playwright.config.ts (verify baseURL handling)
    - .planning/phases/22.6-pre-launch-polish-bundle/22.6-VALIDATION.md (Track A test map, A-req-1..A-req-8)
  </read_first>
  <action>
Create `packages/e2e-tools/tests/events.spec.ts` with the following EXACT structure (copy the test.describe/test.fixme grouping verbatim, names matter — later plans grep them):

```ts
import { test, expect } from '@playwright/test';

const EVENTS_URL = '/events';

test.describe('events page — Track A smoke', () => {
  test.fixme('lädt: /events rendert Hero + Grid (A-req-1, A-req-2)', async ({ page }) => {
    await page.goto(EVENTS_URL);
    await expect(page.getByRole('heading', { level: 1 })).toBeVisible();
    await expect(page.locator('[data-section="events-hero"]')).toBeVisible();
    await expect(page.locator('[data-section="events-grid"]')).toBeVisible();
  });

  test.fixme('grid: zeigt mindestens 1 kommendes Event aus MDX (A-req-2)', async ({ page }) => {
    await page.goto(EVENTS_URL);
    const cards = page.locator('[data-event-card]');
    await expect(cards.first()).toBeVisible();
  });

  test.fixme('mehr anzeigen: Button erscheint bei >3 Events und expandiert client-side (A-req-3)', async ({ page }) => {
    await page.goto(EVENTS_URL);
    // Filled by Plan 03 once Mehr-Anzeigen state exists
  });

  test.fixme('modal: Klick auf Event-Card öffnet Modal mit aria-modal + Escape schließt (A-req-4)', async ({ page }) => {
    await page.goto(EVENTS_URL);
    // Filled by Plan 04 once Modal exists
  });

  test.fixme('redirect: Anmelde-Click logged-out redirected zu /join?redirect_after=/events/[slug] (A-req-5)', async ({ page }) => {
    await page.goto(EVENTS_URL);
    // Filled by Plan 04 once Anmelde-Flow lebt
  });

  test.fixme('slug page: /events/[slug] rendert Standalone (A-req-6)', async ({ page }) => {
    await page.goto('/events/2026-05-15-prompting-masterclass');
    await expect(page.getByRole('heading', { level: 1 })).toBeVisible();
  });

  test.fixme('sitemap: /sitemap.xml enthält /events + /events/[slug] (A-req-8)', async ({ request }) => {
    const res = await request.get('/sitemap.xml');
    const xml = await res.text();
    expect(xml).toContain('/events');
  });
});
```

Why test.fixme: Tests are registered (Playwright sees them, reporter shows them as "skipped — fixme") but NEVER fail the build. Plans 02-05 each remove one `.fixme` and fill the test body.
  </action>
  <verify>
    <automated>pnpm --filter @genai/e2e-tools exec playwright test tests/events.spec.ts --reporter=line --list</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/e2e-tools/tests/events.spec.ts` exists
    - File contains exactly 7 `test.fixme(` occurrences (one per A-req)
    - File contains string `data-section="events-hero"`
    - File contains string `redirect_after=/events/`
    - `playwright test --list` lists 7 tests under `events page — Track A smoke`
    - No test runs (all fixme) → exit code 0
  </acceptance_criteria>
  <done>Stub Playwright spec exists, all 7 tests registered + skipped via fixme, no build break.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Vitest unit-test scaffold for events.ts MDX adapter (A-req-7)</name>
  <files>apps/website/lib/mdx/__tests__/events.test.ts</files>
  <read_first>
    - apps/website/lib/mdx/__tests__/community.test.ts (Phase 26 vitest example — mirror structure)
    - apps/website/vitest.config.ts (verify __tests__ folder is picked up)
    - apps/website/lib/mdx/community.ts (the adapter pattern that events.ts will follow)
  </read_first>
  <action>
Create `apps/website/lib/mdx/__tests__/events.test.ts` with this EXACT scaffold (test names matter, Plan 02 fills bodies):

```ts
import { describe, it } from 'vitest';

describe('lib/mdx/events — MDX adapter (Phase 22.6 Plan 02 fills bodies)', () => {
  it.todo('getUpcomingEvents() returns only events with date >= now (A-req-7)');
  it.todo('getUpcomingEvents() sorts ascending (next event first) (A-req-7)');
  it.todo('getPastEvents() returns events from content/events/past/');
  it.todo('getPastEvents() sorts descending (most recent first)');
  it.todo('validateEventFrontmatter throws when raw.slug !== fileSlug (Pitfall 4)');
  it.todo('validateEventFrontmatter rejects unknown format value (not Workshop/Speaker Session/Masterclass)');
  it.todo('getEventBySlug returns null for unknown slug');
});
```

`it.todo()` registers tests as TODO (vitest marks them yellow, never fails). Plan 02 swaps `.todo` → `(name, fn)` with real assertions.
  </action>
  <verify>
    <automated>pnpm --filter @genai/website test lib/mdx/__tests__/events.test.ts --run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/website/lib/mdx/__tests__/events.test.ts` exists
    - File contains exactly 7 `it.todo(` occurrences
    - File imports from `'vitest'`
    - Vitest run shows 7 todos, 0 failures, 0 passes
    - Exit code 0
  </acceptance_criteria>
  <done>Vitest stub exists with 7 it.todo() registrations, suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner ↔ filesystem | Stubs are read-only files in test directories, no execution surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22.6-W0 | N/A | test scaffolds | accept | Pure test infra, no security surface. Stubs use test.fixme/it.todo so they cannot accidentally pass false-positive |

None applicable for this plan — pure test scaffold task.
</threat_model>

<verification>
- `playwright test tests/events.spec.ts --list` lists 7 tests
- `vitest run lib/mdx/__tests__/events.test.ts` shows 7 todos
- No build break in monorepo
</verification>

<success_criteria>
- Both stub files exist and are git-tracked
- Plans 02-05 can reference test names + line ranges in their verify blocks
- Suite remains green throughout phase (fixme/todo never fail)
</success_criteria>

<output>
Create `.planning/phases/22.6-pre-launch-polish-bundle/22.6-01-SUMMARY.md` documenting both stub files + their test name lists.
</output>
