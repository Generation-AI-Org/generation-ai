---
phase: 22.6
plan: 06
type: execute
wave: 5
branch: feature/phase-22.6-pre-launch-polish
depends_on:
  - 22.6-05
files_modified:
  - packages/e2e-tools/tests/tools-app.spec.ts
autonomous: true
requirements:
  - B-req-1
  - B-req-2
  - B-req-3
  - B-req-4
  - B-req-5
must_haves:
  truths:
    - "Track B test stub file exists and parses"
    - "Stubs use test.fixme so suite stays green while later plans fill them"
    - "Playwright can discover packages/e2e-tools/tests/tools-app.spec.ts against tools-app baseURL"
  artifacts:
    - path: "packages/e2e-tools/tests/tools-app.spec.ts"
      provides: "Track B E2E suite skeleton with fixme placeholders for B-req-1..B-req-5"
      min_lines: 60
  key_links:
    - from: "packages/e2e-tools/tests/tools-app.spec.ts"
      to: "playwright.config.ts"
      via: "test.describe + test.fixme registered with default project (baseURL via E2E_TOOLS_BASE_URL or hardcoded fallback)"
      pattern: "test\\.describe|test\\.fixme"
---

<objective>
Wave 0 for Track B: Create the Playwright spec stub for tools-app polish so plans 07-09 can fill assertions.

Purpose: Mirror Plan 01's role for Track B. Without this, plans 07-09 reference a non-existent test file.
Output: One stub file with 5 `test.fixme()` registrations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/22.6-pre-launch-polish-bundle/22.6-CONTEXT.md
@.planning/phases/22.6-pre-launch-polish-bundle/22.6-VALIDATION.md
@.planning/phases/22.6-pre-launch-polish-bundle/22.6-RESEARCH.md

<interfaces>
<!-- E2E config -->
From `packages/e2e-tools/playwright.config.ts`:
- `baseURL = process.env.E2E_BASE_URL ?? 'http://localhost:3000'` (website default)
- For tools-app, override via env: `E2E_BASE_URL=http://localhost:3001 pnpm e2e --grep "tools-app"` OR hardcode `http://localhost:3001` in spec.

Decision per RESEARCH.md §Open-Questions #3: hardcode `TOOLS_URL = 'http://localhost:3001'` constant in spec, override via `process.env.TOOLS_URL` for CI/Vercel-Preview.

<!-- Existing E2E patterns -->
From `packages/e2e-tools/tests/auth.spec.ts` (Phase 13/19): TOOLS_URL constant pattern is already in repo conventions. Match it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Playwright spec scaffold for tools-app polish (Track B)</name>
  <files>packages/e2e-tools/tests/tools-app.spec.ts</files>
  <read_first>
    - packages/e2e-tools/tests/auth.spec.ts (TOOLS_URL constant + tools-app addressing pattern)
    - packages/e2e-tools/playwright.config.ts (baseURL handling)
    - .planning/phases/22.6-pre-launch-polish-bundle/22.6-VALIDATION.md (Track B test map)
    - .planning/phases/22.6-pre-launch-polish-bundle/22.6-UI-SPEC.md (DOM contracts for nav + hero)
  </read_first>
  <action>
Create `packages/e2e-tools/tests/tools-app.spec.ts` with this EXACT scaffold:

```ts
import { test, expect } from '@playwright/test';

// tools-app runs on its own subdomain in prod and on localhost:3001 in dev.
// Override via TOOLS_URL env for CI / Vercel-Preview.
const TOOLS_URL = process.env.TOOLS_URL ?? 'http://localhost:3001';

test.describe('tools-app polish — Track B smoke (Phase 22.6)', () => {
  test.fixme('login: 2 sichtbare Elemente (Primary CTA + Secondary Link) when logged-out (B-req-1)', async ({ page }) => {
    await page.goto(TOOLS_URL);
    // Filled by Plan 07: assert "Kostenlos registrieren" button + "Bereits Mitglied? Einloggen" link
  });

  test.fixme('utm: "Kostenlos registrieren" href hat ?utm_source=tools (B-req-2)', async ({ page }) => {
    await page.goto(TOOLS_URL);
    // Filled by Plan 07: assert href ends with /join?utm_source=tools
  });

  test.fixme('hero: Hero-Sektion sichtbar zwischen Header und FilterBar mit H1 "KI-Tools" (B-req-3)', async ({ page }) => {
    await page.goto(TOOLS_URL);
    // Filled by Plan 08: data-section="tools-hero" + H1 contains "KI-Tools"
  });

  test.fixme('nav: alle Items sichtbar (Events, Tools active, Community, Für Partner, Über uns, CTA) (B-req-4)', async ({ page }) => {
    await page.goto(TOOLS_URL);
    // Filled by Plan 09: assert nav text + Tools aria-current="page"
  });

  test.fixme('eingeloggt: User-Menu statt CTA-Buttons sichtbar — kein Regression (B-req-5)', async ({ page }) => {
    await page.goto(TOOLS_URL);
    // Filled by Plan 09: log in + assert user-menu present + CTAs absent
  });
});
```
  </action>
  <verify>
    <automated>pnpm --filter @genai/e2e-tools exec playwright test tests/tools-app.spec.ts --reporter=line --list</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/e2e-tools/tests/tools-app.spec.ts` exists
    - File contains exactly 5 `test.fixme(` occurrences (one per B-req)
    - File contains `TOOLS_URL` constant with `process.env.TOOLS_URL` fallback
    - File contains `localhost:3001`
    - `playwright test --list` shows 5 tests under `tools-app polish — Track B smoke`
    - No test executes (all fixme) → exit code 0
  </acceptance_criteria>
  <done>Track B spec stub exists, 5 tests registered + skipped via fixme.</done>
</task>

</tasks>

<threat_model>
None applicable for this plan — pure test scaffold task.
</threat_model>

<verification>
- Playwright lists 5 tests
- Suite green, no test runs
</verification>

<success_criteria>
- Stub file in place, plans 07-09 can swap fixme → active test
</success_criteria>

<output>
Create `.planning/phases/22.6-pre-launch-polish-bundle/22.6-06-SUMMARY.md` listing the 5 fixme test names.
</output>
