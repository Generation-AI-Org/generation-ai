---
phase: "22.6"
plan: "05"
subsystem: "apps/website — /events standalone page, archive, sitemap"
tags: ["events", "seo", "sitemap", "playwright", "csp", "track-a"]
dependency_graph:
  requires: ["22.6-04"]
  provides: ["events-slug-page", "events-archive", "sitemap-events"]
  affects: ["apps/website/app/events/[slug]", "apps/website/app/sitemap.ts", "packages/e2e-tools"]
tech_stack:
  added: []
  patterns: ["generateStaticParams+dynamicParams=false for 404 gate", "whitespace-pre-wrap V1 body rendering"]
key_files:
  created:
    - apps/website/app/events/[slug]/page.tsx
  modified:
    - apps/website/app/events/components/events-archive.tsx
    - apps/website/app/sitemap.ts
    - packages/e2e-tools/tests/events.spec.ts
decisions:
  - "body field absent from FrontmatterEntry — V1 renders description as lede only, MDX body deferred to Phase 28+"
  - "dynamicParams=false on slug page: generateStaticParams whitelist + 404 for unknown slugs without FS hit"
  - "Lighthouse checkpoint auto-approved per auto_chain_directive: final gate is Vercel Preview after merge to develop"
metrics:
  duration_minutes: 5
  tasks_completed: 3
  tasks_total: 3
  files_created: 1
  files_modified: 3
  completed_date: "2026-04-25"
requirements:
  - A-req-6
  - A-req-8
---

# Phase 22.6 Plan 05: Event Standalone Page + Archive + Sitemap Summary

**One-liner:** `/events/[slug]` standalone server page with CSP guard (`ƒ`), full archive section (A-06: no modal/CTA), sitemap extended with `/events` + per-slug entries, all 7 Track A Playwright tests active and green.

---

## What Was Built

### Task 1 — `/events/[slug]` standalone page + EventsArchive replacement + sitemap

**`apps/website/app/events/[slug]/page.tsx`** (new, 195 LOC):
- Server Component with `await headers()` for CSP nonce — confirmed `ƒ /events/[slug]` in build output
- `generateStaticParams()` from `getEventSlugs()` whitelists all known slugs
- `dynamicParams = false` — unknown slugs return 404 (no FS hit, T-22.6-A-404)
- `generateMetadata` with canonical URL, title, description, OG tags
- Header image with brand-gradient fallback, `BeispielBadge` when `frontmatter.example === true`
- Format + level + partner tags
- H1 from frontmatter title, date + location meta row
- Speaker list with avatar placeholders
- `rel="noopener noreferrer"` on external ctaUrl link (T-22.6-A-CTA-NEWTAB mitigated)
- Back-link to `/events` + secondary "Erst Mitglied werden" → `/join`

**`apps/website/app/events/components/events-archive.tsx`** (stub replaced, 100 LOC):
- Full implementation replacing the Plan 03 `return null` stub
- `data-section="events-archive"`, `data-archive-card` on each card
- Contains "Vergangene Events" H2 heading
- 1-col / 2-col grid, cards with image + title + date + location + recap
- `BeispielBadge` when `frontmatter.example === true`
- NO CTA button, NO modal trigger (A-06 contract)
- `formatShortDate()` for compact date display

**`apps/website/app/sitemap.ts`** (extended, +25 LOC):
- Imports `getUpcomingEvents` + `getPastEvents` from `@/lib/mdx/events`
- Static `/events` entry: priority 0.9, changeFrequency "weekly"
- Dynamic per-slug entries for all upcoming + past events: priority 0.8, changeFrequency "weekly"
- Existing entries (home, about, join, test, community, articles) preserved intact

### Task 2 — Final 2 Playwright tests filled

**`packages/e2e-tools/tests/events.spec.ts`**:
- Replaced `test.fixme` for A-req-6 (slug page) with full assertions:
  - H1 contains "Prompting Masterclass" (level 1)
  - External "Zum Event anmelden" link has `https?://` href
  - Unknown slug `/events/this-slug-does-not-exist` returns 404
- Replaced `test.fixme` for A-req-8 (sitemap) with full assertions:
  - `/sitemap.xml` returns 200
  - Contains `/events` (static entry)
  - Contains `/events/2026-05-15-prompting-masterclass` (upcoming)
  - Contains `/events/2026-03-10-launch-event` (archive)
- Result: **7 active tests, 0 `test.fixme`**, all 7 passed

### Task 3 — Lighthouse checkpoint (auto-approved)

Per `auto_chain_directive` (workflow._auto_chain_active=true), the `checkpoint:human-verify` is auto-approved.

⚡ Auto-approved: Build success + `ƒ /events` + `ƒ /events/[slug]` + all 7 Playwright tests green = sufficient signal for auto-chain.

**Real Lighthouse gate deferred to Vercel Preview run after merge to develop.** Preview URL: https://website-git-feature-phase-22-6-pre-launch-polish-lucas-projects-e78962e9.vercel.app/events

---

## Build Verification

```
Route (app)
├ ƒ /events          ← dynamic (CSP nonce, not static)
├ ƒ /events/[slug]   ← dynamic (CSP nonce, not static)
```

Both routes show `ƒ` (dynamic) — CSP-safe per LEARNINGS.md requirements.

---

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `FrontmatterEntry` has no `body` field**
- **Found during:** Task 1, TypeScript build check
- **Issue:** Plan 05 task action specified rendering `event.body.trim()`, but `FrontmatterEntry<T>` from `lib/mdx/reader.ts` only exposes `{ slug, frontmatter }` — no `body` field. The reader uses `gray-matter` with `const { data } = matter(raw)` which discards the MDX body.
- **Fix:** Removed body rendering block. V1 standalone page uses `frontmatter.description` as the page lede (already present in the description field). MDX body rendering via `@next/mdx` deferred to Phase 28+ as originally planned.
- **Files modified:** `apps/website/app/events/[slug]/page.tsx`
- **Commit:** `6f2669a`

---

## Known Stubs

None that block the plan's goal. All placeholder MDX files have sufficient frontmatter for the standalone page to render meaningfully (title, description, format, level, date, location, speakers, ctaUrl). The `imageUrl` fields are absent — the decorative gradient fallback is intentional.

---

## Threat Surface Scan

No new trust boundaries introduced beyond those documented in the plan's threat model:
- T-22.6-A-CSP: `await headers()` present — mitigated
- T-22.6-A-MDX: body rendered via React tree (no `dangerouslySetInnerHTML`) — mitigated (body rendering removed entirely in V1)
- T-22.6-A-CTA-NEWTAB: `rel="noopener noreferrer"` present on ctaUrl link — mitigated
- T-22.6-A-404: `notFound()` called for unknown slugs — mitigated
- T-22.6-A-SITEMAP: sitemap enumerates all event slugs intentionally — accepted

---

## Track A Status

**Track A `/events` is FEATURE-COMPLETE after this plan:**

| Plan | Status | What |
|------|--------|------|
| 22.6-01 | ✅ | Test scaffold (Playwright + Vitest stubs) |
| 22.6-02 | ✅ | events.ts MDX adapter + placeholder MDX files + 7 Vitest unit tests |
| 22.6-03 | ✅ | /events page shell (Hero + Grid + Formats + Members-Only + Final-CTA) |
| 22.6-04 | ✅ | EventModal + Anmelde-Flow (auth-aware, mobile sheet, open-redirect guard) |
| 22.6-05 | ✅ | /events/[slug] standalone + EventsArchive + sitemap + 7 Playwright tests |

**All 7 Track A Playwright tests:** 7 passed, 0 failed, 0 fixme

---

## Commits

| Hash | Type | Description |
|------|------|-------------|
| `6f2669a` | feat | /events/[slug] standalone page + archive section + sitemap events |
| `61f4323` | test | fill final 2 Playwright tests — slug page + sitemap (A-req-6, A-req-8) |

## Self-Check: PASSED
