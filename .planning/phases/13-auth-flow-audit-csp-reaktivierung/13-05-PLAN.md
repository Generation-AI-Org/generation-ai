---
phase: 13
plan: 05
type: execute
wave: 2
depends_on: [13-02]
files_modified:
  - apps/tools-app/proxy.ts
  - apps/tools-app/lib/csp.ts
  - apps/tools-app/next.config.ts
  - docs/AUTH-FLOW.md
autonomous: false
requirements:
  - CSP-01
  - CSP-03
must_haves:
  truths:
    - "tools-app proxy.ts setzt enforced CSP mit Nonce (Next.js 16 Pattern)"
    - "CSP hosts: Supabase, Sentry DE-Region, Deepgram, Clearbit, Vercel Speed Insights"
    - "Production-Response für https://tools.generation-ai.org/ enthält Content-Security-Policy Header (enforced)"
    - "Keine CSP-Violations in Browser-Console auf allen Haupt-Routes (/, /login, /chat, /settings)"
    - "Voice-Input (Deepgram WSS) funktioniert weiterhin"
    - "Sentry-Reporting funktioniert weiterhin (test-error erscheint in Sentry Dashboard)"
    - "securityheaders.com zeigt A oder A+ für tools.generation-ai.org"
    - "E2E-Suite bleibt grün, inkl. Auth-Pfade 1-6 aus Plan 02"
  artifacts:
    - path: apps/tools-app/proxy.ts
      provides: "updateSession + Nonce + enforced CSP"
      contains: "Content-Security-Policy"
    - path: apps/tools-app/lib/csp.ts
      provides: "buildCspDirectives(nonce, isDev)"
      exports: ["buildCspDirectives"]
  key_links:
    - from: apps/tools-app/proxy.ts
      to: apps/tools-app/lib/csp.ts
      via: "import buildCspDirectives"
      pattern: "buildCspDirectives"
    - from: apps/tools-app/proxy.ts
      to: packages/auth/src/middleware.ts
      via: "updateSession() response wird mit CSP headers erweitert (Pitfall 1)"
      pattern: "updateSession"
---

<objective>
Wave 2 (parallel zu Plan 04): tools-app bekommt neu CSP via proxy.ts. Vorher: KEINE CSP. Nachher: enforced, nonce-basiert, mit allen benötigten Hosts (Supabase, Sentry DE, Deepgram, Clearbit, Vercel).

Purpose: R1.3/R1.5 + CONCERNS.md "CSP Header Disabled". tools-app hat bisher zero CSP-Protection trotz mehr Attack-Surface (Chat-Input, User-Content).
Output: Enforced CSP auf https://tools.generation-ai.org, A/A+ auf securityheaders.com, alle Features (Auth, Chat, Voice, Sentry) funktionieren.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-auth-flow-audit-csp-reaktivierung/13-CONTEXT.md
@.planning/phases/13-auth-flow-audit-csp-reaktivierung/13-RESEARCH.md
@apps/tools-app/proxy.ts
@apps/tools-app/next.config.ts
@apps/tools-app/app/layout.tsx
@apps/tools-app/sentry.edge.config.ts
@apps/tools-app/instrumentation-client.ts

<interfaces>
From packages/auth/src/middleware.ts: `updateSession(request: NextRequest): Promise<NextResponse>`
Canonical Phase-12 pattern — CSP auf diese Response setzen, keine neue erstellen.

Sentry DSN host (verified in 13-RESEARCH.md): `https://o4511218002362368.ingest.de.sentry.io` (DE-Region, nicht *.sentry.io wildcard)

Deepgram hosts: `https://api.deepgram.com` + `wss://api.deepgram.com`
Clearbit (ToolLogo img): `https://logo.clearbit.com`
Vercel Speed Insights: `https://va.vercel-scripts.com` + `https://vitals.vercel-insights.com`
Supabase: `https://wbohulnuwqrhystaamjc.supabase.co` + `wss://wbohulnuwqrhystaamjc.supabase.co`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: lib/csp.ts für tools-app (mit Sentry/Deepgram/Clearbit Erweiterungen)</name>
  <files>apps/tools-app/lib/csp.ts</files>
  <read_first>
    - apps/tools-app/instrumentation-client.ts (Sentry DSN bestätigen)
    - apps/tools-app/sentry.edge.config.ts (DSN)
    - apps/tools-app/components/ToolLogo.tsx oder ähnliches (Clearbit img src bestätigen)
    - apps/tools-app/hooks/useDeepgramVoice.ts (Deepgram WSS URL bestätigen)
    - .planning/phases/13-auth-flow-audit-csp-reaktivierung/13-RESEARCH.md (Sektion "CSP-Directives — Vollständige Host-Liste" tools-app + Code Example proxy.ts)
  </read_first>
  <action>
Neue Datei `apps/tools-app/lib/csp.ts` mit EXAKT diesem Inhalt:

```typescript
/**
 * Content-Security-Policy builder für tools-app.
 * Hosts: Supabase Auth+Realtime, Sentry DE-Region, Deepgram (voice WSS),
 *        Clearbit (ToolLogo), Vercel Speed Insights + Web Vitals.
 * Reference: Phase 13 RESEARCH.md "CSP-Directives — Vollständige Host-Liste" tools-app.
 */
export function buildCspDirectives(nonce: string, isDev: boolean): string {
  return [
    "default-src 'self'",
    // 'strict-dynamic' + Nonce erlauben React/Next/Sentry-SDKs; expliziter Host für Vercel Speed Insights loader.
    // 'unsafe-eval' NUR in dev (React Refresh + Sentry SDK source-map replay).
    `script-src 'self' 'nonce-${nonce}' 'strict-dynamic' https://va.vercel-scripts.com${isDev ? " 'unsafe-eval'" : ""}`,
    // Tailwind v4 inline styles → unsafe-inline für styles (für scripts NICHT).
    "style-src 'self' 'unsafe-inline'",
    // ToolLogo-Komponente lädt Logos von Clearbit (verified in Research).
    "img-src 'self' data: blob: https://logo.clearbit.com",
    "font-src 'self'",
    [
      "connect-src 'self'",
      // Supabase Auth + Realtime (WSS)
      "https://wbohulnuwqrhystaamjc.supabase.co",
      "wss://wbohulnuwqrhystaamjc.supabase.co",
      // Sentry — DE-Region, spezifische Org-Subdomain (kein *.sentry.io wildcard!)
      "https://o4511218002362368.ingest.de.sentry.io",
      // Deepgram Voice (REST + WSS)
      "https://api.deepgram.com",
      "wss://api.deepgram.com",
      // Vercel Speed Insights + Web Vitals reporting
      "https://va.vercel-scripts.com",
      "https://vitals.vercel-insights.com",
    ].join(" "),
    "object-src 'none'",
    "base-uri 'self'",
    // form-action 'self' reicht (Supabase-Auth wird via fetch/POST zu same-origin /api/auth/* gemacht).
    "form-action 'self'",
    "frame-ancestors 'none'",
    "upgrade-insecure-requests",
  ].join("; ")
}
```
  </action>
  <verify>
    <automated>test -f apps/tools-app/lib/csp.ts &amp;&amp; grep -q "o4511218002362368.ingest.de.sentry.io" apps/tools-app/lib/csp.ts &amp;&amp; grep -q "api.deepgram.com" apps/tools-app/lib/csp.ts &amp;&amp; grep -q "logo.clearbit.com" apps/tools-app/lib/csp.ts &amp;&amp; grep -q "strict-dynamic" apps/tools-app/lib/csp.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function buildCspDirectives" apps/tools-app/lib/csp.ts`
    - Hosts vollständig: Supabase (https + wss), Sentry DE-Region, Deepgram (https + wss), Clearbit (img), Vercel (va + vitals)
    - `grep -c "unsafe-inline" apps/tools-app/lib/csp.ts` → genau 1 (NUR in style-src)
    - `grep -q "frame-ancestors 'none'" apps/tools-app/lib/csp.ts`
    - KEIN `*.sentry.io` Wildcard (nur exakter DSN-Host)
    - KEIN `https:` Wildcard in img-src
  </acceptance_criteria>
  <done>CSP-Builder-Funktion mit allen tools-app-Hosts existiert.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: proxy.ts erweitern (updateSession + Nonce + CSP)</name>
  <files>apps/tools-app/proxy.ts</files>
  <read_first>
    - apps/tools-app/proxy.ts (aktuell 12 Zeilen: nur updateSession, gleich wie website)
    - packages/auth/src/middleware.ts (updateSession-Signatur)
    - .planning/phases/13-auth-flow-audit-csp-reaktivierung/13-RESEARCH.md (Pattern 1 Code Example)
  </read_first>
  <action>
`apps/tools-app/proxy.ts` KOMPLETT ersetzen mit:

```typescript
import { updateSession } from "@genai/auth/middleware"
import { type NextRequest } from "next/server"
import { buildCspDirectives } from "./lib/csp"

export async function proxy(request: NextRequest) {
  // Phase-12 Pattern: Session-Refresh zuerst; response trägt Auth-Cookies.
  // Phase-13: CSP-Header wird auf DIESE Response gesetzt (Pitfall 1 aus RESEARCH: keine neue Response!).
  const response = await updateSession(request)

  const nonce = Buffer.from(crypto.randomUUID()).toString("base64")
  const isDev = process.env.NODE_ENV === "development"
  const csp = buildCspDirectives(nonce, isDev)

  response.headers.set("Content-Security-Policy", csp)
  response.headers.set("x-nonce", nonce)

  return response
}

export const config = {
  matcher: [
    // api-routes + static assets ausschließen.
    // Next.js 16 CSP Pattern: prefetches nicht matchen (Cache-Konsistenz bei Nonce).
    {
      source:
        "/((?!api|_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)",
      missing: [
        { type: "header", key: "next-router-prefetch" },
        { type: "header", key: "purpose", value: "prefetch" },
      ],
    },
  ],
}
```

Hinweis: `apps/tools-app/next.config.ts` hat KEIN CSP (nur Sentry + statische Headers) — muss nicht angefasst werden. Existing Headers (HSTS, X-Content-Type-Options, etc.) bleiben.
  </action>
  <verify>
    <automated>grep -q "buildCspDirectives" apps/tools-app/proxy.ts &amp;&amp; grep -q "Content-Security-Policy" apps/tools-app/proxy.ts &amp;&amp; grep -q "next-router-prefetch" apps/tools-app/proxy.ts &amp;&amp; cd apps/tools-app &amp;&amp; pnpm build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "buildCspDirectives" apps/tools-app/proxy.ts`
    - `grep -q "crypto.randomUUID" apps/tools-app/proxy.ts`
    - `grep -q "next-router-prefetch" apps/tools-app/proxy.ts` (Prefetch-Skip)
    - `grep -q "await updateSession" apps/tools-app/proxy.ts` (Session refresh bleibt)
    - `pnpm -F @genai/tools-app build` → 0 errors
    - `pnpm -F @genai/tools-app test` → alle bestehenden Tests grün
  </acceptance_criteria>
  <done>tools-app proxy.ts setzt enforced CSP + Nonce. Build grün.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Preview-Deploy Review + Freigabe für Prod-Deploy (tools-app)</name>
  <files>(none — deployment-gate, no files changed)</files>
  <action>Checkpoint-Task — siehe &lt;decision&gt; + &lt;options&gt; oben. Claude deployt Preview mit Voice/Sentry-Test, Luca entscheidet.</action>
  <verify>Manual: Luca-Response (deploy-prod | more-preview-testing | revert).</verify>
  <done>Entscheidung festgehalten, Task 4 führt aus oder Plan abgebrochen.</done>
  <decision>CSP-Änderung auf Vercel Preview gegen-checken, dann Prod-Deploy freigeben?</decision>
  <context>
    Claude hat:
    1. `vercel --yes` für tools-app ausgeführt → Preview-URL mit Test-Env-Vars
    2. Preview-URL mit Playwright-MCP gegen-gecheckt auf ALLEN kritischen Routes:
       - `/` → Homepage lädt, keine Console-CSP-Violations
       - `/login` → Login-Form rendert, Auth funktioniert
       - `/chat` (nach Login) → FloatingChat lädt, Voice-Button funktioniert (optional: echten Voice-Stream starten)
       - `/settings` → rendert
       - ToolLogo-Komponente: Clearbit-Images laden (img-src korrekt)
       - Sentry Test-Error: `window.throw()` in DevTools → Sentry Dashboard empfängt Event
    3. Response-Header enthält `Content-Security-Policy` mit allen Hosts (curl + grep)
    4. securityheaders.com Preview-Rating mind. A

    Luca muss jetzt Prod-Deploy freigeben. Per CLAUDE.md: kein Prod-Deploy ohne OK.

    Rollback-Plan: `git revert {commit-hash} && git push` → Vercel re-deployed pre-CSP-Stand.

    Hohes Risiko falls Deepgram/Sentry Host falsch: Voice-Feature kaputt ODER Sentry-Events kommen nicht mehr an (silent failure). Preview-Check MUSS beides verifizieren.
  </context>
  <options>
    <option id="deploy-prod">
      <name>Prod-Deploy freigeben (alle Preview-Checks grün)</name>
      <pros>CSP live, tools-app nicht mehr CSP-leer</pros>
      <cons>Falls versteckte Route mit externer Connection → CSP-Violation in Prod</cons>
    </option>
    <option id="more-preview-testing">
      <name>Mehr Preview-Testing (Voice end-to-end, Sentry-Flow)</name>
      <pros>Höhere Sicherheit</pros>
      <cons>Länger</cons>
    </option>
    <option id="revert">
      <name>Revidieren</name>
      <pros>Null Risiko</pros>
      <cons>Ziel nicht erreicht</cons>
    </option>
  </options>
  <resume-signal>Antworte mit "deploy-prod" / "more-preview-testing" / "revert" oder konkrete Anweisung.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Prod-Deploy + Post-Deploy-Verification + AUTH-FLOW.md Update</name>
  <files>docs/AUTH-FLOW.md</files>
  <read_first>
    - docs/AUTH-FLOW.md (mit Plan 04 CSP-Rollout-Sektion)
  </read_first>
  <action>
Nach Lucas Freigabe (Task 3 → "deploy-prod"):

1. Prod-Deploy (per Vercel auto-deploy nach merge/push — Claude fragt explizit "Push jetzt?" per CLAUDE.md)

2. Post-Deploy-Check (nach ~90s):
   - `curl -sI https://tools.generation-ai.org` → `content-security-policy:` header enthält `nonce-` + `o4511218002362368.ingest.de.sentry.io` + `api.deepgram.com`
   - securityheaders.com → https://securityheaders.com/?q=https%3A%2F%2Ftools.generation-ai.org → Rating notieren
   - E2E-Suite: `cd packages/e2e-tools && BASE_URL=https://tools.generation-ai.org pnpm exec playwright test auth.spec.ts` → alle Pfade 1-6 grün (Auth darf durch CSP nicht brechen)
   - Manual-Check (Playwright-MCP): 
     - Login auf Prod
     - /chat besuchen, Voice-Button klicken (falls möglich)
     - Browser-Console checken: KEINE CSP-Violations
     - Sentry: optional Test-Error trigger, in Sentry Dashboard prüfen ob angekommen

3. `docs/AUTH-FLOW.md` erweitern mit Abschnitt `## CSP Rollout — tools-app`:
```markdown
## CSP Rollout — tools-app (Plan 13-05)

Date deployed: {YYYY-MM-DD}
Commit: {hash}

### Response Headers (Production)
```
content-security-policy: default-src 'self'; script-src 'self' 'nonce-{redacted}' 'strict-dynamic' https://va.vercel-scripts.com; ... connect-src 'self' https://wbohulnuwqrhystaamjc.supabase.co ... https://o4511218002362368.ingest.de.sentry.io https://api.deepgram.com wss://api.deepgram.com ...
```

### securityheaders.com Result
Rating: {A | A+}
Screenshot: docs/auth-flow-screenshots/securityheaders-tools-app.png

### Feature Verification
- [ ] Auth: Login/Logout funktional
- [ ] Chat: Streaming funktional
- [ ] Voice: Deepgram WSS connect erfolgreich
- [ ] Sentry: Test-Event erreicht Dashboard
- [ ] ToolLogo: Clearbit-Images laden

### Violations observed
{none | list}
```
  </action>
  <verify>
    <automated>curl -sI https://tools.generation-ai.org 2>&amp;1 | grep -iE "^content-security-policy:" | grep -q "nonce-" &amp;&amp; curl -sI https://tools.generation-ai.org 2>&amp;1 | grep -iE "^content-security-policy:" | grep -q "o4511218002362368" &amp;&amp; curl -sI https://tools.generation-ai.org 2>&amp;1 | grep -iE "^content-security-policy:" | grep -q "api.deepgram.com" &amp;&amp; grep -q "## CSP Rollout — tools-app" docs/AUTH-FLOW.md</automated>
  </verify>
  <acceptance_criteria>
    - `curl -sI https://tools.generation-ai.org | grep -iE "^content-security-policy:"` matcht, enthält `nonce-`, Sentry-Host, Deepgram-Host
    - `curl -sI https://tools.generation-ai.org | grep -i "content-security-policy-report-only"` → KEIN Match
    - E2E-Suite: `pnpm exec playwright test auth.spec.ts` → alle aktiven Tests grün
    - Voice-Endpoint erreichbar: `curl -sI https://tools.generation-ai.org/api/voice/token` → 200 oder 401/503 (aber kein CSP-bedingter Abbruch)
    - securityheaders.com Rating mind. A
    - `docs/AUTH-FLOW.md` enthält CSP-Rollout-Abschnitt mit Feature-Verification-Checkliste
  </acceptance_criteria>
  <done>tools-app hat enforced CSP auf Prod; Auth/Chat/Voice/Sentry funktionieren; Dokumentation aktualisiert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → tools-app HTML | CSP kontrolliert Scripts/Styles/Connections → XSS-Defense. |
| tools-app → Sentry/Deepgram/Supabase | Third-party WSS + HTTPS Connections; müssen explizit in connect-src. |
| User-generated content (Chat-Input, KB-Content) → rendered HTML | Markdown-rendered content könnte injection versuchen → CSP als letzter Schutz. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-18 | Tampering (XSS) | Chat-Markdown-Rendering injects script | mitigate | `script-src 'nonce'` ohne `'unsafe-inline'` blockt inline injection auch wenn Markdown-Sanitizer umgangen wird |
| T-13-19 | Information Disclosure | Clickjack via iframe | mitigate | `frame-ancestors 'none'` |
| T-13-20 | Availability | CSP blockt Deepgram-WSS → Voice kaputt | mitigate | `wss://api.deepgram.com` explizit in connect-src + Preview-Check Task 3 |
| T-13-21 | Availability | CSP blockt Sentry-Events → silent monitoring loss | mitigate | Sentry DSN-Host (DE-Region) exakt in connect-src; Test-Event in Task 3/4 Verification |
| T-13-22 | Tampering | Clearbit serves compromised logo img | accept | Low-risk (Clearbit ist established brand lookup); img-src `https://logo.clearbit.com` nicht wildcard |
| T-13-23 | Tampering (Nonce-Cache) | Prefetch cached alte Nonce → 2. Request CSP-Violation | mitigate | matcher.missing inkl. next-router-prefetch + purpose:prefetch |
| T-13-24 | Elevation of Privilege | Open-Redirect via form-action auf externer Domain | mitigate | `form-action 'self'` (Supabase-Auth geht via fetch zu same-origin) |
</threat_model>

<verification>
- Build grün für tools-app
- E2E-Suite grün gegen Prod (Plan 02 Auth-Tests 1-6)
- CSP-Header enforced auf https://tools.generation-ai.org
- Deepgram, Sentry, Supabase, Clearbit alle reachable (keine Connect-Blocks)
- securityheaders.com mind. A
</verification>

<success_criteria>
- tools-app hat erstmals CSP (vorher: zero protection)
- Alle Features funktionieren weiter (Auth, Chat, Voice, Monitoring)
- Rollback-Pfad dokumentiert + getestet (git revert)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/13-auth-flow-audit-csp-reaktivierung/13-05-SUMMARY.md`:
- Deploy-Zeitpunkt + Commit-Hash
- securityheaders.com Rating
- Feature-Verification-Liste (Auth, Chat, Voice, Sentry alle ok)
- Rollback-Anleitung
</output>
