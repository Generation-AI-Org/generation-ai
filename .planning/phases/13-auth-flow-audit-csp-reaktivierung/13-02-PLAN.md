---
phase: 13
plan: 02
type: execute
wave: 1
depends_on: [13-01]
files_modified:
  - packages/e2e-tools/tests/auth.spec.ts
  - docs/AUTH-FLOW.md
  - .planning/BACKLOG.md
autonomous: false
requirements:
  - AUTH-AUDIT-01
  - AUTH-AUDIT-02
  - AUTH-AUDIT-03
  - AUTH-AUDIT-04
  - AUTH-AUDIT-05
  - AUTH-AUDIT-06
must_haves:
  truths:
    - "Pfad 1 (Login E+P): E2E-Test grün, Cookie-Domain verifiziert"
    - "Pfad 2 (Magic Link): E2E-Test via admin generateLink grün"
    - "Pfad 3 (Session Refresh): Manuell per Playwright-MCP geprüft, Findings in AUTH-FLOW.md"
    - "Pfad 4 (Signout): POST setzt Cookies clear, GET weiterhin 405 (Regression)"
    - "Pfad 5 (Password-Reset): generateRecoveryLink → set-password → re-login grün ODER Finding dokumentiert + inline gefixt"
    - "Pfad 6 (Cross-Domain): Cookie-Domain=.generation-ai.org verifiziert auf beiden Subdomains"
    - "Findings-Sektion in AUTH-FLOW.md pro Pfad: status=verified-ok|fixed|backlog"
    - "Signup-503 dokumentiert in AUTH-FLOW.md (nicht getestet, laut D-17 absichtlich disabled)"
  artifacts:
    - path: packages/e2e-tools/tests/auth.spec.ts
      provides: "6 aktivierte Tests (skip-Marker entfernt), mindestens 8 test() blocks"
      min_lines: 180
    - path: docs/AUTH-FLOW.md
      provides: "Audit-Ergebnisse pro Pfad mit Findings-Tabelle (skeleton — Mermaid + final copy in Plan 06)"
      contains: "## Findings"
    - path: .planning/BACKLOG.md
      provides: "Neue Items für größere Findings (per D-06)"
  key_links:
    - from: packages/e2e-tools/tests/auth.spec.ts
      to: packages/e2e-tools/helpers/supabase-admin.ts
      via: "generateMagicLink / generateRecoveryLink in Tests"
      pattern: "generateMagicLink|generateRecoveryLink"
    - from: docs/AUTH-FLOW.md
      to: packages/e2e-tools/tests/auth.spec.ts
      via: "Findings-Tabelle referenziert test-Namen"
      pattern: "auth\\.spec\\.ts"
user_setup:
  - service: supabase
    why: "Test-User für E2E-Audit benötigt"
    env_vars:
      - name: TEST_USER_EMAIL
        source: "Supabase Dashboard → Auth → Users → Add user (z.B. test+e2e@generation-ai.org) → in packages/e2e-tools/.env.test.local eintragen"
      - name: TEST_USER_PASSWORD
        source: "Beim User-Anlegen vergebenes Passwort → .env.test.local"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard → Project Settings → API → service_role → .env.test.local"
    dashboard_config:
      - task: "Test-User mit email_confirm=true anlegen"
        location: "Auth → Users → Add user, Auto-Confirm-User aktivieren"
---

<objective>
Wave 1: Systematisches Audit aller 6 Auth-Pfade gegen Production. E2E-Tests aus Wave 0 Skeleton ausfüllen + manuelle Playwright-MCP-Läufe dokumentieren. Kleine Bugs inline fixen (D-05/D-07), größere als Backlog (D-06).

Purpose: Beweis dass Auth end-to-end funktioniert + Findings-Datenbasis für Plan 06 (AUTH-FLOW.md Final).
Output: auth.spec.ts mit grünen Tests für alle 6 Pfade + Draft-Version von docs/AUTH-FLOW.md mit Findings-Tabelle + neue Backlog-Items (falls Bugs gefunden).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-auth-flow-audit-csp-reaktivierung/13-CONTEXT.md
@.planning/phases/13-auth-flow-audit-csp-reaktivierung/13-RESEARCH.md
@.planning/phases/13-auth-flow-audit-csp-reaktivierung/13-01-SUMMARY.md
@packages/e2e-tools/tests/auth.spec.ts
@packages/e2e-tools/fixtures/test-user.ts
@packages/e2e-tools/helpers/supabase-admin.ts
@apps/tools-app/app/auth/signout/route.ts
@apps/tools-app/app/auth/set-password/page.tsx
@apps/tools-app/app/auth/confirm/route.ts
@.planning/BACKLOG.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Pre-flight — Luca füllt .env.test.local + legt Test-User an</name>
  <files>packages/e2e-tools/.env.test.local (manuell von Luca erstellt, nicht committed)</files>
  <action>Checkpoint-Task — siehe &lt;what-built&gt; + &lt;how-to-verify&gt; oben. Luca führt manuelle Schritte aus, Claude wartet auf resume-signal.</action>
  <verify>Manual: `test -f packages/e2e-tools/.env.test.local &amp;&amp; grep -q TEST_USER_EMAIL packages/e2e-tools/.env.test.local`</verify>
  <done>Luca antwortet mit 'ready' oder 'blocked: ...'</done>
  <what-built>Wave 0 hat .env.test.local.example erstellt. Luca muss jetzt manuell:
    1. Supabase Dashboard → Auth → Users → Add user (z.B. `test+e2e@generation-ai.org` + sicheres Passwort, Auto-Confirm aktivieren)
    2. Supabase Dashboard → Project Settings → API → `service_role` Key kopieren
    3. `cp packages/e2e-tools/.env.test.local.example packages/e2e-tools/.env.test.local`
    4. Werte in `.env.test.local` eintragen (TEST_USER_EMAIL, TEST_USER_PASSWORD, SUPABASE_SERVICE_ROLE_KEY)
  </what-built>
  <how-to-verify>
    1. `cd packages/e2e-tools && cat .env.test.local` zeigt alle 5 Werte befüllt (TEST_USER_EMAIL, TEST_USER_PASSWORD, NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, BASE_URL)
    2. Smoke: `cd packages/e2e-tools && pnpm exec playwright test --grep "login page renders form"` → grün (schon Wave 0 funktional, aber Dep-Check)
    3. Smoke-Admin: `cd packages/e2e-tools && node -e "require('dotenv').config({path:'.env.test.local'}); const {ensureTestUser} = require('./helpers/supabase-admin.ts'); /* or tsx equiv */"` — einfacher: in einem temp-test `ensureTestUser(process.env.TEST_USER_EMAIL, process.env.TEST_USER_PASSWORD)` aufrufen, expect keine errors
  </how-to-verify>
  <resume-signal>Antworte mit "ready" wenn .env.test.local befüllt ist und Test-User existiert. Oder "blocked: {Grund}" wenn etwas nicht klappt.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tests für Pfad 1, 2, 4, 5, 6 ausfüllen (skip-Marker entfernen + Test-Bodies schreiben)</name>
  <files>packages/e2e-tools/tests/auth.spec.ts</files>
  <read_first>
    - packages/e2e-tools/tests/auth.spec.ts (Wave 0 skeleton mit .skip Markern)
    - apps/tools-app/app/auth/signout/route.ts (POST-only Handler — prüfen welchen Cookie-Key Supabase setzt, z.B. via grep `sb-` in @supabase/ssr)
    - apps/tools-app/app/auth/confirm/route.ts (Recovery-Redirect-Ziel)
    - apps/tools-app/app/auth/set-password/page.tsx (Form-Struktur für Pfad 5)
    - apps/tools-app/app/login/page.tsx (Form-Fields Login für Pfad 1)
  </read_first>
  <action>
    Nacheinander in `packages/e2e-tools/tests/auth.spec.ts` die `test.skip` durch `test` ersetzen und Bodies ausfüllen:

    **Pfad 1 — "password login sets sb- cookie":**
    - `page.goto(TOOLS_URL + "/login")`
    - fill email/password, click submit
    - `page.waitForURL(url => !url.pathname.includes("/login"), { timeout: 15_000 })`
    - `cookies = await page.context().cookies(TOOLS_URL)`
    - expect einen Cookie namens beginnend mit `sb-` existiert
    - expect `sessionCookie.domain === ".generation-ai.org"` (Phase 12 pattern)
    - expect `sessionCookie.httpOnly === true` + `sessionCookie.secure === true`

    **Pfad 1 — "session persists across reload":**
    - Login wie oben
    - `await page.reload()`
    - Cookie bleibt, Page rendert authed UI (z.B. expect `page.locator('[data-testid="appshell"]')` ODER fallback: expect dass keine Redirect zu /login passiert)

    **Pfad 2 — "magic link signs user in":**
    - `const { actionLink } = await generateMagicLink(user.email, TOOLS_URL + "/auth/confirm")`
    - `await page.goto(actionLink)`
    - `page.waitForURL((url) => !url.pathname.includes("/auth/confirm") && !url.pathname.includes("/login"))`
    - expect sb-cookie gesetzt mit Domain `.generation-ai.org`

    **Pfad 4 — "POST /auth/signout clears sb- cookies":**
    - Login per Pfad 1
    - POST per `await page.request.post(TOOLS_URL + "/auth/signout", { maxRedirects: 0 })`
    - Response-Status 302 oder 303 erwarten; `set-cookie`-Headers müssen `sb-*` mit expired Max-Age haben
    - Kontext-Cookies nach request prüfen: sb-Cookie weg / expired

    **Pfad 5 — "recovery flow works end-to-end":**
    - `const { actionLink } = await generateRecoveryLink(user.email, TOOLS_URL + "/auth/confirm")`
    - `await page.goto(actionLink)` → Redirect zu `/auth/set-password`
    - expect page.getByLabel(/passwort/i) visible
    - Neues Passwort eintragen (generiert zur Laufzeit: `"Reset!" + Date.now()`), submit
    - Erfolgs-Indicator abwarten (Redirect zu / oder toast)
    - `await page.context().clearCookies()`
    - Re-Login mit NEUEM Passwort → erfolgreich
    - Cleanup: zurück auf ORIGINAL `user.password` setzen (Re-Login → set-password mit altem PW) ODER `ensureTestUser` wieder mit Original-PW → delete+recreate als Cleanup
    - **Wichtig:** Falls dieser Test scheitert wegen Redirect-Broken / set-password-Endpoint broken → STOP, ergebnis in AUTH-FLOW.md dokumentieren, Task 4 entscheidet ob inline-fix (D-07) oder Backlog

    **Pfad 6 — "cross-domain cookie":**
    - Login auf tools.generation-ai.org per Pfad 1 (context 1)
    - In gleichem browser context: `await page.goto(WEBSITE_URL)` → Website mit demselben Cookie-Jar
    - `cookies = await page.context().cookies(WEBSITE_URL)` — prüfe dass sb-Cookie auch hier erscheint (wegen Domain-Attribut)
    - expect `sessionCookie.domain === ".generation-ai.org"`

    **Pfad 3 — "session refresh" bleibt skipped** mit Kommentar:
    ```
    test.skip("manual-only: session refresh verified per Playwright-MCP run in AUTH-FLOW.md", ...)
    ```
    Laut Research Open Question 1: Token-Expiry-Simulation in kurzen Tests nicht machbar → wird per Playwright-MCP-Run manuell geprüft (Task 3).

    Für jeden Test: `test.setTimeout(60_000)` einzeln setzen weil Prod-Latenz.
  </action>
  <verify>
    <automated>cd packages/e2e-tools &amp;&amp; BASE_URL=https://tools.generation-ai.org WEBSITE_URL=https://generation-ai.org pnpm exec playwright test auth.spec.ts --reporter=line 2>&amp;1 | tee /tmp/p13-02-playwright.log; grep -E "(passed|failed)" /tmp/p13-02-playwright.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test.skip" packages/e2e-tools/tests/auth.spec.ts` → maximal 2 (Pfad 3 manual-only + CSP Wave 2 TODO)
    - `grep -c "test(" packages/e2e-tools/tests/auth.spec.ts` → mindestens 8 aktive Tests
    - Playwright-Run: `0 failed` in allen Pfaden (1, 2, 4, 5, 6 + GET-Signout-405-Regression + /login renders)
    - `grep -q "generateMagicLink" packages/e2e-tools/tests/auth.spec.ts` → match
    - `grep -q "generateRecoveryLink" packages/e2e-tools/tests/auth.spec.ts` → match
    - Falls Pfad 5 Test scheitert: Finding dokumentiert (siehe Task 4), Test bleibt skipped mit `test.skip(..., { annotation: "backlog: set-password endpoint broken" })`
  </acceptance_criteria>
  <done>Playwright-Suite grün gegen Production für Pfade 1, 2, 4, 5 (oder dokumentiert broken + Backlog), 6. Pfad 3 dokumentiert als manual-only.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Playwright-MCP Manual-Runs für Pfade 3 + Screenshots + Network-Logs</name>
  <files>docs/AUTH-FLOW.md (new, draft)</files>
  <read_first>
    - .planning/phases/13-auth-flow-audit-csp-reaktivierung/13-CONTEXT.md (D-03: dokumentiere Network-Requests, Console-Errors, Set-Cookie-Header)
    - packages/auth/src/middleware.ts (updateSession - session-refresh logic)
  </read_first>
  <action>
    Manuelle Playwright-MCP-Runs (via `mcp__playwright__*` tools oder lokaler Playwright --headed session):

    1. **Pfad 3 Session-Refresh Manual:**
       - Login auf tools.generation-ai.org
       - Cookie-Expiry inspizieren (sb-access-token expires_at)
       - Seite lange offen lassen ODER `page.evaluate(() => document.cookie)` vor + nach Navigation — updateSession rotiert Token bei Bedarf
       - Network-Tab: Requests zu `https://wbohulnuwqrhystaamjc.supabase.co/auth/v1/token?grant_type=refresh_token` dokumentieren
       - Screenshot speichern unter `docs/auth-flow-screenshots/path-3-refresh.png`
       - Ergebnis als Notiz in AUTH-FLOW.md Draft

    2. **Pfad 4 Signout Prefetch-Check (Regression):**
       - Zu `/chat` navigieren, hover über nav-Items mit `<Link>` Component → Network-Tab beobachten: KEINE GET /auth/signout darf auftauchen (f5f9cb7 fix)
       - Signout-Button klicken → POST 302 → Login-Page, sb-Cookie weg
       - Screenshots: `path-4-prefetch-check.png`, `path-4-signout-result.png`

    3. **Signup 503 Dokumentation (D-17):**
       - POST auf `/api/auth/signup` → 503 verifiziert
       - Screenshot: `path-signup-503.png`
       - Nicht als Bug: dokumentiere als "intentionally disabled" mit Verweis auf STATE.md

    4. `docs/AUTH-FLOW.md` als DRAFT anlegen mit folgenden Sections (Plan 06 finalisiert mit Mermaid):
       ```markdown
       # Auth Flow Audit — Phase 13 (DRAFT)

       Status: DRAFT — finalisiert in Phase 13 Plan 06.
       Audit-Date: {date}
       Test-User: {email (redacted domain)}

       ## Paths Audited

       1. Login via Email+Passwort — status: verified-ok | fixed | backlog
       2. Magic Link (admin-generated) — status: ...
       3. Session-Refresh (manual-only) — status: ...
       4. Signout POST-only (regression) — status: verified-ok
       5. Password-Reset End-to-End — status: ...
       6. Cross-Domain Cookie — status: ...

       ## Findings

       | # | Pfad | Finding | Severity | Status | Fix/Backlog |
       |---|------|---------|----------|--------|-------------|
       | F1 | {path} | {desc} | small/large | fixed/backlog'd | {commit or BACKLOG entry} |

       ## Signup (disabled by design)
       Per D-17: /api/auth/signup returns 503. Not tested. See STATE.md for rationale.

       ## Manual-Only Evidence
       - Screenshots in docs/auth-flow-screenshots/
       - Network-logs: Pfad 3 token refresh at {timestamp}
       ```

    Für jeden gefundenen Bug: Finding-Zeile + Playwright-Evidence-Link + nach Task 4 ergänzen mit Fix-Entscheidung.
  </action>
  <verify>
    <automated>test -f docs/AUTH-FLOW.md &amp;&amp; grep -q "## Paths Audited" docs/AUTH-FLOW.md &amp;&amp; grep -q "## Findings" docs/AUTH-FLOW.md &amp;&amp; grep -q "Signup.*disabled" docs/AUTH-FLOW.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/AUTH-FLOW.md`
    - Findings-Tabelle existiert (auch wenn leer = alle verified-ok)
    - Für jeden der 6 Pfade Status gesetzt (verified-ok | fixed | backlog | manual-only)
    - Signup-503 dokumentiert
    - Mindestens Pfad 3 + Pfad 4 haben Manual-Evidence-Note (Screenshot oder Network-log-Beschreibung)
  </acceptance_criteria>
  <done>AUTH-FLOW.md DRAFT existiert, alle 6 Pfade haben Status-Einträge. Pfad 3 manuell verifiziert, Pfad 4 Prefetch-Regression aktiv gegengecheckt.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: Finding-Triage — inline fix vs Backlog-Entscheidung</name>
  <files>docs/AUTH-FLOW.md (Findings-Tabelle Zwischenstand)</files>
  <action>Checkpoint-Task — siehe &lt;decision&gt; + &lt;options&gt; oben. Claude präsentiert Findings-Liste, Luca wählt Triage-Option.</action>
  <verify>Manual: Luca-Response eindeutig (as-proposed | override-per-finding | all-backlog).</verify>
  <done>Triage-Entscheidung festgehalten, Task 5 kann starten.</done>
  <decision>Wie mit gefundenen Bugs umgehen?</decision>
  <context>
    Nach Task 2 + 3 liegt eine Findings-Liste vor. Laut D-05, D-06, D-07:
    - Klein (<30 Zeilen, klarer Root-Cause, gleicher Scope) → inline fix in dieser Phase
    - Groß (Refactoring, neue Features, UX-Gaps) → BACKLOG.md
    - Password-Reset broken → inline fix (D-07) außer "UI neu bauen" (dann Backlog)

    Vor dem Weitermachen: Claude präsentiert die Findings-Liste, schlägt pro Finding Inline-Fix oder Backlog vor, wartet auf Lucas Entscheidung.
  </context>
  <options>
    <option id="as-proposed">
      <name>Claude's Triage akzeptieren</name>
      <pros>Schnell, konsistent mit D-05..D-07</pros>
      <cons>Luca hat weniger Kontrolle bei Grenzfällen</cons>
    </option>
    <option id="override-per-finding">
      <name>Pro Finding explizit entscheiden</name>
      <pros>Luca kontrolliert Scope</pros>
      <cons>Mehr Checkpoint-Interaktion</cons>
    </option>
    <option id="all-backlog">
      <name>Alle Findings → Backlog (keine Inline-Fixes, strenger Scope)</name>
      <pros>Phase-13 bleibt klein, kein Regressionsrisiko</pros>
      <cons>Verliert D-07 Password-Reset-inline-fix-Intention</cons>
    </option>
  </options>
  <resume-signal>Antworte mit "as-proposed" / "override-per-finding" / "all-backlog" oder sage konkret welche Findings wie behandelt werden.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Inline-Fixes anwenden + BACKLOG.md erweitern + AUTH-FLOW.md Findings-Tabelle finalisieren</name>
  <files>docs/AUTH-FLOW.md, .planning/BACKLOG.md, ggf. apps/tools-app/app/auth/**, apps/website/app/api/auth/**</files>
  <read_first>
    - .planning/BACKLOG.md (aktuelle Struktur / Sektionen)
    - AUTH-FLOW.md Findings aus Task 3
    - Triage-Entscheidung aus Task 4
  </read_first>
  <action>
    Für jede Finding-Zeile aus AUTH-FLOW.md:

    **Inline-Fix (D-05 kleine Fixes, D-07 Password-Reset):**
    - Kleinst möglichen Patch anwenden (< 30 Zeilen)
    - Test in auth.spec.ts erweitern/entsperren um Fix zu verifizieren
    - Atomic commit: `fix(auth): {description} — Phase 13 audit finding F{N}`
    - AUTH-FLOW.md Findings-Tabelle: status `fixed`, commit-Hash eintragen

    **Backlog (D-06 große Findings):**
    - `.planning/BACKLOG.md` — unter Sektion "🔐 Auth (aus Phase 13 Audit)" eintragen:
      ```
      - [ ] {title} — gefunden in Phase 13 Audit, siehe docs/AUTH-FLOW.md Finding F{N}. Impact: {...}. Fix approach: {...}.
      ```
    - AUTH-FLOW.md Findings-Tabelle: status `backlog'd`, Link auf BACKLOG.md-Zeile (via anchor oder Zeilen-Nr)

    Falls KEINE Findings: AUTH-FLOW.md Findings-Tabelle bekommt Zeile "No bugs found — all paths verified-ok. See Manual-Evidence screenshots."

    **Wichtig:** Keine Scope-Creep. Wenn Finding vage ist → Backlog (nicht inline fixen).
  </action>
  <verify>
    <automated>cd packages/e2e-tools &amp;&amp; BASE_URL=https://tools.generation-ai.org WEBSITE_URL=https://generation-ai.org pnpm exec playwright test auth.spec.ts --reporter=line 2>&amp;1 | grep -E "passed|failed"; cd ../.. &amp;&amp; pnpm -F @genai/auth test 2>&amp;1 | grep -E "Tests"</automated>
  </verify>
  <acceptance_criteria>
    - AUTH-FLOW.md: jede Finding hat Status (fixed/backlog'd/verified-ok) — keine offenen Items
    - Für jede `fixed`-Finding: Commit-Hash eingetragen
    - Für jede `backlog'd`-Finding: BACKLOG.md enthält entsprechendes Item (grep -q "{finding title}" .planning/BACKLOG.md)
    - `pnpm -F @genai/auth test` → alle Tests grün (keine Auth-Regression durch Inline-Fixes)
    - `cd packages/e2e-tools && pnpm exec playwright test auth.spec.ts` → alle Tests grün (Fixes haben neue Tests nicht gebrochen)
    - Signout-Regression-Test (GET→405) weiterhin grün (f5f9cb7 Fix hält)
    - Signup-Endpoint weiterhin 503 (nicht angefasst per D-17): `curl -X POST https://generation-ai.org/api/auth/signup` → 503
  </acceptance_criteria>
  <done>Alle Findings abgehakt. E2E-Suite grün. Unit-Tests grün. BACKLOG.md aktualisiert. AUTH-FLOW.md Findings-Tabelle komplett.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test runner → Production Supabase | Tests mutieren Production Auth-DB (Passwort-Reset-Test ändert Passwort temporär). Mitigation: Test-User klar separiert, Cleanup-Schritt wiederherstellt Original-PW. |
| Playwright browser context → set-password page | Tester lädt action_link mit recovery token; token muss innerhalb TTL (10min) eingelöst werden. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Tampering | Password-Reset-Test ändert Prod-User-PW und stellt nicht wieder her → Test-User wird unbrauchbar | mitigate | Test-Body enthält explicit Cleanup-Phase (re-reset zu Original-PW via zweiter generateRecoveryLink) + Finally-Block |
| T-13-06 | Repudiation | Audit findings werden nicht dokumentiert → Findings verloren | mitigate | AUTH-FLOW.md Pflicht-Output; Task 5 acceptance_criteria erzwingt Findings-Tabelle |
| T-13-07 | Information Disclosure | Screenshots enthalten Session-Cookies / Tokens | mitigate | Vor Screenshot: cookies clear ODER redact im Bild; oder Screenshot nur von DevTools-Panels (nicht Network-Cookie-Werte) |
| T-13-08 | Elevation of Privilege | generateLink mit SERVICE_ROLE_KEY kann jeden User-Account öffnen → falls Code leakt, Full-Account-Takeover | mitigate | Helper liegt in packages/e2e-tools (node-only), nie in apps/ importiert; T-13-02 Kontrolle |
| T-13-09 | Tampering (Regression) | Signout wird versehentlich wieder GET-handled → Session-Drop-Bug zurück | mitigate | Wave 0 Regression-Test (GET /auth/signout → 405) bleibt unskipped; CI-bereit |
</threat_model>

<verification>
- `pnpm exec playwright test auth.spec.ts` grün (≥ 8 aktive Tests)
- `pnpm test` (Root) grün (keine Auth-Regression)
- `docs/AUTH-FLOW.md` existiert mit Findings-Tabelle
- `.planning/BACKLOG.md` hat neue Items (falls Findings → Backlog)
- Signup bleibt 503 (manual curl check)
- Signout bleibt POST-only (GET → 405 Regression-Test)
</verification>

<success_criteria>
- Alle 6 Auth-Pfade haben einen dokumentierten Status (verified-ok | fixed | backlog | manual-only)
- Keine neuen Auth-Bugs unfixed offen (alle entweder fixed oder backlog'd)
- E2E-Suite CI-ready (in Wave 2/3 bleibt sie grün gegen Prod)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/13-auth-flow-audit-csp-reaktivierung/13-02-SUMMARY.md`:
- Findings-Liste (Anzahl fixed / backlog'd / verified-ok)
- Commits-Liste (Inline-Fixes)
- Neue BACKLOG.md-Einträge (Titel)
- Verweis auf docs/AUTH-FLOW.md als Single-Source-of-Truth
</output>
