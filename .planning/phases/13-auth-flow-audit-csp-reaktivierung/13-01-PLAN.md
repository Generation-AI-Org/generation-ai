---
phase: 13
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/e2e-tools/playwright.config.ts
  - packages/e2e-tools/package.json
  - packages/e2e-tools/.env.test.local.example
  - packages/e2e-tools/.gitignore
  - packages/e2e-tools/fixtures/test-user.ts
  - packages/e2e-tools/helpers/supabase-admin.ts
  - packages/e2e-tools/helpers/csp-assertions.ts
  - packages/e2e-tools/tests/auth.spec.ts
autonomous: true
requirements:
  - AUTH-AUDIT-00
must_haves:
  truths:
    - "E2E-Test-Infrastructure lädt Test-User-Credentials aus .env.test.local"
    - "Supabase-Admin-Helper kann magic links generieren (server-seitig, Service Role Key)"
    - "CSP-Violation-Assertion-Helper erfasst console errors + header inspection"
    - ".env.test.local.example dokumentiert alle benötigten Variablen"
    - "auth.spec.ts enthält test.describe Blöcke für alle 6 Pfade (stubs, teils skip bis Wave 1)"
  artifacts:
    - path: packages/e2e-tools/fixtures/test-user.ts
      provides: "Test-User-Credential-Loader"
      exports: ["getTestUser"]
    - path: packages/e2e-tools/helpers/supabase-admin.ts
      provides: "Admin-API-Helper: generateLink, deleteUser"
      exports: ["generateMagicLink", "ensureTestUser"]
    - path: packages/e2e-tools/helpers/csp-assertions.ts
      provides: "CSP console-listener + header-inspector"
      exports: ["collectCspViolations", "assertCspHeader"]
    - path: packages/e2e-tools/.env.test.local.example
      provides: "Template für TEST_USER_EMAIL, TEST_USER_PASSWORD, SUPABASE_SERVICE_ROLE_KEY, NEXT_PUBLIC_SUPABASE_URL, BASE_URL"
    - path: packages/e2e-tools/tests/auth.spec.ts
      provides: "6 test.describe Blöcke für Login-Passwort, Magic-Link, Session-Refresh, Signout, Password-Reset, Cross-Domain"
  key_links:
    - from: packages/e2e-tools/helpers/supabase-admin.ts
      to: packages/auth/src/admin.ts
      via: "import { createAdminClient } from '@genai/auth/admin'"
      pattern: "from ['\"]@genai/auth/admin['\"]"
    - from: packages/e2e-tools/tests/auth.spec.ts
      to: packages/e2e-tools/fixtures/test-user.ts
      via: "import getTestUser"
      pattern: "from ['\"]\\.\\.(/|\\\\)fixtures(/|\\\\)test-user"
---

<objective>
Wave 0: Test-Infrastructure für Phase 13 Auth-Audit aufbauen.

Purpose: Alle Wave-1 E2E-Tests bauen auf dieser Infrastruktur auf. Ohne Test-User-Fixture, Supabase-Admin-Helper und CSP-Assertion-Helper können die 6 Pfade nicht reproduzierbar getestet werden. Research D-04 + 13-VALIDATION.md Wave 0 Requirements.
Output: Test-Infrastruktur in `packages/e2e-tools/` — Fixtures, Helpers, .env-Template, erweiterte playwright.config, auth.spec.ts-Skeleton mit describe-Blöcken (Tests meist .skip bis Wave 1 sie ausfüllt).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-auth-flow-audit-csp-reaktivierung/13-CONTEXT.md
@.planning/phases/13-auth-flow-audit-csp-reaktivierung/13-RESEARCH.md
@.planning/phases/13-auth-flow-audit-csp-reaktivierung/13-VALIDATION.md
@packages/e2e-tools/playwright.config.ts
@packages/e2e-tools/tests/auth.spec.ts
@packages/e2e-tools/package.json
@packages/auth/src/admin.ts

<interfaces>
From packages/auth/src/admin.ts (verify via `grep -n "export" packages/auth/src/admin.ts`):
```typescript
// createAdminClient() → SupabaseClient mit SERVICE_ROLE_KEY
export function createAdminClient(): SupabaseClient
```

From @supabase/supabase-js GoTrueAdminApi:
```typescript
admin.auth.admin.generateLink({
  type: 'magiclink' | 'recovery' | 'invite',
  email: string,
  options?: { redirectTo?: string }
}): Promise<{ data: { properties: { action_link, hashed_token, redirect_to }, user }, error }>

admin.auth.admin.listUsers(): Promise<{ data: { users: User[] }, error }>
admin.auth.admin.createUser({ email, password, email_confirm: true }): Promise<{ data: { user }, error }>
admin.auth.admin.deleteUser(id: string): Promise<{ data, error }>
```

From @playwright/test v1.52:
```typescript
test.describe(name, fn), test(name, fn), test.skip(name, fn)
page.on('console', cb), page.context().cookies(url), page.goto(url), expect(..).toBe(..)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: playwright.config + package.json + .env Templates + .gitignore</name>
  <files>packages/e2e-tools/playwright.config.ts, packages/e2e-tools/package.json, packages/e2e-tools/.env.test.local.example, packages/e2e-tools/.gitignore</files>
  <read_first>
    - packages/e2e-tools/playwright.config.ts (aktueller Stand, 17 Zeilen)
    - packages/e2e-tools/package.json (aktuelle Scripts + devDependencies)
    - .gitignore im Repo-Root (prüfen ob .env.test.local schon ignoriert wird)
    - .planning/phases/13-auth-flow-audit-csp-reaktivierung/13-RESEARCH.md (Abschnitt "Validation Architecture" + Code Example für proxy.ts)
  </read_first>
  <action>
    1. `packages/e2e-tools/.env.test.local.example` neu anlegen mit EXAKT diesem Inhalt:
    ```
    # Copy to .env.test.local (git-ignored) and fill in real values.
    # Test-User (in Supabase Dashboard → Auth → Users manually angelegt, NICHT Luca's Account):
    TEST_USER_EMAIL=
    TEST_USER_PASSWORD=

    # Supabase (Service Role Key nur server-seitig, NIE committen):
    NEXT_PUBLIC_SUPABASE_URL=https://wbohulnuwqrhystaamjc.supabase.co
    SUPABASE_SERVICE_ROLE_KEY=

    # Ziel-URL für E2E (Default: Prod tools-app):
    BASE_URL=https://tools.generation-ai.org
    WEBSITE_URL=https://generation-ai.org
    ```

    2. `packages/e2e-tools/.gitignore` neu anlegen mit Inhalt:
    ```
    .env.test.local
    test-results/
    playwright-report/
    .playwright/
    ```

    3. `packages/e2e-tools/package.json` erweitern:
    - `devDependencies`: `"dotenv": "^16.4.5"` und `"@genai/auth": "workspace:*"` und `"@supabase/supabase-js": "^2.103.0"` hinzufügen (Admin-Helper braucht diese)
    - `scripts`: `"test"` hinzufügen als Alias für `"playwright test"` (turbo erwartet `test`)

    4. `packages/e2e-tools/playwright.config.ts` erweitern:
    - Ganz oben `import "dotenv/config"` + `import * as dotenv from "dotenv"; dotenv.config({ path: ".env.test.local" })` (ENV aus .env.test.local laden BEVOR defineConfig)
    - `baseURL` default bleibt, aber Kommentar: `// override via BASE_URL=https://tools.generation-ai.org in .env.test.local`
    - `timeout: 30_000` (30s pro Test — Prod Latenz berücksichtigen)
    - `expect.timeout: 10_000`
    - Keine weiteren projects (chromium bleibt einziger Browser für Wave 1; Firefox/Webkit = Backlog)
  </action>
  <verify>
    <automated>test -f packages/e2e-tools/.env.test.local.example &amp;&amp; test -f packages/e2e-tools/.gitignore &amp;&amp; node -e "require('./packages/e2e-tools/package.json').devDependencies.dotenv" &amp;&amp; grep -q "dotenv" packages/e2e-tools/playwright.config.ts &amp;&amp; grep -q "\.env\.test\.local" packages/e2e-tools/.gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "TEST_USER_EMAIL" packages/e2e-tools/.env.test.local.example` → match
    - `grep -q "SUPABASE_SERVICE_ROLE_KEY" packages/e2e-tools/.env.test.local.example` → match
    - `grep -q "BASE_URL=https://tools.generation-ai.org" packages/e2e-tools/.env.test.local.example` → match
    - `node -e "console.log(require('./packages/e2e-tools/package.json').scripts.test)"` gibt etwas aus (nicht undefined)
    - `node -e "console.log(require('./packages/e2e-tools/package.json').devDependencies['@genai/auth'])"` gibt `workspace:*` aus
    - `grep -q "\.env\.test\.local" .gitignore || grep -q "\.env\.test\.local" packages/e2e-tools/.gitignore` → match (doppelt ignoriert ist OK)
    - playwright.config.ts importiert dotenv VOR defineConfig (grep `dotenv` steht über `defineConfig`)
  </acceptance_criteria>
  <done>Templates, Config und Dependency-Deklaration sind committet, `pnpm install` läuft clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fixtures + Helpers (test-user, supabase-admin, csp-assertions)</name>
  <files>packages/e2e-tools/fixtures/test-user.ts, packages/e2e-tools/helpers/supabase-admin.ts, packages/e2e-tools/helpers/csp-assertions.ts</files>
  <read_first>
    - packages/auth/src/admin.ts (exakter export von createAdminClient)
    - packages/auth/src/index.ts (prüfen was @genai/auth re-exportiert)
    - .planning/phases/13-auth-flow-audit-csp-reaktivierung/13-RESEARCH.md (Code-Examples für generateLink + CSP Violation Check)
  </read_first>
  <action>
    1. `packages/e2e-tools/fixtures/test-user.ts` anlegen mit EXAKTEM Inhalt:
    ```typescript
    /**
     * Test-User-Credential-Loader.
     * Werte kommen aus .env.test.local (via dotenv in playwright.config).
     * Wenn fehlend → wirft explizit (damit Tests skip'en können via test.skip(!user.email)).
     */
    export type TestUser = {
      email: string
      password: string
    }

    export function getTestUser(): TestUser | null {
      const email = process.env.TEST_USER_EMAIL
      const password = process.env.TEST_USER_PASSWORD
      if (!email || !password) return null
      return { email, password }
    }

    export function requireTestUser(): TestUser {
      const u = getTestUser()
      if (!u) {
        throw new Error(
          "TEST_USER_EMAIL / TEST_USER_PASSWORD missing — copy .env.test.local.example to .env.test.local and fill in a real Supabase test user."
        )
      }
      return u
    }
    ```

    2. `packages/e2e-tools/helpers/supabase-admin.ts` anlegen mit EXAKTEM Inhalt:
    ```typescript
    /**
     * Supabase Admin Helper for E2E tests.
     * Runs in Node.js context (not browser). Uses SERVICE_ROLE_KEY.
     * Reference: Phase 13 RESEARCH.md "Don't Hand-Roll" + "Code Examples".
     */
    import { createClient, type SupabaseClient } from "@supabase/supabase-js"

    function adminClient(): SupabaseClient {
      const url = process.env.NEXT_PUBLIC_SUPABASE_URL
      const key = process.env.SUPABASE_SERVICE_ROLE_KEY
      if (!url || !key) {
        throw new Error(
          "NEXT_PUBLIC_SUPABASE_URL or SUPABASE_SERVICE_ROLE_KEY missing in .env.test.local"
        )
      }
      return createClient(url, key, {
        auth: { autoRefreshToken: false, persistSession: false },
      })
    }

    export async function generateMagicLink(email: string, redirectTo?: string) {
      const { data, error } = await adminClient().auth.admin.generateLink({
        type: "magiclink",
        email,
        options: redirectTo ? { redirectTo } : undefined,
      })
      if (error) throw error
      return {
        actionLink: data.properties.action_link as string,
        hashedToken: data.properties.hashed_token as string,
      }
    }

    export async function generateRecoveryLink(email: string, redirectTo?: string) {
      const { data, error } = await adminClient().auth.admin.generateLink({
        type: "recovery",
        email,
        options: redirectTo ? { redirectTo } : undefined,
      })
      if (error) throw error
      return {
        actionLink: data.properties.action_link as string,
        hashedToken: data.properties.hashed_token as string,
      }
    }

    export async function ensureTestUser(email: string, password: string) {
      const admin = adminClient()
      const { data: list, error: listErr } = await admin.auth.admin.listUsers()
      if (listErr) throw listErr
      const existing = list.users.find((u) => u.email === email)
      if (existing) return existing
      const { data, error } = await admin.auth.admin.createUser({
        email,
        password,
        email_confirm: true,
      })
      if (error) throw error
      return data.user
    }
    ```

    3. `packages/e2e-tools/helpers/csp-assertions.ts` anlegen mit EXAKTEM Inhalt:
    ```typescript
    /**
     * CSP Assertion Helpers.
     * collectCspViolations: hängt console-listener an, sammelt CSP-bezogene errors.
     * assertCspHeader: inspiziert Response-Header auf Content-Security-Policy.
     */
    import type { Page, Response } from "@playwright/test"

    export type CspViolation = { text: string; location?: string }

    export function collectCspViolations(page: Page): CspViolation[] {
      const violations: CspViolation[] = []
      page.on("console", (msg) => {
        if (msg.type() !== "error") return
        const text = msg.text()
        if (
          text.includes("Content Security Policy") ||
          text.includes("Content-Security-Policy") ||
          text.includes("Refused to execute inline script") ||
          text.includes("Refused to load") ||
          text.includes("violates the following")
        ) {
          violations.push({ text, location: msg.location().url })
        }
      })
      page.on("pageerror", (err) => {
        if (err.message.toLowerCase().includes("content security policy")) {
          violations.push({ text: err.message })
        }
      })
      return violations
    }

    export function assertCspHeader(
      response: Response | null,
      opts: { mode?: "enforced" | "report-only"; mustContain?: string[] } = {}
    ) {
      if (!response) throw new Error("assertCspHeader: response is null")
      const headers = response.headers()
      const headerKey =
        opts.mode === "report-only"
          ? "content-security-policy-report-only"
          : "content-security-policy"
      const value = headers[headerKey]
      if (!value) {
        throw new Error(
          `Missing ${headerKey} header. Got: ${Object.keys(headers).join(", ")}`
        )
      }
      for (const directive of opts.mustContain ?? []) {
        if (!value.includes(directive)) {
          throw new Error(
            `CSP header missing directive "${directive}". Got: ${value}`
          )
        }
      }
      return value
    }
    ```
  </action>
  <verify>
    <automated>cd packages/e2e-tools &amp;&amp; npx tsc --noEmit fixtures/test-user.ts helpers/supabase-admin.ts helpers/csp-assertions.ts --esModuleInterop --skipLibCheck --moduleResolution node --module commonjs --target es2020 2>&amp;1 | tee /tmp/p13-01-tsc.log; test ! -s /tmp/p13-01-tsc.log</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/e2e-tools/fixtures/test-user.ts &amp;&amp; test -f packages/e2e-tools/helpers/supabase-admin.ts &amp;&amp; test -f packages/e2e-tools/helpers/csp-assertions.ts`
    - `grep -q "export function requireTestUser" packages/e2e-tools/fixtures/test-user.ts`
    - `grep -q "export async function generateMagicLink" packages/e2e-tools/helpers/supabase-admin.ts`
    - `grep -q "export async function generateRecoveryLink" packages/e2e-tools/helpers/supabase-admin.ts`
    - `grep -q "export async function ensureTestUser" packages/e2e-tools/helpers/supabase-admin.ts`
    - `grep -q "export function collectCspViolations" packages/e2e-tools/helpers/csp-assertions.ts`
    - `grep -q "export function assertCspHeader" packages/e2e-tools/helpers/csp-assertions.ts`
    - `cd packages/e2e-tools &amp;&amp; pnpm exec tsc --noEmit` → 0 errors (nach Dep-Install)
  </acceptance_criteria>
  <done>Alle drei Helper-Dateien existieren, TypeScript-kompilieren clean, Exports stimmen mit must_haves interfaces überein.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: auth.spec.ts — Skeleton mit 6 describe-Blöcken (Tests stubs/skip)</name>
  <files>packages/e2e-tools/tests/auth.spec.ts</files>
  <read_first>
    - packages/e2e-tools/tests/auth.spec.ts (aktueller 45-Zeiler)
    - .planning/phases/13-auth-flow-audit-csp-reaktivierung/13-CONTEXT.md (D-01: 6 Pfade exakt)
    - .planning/phases/13-auth-flow-audit-csp-reaktivierung/13-RESEARCH.md (Validation Architecture Tabelle)
  </read_first>
  <action>
    Existing Datei KOMPLETT ersetzen. Struktur: 6 `test.describe`-Blöcke, jeweils mit test-stubs. Tests die Wave-1 echt ausfüllen wird sind `test.skip(...)`; triviale Sanity-Tests (wie `/login` lädt) bleiben `test(...)`. EXAKTER Inhalt:

    ```typescript
    import { test, expect } from "@playwright/test"
    import { getTestUser, requireTestUser } from "../fixtures/test-user"
    import { collectCspViolations, assertCspHeader } from "../helpers/csp-assertions"

    const TOOLS_URL = process.env.BASE_URL || "https://tools.generation-ai.org"
    const WEBSITE_URL = process.env.WEBSITE_URL || "https://generation-ai.org"

    // ─── Pfad 1: Login via Email+Passwort ────────────────────────────────
    test.describe("Auth Path 1: Password Login", () => {
      test("login page renders form", async ({ page }) => {
        await page.goto(`${TOOLS_URL}/login`)
        await expect(page.getByLabel(/email/i)).toBeVisible({ timeout: 10_000 })
        await expect(page.getByLabel(/passwort|password/i)).toBeVisible()
      })

      test.skip("TODO-wave1: password login sets sb- cookie with domain=.generation-ai.org", async ({ page }) => {
        const user = requireTestUser()
        await page.goto(`${TOOLS_URL}/login`)
        await page.getByLabel(/email/i).fill(user.email)
        await page.getByLabel(/passwort|password/i).fill(user.password)
        await page.getByRole("button", { name: /anmelden|login/i }).click()
        await page.waitForURL((url) => !url.pathname.includes("/login"), { timeout: 10_000 })
        const cookies = await page.context().cookies(TOOLS_URL)
        const session = cookies.find((c) => c.name.startsWith("sb-"))
        expect(session).toBeTruthy()
        expect(session?.domain).toBe(".generation-ai.org")
      })

      test.skip("TODO-wave1: session persists across reload", async ({ page }) => {
        // Plan 02 implementiert
      })
    })

    // ─── Pfad 2: Magic Link ──────────────────────────────────────────────
    test.describe("Auth Path 2: Magic Link", () => {
      test.skip("TODO-wave1: admin-generated magic link signs user in", async () => {
        // Plan 02 implementiert: generateMagicLink → page.goto(actionLink) → verify Session
      })
    })

    // ─── Pfad 3: Session-Refresh ─────────────────────────────────────────
    test.describe("Auth Path 3: Session Refresh", () => {
      test.skip("TODO-wave1/manual: token rotation via proxy.ts", async () => {
        // Laut Research Open Question 1: evtl. manual-only. Plan 02 entscheidet.
      })
    })

    // ─── Pfad 4: Signout ─────────────────────────────────────────────────
    test.describe("Auth Path 4: Signout (POST-only regression)", () => {
      test("GET /auth/signout returns 405 (regression test für Session-Drop-Bug f5f9cb7)", async ({ request }) => {
        const response = await request.get(`${TOOLS_URL}/auth/signout`, { maxRedirects: 0 })
        expect(response.status()).toBe(405)
      })

      test.skip("TODO-wave1: POST /auth/signout clears sb- cookies", async () => {
        // Plan 02 implementiert
      })
    })

    // ─── Pfad 5: Password-Reset ──────────────────────────────────────────
    test.describe("Auth Path 5: Password Reset End-to-End", () => {
      test.skip("TODO-wave1: recovery link → /auth/set-password → re-login works", async () => {
        // Plan 02 implementiert: generateRecoveryLink → navigate → updateUser → re-login
      })
    })

    // ─── Pfad 6: Cross-Domain Session ────────────────────────────────────
    test.describe("Auth Path 6: Cross-Domain Cookie", () => {
      test.skip("TODO-wave1: login on tools-app, cookie valid on website", async () => {
        // Plan 02 implementiert
      })
    })

    // ─── CSP Smoke (Baseline — Plan 04/05 erweitert) ────────────────────
    test.describe("CSP Baseline", () => {
      test("no CSP-violations on /login load (may not have CSP yet → still passes)", async ({ page }) => {
        const violations = collectCspViolations(page)
        await page.goto(`${TOOLS_URL}/login`)
        await page.waitForLoadState("networkidle")
        // Vor Plan 05: kein CSP → keine Violations. Nach Plan 05: enforced CSP → immer noch keine.
        expect(violations).toHaveLength(0)
      })

      test.skip("TODO-wave2: tools-app /login serves enforced Content-Security-Policy header", async ({ page }) => {
        const response = await page.goto(`${TOOLS_URL}/login`)
        const csp = assertCspHeader(response, {
          mode: "enforced",
          mustContain: ["default-src 'self'", "frame-ancestors 'none'", "'nonce-"],
        })
        expect(csp).not.toContain("'unsafe-inline'")
      })
    })

    // Sanity holdover aus pre-Phase-13 — bleiben
    test.describe("General", () => {
      test("homepage loads", async ({ page }) => {
        await page.goto(TOOLS_URL)
        await expect(page.locator("body")).toBeVisible()
      })
    })
    ```

    Wichtig: **Keine** `.skip` auf ganze `describe`-Blöcke — nur auf einzelne Tests. Der CI-Run dieses Wave-0-Artefakts muss grün sein (die nicht-skip Tests sind alle trivial erreichbar: /login render, GET signout → 405, CSP-Console-Check).
  </action>
  <verify>
    <automated>cd packages/e2e-tools &amp;&amp; BASE_URL=https://tools.generation-ai.org pnpm exec playwright test --list auth.spec.ts 2>&amp;1 | grep -E "(Path [1-6]|CSP Baseline|General)" | wc -l | awk '{exit ($1 &lt; 8)}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test.describe" packages/e2e-tools/tests/auth.spec.ts` → mindestens 8 (6 Pfade + CSP Baseline + General)
    - `grep -c "Path 1:" packages/e2e-tools/tests/auth.spec.ts` → 1
    - `grep -c "Path 6:" packages/e2e-tools/tests/auth.spec.ts` → 1
    - `grep -q "GET /auth/signout returns 405" packages/e2e-tools/tests/auth.spec.ts` → match (Regression-Anker)
    - `grep -q "f5f9cb7" packages/e2e-tools/tests/auth.spec.ts` → match (Commit-Referenz zur Herkunft des Regression-Tests)
    - `grep -q "import.*getTestUser.*from.*fixtures/test-user" packages/e2e-tools/tests/auth.spec.ts` → match
    - `grep -q "import.*collectCspViolations.*from.*helpers/csp-assertions" packages/e2e-tools/tests/auth.spec.ts` → match
    - `cd packages/e2e-tools &amp;&amp; pnpm exec playwright test --list auth.spec.ts` zeigt alle Tests auf (≥8 describe-Blöcke, inkl. skip'ed)
  </acceptance_criteria>
  <done>auth.spec.ts enthält Skeleton für alle 6 Pfade + CSP-Baseline + Signout-Regression. `playwright test --list` listet alle Tests. Wave 1 Plan 02 kann die `.skip`-Marker entfernen und Test-Bodies ausfüllen ohne Struktur-Änderung.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env.test.local → node process | Test-Credentials (User-Email+PW, Service Role Key) werden in Node-Prozess geladen. Nie an Browser leiten. |
| node test-runner → Supabase Admin API | Service Role Key wird ausschließlich server-seitig in `helpers/supabase-admin.ts` verwendet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | `.env.test.local` committed zu git | mitigate | `.gitignore` listet `.env.test.local`; `.env.test.local.example` hat nur leere Slots |
| T-13-02 | Elevation of Privilege | SERVICE_ROLE_KEY leakt in Browser-Bundle | mitigate | Helpers liegen in `packages/e2e-tools/helpers/` (Node-only, nie von apps/ importiert); Playwright tests laufen in Node-Kontext, Admin-Calls in `test()` body nutzen Helper nicht in `page.evaluate()` |
| T-13-03 | Spoofing | Test-User-Credentials werden auf Production Supabase verwendet → echter User landet in prod auth.users Tabelle | accept | User wird manuell von Luca angelegt und ist als Test-User erkennbar (z.B. test+e2e@generation-ai.org); keine Billing/PII |
| T-13-04 | Tampering | Ein geleakter Service-Role-Key ermöglicht Full-DB-Access | mitigate | Key ausschließlich in `.env.test.local` (git-ignored); nie in CI bis separat geplant (CI-Run via GitHub Secret ist Backlog nicht Wave-0-Scope) |
</threat_model>

<verification>
- `pnpm install` läuft clean (neue deps aufgenommen)
- `cd packages/e2e-tools && pnpm exec tsc --noEmit` → 0 errors
- `cd packages/e2e-tools && pnpm exec playwright test --list auth.spec.ts` listet ≥8 tests
- `.env.test.local` ist git-ignored (`git check-ignore packages/e2e-tools/.env.test.local` → Exit 0)
- Regression-Test für Signout-POST-Only ist unskipped und aktiv
</verification>

<success_criteria>
- Alle Artefakte (fixtures, helpers, .env template, auth.spec.ts skeleton) existieren und kompilieren
- Wave 1 Plan 02 kann `requireTestUser()` / `generateMagicLink()` / `generateRecoveryLink()` / `collectCspViolations()` / `assertCspHeader()` direkt importieren
- Signout-POST-only Regression-Test läuft grün gegen Production
- Luca weiss was er in `.env.test.local` eintragen muss (Template dokumentiert alles)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/13-auth-flow-audit-csp-reaktivierung/13-01-SUMMARY.md` erstellen mit:
- Liste der erstellten Artefakte + exports
- Anleitung für Luca: "Jetzt Test-User in Supabase Dashboard anlegen, Credentials in packages/e2e-tools/.env.test.local eintragen" (vor Wave 1)
- Signal zur Orchestrator: Wave 0 COMPLETE, Wave 1 kann starten SOBALD .env.test.local befüllt ist
</output>
