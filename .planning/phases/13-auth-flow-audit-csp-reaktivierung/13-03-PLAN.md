---
phase: 13
plan: 03
type: execute
wave: 1
depends_on: [13-01]
files_modified:
  - docs/AUTH-FLOW.md
autonomous: true
requirements:
  - CONSOL-01
must_haves:
  truths:
    - "Keine direkten @supabase/ssr Imports in apps/ (nur über @genai/auth)"
    - "Keine manuellen document.cookie Schreibzugriffe in apps/"
    - "Keine btoa/saveSessionToCookie Hacks in apps/"
    - "apps/tools-app/lib/auth.ts ist nachweislich ein dünner Shim über @genai/auth"
    - "apps/website/lib/supabase/{client,server}.ts re-exportieren ausschließlich @genai/auth"
    - "Konsolidierungs-Status in docs/AUTH-FLOW.md als 'verified clean, no drift' dokumentiert"
  artifacts:
    - path: docs/AUTH-FLOW.md
      provides: "Consolidation-Check-Ergebnis als Abschnitt"
      contains: "## Consolidation Audit"
  key_links:
    - from: docs/AUTH-FLOW.md
      to: packages/auth/src/index.ts
      via: "Liste aller @genai/auth exports als canonical reference"
      pattern: "@genai/auth"
---

<objective>
Wave 1 (parallel zu Plan 02): Konsolidierungs-Check per grep — verifizieren dass Phase-12-Rewrite kein Drift hinterlassen hat. Kein Refactoring in dieser Phase (D-14) — nur Read-Only-Audit + Status-Dokumentation.

Purpose: Beweisen dass `@genai/auth` canonical ist und keine parallele Auth-Logik mehr in apps/ existiert. Research hat das bereits per Code-Lesen verifiziert (RESEARCH.md "Konsolidierungs-Audit Ergebnis") — dieser Plan verankert die Findings automatisiert per grep und dokumentiert in AUTH-FLOW.md.
Output: Consolidation-Audit-Abschnitt in AUTH-FLOW.md mit grep-Belegen + (falls Drift gefunden) BACKLOG-Item per D-14.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-auth-flow-audit-csp-reaktivierung/13-CONTEXT.md
@.planning/phases/13-auth-flow-audit-csp-reaktivierung/13-RESEARCH.md
@.planning/codebase/CONCERNS.md
@packages/auth/src/index.ts
@apps/tools-app/lib/auth.ts
@apps/website/lib/supabase/client.ts
@apps/website/lib/supabase/server.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Grep-basierter Drift-Scan + Dokumentation in AUTH-FLOW.md</name>
  <files>docs/AUTH-FLOW.md</files>
  <read_first>
    - packages/auth/src/index.ts (canonical exports)
    - apps/tools-app/lib/auth.ts (Research sagt: Shim, 7 Zeilen)
    - apps/website/lib/supabase/client.ts (Re-export von @genai/auth)
    - apps/website/lib/supabase/server.ts (Re-export createAdminClient — naming quirk aber kein Drift)
    - .planning/phases/13-auth-flow-audit-csp-reaktivierung/13-RESEARCH.md (Grep-Kommandos für den Planer zur Verifikation)
  </read_first>
  <action>
    Führe EXAKT diese drei greps aus und capture Outputs. Alle 3 sollen 0 Treffer liefern (außer `packages/auth/` selbst):

    ```bash
    # 1. Direkte @supabase/ssr Imports außerhalb von packages/auth
    echo "=== @supabase/ssr imports in apps/ ==="
    grep -rn "from ['\"]@supabase/ssr['\"]" apps/ --include="*.ts" --include="*.tsx" || echo "NONE"

    # 2. Manuelle document.cookie Schreibzugriffe
    echo "=== document.cookie writes in apps/ ==="
    grep -rn "document\.cookie\s*=" apps/ --include="*.ts" --include="*.tsx" || echo "NONE"

    # 3. btoa/saveSessionToCookie Hacks (Phase-12-Ursprungsfehler)
    echo "=== btoa/saveSessionToCookie in apps/ ==="
    grep -rn "btoa\|saveSessionToCookie" apps/ --include="*.ts" --include="*.tsx" || echo "NONE"
    ```

    Zusätzliche Verifikation:
    ```bash
    # 4. Line-Counts der Legacy-Dateien — Research sagt tools-app/lib/auth.ts = ~7 Zeilen (Shim)
    wc -l apps/tools-app/lib/auth.ts apps/website/lib/supabase/client.ts apps/website/lib/supabase/server.ts

    # 5. Content-Snapshots (zeigt dass es dünne Shims sind)
    cat apps/tools-app/lib/auth.ts
    cat apps/website/lib/supabase/client.ts
    cat apps/website/lib/supabase/server.ts
    ```

    Ergebnis in `docs/AUTH-FLOW.md` unter neuer Sektion `## Consolidation Audit` anhängen (Datei wurde von Plan 02 angelegt — hier anhängen, nicht überschreiben):

    ```markdown
    ## Consolidation Audit (Plan 13-03)

    Status: **verified clean — no drift from @genai/auth canonical**

    Date: {YYYY-MM-DD}

    ### Grep Evidence

    | Check | Command | Result |
    |-------|---------|--------|
    | Direct @supabase/ssr imports in apps/ | `grep -rn "from '@supabase/ssr'" apps/ --include="*.ts" --include="*.tsx"` | {0 matches / N matches (list)} |
    | Manual document.cookie writes in apps/ | `grep -rn "document\.cookie\s*=" apps/ --include="*.ts" --include="*.tsx"` | {0 matches / N matches} |
    | btoa / saveSessionToCookie hacks | `grep -rn "btoa\|saveSessionToCookie" apps/ --include="*.ts" --include="*.tsx"` | {0 matches / N matches} |

    ### Legacy Shim Files (verified thin)

    | File | Lines | Status | Verified Content |
    |------|-------|--------|------------------|
    | apps/tools-app/lib/auth.ts | {N} | shim over @genai/auth | re-exports {...} |
    | apps/website/lib/supabase/client.ts | {N} | re-export only | `createBrowserClient` from @genai/auth |
    | apps/website/lib/supabase/server.ts | {N} | re-export only | `createAdminClient` from @genai/auth (naming quirk noted) |

    ### Naming Quirk (non-blocker per D-14)

    `apps/website/lib/supabase/server.ts` re-exports `createAdminClient` (nicht `createServerClient`) — missverständliches Naming, aber kein Drift. Backlog-Kandidat für Rename (optional).

    ### Decision

    Per D-13/D-14: Konsolidierung ist vollständig. Kein Non-Trivial-Refactor in Phase 13. Keine Fixes nötig.
    ```

    Falls grep 1-3 UNEXPECTED matches liefert (Drift gefunden):
    - Liste sie auf in der Tabelle
    - Pro Match: Zeile + Datei + bewertung (klein / groß)
    - Falls klein (< 10 Zeilen Fix): als neuer Finding zu Plan 02's AUTH-FLOW.md Findings-Tabelle ergänzen + an Plan 02 Task 4/5 zurückgeben
    - Falls groß: BACKLOG.md Eintrag unter "🔐 Auth — Konsolidierung" erstellen mit Link auf AUTH-FLOW.md Zeile

    Falls grep 4 (Line-Counts) überraschend hohe Zahlen liefert (> 50 Zeilen in einer der Legacy-Dateien → möglicher Drift):
    - In Consolidation Audit Sektion als "🟡 Warning: {file} has N lines, expected thin shim" markieren
    - Backlog-Item erstellen (nicht inline fixen per D-14)
  </action>
  <verify>
    <automated>grep -q "## Consolidation Audit" docs/AUTH-FLOW.md &amp;&amp; grep -q "verified clean\|no drift" docs/AUTH-FLOW.md &amp;&amp; echo "=== Drift re-check ===" &amp;&amp; (grep -rn "from ['\"]@supabase/ssr['\"]" apps/ --include="*.ts" --include="*.tsx" || echo "0 matches OK") &amp;&amp; (grep -rn "document\.cookie\s*=" apps/ --include="*.ts" --include="*.tsx" || echo "0 matches OK")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "## Consolidation Audit" docs/AUTH-FLOW.md` → match
    - `grep -q "Grep Evidence" docs/AUTH-FLOW.md` → match
    - Tabelle enthält alle 3 grep-checks (search strings visible in markdown)
    - `wc -l apps/tools-app/lib/auth.ts` liefert ≤ 20 Zeilen (Research: 7) — wenn mehr, Warning eingetragen
    - Kein neuer Code in `apps/` außerhalb docs/* (dieser Plan ist Read-Only per Dokumentation)
    - Falls Drift gefunden: entweder Plan 02 Findings-Update ODER BACKLOG.md-Item existiert
  </acceptance_criteria>
  <done>Consolidation Audit dokumentiert, alle 3 grep-checks ausgeführt und Ergebnisse in AUTH-FLOW.md festgehalten. Status klar (clean vs. drift-gefunden).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Keine neuen Trust-Boundaries — dieser Plan ist Read-Only (grep + Dokumentation).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-10 | Information Disclosure | grep-Output könnte versehentlich Secrets capturen und in AUTH-FLOW.md landen | mitigate | greps targeting auf spezifische imports/patterns, nicht auf env/secret-patterns. Vor commit: `grep -iE "SERVICE_ROLE\|password\|secret" docs/AUTH-FLOW.md` → sollte 0 substantive matches |
</threat_model>

<verification>
- AUTH-FLOW.md hat Sektion "## Consolidation Audit"
- 3 grep-checks dokumentiert mit Ergebnissen
- Falls 0 drift: Status "verified clean"
- Falls drift: Status + Folge-Action (Plan 02 Finding oder Backlog-Item)
</verification>

<success_criteria>
- Nachweis: Phase-12-Konsolidierung ist vollständig (oder Drift ist dokumentiert + getriaged)
- Grep-Kommandos sind reproduzierbar (stehen in AUTH-FLOW.md)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/13-auth-flow-audit-csp-reaktivierung/13-03-SUMMARY.md`:
- Drift-Status (clean / drift-found)
- Liste evtl. Folge-Actions
- Verweis auf AUTH-FLOW.md Consolidation Audit Sektion
</output>
