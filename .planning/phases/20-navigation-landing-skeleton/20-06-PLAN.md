---
phase: 20
plan: 06
type: execute
wave: 4
depends_on: ["20-03", "20-04", "20-05"]
files_modified:
  - .changeset/phase-20-navigation-landing-skeleton.md
  - .planning/phases/20-navigation-landing-skeleton/20-VALIDATION.md
autonomous: false
requirements: [R1.1, R1.2, R1.3, R1.4, R1.5, R1.6, R1.7, R1.8, R1.9, R1.10]
must_haves:
  truths:
    - "pnpm build green and Build-Output for / route is ƒ (force-dynamic), not ○"
    - "All landing routes that the matcher in proxy.ts serves are ƒ (specifically: /, /impressum, /datenschutz)"
    - "Local prod server (NODE_ENV=production pnpm start) returns CSP header containing 'nonce-' on GET /"
    - "Playwright landing.spec.ts: ALL tests green (R1.1 dropdown click, R1.1 dropdown keyboard, R1.1 mobile-nav, R1.2 hero CTA, R1.5 tool-showcase badge, R1.6 community-preview badges, R1.8 reduced-motion marquee, CSP no-error)"
    - "Lighthouse-CI run against local prod: Performance ≥ 0.9, Accessibility ≥ 0.9, Best-Practices ≥ 0.9, SEO ≥ 0.9, CLS ≤ 0.1"
    - "Manual UAT (human-verify) green: all 8 sections render in correct order both Dark + Light, both Desktop + Mobile, Wow-Peaks 1/2/3 subjectively work"
    - "Changeset .changeset/phase-20-navigation-landing-skeleton.md exists declaring @genai/website as minor (target version v4.5.0 per D-31)"
    - "VALIDATION.md per-task map fully populated and nyquist_compliant: true set"
  artifacts:
    - path: ".changeset/phase-20-navigation-landing-skeleton.md"
      provides: "Changeset entry for v4.5.0 minor release"
      contains: "@genai/website"
    - path: ".planning/phases/20-navigation-landing-skeleton/20-VALIDATION.md"
      provides: "Updated validation contract with per-task status + nyquist_compliant flag"
      contains: "nyquist_compliant: true"
  key_links:
    - from: ".changeset/phase-20-navigation-landing-skeleton.md"
      to: "@genai/website package version bump"
      via: "Changesets format"
      pattern: '"@genai/website": minor'
---

<objective>
Wave-4 Phase-Gate: Vollständige Verifikation aller R1.x-Akzeptanzkriterien aus Plans 01-05. Hard-Gates: Build mit `ƒ /`, lokaler Prod-CSP-Smoke clean, alle Playwright-landing-Tests grün, Lighthouse > 90 in 4 Kategorien + CLS ≤ 0.1, Manual-UAT durch Lucas (Wow-Wahrnehmung + Brand-Stimmigkeit + Stub-Erkennbarkeit).

Purpose: Letzte Verteidigungslinie vor Phase-Verifier. Wenn dieser Plan grün ist, ist Phase 20 ready für `/gsd-verify-work` und Release-Promotion. Manueller Checkpoint ist hier Pflicht weil "Wow-Wahrnehmung" und "Microcopy-Tone" laut VALIDATION.md "Manual-Only Verifications" nicht automatisierbar sind.

Output: Verified Build, Changeset für v4.5.0, gepflegte VALIDATION.md mit nyquist_compliant=true, Manual-UAT-Sign-off von Lucas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/20-navigation-landing-skeleton/20-CONTEXT.md
@.planning/phases/20-navigation-landing-skeleton/20-VALIDATION.md
@.planning/phases/20-navigation-landing-skeleton/20-01-PLAN.md
@.planning/phases/20-navigation-landing-skeleton/20-02-PLAN.md
@.planning/phases/20-navigation-landing-skeleton/20-03-PLAN.md
@.planning/phases/20-navigation-landing-skeleton/20-04-PLAN.md
@.planning/phases/20-navigation-landing-skeleton/20-05-PLAN.md
@LEARNINGS.md
@CLAUDE.md
@lighthouserc.json
@packages/e2e-tools/tests/landing.spec.ts
@.changeset/config.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Automated Phase-Gate — Build, CSP, Playwright Full, Lighthouse Run</name>
  <files></files>
  <read_first>
    - LEARNINGS.md (CSP-Incident — was bei `○ /` schief geht, lokaler Prod-Smoke-Pflicht)
    - .planning/phases/20-navigation-landing-skeleton/20-RESEARCH.md § Validation Architecture + § Testing Strategy (welche Commands wie laufen)
    - lighthouserc.json (Schwellwerte: 0.9 + CLS 0.1)
    - packages/e2e-tools/tests/landing.spec.ts (Test-Inventar — alle Tests die jetzt grün sein müssen)
    - .planning/phases/20-navigation-landing-skeleton/20-VALIDATION.md (Per-Task Map — auszufüllen)
  </read_first>
  <action>
    **Schritt 1: Build-Gate**
    ```bash
    cd /Users/lucaschweigmann/projects/generation-ai
    pnpm --filter @genai/website build 2>&1 | tee /tmp/phase-20-build.log
    ```
    - Build muss exit 0
    - Output muss `ƒ /` zeigen (parsen via `grep -E "ƒ\\s+/\\s" /tmp/phase-20-build.log` exit 0)
    - KEIN `○ /` (parsen via `! grep -E "○\\s+/\\s" /tmp/phase-20-build.log` exit 0)
    - Auch `/impressum` und `/datenschutz` müssen `ƒ` sein (sind Server-Components mit force-dynamic via Root-Layout — sicherheitshalber prüfen)

    Falls Build failed oder Routes static sind → STOPP, LEARNINGS.md re-readen, an Lucas eskalieren.

    **Schritt 2: Lokaler Prod-CSP-Smoke**
    ```bash
    cd apps/website && NODE_ENV=production pnpm start &
    SERVER_PID=$!
    sleep 4
    # CSP-Header muss nonce- enthalten
    CSP=$(curl -sI http://localhost:3000 | grep -i "content-security-policy")
    echo "$CSP"
    echo "$CSP" | grep -q "nonce-" || { echo "FAIL: CSP-Header fehlt nonce"; kill $SERVER_PID; exit 1; }
    # Playwright CSP-Test
    cd ../../packages/e2e-tools && E2E_BASE_URL=http://localhost:3000 pnpm exec playwright test landing.spec.ts -g "CSP" --reporter=line || { kill $SERVER_PID; exit 1; }
    ```

    **Schritt 3: Playwright Full Suite gegen Local-Prod (alle landing-Tests müssen grün sein)**
    ```bash
    # Server läuft noch
    cd packages/e2e-tools && E2E_BASE_URL=http://localhost:3000 pnpm exec playwright test landing.spec.ts --reporter=line
    ```
    Erwartet: ALLE Tests grün. Bei Fail → STOPP, an entsprechenden Section-Plan zurückeskalieren (Plan 02 für R1.1, Plan 03 für R1.2, Plan 04 für R1.5/R1.6, Plan 05 für R1.8).

    **Schritt 4: Lighthouse-CI Run**
    ```bash
    # Server läuft noch
    cd /Users/lucaschweigmann/projects/generation-ai
    pnpm dlx @lhci/cli@latest autorun --config=./lighthouserc.json 2>&1 | tee /tmp/phase-20-lhci.log
    LHCI_EXIT=$?
    # Background-Server killen
    kill $SERVER_PID 2>/dev/null || true
    wait 2>/dev/null || true
    test $LHCI_EXIT -eq 0 || { echo "FAIL: Lighthouse-CI assertions"; cat /tmp/phase-20-lhci.log | tail -30; exit 1; }
    ```
    Erwartet: Lighthouse-CI exit 0 (alle 4 Kategorien ≥ 0.9, CLS ≤ 0.1). Falls fail → cat /tmp/phase-20-lhci.log analysieren, Bottleneck identifizieren (Bundle-Size? CLS? A11y-Issue?), an entsprechenden Section-Plan eskalieren.

    **Schritt 5: VALIDATION.md aktualisieren**
    Per-Task Verification Map vollständig ausfüllen für alle Tasks aus Plans 01-05 (insgesamt ~15 Tasks). Jede Zeile: Task-ID, Plan, Wave, Requirement, Test-Type, Automated Command, File-Exists ✅, Status (✅ green wenn alles oben grün ist).

    Frontmatter-Update:
    ```yaml
    nyquist_compliant: true
    wave_0_complete: true
    status: ready_for_verify
    ```

    Validation-Sign-Off Section am Ende: alle Checkboxes auf [x].
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && pnpm --filter @genai/website build 2>&1 > /tmp/phase-20-final-build.log && grep -qE "ƒ\\s+/\\s" /tmp/phase-20-final-build.log && ! grep -qE "○\\s+/\\s" /tmp/phase-20-final-build.log && grep -q "nyquist_compliant: true" .planning/phases/20-navigation-landing-skeleton/20-VALIDATION.md && echo "TASK 1 PASS (build + validation)"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @genai/website build` exit 0
    - Build-Log enthält Zeile mit `ƒ /` (Homepage force-dynamic)
    - Build-Log enthält KEINE Zeile mit `○ /` (CSP-Incident-2.0 prevention)
    - Lokaler Prod-Server liefert CSP-Header mit `nonce-`
    - Alle landing.spec.ts Tests grün (Playwright exit 0): R1.1 (3 Tests), R1.2, R1.5, R1.6, R1.8, CSP — insgesamt 8 Tests
    - Lighthouse-CI exit 0 (alle 4 Kategorien ≥ 0.9 + CLS ≤ 0.1)
    - 20-VALIDATION.md frontmatter `nyquist_compliant: true` gesetzt
    - 20-VALIDATION.md Per-Task Map enthält ≥10 Zeilen (für alle Tasks aus Plans 01-05)
  </acceptance_criteria>
  <done>
    Alle automatisierten Gates grün: Build mit `ƒ /`, lokaler Prod-CSP, Playwright Full, Lighthouse > 90 + CLS ≤ 0.1. VALIDATION.md vollständig dokumentiert. Bereit für Manual-UAT.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual UAT — Wow-Peaks + Brand-Stimmigkeit + Stub-Erkennbarkeit + Mobile/Dark/Light</name>
  <files></files>
  <what-built>
    Vollständige Phase 20 Landing: neuer Header (Desktop-Nav + Dropdown + Mobile-Sheet), 8 Sections in finaler Reihenfolge (Hero → Discrepancy → Offering → Tool-Showcase → Community-Preview → Audience-Split → Trust → Final-CTA), erweiterter Footer. Lokaler Prod-Server läuft auf http://localhost:3000.

    Plan 06 Task 1 hat bereits verifiziert:
    - Build grün mit `ƒ /`
    - Lighthouse > 90 in allen 4 Kategorien + CLS ≤ 0.1
    - Alle Playwright-Tests grün
    - CSP clean
  </what-built>
  <how-to-verify>
    Lokalen Prod-Server starten falls nicht mehr läuft:
    ```bash
    cd /Users/lucaschweigmann/projects/generation-ai
    pnpm --filter @genai/website build
    cd apps/website && NODE_ENV=production pnpm start
    ```
    Browser → http://localhost:3000

    **Checkliste — Desktop Dark Mode (Default):**
    1. Terminal-Splash erscheint beim ersten Visit, läuft durch
    2. Header sichtbar mit: Logo links, Nav (Tools, Community, Für Partner ▾, Über uns) mittig, Theme-Toggle + "Jetzt beitreten" rechts
    3. Click auf "Für Partner" öffnet Dropdown mit 3 Items (Unternehmen, Stiftungen, Hochschulen). Tab-Key navigiert durch Dropdown. Escape schließt.
    4. Scroll: Hero mit Aurora-Background, Claim-Placeholder, CTA → /join sichtbar
    5. Discrepancy-Section: 6 Number-Tickers laufen, Spalten driften beim Scroll leicht auseinander, Closer-Zeile "Generation AI schließt diese Lücke." erscheint
    6. **Wow-Peak-Check: Discrepancy fühlt sich wie ein Höhepunkt an** (subjektiv)
    7. Offering: 4 Cards mit Icons, Hover-Glow funktioniert
    8. Tool-Showcase: Marquee scrollt durch 5 Stub-Tools, "Beispiel"-Badge sichtbar im Header der Section
    9. Community-Preview: 2 Spalten mit 3 Artikeln + 2 Events, jeweils mit "Beispiel"-Badge pro Karte
    10. Audience-Split: Großer Studi-Block oben, kleinerer B2B-Strip unten
    11. Trust: Marquee mit 6 Sparringspartner-Tiles, "N=109 · März 2026" als Caption
    12. Final-CTA: Lamp-Effect-Hintergrund, Claim-Placeholder, "Jetzt beitreten" + "Erst mal umschauen → tools.generation-ai.org"
    13. **Wow-Peak-Check: Final-CTA fühlt sich wie Höhepunkt an**
    14. Footer: 4 Spalten (Logo, Sitemap, Legal, Kontakt+LinkedIn), Copyright-Zeile mit "Generation AI e.V."

    **Checkliste — Light Mode (Theme-Toggle drücken):**
    15. Theme wechselt sauber zu Light (Pink/Red CI)
    16. Aurora wechselt auf Light-Variante (Pink/Red Töne statt Blue/Neon)
    17. Beispiel-Badges wechseln auf Red-Variante
    18. Kein Brand-Token-Fail (kein Cyan/Purple unerwartet sichtbar)

    **Checkliste — Mobile (DevTools Responsive Mode, 375×812):**
    19. Header: Logo + Theme-Toggle + Hamburger-Icon (kein Desktop-Nav sichtbar)
    20. Hamburger drücken → Sheet-Overlay öffnet von rechts mit allen Nav-Items + Partner-Accordion
    21. "Jetzt beitreten" als Full-Width-Button im Mobile-Menu
    22. Sections stacken sauber, kein horizontales Scrolling
    23. Discrepancy: Spalten stacken (1-Spalte statt 2)
    24. Footer: 1-Spalte stacked

    **Checkliste — Reduced-Motion (DevTools → Rendering → Emulate prefers-reduced-motion: reduce):**
    25. Aurora-Animation pausiert
    26. Marquee in Tool-Showcase pausiert
    27. Marquee in Trust pausiert
    28. Discrepancy-Spalten driften nicht beim Scroll (statisch)
    29. Number-Tickers zeigen End-Werte sofort

    **Checkliste — Microcopy & Brand:**
    30. Microcopy klingt warm + direkt (kein Corporate-Sprech), wirkt zu brand/VOICE.md passend
    31. Stub-Marker eindeutig — User versteht sofort dass Tool-Showcase + Community-Preview Beispiel-Inhalt sind
    32. Keine Umlaute-Fehler (überall "ö/ä/ü/ß" statt "oe/ae/ue/ss") in Sichtprüfung

    **Bei jedem Fail:** notieren, welcher Plan zurückeskaliert werden muss (z.B. "Hero-Wow zu schwach → Plan 03 Task 1 reopen" oder "Mobile-Nav A11y broken → Plan 02 Task 1 reopen").

    Background-Server killen wenn fertig.
  </how-to-verify>
  <resume-signal>
    Type "approved" wenn alles passt — oder beschreibe Issues mit Verweis auf entsprechenden Plan/Task. Bei "approved" geht es weiter zu Task 3 (Changeset).
  </resume-signal>
  <action>
    Pause für Manual UAT durch Lucas. Inhalt + Schritte siehe <how-to-verify> oben (Desktop Dark, Light Mode, Mobile, Reduced-Motion, Microcopy + Brand). Executor wartet auf resume-signal "approved" oder Fail-Notiz.
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify — manual UAT, no automation possible. See <how-to-verify> for the 32-point checklist."</automated>
  </verify>
  <done>
    Lucas hat "approved" geschrieben (oder alle gemeldeten Fails sind in den jeweils referenzierten Plans/Tasks adressiert worden und re-verified). Kein automatisches Done-Kriterium — gate=blocking, Plan kann nicht ohne Approval weiter zu Task 3.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create changeset for v4.5.0 minor release + final VALIDATION.md sign-off</name>
  <files>
    .changeset/phase-20-navigation-landing-skeleton.md,
    .planning/phases/20-navigation-landing-skeleton/20-VALIDATION.md
  </files>
  <read_first>
    - CLAUDE.md § Changelog & Versioning (Changeset-Pflicht-Pattern)
    - .changeset/config.json (linked: [website, tools-app] — minor auf website bumpt tools-app auch)
    - .planning/phases/20-navigation-landing-skeleton/20-CONTEXT.md (D-31: minor v5.0.0-alpha ODER v4.5.0)
    - ROADMAP.md § Phase 20 (Release minor (v5.0.0-alpha oder v4.5.0))
    - .planning/phases/20-navigation-landing-skeleton/20-VALIDATION.md (Sign-Off-Section am Ende)
  </read_first>
  <action>
    **Schritt 1: Changeset anlegen**

    Datei `.changeset/phase-20-navigation-landing-skeleton.md` mit Inhalt:

    ```markdown
    ---
    "@genai/website": minor
    ---

    Phase 20 — Navigation + Landing-Skeleton

    Komplett neue Top-Nav und Landing-Page Architektur als Conversion-Entry-Point für Milestone v4.0.

    **Navigation:**
    - Top-Nav mit Tools (extern), Community (extern), Für Partner ▾ Dropdown, Über uns, Jetzt beitreten
    - Mobile: Hamburger-Icon → Full-Screen Sheet-Overlay mit Accordion-Sub-Items
    - Theme-Toggle bleibt im Header
    - Kein Login-Button mehr im Header

    **Landing — 8 Sections in neuer Reihenfolge:**
    1. Hero — Aurora-Background + Claim-Placeholder + Primary-CTA → /join
    2. Diskrepanz — Custom Bento-Split mit 6 Number-Tickern, scroll-getriebene Divergenz, Closer-Reveal
    3. Angebot — 4-Card Bento-Grid (Community, Wissensplattform, Events, Expert-Formate)
    4. Tool-Showcase — Infinite-Moving-Cards mit 5 Stub-Tools + "Beispiel"-Badge
    5. Community-Preview — 2-Spalten (3 Artikel-Stubs + 2 Event-Stubs) mit "Beispiel"-Badges
    6. Zielgruppen-Split — Studi-Block groß + Primary-CTA, B2B-Streifen dezenter
    7. Trust — MagicUI Marquee mit Sparringspartner-Placeholders + "N=109 · März 2026"
    8. Final-CTA — Aceternity Lamp Effect + Primary-CTA → /join + Sekundär-Link

    **Footer-Erweiterung:**
    - 4-Spalten-Layout (Logo+Tagline | Sitemap | Legal | Kontakt+LinkedIn)
    - Copyright mit Vereinsnennung

    **Stack-Additions:**
    - motion (ehem. Framer Motion) — Animation-Library
    - shadcn/ui Dropdown-Menu + Sheet — A11y-Primitives
    - Aceternity Aurora, Bento-Grid, Infinite-Moving-Cards, Lamp — UI-Components
    - MagicUI Number-Ticker, Marquee — UI-Components

    **Brand- und CSP-Konformität:**
    - Alle externen Komponenten auf brand-{neon|blue|pink|red}-{1-12} Skalen umgefärbt
    - Lighthouse Landing > 90 in allen 4 Kategorien
    - CLS ≤ 0.1 bei scroll-triggered Animationen
    - prefers-reduced-motion respektiert (Aurora pausiert, Marquees pausieren, Discrepancy-Drift neutralisiert)
    - Force-dynamic + Nonce-CSP intakt (Build-Output `ƒ /`)

    **Stub-Strategie:**
    - Tool-Showcase + Community-Preview verwenden realistischen Dummy-Content mit sichtbaren "Beispiel"-Badges, keine Skeleton-Loader
    - Echte API-Daten kommen in Phase 26 (Subdomain-Integration)

    **Out-of-Scope (planmäßig):**
    - Hero-Claim + Final-CTA finales Wording (späterer Marketing-Pass)
    - Echte Tool-Showcase- und Community-Preview-Daten (Phase 26)
    - Sparringspartner-Logos (Asset-Delivery durch Lucas)
    - /about, /partner, /join, /level-test (Phasen 21-24)
    ```

    **Schritt 2: VALIDATION.md final sign-off**

    Im File `.planning/phases/20-navigation-landing-skeleton/20-VALIDATION.md`:

    1. Frontmatter `status: complete` setzen
    2. Im Abschnitt "Validation Sign-Off": **Approval:** `approved (Lucas via Manual UAT)` setzen
    3. Per-Task-Map Status-Spalte für alle Plan-01-bis-05-Tasks auf `✅ green` setzen
    4. Manual-Only-Verifications Tabelle: alle 3 Zeilen mit Status-Note (z.B. "Approved 2026-04-XX")

    **Schritt 3: Git-Commit**

    Atomic commit: changeset + VALIDATION.md
    ```bash
    cd /Users/lucaschweigmann/projects/generation-ai
    git add .changeset/phase-20-navigation-landing-skeleton.md .planning/phases/20-navigation-landing-skeleton/20-VALIDATION.md
    git commit -m "$(cat <<'EOF'
    chore(20): add changeset + validation sign-off

    Phase 20 — Navigation + Landing-Skeleton — verified end-to-end:
    - Build green with ƒ /
    - Lighthouse > 90 (Performance, A11y, Best-Practices, SEO) + CLS ≤ 0.1
    - Playwright landing.spec.ts: all 8 tests green
    - Manual UAT approved by Lucas

    Release: v4.5.0 (minor) — new nav + landing structure
    EOF
    )"
    ```

    Hinweis: Push auf main NICHT autonom — Lucas-Approval einholen vor `git push` (per CLAUDE.md global rules).
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && test -f .changeset/phase-20-navigation-landing-skeleton.md && grep -q '"@genai/website": minor' .changeset/phase-20-navigation-landing-skeleton.md && grep -q "approved" .planning/phases/20-navigation-landing-skeleton/20-VALIDATION.md && grep -q "status: complete" .planning/phases/20-navigation-landing-skeleton/20-VALIDATION.md && git log -1 --pretty=%s | grep -qE "chore\\(20\\)|docs\\(20\\)|feat\\(20\\)" && echo "TASK 3 PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `.changeset/phase-20-navigation-landing-skeleton.md` existiert
    - Changeset enthält `"@genai/website": minor`
    - Changeset Body listet alle 8 Sections + Stack-Additions + Out-of-Scope
    - 20-VALIDATION.md frontmatter `status: complete`
    - 20-VALIDATION.md Sign-Off `approved`
    - Git-Commit mit Phase-20 Scope erstellt (HEAD shows `(20)` in subject)
  </acceptance_criteria>
  <done>
    Phase 20 ist offiziell ready-for-release. Changeset für v4.5.0 angelegt, VALIDATION.md mit Lucas-Approval signiert, Commit erstellt. Phase-Verifier (`/gsd-verify-work`) kann jetzt laufen.
  </done>
</task>

</tasks>

<verification>
**Plan-Abschluss-Verify:**

1. Build grün mit `ƒ /` (Task 1)
2. Lighthouse > 90 alle 4 Kategorien + CLS ≤ 0.1 (Task 1)
3. Alle 8 Playwright-Tests grün (Task 1)
4. CSP clean (Task 1)
5. Manual UAT approved (Task 2)
6. Changeset + Sign-Off committed (Task 3)
7. ROADMAP.md Plans-Sektion: alle 6 PLANs als `[x]` markiert (in Plan 06 SUMMARY festhalten — Update der ROADMAP.md selbst passiert in `/gsd-verify-work`-Schritt)
</verification>

<success_criteria>
- Phase 20 erfüllt alle 10 Akzeptanzkriterien aus REQUIREMENTS.md § R1 + alle 4 Success Criteria aus ROADMAP.md § Phase 20
- Build grün mit `ƒ /`
- Lighthouse > 90 alle 4 Kategorien
- CLS ≤ 0.1
- Playwright landing.spec.ts: 8/8 grün
- CSP clean
- Manual UAT approved
- Changeset für v4.5.0 (oder v5.0.0-alpha falls Lucas das stattdessen wählt)
- VALIDATION.md nyquist_compliant=true + status=complete
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/20-navigation-landing-skeleton/20-06-SUMMARY.md` mit:
- Build-Output `ƒ /` Snippet
- Lighthouse-CI Score-Snippet (Performance/A11y/BP/SEO + CLS)
- Playwright Full-Run Output (8/8 passed)
- Manual UAT Lucas-Notes (was war gut, was musste nachgebessert werden)
- Changeset-File-Path
- Commit-SHA
- Empfehlung für Release: `pnpm version` ausführen wann (jetzt vs. nach `/gsd-verify-work`)
- Backlog-Items die in Phase 20 explicit nicht adressiert wurden (Sparringspartner-Logos, Hero-Claim Final-Wording, Final-CTA Final-Wording — alle laut CONTEXT Deferred)
</output>
