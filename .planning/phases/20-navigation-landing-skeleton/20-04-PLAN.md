---
phase: 20
plan: 04
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - apps/website/components/sections/offering-section.tsx
  - apps/website/components/sections/tool-showcase-section.tsx
  - apps/website/components/sections/community-preview-section.tsx
autonomous: true
requirements: [R1.4, R1.5, R1.6]
must_haves:
  truths:
    - "Offering-Section renders 4 cards (Community, Wissensplattform, Events & Workshops, Expert-Formate) using Bento-Grid pattern, each with icon + title + 1-line description + deep-link"
    - "Offering deep-links: Community → https://community.generation-ai.org, Wissensplattform → https://tools.generation-ai.org, Events → /about (placeholder until /events exists), Expert-Formate → /about"
    - "Tool-Showcase renders 3-5 fake tool entries via Infinite-Moving-Cards with realistic dummy data and a visible 'Beispiel' badge per card"
    - "Tool-Showcase CTA links to https://tools.generation-ai.org"
    - "Tool-Showcase respects prefers-reduced-motion (animation-play-state: paused via globals.css guard)"
    - "Community-Preview renders two columns: 3 articles left + 2 events right with a visible 'Beispiel' badge per item, no animation"
    - "Both Tool-Showcase and Community-Preview use the same 'Beispiel' badge component (consistent style across both)"
    - "Build green with ƒ /, CSP-smoke clean"
  artifacts:
    - path: "apps/website/components/sections/offering-section.tsx"
      provides: "4-card offering grid using BentoGrid pattern"
      contains: "BentoGrid"
    - path: "apps/website/components/sections/tool-showcase-section.tsx"
      provides: "Infinite cards carousel with 3-5 fake tools + Beispiel badge"
      contains: "InfiniteMovingCards"
    - path: "apps/website/components/sections/community-preview-section.tsx"
      provides: "Two-column static preview (articles + events) with Beispiel badges"
      contains: "Beispiel"
  key_links:
    - from: "apps/website/components/sections/offering-section.tsx"
      to: "@/components/ui/bento-grid"
      via: "ES import"
      pattern: "from ['\"]@/components/ui/bento-grid['\"]"
    - from: "apps/website/components/sections/tool-showcase-section.tsx"
      to: "@/components/ui/infinite-moving-cards"
      via: "ES import"
      pattern: "from ['\"]@/components/ui/infinite-moving-cards['\"]"
    - from: "apps/website/components/sections/tool-showcase-section.tsx"
      to: "lucide-react Users + BookOpen + Calendar + Mic2 icons"
      via: "ES import"
      pattern: "from ['\"]lucide-react['\"]"
---

<objective>
Wave-3 ruhige Sections (parallel zu Plan 03 + 05): die drei "ruhigen Mitte"-Sections der Landing — 4-Card-Angebot (R1.4), Tool-Showcase mit Stub-Daten (R1.5), Community-Preview mit Stub-Daten (R1.6). Diese drei sind bewusst NICHT die Wow-Peaks (D-05), sondern bilden die ruhigere Mitte zwischen Hero/Discrepancy und Final-CTA. Schwerpunkt: solide Information + sichtbarer "Beispiel"-Badge pro Stub-Card (D-24/D-25), KEIN Skeleton-Loader.

Purpose: Drei Sections in einem Plan weil sie alle "ruhig" sind, kein Custom-Wow-Code haben, und gemeinsam die ehrliche Stub-Strategie (echt aussehender Dummy-Content + sichtbarer Beispiel-Hinweis) demonstrieren. Plan 04 läuft parallel zu 03 + 05 — keine Cross-Section-Dependencies.

Output: Drei lauffähige Sections die im DOM mit realistischem Stub-Content erscheinen, jeweils mit eindeutiger "Beispiel"-Markierung. CSP-konform, Brand-Token-konform.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/20-navigation-landing-skeleton/20-CONTEXT.md
@.planning/phases/20-navigation-landing-skeleton/20-RESEARCH.md
@.planning/phases/20-navigation-landing-skeleton/20-VALIDATION.md
@.planning/phases/20-navigation-landing-skeleton/20-02-PLAN.md
@LEARNINGS.md
@apps/website/components/sections/offering-section.tsx
@apps/website/components/sections/tool-showcase-section.tsx
@apps/website/components/sections/community-preview-section.tsx
@apps/website/components/ui/bento-grid.tsx
@apps/website/components/ui/infinite-moving-cards.tsx
@apps/website/app/globals.css
@packages/config/tailwind/base.css

<interfaces>
<!-- Aus Plan 01: -->
<!-- @/components/ui/bento-grid — exports: BentoGrid + BentoGridItem (typische Aceternity-Signatur, Props: title, description, header, icon, className) -->
<!-- @/components/ui/infinite-moving-cards — exports: InfiniteMovingCards (props: items: Array<{ name: string; title?: string; quote?: string; … }>, direction?: "left"|"right", speed?: "slow"|"normal"|"fast", pauseOnHover?: boolean) -->

<!-- Beispiel-Badge-Snippet (aus 20-RESEARCH.md § Component Recipes D-11): -->
<!--   <span className="absolute top-2 right-2 text-[11px] font-mono font-bold rounded-full px-2 py-0.5 bg-brand-neon-3 text-brand-neon-12 dark:block hidden"> Beispiel </span> -->
<!--   <span className="absolute top-2 right-2 text-[11px] font-mono font-bold rounded-full px-2 py-0.5 bg-brand-red-3 text-brand-red-12 dark:hidden block"> Beispiel </span> -->
<!-- ABER: bei uns ist DARK default. Theme-Switch passiert via .light auf <html>. Daher: -->
<!--   Der Standard ist Dark (kein .light) → neon-Variante zeigen -->
<!--   Bei .light → red-Variante zeigen -->
<!-- Tailwind dark:-Variant funktioniert in unserem Setup so: tailwind dark-mode wird via .dark class gesteuert standard. WIR steuern via .light. Daher: -->
<!--   nutze NICHT dark:-prefix sondern eigene CSS-Variable bzw. .light-Selector via globals.css ODER eine kleine Wrapper-Komponente die useTheme() liest. -->
<!--   Pragmatisch: BeispielBadge als kleine Client-Komponente: useTheme() → { theme }, render conditional className. -->

<!-- Section-Container Convention (aus Plan 02 stub): jede Section behält ihr <section data-section="…" aria-labelledby="…"> Wrapper -->

<!-- Stub-Daten Tool-Showcase (LOCKED aus 20-RESEARCH.md § Component Recipes D-11): -->
<!-- "ChatPDF Pro" — PDFs analysieren und zusammenfassen -->
<!-- "Notion AI" — KI-gestützte Notizen und Planung -->
<!-- "Perplexity" — KI-Suchmaschine mit Quellen -->
<!-- "ElevenLabs" — Text-zu-Sprache für Präsentationen -->
<!-- "Gamma" — KI-Präsentationserstellung -->

<!-- Stub-Daten Community-Preview (LOCKED aus 20-RESEARCH.md § Component Recipes D-12): -->
<!-- Artikel: -->
<!--   "Wie ich ChatGPT für meine Bachelorarbeit genutzt habe" -->
<!--   "5 KI-Tools die jeder BWL-Student kennen sollte" -->
<!--   "Prompt Engineering für Anfänger: Der komplette Guide" -->
<!-- Events: -->
<!--   "KI-Basics Workshop — 28. April 2026, Online" -->
<!--   "Masterclass: Automatisierung mit Make — 05. Mai 2026" -->

<!-- 4-Card-Angebot LOCKED (aus 20-RESEARCH § Component Recipes D-10): -->
<!-- 1. Community — Icon: Users (lucide), Link: https://community.generation-ai.org -->
<!-- 2. Wissensplattform — Icon: BookOpen, Link: https://tools.generation-ai.org -->
<!-- 3. Events & Workshops — Icon: Calendar, Link: /about#events (placeholder) -->
<!-- 4. Expert-Formate — Icon: Mic2, Link: /about#expert (placeholder) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Offering-Section — 4-Card Bento-Grid (Community, Wissensplattform, Events, Expert-Formate)</name>
  <files>apps/website/components/sections/offering-section.tsx</files>
  <read_first>
    - apps/website/components/sections/offering-section.tsx (Stub aus Plan 02)
    - apps/website/components/ui/bento-grid.tsx (Aceternity Bento-Grid — Export-Namen + Prop-Signatur prüfen, typisch: BentoGrid + BentoGridItem)
    - .planning/phases/20-navigation-landing-skeleton/20-RESEARCH.md § Component Recipes D-10 (Token-Mapping + 4 Cards locked)
    - .planning/phases/20-navigation-landing-skeleton/20-CONTEXT.md (D-10: Hover-Glow auf brand-neon-alpha)
    - packages/config/tailwind/base.css (verfügbare Brand-Klassen)
    - .planning/REQUIREMENTS.md § R1.4 ("4-Card-Angebot mit Icon + Titel + 1-Satz + Deep-Link")
  </read_first>
  <action>
    Datei `apps/website/components/sections/offering-section.tsx` vollständig ersetzen.

    **Wenn der Aceternity-Bento-Grid eine spezifische Prop-API hat (typisch: BentoGrid wrapper + BentoGridItem mit `title`, `description`, `header`, `icon`, `className`):** Nutze diese API direkt. **Falls die Komponente abweicht:** baue ein einfaches `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4` direkt — wir nutzen das Bento-Pattern visuell, brauchen aber nicht zwingend die Aceternity-API wenn sie nicht passt. Dokumentiere im SUMMARY welcher Weg gegangen wurde.

    **Inhalt:**
    ```tsx
    'use client'

    import Link from "next/link"
    import { Users, BookOpen, Calendar, Mic2, ArrowUpRight } from "lucide-react"
    import { BentoGrid, BentoGridItem } from "@/components/ui/bento-grid"

    type OfferingCard = {
      title: string
      description: string
      href: string
      external: boolean
      icon: React.ReactNode
    }

    const cards: OfferingCard[] = [
      {
        title: "Community",
        description: "Lebendiger Austausch, Peer-Learning und Sparring im Circle.",
        href: "https://community.generation-ai.org",
        external: true,
        icon: <Users className="w-6 h-6" aria-hidden="true" />,
      },
      {
        title: "Wissensplattform",
        description: "Kuratierte KI-Tools mit Anleitungen für den Studi-Alltag.",
        href: "https://tools.generation-ai.org",
        external: true,
        icon: <BookOpen className="w-6 h-6" aria-hidden="true" />,
      },
      {
        title: "Events & Workshops",
        description: "Hands-on Sessions zu Prompting, Automatisierung, Tools.",
        href: "/about",
        external: false,
        icon: <Calendar className="w-6 h-6" aria-hidden="true" />,
      },
      {
        title: "Expert-Formate",
        description: "Masterclasses und Q&A mit Praktiker:innen aus der Branche.",
        href: "/about",
        external: false,
        icon: <Mic2 className="w-6 h-6" aria-hidden="true" />,
      },
    ]

    export function OfferingSection() {
      return (
        <section
          aria-labelledby="offering-heading"
          data-section="offering"
          className="bg-bg py-24 sm:py-32 border-b border-border"
        >
          <div className="mx-auto max-w-6xl px-6">
            <div className="text-center mb-16">
              <p className="font-mono text-xs uppercase tracking-[0.2em] text-text-muted mb-3">
                Was wir anbieten
              </p>
              <h2 id="offering-heading" className="text-3xl sm:text-4xl lg:text-5xl font-bold tracking-tight text-text">
                Vier Säulen, ein Ziel.
              </h2>
            </div>

            <BentoGrid className="max-w-5xl mx-auto md:grid-cols-2 lg:grid-cols-4 auto-rows-[14rem]">
              {cards.map((card) => {
                const linkProps = card.external
                  ? { href: card.href, target: "_blank" as const, rel: "noopener noreferrer" }
                  : { href: card.href }
                const Wrapper = card.external ? "a" : Link
                return (
                  <Wrapper
                    key={card.title}
                    {...linkProps}
                    className="group block"
                  >
                    <BentoGridItem
                      title={
                        <span className="inline-flex items-center gap-2">
                          {card.title}
                          <ArrowUpRight className="w-4 h-4 opacity-0 -translate-x-1 group-hover:opacity-100 group-hover:translate-x-0 transition-all" aria-hidden="true" />
                        </span>
                      }
                      description={card.description}
                      icon={
                        <div className="inline-flex items-center justify-center w-10 h-10 rounded-lg bg-[var(--accent-soft)] text-[var(--accent)]">
                          {card.icon}
                        </div>
                      }
                      className="h-full bg-bg-card border border-border hover:border-brand-neon-6 hover:shadow-[0_0_20px_var(--accent-glow)] transition-all duration-300"
                    />
                  </Wrapper>
                )
              })}
            </BentoGrid>
          </div>
        </section>
      )
    }
    ```

    **Falls BentoGrid/BentoGridItem-API anders ist:** Custom-fallback ohne Aceternity-Wrapper:
    ```tsx
    <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 max-w-5xl mx-auto">
      {cards.map(card => (
        <Link/Wrapper className="group block bg-bg-card border border-border hover:border-brand-neon-6 hover:shadow-[0_0_20px_var(--accent-glow)] rounded-2xl p-6 transition-all duration-300">
          <div className="inline-flex items-center justify-center w-10 h-10 rounded-lg bg-[var(--accent-soft)] text-[var(--accent)] mb-4">{card.icon}</div>
          <h3 className="font-mono text-lg font-bold text-text mb-2 inline-flex items-center gap-2">{card.title} <ArrowUpRight className="w-4 h-4 opacity-0 group-hover:opacity-100 transition" /></h3>
          <p className="text-sm text-text-secondary">{card.description}</p>
        </Link/Wrapper>
      ))}
    </div>
    ```

    **Pflicht-Verifications:**
    - `pnpm --filter @genai/website build` grün, `/` ist `ƒ`
    - `pnpm --filter @genai/website lint` grün
    - Lokaler Prod-curl gegen `/` enthält alle 4 Card-Titles + alle 4 Hrefs
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && grep -q "Community" apps/website/components/sections/offering-section.tsx && grep -q "Wissensplattform" apps/website/components/sections/offering-section.tsx && grep -q "Events & Workshops" apps/website/components/sections/offering-section.tsx && grep -q "Expert-Formate" apps/website/components/sections/offering-section.tsx && grep -q "https://community.generation-ai.org" apps/website/components/sections/offering-section.tsx && grep -q "https://tools.generation-ai.org" apps/website/components/sections/offering-section.tsx && grep -q "Users" apps/website/components/sections/offering-section.tsx && grep -q "BookOpen" apps/website/components/sections/offering-section.tsx && grep -q "Calendar" apps/website/components/sections/offering-section.tsx && grep -q "Mic2" apps/website/components/sections/offering-section.tsx && grep -q 'data-section="offering"' apps/website/components/sections/offering-section.tsx && grep -qE "brand-neon|var\\(--accent" apps/website/components/sections/offering-section.tsx && ! grep -E "(from|via|to|text|bg|border)-(cyan|purple|violet|fuchsia|indigo)-[0-9]+" apps/website/components/sections/offering-section.tsx && pnpm --filter @genai/website build 2>&1 | tee /tmp/build-20-04-t1.log | grep -E "ƒ\\s+/\\s" && echo "TASK 1 PASS"</automated>
  </verify>
  <acceptance_criteria>
    - 4 Card-Titel exakt im Code: `Community`, `Wissensplattform`, `Events & Workshops`, `Expert-Formate`
    - 2 externe Links mit `target="_blank"` + `rel="noopener noreferrer"` zu tools/community
    - 4 lucide-Icons importiert: `Users`, `BookOpen`, `Calendar`, `Mic2`
    - Hover-Glow vorhanden (`hover:shadow-[0_0_20px_var(--accent-glow)]` oder vergleichbar mit `var(--accent-glow)`)
    - `data-section="offering"` gesetzt
    - Mindestens ein `brand-*` Token oder `var(--accent…)` verwendet
    - KEIN Tailwind-default `cyan-/purple-/violet-/fuchsia-/indigo-` Treffer
    - Build grün mit `ƒ /`, Lint grün
  </acceptance_criteria>
  <done>
    Offering-Section liefert R1.4 vollständig: 4 Cards mit korrekten Titles + Deep-Links + Lucide-Icons + Hover-Glow. Brand-Token-konform.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tool-Showcase-Section — Infinite-Moving-Cards mit 5 Stub-Tools + sichtbarem Beispiel-Badge + CTA</name>
  <files>apps/website/components/sections/tool-showcase-section.tsx</files>
  <read_first>
    - apps/website/components/sections/tool-showcase-section.tsx (Stub aus Plan 02)
    - apps/website/components/ui/infinite-moving-cards.tsx (Aceternity-Komponente — Item-Schema prüfen: typischerweise `{ quote: string, name: string, title?: string }` oder ähnlich; ggf. anpassen wenn API abweicht)
    - apps/website/app/globals.css (verifizieren: `@keyframes scroll` vorhanden + `prefers-reduced-motion` Guard für `.animate-scroll`)
    - .planning/phases/20-navigation-landing-skeleton/20-RESEARCH.md § Component Recipes D-11 (Token-Mapping, Beispiel-Badge-Snippet, Stub-Tools)
    - .planning/phases/20-navigation-landing-skeleton/20-CONTEXT.md (D-11, D-24, D-25 — Beispiel-Badge-Strategie)
    - .planning/REQUIREMENTS.md § R1.5
  </read_first>
  <action>
    Datei `apps/website/components/sections/tool-showcase-section.tsx` vollständig ersetzen.

    **Stub-Daten (LOCKED, identisch mit RESEARCH § D-11):**
    ```ts
    const stubTools = [
      { name: "ChatPDF Pro",  title: "PDFs analysieren und zusammenfassen" },
      { name: "Notion AI",    title: "KI-gestützte Notizen und Planung" },
      { name: "Perplexity",   title: "KI-Suchmaschine mit Quellen" },
      { name: "ElevenLabs",   title: "Text-zu-Sprache für Präsentationen" },
      { name: "Gamma",        title: "KI-Präsentationserstellung" },
    ] as const
    ```

    **InfiniteMovingCards-Items-Mapping:** Aceternity erwartet typischerweise `{ quote, name, title }`. Wir mappen unsere Tools so:
    - `quote` = die Beschreibung (Hauptzeile in der Card)
    - `name` = Tool-Name (Subzeile)
    - `title` = Kategorie-Label (z.B. "KI-Tool · Beispiel")

    **Beispiel-Badge-Strategie:** Da unser Theme-System auf `.light` Class auf `<html>` basiert (nicht auf `.dark`), funktioniert Tailwinds dark:-Variant nicht out-of-the-box wie in der Aceternity-Doc. Lösung: Statt zwei stack'd Spans mit dark:-Classes, nutze eine kleine inline `BeispielBadge` Komponente die `useTheme()` liest:

    ```tsx
    'use client'
    import { useTheme } from "@/components/ThemeProvider"

    export function BeispielBadge({ className = "" }: { className?: string }) {
      const { theme } = useTheme()
      const tone = theme === "light"
        ? "bg-brand-red-3 text-brand-red-12"
        : "bg-brand-neon-3 text-brand-neon-12"
      return (
        <span className={`inline-block text-[11px] font-mono font-bold rounded-full px-2 py-0.5 ${tone} ${className}`}>
          Beispiel
        </span>
      )
    }
    ```

    Diese BeispielBadge-Komponente wird HIER lokal in der Datei definiert (oder als geteiltes Snippet, aber für Phase 20 inline reicht — Plan 04 + Plan 05 nutzen sie beide → wir definieren sie in `tool-showcase-section.tsx` und Plan 04 Task 3 importiert sie aus dieser Datei). Cleaner Weg: separate Datei `apps/website/components/ui/beispiel-badge.tsx` anlegen — aber das wäre files_modified-Konflikt. Pragmatisch: BeispielBadge in `tool-showcase-section.tsx` exportieren, und Task 3 (community-preview-section.tsx) importiert via `import { BeispielBadge } from "@/components/sections/tool-showcase-section"`.

    **Vollständiger Code:**
    ```tsx
    'use client'

    import Link from "next/link"
    import { ArrowUpRight } from "lucide-react"
    import { useTheme } from "@/components/ThemeProvider"
    import { InfiniteMovingCards } from "@/components/ui/infinite-moving-cards"

    export function BeispielBadge({ className = "" }: { className?: string }) {
      const { theme } = useTheme()
      const tone = theme === "light"
        ? "bg-brand-red-3 text-brand-red-12"
        : "bg-brand-neon-3 text-brand-neon-12"
      return (
        <span className={`inline-block text-[11px] font-mono font-bold rounded-full px-2 py-0.5 ${tone} ${className}`}>
          Beispiel
        </span>
      )
    }

    const stubTools = [
      { quote: "PDFs analysieren und zusammenfassen.",      name: "ChatPDF Pro", title: "Recherche · Beispiel" },
      { quote: "KI-gestützte Notizen und Planung.",         name: "Notion AI",   title: "Produktivität · Beispiel" },
      { quote: "KI-Suchmaschine mit Quellen.",              name: "Perplexity",  title: "Recherche · Beispiel" },
      { quote: "Text-zu-Sprache für Präsentationen.",       name: "ElevenLabs",  title: "Audio · Beispiel" },
      { quote: "KI-Präsentationserstellung.",               name: "Gamma",       title: "Slides · Beispiel" },
    ] as const

    export function ToolShowcaseSection() {
      return (
        <section
          aria-labelledby="tool-showcase-heading"
          data-section="tool-showcase"
          className="bg-bg py-24 sm:py-32 border-b border-border overflow-hidden"
        >
          <div className="mx-auto max-w-6xl px-6">
            <div className="flex flex-col sm:flex-row items-start sm:items-end justify-between gap-6 mb-12">
              <div>
                <p className="font-mono text-xs uppercase tracking-[0.2em] text-text-muted mb-3">
                  Tool-Bibliothek
                </p>
                <h2 id="tool-showcase-heading" className="text-3xl sm:text-4xl font-bold tracking-tight text-text">
                  Über 100 KI-Tools, kuratiert.
                </h2>
                <p className="mt-3 text-base text-text-secondary max-w-xl">
                  Eine wachsende Bibliothek mit Anleitungen — sortiert nach Anwendungsfall.
                  Diese Auswahl ist exemplarisch.
                </p>
              </div>
              <div className="flex items-center gap-3">
                <BeispielBadge />
                <a
                  href="https://tools.generation-ai.org"
                  target="_blank"
                  rel="noopener noreferrer"
                  className="inline-flex items-center gap-1 text-sm font-mono text-[var(--accent)] hover:text-[var(--accent-hover)]"
                >
                  Alle Tools ansehen
                  <ArrowUpRight className="w-4 h-4" aria-hidden="true" />
                </a>
              </div>
            </div>

            <div data-stub="tool-showcase">
              <InfiniteMovingCards
                items={[...stubTools]}
                direction="left"
                speed="slow"
                pauseOnHover
                className="py-4"
              />
            </div>
          </div>
        </section>
      )
    }
    ```

    **Hinweis zur Beispiel-Badge-Sichtbarkeit pro Card:** Aceternity InfiniteMovingCards rendert die Items intern. Wir können die Badge nicht direkt in jede Card injizieren ohne die Komponente zu modifizieren. Lösungs-Alternativen, in dieser Reihenfolge ausprobieren:

    1. **Wenn InfiniteMovingCards `renderItem` prop oder children-render-prop hat:** custom render mit eingeschmolzenem `<BeispielBadge>`-Span pro Card.
    2. **Wenn nicht:** Item-Schema erweitern — `title`-Feld der Items enthält bereits "Beispiel" als Suffix (siehe stubTools oben). Damit ist der "Beispiel"-Text sichtbar pro Card via title-Render.
    3. **Zusätzlich** eine prominente `<BeispielBadge>` im Section-Header (siehe oben — neben "Alle Tools ansehen") als globaler Hinweis auf den Stub-Charakter.

    Der Section-Header-Badge garantiert mindestens 1 sichtbares "Beispiel" für den R1.5-Test. Die Pro-Card-Variante über das `title`-Feld macht es zusätzlich pro Card sichtbar.

    **Pflicht-Verifications:**
    - `pnpm --filter @genai/website build` grün, `/` ist `ƒ`
    - Lokaler Prod-curl: `curl -s localhost:3000 | grep -oE "Beispiel" | wc -l` ≥ 1 für Tool-Showcase (mehr wenn community-preview Plan 04 Task 3 läuft).
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && grep -q "InfiniteMovingCards" apps/website/components/sections/tool-showcase-section.tsx && grep -q "BeispielBadge" apps/website/components/sections/tool-showcase-section.tsx && grep -q "ChatPDF Pro" apps/website/components/sections/tool-showcase-section.tsx && grep -q "Notion AI" apps/website/components/sections/tool-showcase-section.tsx && grep -q "Perplexity" apps/website/components/sections/tool-showcase-section.tsx && grep -q "ElevenLabs" apps/website/components/sections/tool-showcase-section.tsx && grep -q "Gamma" apps/website/components/sections/tool-showcase-section.tsx && grep -q 'href="https://tools.generation-ai.org"' apps/website/components/sections/tool-showcase-section.tsx && grep -q 'data-section="tool-showcase"' apps/website/components/sections/tool-showcase-section.tsx && grep -q "Beispiel" apps/website/components/sections/tool-showcase-section.tsx && ! grep -E "(from|via|to|text|bg|border)-(cyan|purple|violet|fuchsia|indigo)-[0-9]+" apps/website/components/sections/tool-showcase-section.tsx && pnpm --filter @genai/website build 2>&1 | grep -E "ƒ\\s+/\\s" && echo "TASK 2 PASS"</automated>
  </verify>
  <acceptance_criteria>
    - InfiniteMovingCards-Import vorhanden, Items werden gerendert
    - 5 Stub-Tool-Namen alle im Code: `ChatPDF Pro`, `Notion AI`, `Perplexity`, `ElevenLabs`, `Gamma`
    - `BeispielBadge`-Komponente lokal definiert + exportiert (für Plan 04 Task 3 Reuse)
    - Mindestens 1 sichtbarer "Beispiel"-Text (header-Badge oder im title-Feld der Items)
    - CTA-Link zu `https://tools.generation-ai.org` mit `target="_blank"`
    - `data-section="tool-showcase"` gesetzt
    - KEIN Tailwind-default cyan/purple/violet/fuchsia/indigo
    - Build grün mit `ƒ /`
  </acceptance_criteria>
  <done>
    Tool-Showcase rendert 5 Stub-Tools via InfiniteMovingCards mit sichtbarer Beispiel-Markierung, externer CTA-Link zu tools.generation-ai.org. BeispielBadge-Komponente exportiert für Reuse.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Community-Preview-Section — 2-Spalten (3 Artikel + 2 Events) mit Beispiel-Badge pro Item, statisch</name>
  <files>apps/website/components/sections/community-preview-section.tsx</files>
  <read_first>
    - apps/website/components/sections/community-preview-section.tsx (Stub aus Plan 02)
    - apps/website/components/sections/tool-showcase-section.tsx (BeispielBadge-Export von dort übernehmen)
    - .planning/phases/20-navigation-landing-skeleton/20-RESEARCH.md § Component Recipes D-12 (Stub-Daten + Layout-Spec)
    - .planning/phases/20-navigation-landing-skeleton/20-CONTEXT.md (D-12: ruhig, keine Animation-Overkill)
    - .planning/REQUIREMENTS.md § R1.6
  </read_first>
  <action>
    Datei `apps/website/components/sections/community-preview-section.tsx` vollständig ersetzen.

    **Stub-Daten (LOCKED aus RESEARCH § D-12):**

    Artikel:
    1. "Wie ich ChatGPT für meine Bachelorarbeit genutzt habe"
    2. "5 KI-Tools die jeder BWL-Student kennen sollte"
    3. "Prompt Engineering für Anfänger: Der komplette Guide"

    Events:
    1. "KI-Basics Workshop" — 28. April 2026, Online
    2. "Masterclass: Automatisierung mit Make" — 05. Mai 2026

    **Vollständiger Code:**
    ```tsx
    'use client'

    import Link from "next/link"
    import { ArrowUpRight, Calendar, FileText } from "lucide-react"
    import { BeispielBadge } from "@/components/sections/tool-showcase-section"

    type StubArticle = {
      title: string
      readingTime: string
      href: string
    }

    type StubEvent = {
      title: string
      date: string
      location: string
      href: string
    }

    const stubArticles: StubArticle[] = [
      { title: "Wie ich ChatGPT für meine Bachelorarbeit genutzt habe", readingTime: "6 min Lesezeit", href: "https://community.generation-ai.org" },
      { title: "5 KI-Tools die jeder BWL-Student kennen sollte",        readingTime: "4 min Lesezeit", href: "https://community.generation-ai.org" },
      { title: "Prompt Engineering für Anfänger: Der komplette Guide",  readingTime: "9 min Lesezeit", href: "https://community.generation-ai.org" },
    ]

    const stubEvents: StubEvent[] = [
      { title: "KI-Basics Workshop",                       date: "28. April 2026", location: "Online",  href: "https://community.generation-ai.org" },
      { title: "Masterclass: Automatisierung mit Make",    date: "05. Mai 2026",   location: "Online",  href: "https://community.generation-ai.org" },
    ]

    export function CommunityPreviewSection() {
      return (
        <section
          aria-labelledby="community-preview-heading"
          data-section="community-preview"
          className="bg-bg-elevated py-24 sm:py-32 border-b border-border"
        >
          <div className="mx-auto max-w-6xl px-6">
            <div className="text-center mb-16">
              <p className="font-mono text-xs uppercase tracking-[0.2em] text-text-muted mb-3">
                Aus der Community
              </p>
              <h2 id="community-preview-heading" className="text-3xl sm:text-4xl font-bold tracking-tight text-text">
                Was gerade läuft.
              </h2>
              <p className="mt-3 text-sm text-text-muted max-w-2xl mx-auto">
                Ein Einblick in Diskussionen und Termine. Sobald die Community-API live ist,
                erscheinen hier echte Artikel und Events.
              </p>
            </div>

            <div className="grid grid-cols-1 lg:grid-cols-2 gap-8">

              {/* Spalte 1: Artikel */}
              <div>
                <h3 className="font-mono text-sm uppercase tracking-wider text-text-muted mb-6 inline-flex items-center gap-2">
                  <FileText className="w-4 h-4" aria-hidden="true" />
                  Letzte Artikel
                </h3>
                <ul className="space-y-4">
                  {stubArticles.map((article) => (
                    <li key={article.title}>
                      <a
                        href={article.href}
                        target="_blank"
                        rel="noopener noreferrer"
                        className="group block bg-bg-card border border-border rounded-2xl p-5 hover:border-brand-neon-6 transition-colors"
                      >
                        <div className="flex items-start justify-between gap-3 mb-2">
                          <BeispielBadge />
                          <ArrowUpRight className="w-4 h-4 text-text-muted group-hover:text-[var(--accent)] transition-colors" aria-hidden="true" />
                        </div>
                        <p className="font-mono text-base font-bold text-text leading-snug">
                          {article.title}
                        </p>
                        <p className="mt-2 text-xs text-text-muted">{article.readingTime}</p>
                      </a>
                    </li>
                  ))}
                </ul>
              </div>

              {/* Spalte 2: Events */}
              <div>
                <h3 className="font-mono text-sm uppercase tracking-wider text-text-muted mb-6 inline-flex items-center gap-2">
                  <Calendar className="w-4 h-4" aria-hidden="true" />
                  Kommende Events
                </h3>
                <ul className="space-y-4">
                  {stubEvents.map((event) => (
                    <li key={event.title}>
                      <a
                        href={event.href}
                        target="_blank"
                        rel="noopener noreferrer"
                        className="group block bg-bg-card border border-border rounded-2xl p-5 hover:border-brand-neon-6 transition-colors"
                      >
                        <div className="flex items-start justify-between gap-3 mb-2">
                          <BeispielBadge />
                          <ArrowUpRight className="w-4 h-4 text-text-muted group-hover:text-[var(--accent)] transition-colors" aria-hidden="true" />
                        </div>
                        <p className="font-mono text-base font-bold text-text leading-snug">
                          {event.title}
                        </p>
                        <p className="mt-2 text-xs text-text-muted">
                          {event.date} · {event.location}
                        </p>
                      </a>
                    </li>
                  ))}
                </ul>
              </div>

            </div>

            <div className="mt-12 text-center">
              <a
                href="https://community.generation-ai.org"
                target="_blank"
                rel="noopener noreferrer"
                className="inline-flex items-center gap-1 text-sm font-mono text-[var(--accent)] hover:text-[var(--accent-hover)]"
              >
                Zur Community
                <ArrowUpRight className="w-4 h-4" aria-hidden="true" />
              </a>
            </div>
          </div>
        </section>
      )
    }
    ```

    **Pflicht-Verifications:**
    - Build grün mit `ƒ /`
    - Lokaler Prod-curl: `curl -s localhost:3000 | grep -oE "Beispiel" | wc -l` ≥ 6 (3 Artikel + 2 Events + Tool-Showcase Header-Badge = mindestens 6 Treffer)
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && grep -q "BeispielBadge" apps/website/components/sections/community-preview-section.tsx && grep -q "Letzte Artikel" apps/website/components/sections/community-preview-section.tsx && grep -q "Kommende Events" apps/website/components/sections/community-preview-section.tsx && grep -q "Bachelorarbeit" apps/website/components/sections/community-preview-section.tsx && grep -q "BWL-Student" apps/website/components/sections/community-preview-section.tsx && grep -q "Prompt Engineering" apps/website/components/sections/community-preview-section.tsx && grep -q "KI-Basics Workshop" apps/website/components/sections/community-preview-section.tsx && grep -q "Masterclass: Automatisierung mit Make" apps/website/components/sections/community-preview-section.tsx && grep -q 'data-section="community-preview"' apps/website/components/sections/community-preview-section.tsx && ! grep -qi "skeleton" apps/website/components/sections/community-preview-section.tsx && ! grep -E "(from|via|to|text|bg|border)-(cyan|purple|violet|fuchsia|indigo)-[0-9]+" apps/website/components/sections/community-preview-section.tsx && pnpm --filter @genai/website build 2>&1 | grep -E "ƒ\\s+/\\s" && echo "TASK 3 PASS"</automated>
  </verify>
  <acceptance_criteria>
    - 3 Artikel-Titel im Code (alle mit grep auffindbar)
    - 2 Event-Titel im Code (alle mit grep auffindbar)
    - 2-Spalten-Grid mit `lg:grid-cols-2`
    - `BeispielBadge` aus tool-showcase importiert und pro Item gerendert (5 Items × 1 Badge = 5 Stück + Tool-Showcase 1 = 6 insgesamt im DOM)
    - Section enthält KEIN "skeleton" (D-24 explicit)
    - Externe Hrefs zu community.generation-ai.org mit target=_blank + rel
    - `data-section="community-preview"` gesetzt
    - KEIN Tailwind-default cyan/purple/violet/fuchsia/indigo
    - Build grün mit `ƒ /`
  </acceptance_criteria>
  <done>
    Community-Preview rendert 2 Spalten mit 3 Artikel-Stubs + 2 Event-Stubs, jeweils mit BeispielBadge pro Item. Statisch, keine Animation. Klare Markierung als Stub-Inhalt ohne Skeleton-Loader.
  </done>
</task>

</tasks>

<verification>
**Wave-Verify (Plan-Abschluss):**

1. `pnpm --filter @genai/website build` grün mit `ƒ /`
2. Lokaler Prod-Smoke:
   - Server starten
   - `curl -s localhost:3000 | grep -oE "Beispiel" | wc -l` ≥ 6 (5 Community-Items + ≥1 Tool-Showcase Header = 6+)
   - `curl -s localhost:3000 | grep -oE 'data-section="(offering|tool-showcase|community-preview)"' | sort -u | wc -l` = 3
3. R1.5 + R1.6 Playwright Tests:
   - `cd packages/e2e-tools && E2E_BASE_URL=http://localhost:3000 pnpm exec playwright test landing.spec.ts -g "R1.5|R1.6" --reporter=line`
   - Erwartet: beide grün
4. CSP-Test: `pnpm exec playwright test landing.spec.ts -g "CSP"` → grün
5. VALIDATION.md updaten: Plan 04 Task 1/2/3 mit Status.
</verification>

<success_criteria>
- 4 Cards mit Lucide-Icons + Hover-Glow + korrekten Deep-Links (R1.4)
- 5 Stub-Tools in InfiniteMovingCards + sichtbares "Beispiel" im Section-Header (R1.5)
- 3 Artikel + 2 Events in 2-Spalten-Layout, jeweils mit Beispiel-Badge (R1.6)
- BeispielBadge-Komponente einmal definiert, zweimal benutzt
- Build grün, alle Sections im DOM, CSP-Test grün
- R1.5 + R1.6 Playwright-Tests grün
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/20-navigation-landing-skeleton/20-04-SUMMARY.md` mit:
- Welcher BentoGrid-Pfad gewählt wurde (Aceternity-Komponente vs. Custom-Fallback) — und warum
- Welcher Pfad für die Pro-Card-Beispiel-Markierung in Tool-Showcase funktioniert hat (renderItem prop, title-Suffix, oder nur Header-Badge)
- BeispielBadge-Reuse-Notiz: ist exportiert aus tool-showcase-section.tsx
- Build `ƒ /` Snippet
- Lokaler curl Beispiel-Count
- Notiz für Plan 06: ggf. Component-Refactor BeispielBadge in `@/components/ui/beispiel-badge.tsx` falls weitere Sections sie brauchen (nicht in Phase 20 — als Backlog-Hinweis)
</output>
