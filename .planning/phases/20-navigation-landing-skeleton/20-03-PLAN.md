---
phase: 20
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - apps/website/components/sections/hero-section.tsx
  - apps/website/components/sections/discrepancy-section.tsx
autonomous: true
requirements: [R1.2, R1.3]
must_haves:
  truths:
    - "Hero-Section renders Aurora-Background plus a placeholder claim heading + sub-line + Primary-CTA linking to /join"
    - "Hero CTA links exactly to /join with text 'Jetzt beitreten'"
    - "Hero respects prefers-reduced-motion (Aurora pauses via globals.css guard, no entry animation)"
    - "Discrepancy-Section renders 6 number tickers (3 left 'Was Wirtschaft will' / 3 right 'Was Studis mitbringen') with values 7, 56, 73, 83.5, 75, 6.4 and unit suffixes (× / % / % / % / % / %)"
    - "Discrepancy-Section uses motion useScroll + useTransform to translate the two panels in opposite directions (max ±4%)"
    - "Discrepancy-Section closer line reads 'Generation AI schließt diese Lücke.'"
    - "Discrepancy-Section reduced-motion fallback: panels are static (no useScroll-driven transform), number tickers still display final values"
    - "Both sections use brand-* tokens only (no Tailwind default cyan/purple/violet)"
    - "Build green with ƒ /, CSP-smoke clean"
  artifacts:
    - path: "apps/website/components/sections/hero-section.tsx"
      provides: "Hero with Aurora-Background + claim placeholder + CTA"
      contains: "AuroraBackground"
    - path: "apps/website/components/sections/discrepancy-section.tsx"
      provides: "Custom split-panel discrepancy viz with 6 number tickers + closer line"
      contains: "useScroll"
  key_links:
    - from: "apps/website/components/sections/hero-section.tsx"
      to: "@/components/ui/aurora-background"
      via: "ES import"
      pattern: "from ['\"]@/components/ui/aurora-background['\"]"
    - from: "apps/website/components/sections/hero-section.tsx"
      to: "/join route"
      via: "Next Link href"
      pattern: "href=['\"]\\/join['\"]"
    - from: "apps/website/components/sections/discrepancy-section.tsx"
      to: "@/components/ui/number-ticker"
      via: "ES import"
      pattern: "from ['\"]@/components/ui/number-ticker['\"]"
    - from: "apps/website/components/sections/discrepancy-section.tsx"
      to: "motion/react"
      via: "useScroll + useTransform + useInView + useReducedMotion imports"
      pattern: "from ['\"]motion/react['\"]"
---

<objective>
Wave-3 Wow-Peaks 1+2: Die zwei strategisch wichtigsten Sections der Landing — Hero (R1.2, Wow-Peak 1 per D-05) und Discrepancy (R1.3, Wow-Peak 2 per D-05, "DAS zentrale Wow-Stück" per D-09). Hero bekommt Aurora-Background als CSP-safe CSS-only Bewegung. Discrepancy ist Custom-Code mit 6 Number-Tickern in einem Bento-Split-Layout, scroll-getriebene Divergenz-Animation und Closer-Zeile.

Purpose: Die "Wow-Wahrnehmung an Peaks" (D-04/D-05) entsteht hier zur Hälfte. Wenn Hero + Discrepancy stimmig sind, hat die Landing zwei der drei Wow-Momente (Plan 05 liefert den dritten via Final-CTA Lamp Effect).

Output: Zwei vollständige Sections die im DOM sichtbar sind, scroll-triggered animieren, Brand-Tokens verwenden, reduced-motion respektieren und CSP-konform laufen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/20-navigation-landing-skeleton/20-CONTEXT.md
@.planning/phases/20-navigation-landing-skeleton/20-RESEARCH.md
@.planning/phases/20-navigation-landing-skeleton/20-VALIDATION.md
@.planning/phases/20-navigation-landing-skeleton/20-02-PLAN.md
@LEARNINGS.md
@apps/website/components/sections/hero-section.tsx
@apps/website/components/sections/discrepancy-section.tsx
@apps/website/components/ui/aurora-background.tsx
@apps/website/components/ui/number-ticker.tsx
@apps/website/app/globals.css
@packages/config/tailwind/base.css

<interfaces>
<!-- Aus Plan 01 + 02 jetzt verfügbar: -->
<!-- @/components/ui/aurora-background — exports: AuroraBackground (children, className, showRadialGradient props typisch) -->
<!-- @/components/ui/number-ticker — exports: NumberTicker (props value: number, decimalPlaces?: number, direction?: "up"|"down", delay?: number, className?: string) -->
<!-- motion/react — exports: motion, useScroll, useTransform, useInView, useReducedMotion, useRef-pattern -->

<!-- Brand-Tokens (Tailwind utilities aus packages/config/tailwind/base.css): -->
<!-- bg-brand-{neon|blue|pink|red}-{1-12} -->
<!-- text-brand-… -->
<!-- border-brand-… -->
<!-- Plus semantische: bg-bg, bg-bg-card, text-text, text-text-muted, border-border, var(--accent), var(--accent-glow) -->

<!-- Discrepancy 6 Kernzahlen (LOCKED aus SCOPE.md, verifiziert in 20-RESEARCH.md § Component Recipes D-09): -->
<!-- LINKS "Was Wirtschaft will": -->
<!--   1. 7  ×    — Nachfrage nach KI-Talent (2023→2025) -->
<!--   2. 56 %    — Lohnaufschlag für KI-Kompetenz -->
<!--   3. 73 %    — Unternehmen können KI nicht ausschöpfen -->
<!-- RECHTS "Was Studis mitbringen": -->
<!--   4. 83.5 %  — Auf Anfänger-Level -->
<!--   5. 75   %  — "Studium bereitet mich nicht vor" -->
<!--   6. 6.4  %  — Intensive KI-Lehre im Studium -->

<!-- Section-Container Convention (aus Plan 02 stub): jede Section behält ihr <section data-section="…" aria-labelledby="…"> Wrapper -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Hero-Section — Aurora-Background + Claim-Placeholder + Primary-CTA → /join</name>
  <files>apps/website/components/sections/hero-section.tsx</files>
  <read_first>
    - apps/website/components/sections/hero-section.tsx (current Stub aus Plan 02 Task 3)
    - apps/website/components/ui/aurora-background.tsx (Aceternity-Komponente die Plan 01 kopiert hat — Props + Klassen-Struktur prüfen)
    - apps/website/app/globals.css (verifizieren dass `@keyframes aurora` + `--animate-aurora` token gesetzt sind)
    - .planning/phases/20-navigation-landing-skeleton/20-CONTEXT.md (D-08 Hero-Spec, "Hero-Claim finales Wording" ist Deferred — Platzhalter setzen)
    - .planning/phases/20-navigation-landing-skeleton/20-RESEARCH.md § Component Recipes D-08 (Token-Mapping Aurora dark vs. light)
    - packages/config/tailwind/base.css § Component Standards (Primary-Button-Pattern: `bg-[var(--accent)] text-[var(--text-on-accent)] rounded-full px-4 py-2.5 hover:shadow-[0_0_20px_var(--accent-glow)]`)
  </read_first>
  <action>
    Datei `apps/website/components/sections/hero-section.tsx` vollständig ersetzen.

    Imports: `'use client'`, `Link` (next/link), `motion`, `useReducedMotion` (motion/react), `AuroraBackground` (@/components/ui/aurora-background) — exakter Export-Name aus dem Plan-01-Copy verifizieren; falls anders heißt (z.B. `Aurora` oder `AuroraBg`), Import entsprechend anpassen.

    Inhalt:
    ```tsx
    'use client'

    import Link from "next/link"
    import { motion, useReducedMotion } from "motion/react"
    import { AuroraBackground } from "@/components/ui/aurora-background"

    export function HeroSection() {
      const prefersReducedMotion = useReducedMotion()

      // Claim-Wording ist explicit Deferred (CONTEXT.md "Deferred Ideas" + ROADMAP "Out-of-Scope: Hero-Claim finales Wording")
      // Wir setzen einen sprechenden Placeholder, der die Section-Funktion erfüllt aber als TODO markiert ist.
      const heroClaim = "KI-Skills, die im Studium fehlen."
      const heroSubline = "Die Community für Studierende, die KI nicht nur benutzen, sondern verstehen wollen."

      return (
        <section
          aria-labelledby="hero-heading"
          data-section="hero"
          className="relative isolate"
        >
          <AuroraBackground className="min-h-[80vh]">
            <motion.div
              initial={prefersReducedMotion ? false : { opacity: 0, y: 16 }}
              animate={prefersReducedMotion ? {} : { opacity: 1, y: 0 }}
              transition={{ duration: 0.7, ease: "easeOut" }}
              className="relative z-10 mx-auto max-w-4xl px-6 py-24 text-center"
            >
              <p className="font-mono text-xs uppercase tracking-[0.2em] text-text-muted mb-6">
                Generation AI · DACH-Community
              </p>
              <h1
                id="hero-heading"
                className="font-mono text-4xl sm:text-5xl lg:text-6xl font-bold leading-[1.05] tracking-tight text-text"
              >
                {heroClaim}
              </h1>
              <p className="mt-6 text-lg sm:text-xl text-text-secondary max-w-2xl mx-auto">
                {heroSubline}
              </p>
              <div className="mt-10 flex flex-col sm:flex-row items-center justify-center gap-4">
                <Link
                  href="/join"
                  className="inline-flex items-center justify-center bg-[var(--accent)] text-[var(--text-on-accent)] px-6 py-3 rounded-full text-sm font-mono font-bold transition-all duration-300 hover:shadow-[0_0_20px_var(--accent-glow)] hover:scale-[1.03]"
                >
                  Jetzt beitreten
                </Link>
              </div>
              <p className="mt-6 text-xs font-mono text-text-muted">
                Kostenlos · gemeinnützig · für Studierende und Early-Career
              </p>
            </motion.div>
          </AuroraBackground>
        </section>
      )
    }
    ```

    **Export-Name-Sicherheit:** Falls `AuroraBackground` in der Plan-01-kopierten Datei anders heißt → die Hero-Section-Datei nutzt den tatsächlichen Export, das Verify-Script prüft auf `from "@/components/ui/aurora-background"` (Pfad), nicht den Symbol-Namen.

    **Pflicht-Verify:**
    - `pnpm --filter @genai/website build` grün, `/` ist `ƒ`
    - Lokaler Prod-Smoke: `curl -s localhost:3000 | grep -E 'href="/join"'` ≥ 2 Treffer (Header-CTA + Hero-CTA)
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && grep -q 'from "@/components/ui/aurora-background"' apps/website/components/sections/hero-section.tsx && grep -q 'href="/join"' apps/website/components/sections/hero-section.tsx && grep -q "useReducedMotion" apps/website/components/sections/hero-section.tsx && grep -q 'data-section="hero"' apps/website/components/sections/hero-section.tsx && grep -q 'id="hero-heading"' apps/website/components/sections/hero-section.tsx && grep -q "Jetzt beitreten" apps/website/components/sections/hero-section.tsx && ! grep -E "(from|via|to|text|bg|border)-(cyan|purple|violet|fuchsia|indigo)-[0-9]+" apps/website/components/sections/hero-section.tsx && pnpm --filter @genai/website build 2>&1 | tee /tmp/build-20-03-t1.log | grep -E "ƒ\\s+/\\s" && echo "TASK 1 PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `hero-section.tsx` importiert `AuroraBackground` aus `@/components/ui/aurora-background`
    - CTA-Link enthält `href="/join"` und Text `Jetzt beitreten`
    - `useReducedMotion` wird genutzt (Motion-Animation am Hauptinhalt mit Reduced-Motion-Guard)
    - `data-section="hero"` und `id="hero-heading"` (für Tests + a11y) gesetzt
    - KEIN Treffer für Tailwind-default-Farben `cyan-/purple-/violet-/fuchsia-/indigo-` mit Numerik-Suffix
    - Hero verwendet entweder `var(--accent)` (für CTA) oder `brand-*` Klassen — `grep -E "var\\(--accent\\)|brand-" hero-section.tsx` ≥ 1 Treffer
    - Build grün mit `ƒ /`
  </acceptance_criteria>
  <done>
    Hero-Section gerendert mit Aurora-Background, Claim-Platzhalter, CTA → /join, Reduced-Motion-Fallback. Wow-Peak 1 visuell platziert.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Discrepancy-Section — Custom Bento-Split + 6 Number-Ticker + Scroll-Divergenz + Closer</name>
  <files>apps/website/components/sections/discrepancy-section.tsx</files>
  <read_first>
    - apps/website/components/sections/discrepancy-section.tsx (current Stub aus Plan 02)
    - apps/website/components/ui/number-ticker.tsx (Props-Signatur des MagicUI-Number-Tickers verifizieren — value: number, decimalPlaces?: number, delay?: number, direction?: "up"|"down", className?: string sind die typischen Props)
    - .planning/phases/20-navigation-landing-skeleton/20-CONTEXT.md (D-09: das zentrale Wow-Stück, Bento-Split, scroll-triggered Divergenz, Closer-Reveal, Custom-Code)
    - .planning/phases/20-navigation-landing-skeleton/20-RESEARCH.md § Custom Diskrepanz-Section (Implementation-Skizze mit useScroll + useTransform + Number-Ticker + Closer)
    - .planning/phases/20-navigation-landing-skeleton/20-RESEARCH.md § Component Recipes D-09 (die 6 Kernzahlen — LOCKED, NICHT ändern)
    - .planning/phases/20-navigation-landing-skeleton/20-RESEARCH.md § Open Questions OQ-5 (CLS-Mitigation: overflow-hidden + ±4% Transform-Limit)
    - LEARNINGS.md (CSP — keine inline-scripts; motion inline-styles via style-src 'unsafe-inline' OK)
    - packages/config/tailwind/base.css (Brand-Tokens für Spalten-BG: links blue/neon, rechts red/pink — siehe RESEARCH § Component Recipes D-09)
  </read_first>
  <action>
    Datei `apps/website/components/sections/discrepancy-section.tsx` vollständig ersetzen.

    **Daten-Konstante** (LOCKED, identisch mit RESEARCH § Component Recipes D-09):
    ```ts
    const wirtschaftStats = [
      { value: 7,    decimals: 0, suffix: "×",  label: "Nachfrage nach KI-Talent (2023→2025)" },
      { value: 56,   decimals: 0, suffix: " %", label: "Lohnaufschlag für KI-Kompetenz" },
      { value: 73,   decimals: 0, suffix: " %", label: "Unternehmen können KI nicht ausschöpfen" },
    ] as const

    const studisStats = [
      { value: 83.5, decimals: 1, suffix: " %", label: "auf Anfänger-Level" },
      { value: 75,   decimals: 0, suffix: " %", label: "„Studium bereitet mich nicht vor.“" },
      { value: 6.4,  decimals: 1, suffix: " %", label: "intensive KI-Lehre im Studium" },
    ] as const
    ```

    **Vollständiger Code:**
    ```tsx
    'use client'

    import { useRef } from "react"
    import { motion, useScroll, useTransform, useInView, useReducedMotion } from "motion/react"
    import { NumberTicker } from "@/components/ui/number-ticker"

    const wirtschaftStats = [
      { value: 7,    decimals: 0, suffix: "×",  label: "Nachfrage nach KI-Talent (2023→2025)" },
      { value: 56,   decimals: 0, suffix: " %", label: "Lohnaufschlag für KI-Kompetenz" },
      { value: 73,   decimals: 0, suffix: " %", label: "Unternehmen können KI nicht ausschöpfen" },
    ] as const

    const studisStats = [
      { value: 83.5, decimals: 1, suffix: " %", label: "auf Anfänger-Level" },
      { value: 75,   decimals: 0, suffix: " %", label: "„Studium bereitet mich nicht vor.“" },
      { value: 6.4,  decimals: 1, suffix: " %", label: "intensive KI-Lehre im Studium" },
    ] as const

    export function DiscrepancySection() {
      const sectionRef = useRef<HTMLElement>(null)
      const tickersRef = useRef<HTMLDivElement>(null)
      const prefersReducedMotion = useReducedMotion()

      // Scroll-Divergenz: links wandert leicht nach links, rechts nach rechts.
      // Maximal ±4% Transform — innerhalb overflow-hidden, kein Layout-Shift (siehe RESEARCH OQ-5).
      const { scrollYProgress } = useScroll({
        target: sectionRef,
        offset: ["start 80%", "end 20%"],
      })
      // Bei reduced-motion: useTransform identity (kein Movement)
      const leftX  = useTransform(scrollYProgress, [0, 1], prefersReducedMotion ? ["0%", "0%"] : ["0%", "-4%"])
      const rightX = useTransform(scrollYProgress, [0, 1], prefersReducedMotion ? ["0%", "0%"] : ["0%", "4%"])

      const tickersInView = useInView(tickersRef, { once: true, amount: 0.3 })

      return (
        <section
          ref={sectionRef}
          aria-labelledby="discrepancy-heading"
          data-section="discrepancy"
          className="relative overflow-hidden bg-bg py-24 sm:py-32 border-b border-border"
        >
          <div className="mx-auto max-w-6xl px-6">
            {/* Intro */}
            <motion.div
              initial={prefersReducedMotion ? false : { opacity: 0, y: 16 }}
              whileInView={prefersReducedMotion ? {} : { opacity: 1, y: 0 }}
              viewport={{ once: true, amount: 0.4 }}
              transition={{ duration: 0.6 }}
              className="text-center mb-16"
            >
              <p className="font-mono text-xs uppercase tracking-[0.2em] text-text-muted mb-3">
                Die KI-Diskrepanz
              </p>
              <h2 id="discrepancy-heading" className="text-3xl sm:text-4xl lg:text-5xl font-bold tracking-tight text-text">
                Was die Wirtschaft braucht — und was im Studium passiert.
              </h2>
            </motion.div>

            {/* Bento-Split */}
            <div ref={tickersRef} className="grid grid-cols-1 lg:grid-cols-2 gap-px bg-border rounded-2xl overflow-hidden">

              {/* LINKS: Was Wirtschaft will */}
              <motion.div
                style={{ x: leftX, willChange: "transform" }}
                className="bg-brand-blue-2 dark:bg-brand-blue-12/40 p-8 sm:p-10"
              >
                <h3 className="font-mono text-sm uppercase tracking-wider text-brand-blue-11 dark:text-brand-neon-9 mb-8">
                  Was die Wirtschaft will
                </h3>
                <ul className="space-y-8">
                  {wirtschaftStats.map((stat) => (
                    <li key={stat.label}>
                      <p className="font-mono text-5xl sm:text-6xl font-bold text-brand-blue-11 dark:text-brand-neon-9 leading-none">
                        {tickersInView ? (
                          <NumberTicker value={stat.value} decimalPlaces={stat.decimals} />
                        ) : (
                          <span>0</span>
                        )}
                        <span className="text-3xl sm:text-4xl ml-1">{stat.suffix}</span>
                      </p>
                      <p className="mt-2 text-sm text-text-secondary">{stat.label}</p>
                    </li>
                  ))}
                </ul>
              </motion.div>

              {/* RECHTS: Was Studis mitbringen */}
              <motion.div
                style={{ x: rightX, willChange: "transform" }}
                className="bg-brand-red-2 dark:bg-brand-red-12/40 p-8 sm:p-10"
              >
                <h3 className="font-mono text-sm uppercase tracking-wider text-brand-red-11 dark:text-brand-pink-9 mb-8">
                  Was Studierende mitbringen
                </h3>
                <ul className="space-y-8">
                  {studisStats.map((stat) => (
                    <li key={stat.label}>
                      <p className="font-mono text-5xl sm:text-6xl font-bold text-brand-red-11 dark:text-brand-pink-9 leading-none">
                        {tickersInView ? (
                          <NumberTicker value={stat.value} decimalPlaces={stat.decimals} />
                        ) : (
                          <span>0</span>
                        )}
                        <span className="text-3xl sm:text-4xl ml-1">{stat.suffix}</span>
                      </p>
                      <p className="mt-2 text-sm text-text-secondary">{stat.label}</p>
                    </li>
                  ))}
                </ul>
              </motion.div>

            </div>

            {/* Closer-Zeile (R1.3 explicit) */}
            <motion.p
              initial={prefersReducedMotion ? false : { opacity: 0, y: 12 }}
              whileInView={prefersReducedMotion ? {} : { opacity: 1, y: 0 }}
              viewport={{ once: true, amount: 0.6 }}
              transition={{ delay: 0.4, duration: 0.8, ease: "easeOut" }}
              className="mt-16 text-center font-mono text-2xl sm:text-3xl text-text"
            >
              Generation AI schließt diese Lücke.
            </motion.p>
          </div>
        </section>
      )
    }
    ```

    **Pflicht-Verifications:**
    - `pnpm --filter @genai/website build` grün, `/` ist `ƒ`
    - Lokaler Prod-Smoke: `curl -s localhost:3000 | grep "Generation AI schließt diese Lücke"` exit 0
    - `curl -s localhost:3000 | grep -oE "data-section=\"discrepancy\"" | wc -l` = 1
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && grep -q "useScroll" apps/website/components/sections/discrepancy-section.tsx && grep -q "useTransform" apps/website/components/sections/discrepancy-section.tsx && grep -q "NumberTicker" apps/website/components/sections/discrepancy-section.tsx && grep -q "useReducedMotion" apps/website/components/sections/discrepancy-section.tsx && grep -q "Generation AI schließt diese Lücke" apps/website/components/sections/discrepancy-section.tsx && grep -q "83.5" apps/website/components/sections/discrepancy-section.tsx && grep -q "6.4" apps/website/components/sections/discrepancy-section.tsx && grep -qE "value: 7\\b" apps/website/components/sections/discrepancy-section.tsx && grep -qE "value: 56\\b" apps/website/components/sections/discrepancy-section.tsx && grep -qE "value: 73\\b" apps/website/components/sections/discrepancy-section.tsx && grep -qE "value: 75\\b" apps/website/components/sections/discrepancy-section.tsx && grep -q "overflow-hidden" apps/website/components/sections/discrepancy-section.tsx && grep -q "brand-blue" apps/website/components/sections/discrepancy-section.tsx && grep -q "brand-red" apps/website/components/sections/discrepancy-section.tsx && ! grep -E "(from|via|to|text|bg|border)-(cyan|purple|violet|fuchsia|indigo)-[0-9]+" apps/website/components/sections/discrepancy-section.tsx && pnpm --filter @genai/website build 2>&1 | tee /tmp/build-20-03-t2.log | grep -E "ƒ\\s+/\\s" && echo "TASK 2 PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `discrepancy-section.tsx` importiert: `useScroll`, `useTransform`, `useInView`, `useReducedMotion` aus `motion/react`, plus `NumberTicker`
    - Alle 6 Werte hardcoded vorhanden: `7`, `56`, `73`, `83.5`, `75`, `6.4` (separate grep-Treffer für jede Zahl)
    - Closer-Zeile exakt: `Generation AI schließt diese Lücke.`
    - `overflow-hidden` ist auf der Section gesetzt (CLS-Mitigation per OQ-5)
    - Beide Spalten verwenden Brand-Tokens: mindestens je ein `brand-blue-*` (links) und `brand-red-*` (rechts) Treffer
    - KEIN Treffer für Tailwind-default `cyan-/purple-/violet-/fuchsia-/indigo-` Farben
    - `data-section="discrepancy"` und `id="discrepancy-heading"` gesetzt
    - Build grün mit `ƒ /`
    - Lokaler Prod-Server enthält im HTML die Closer-Zeile (curl-test)
  </acceptance_criteria>
  <done>
    Discrepancy-Section liefert die 6 Kernzahlen via NumberTicker, scroll-getriebene gegenläufige Spalten-Divergenz innerhalb ±4% (CLS-sicher), Closer-Zeile als Text-Reveal. Reduced-Motion neutralisiert die Transformation. Wow-Peak 2 erfüllt R1.3 vollständig.
  </done>
</task>

</tasks>

<verification>
**Wave-Verify (Plan-Abschluss):**

1. `pnpm --filter @genai/website build` grün mit `ƒ /`
2. Lokaler Prod-Smoke gegen Plan-2-Sections + diese:
   - `pnpm --filter @genai/website build && cd apps/website && NODE_ENV=production pnpm start &`
   - `sleep 3 && curl -s http://localhost:3000 | grep -E "(Jetzt beitreten|Generation AI schließt diese Lücke)" | wc -l` ≥ 2
3. CSP-Console-Test:
   - `cd packages/e2e-tools && E2E_BASE_URL=http://localhost:3000 pnpm exec playwright test landing.spec.ts -g "CSP" --reporter=line` → grün
4. R1.2-Test (Hero CTA):
   - `pnpm exec playwright test landing.spec.ts -g "R1.2" --reporter=line` → grün
5. VALIDATION.md updaten: Plan 03 Task 1 + 2 mit Status.
</verification>

<success_criteria>
- Hero rendert Aurora-Background + Claim + CTA → /join
- Discrepancy rendert 6 Number-Ticker mit korrekten Werten + Closer
- Scroll-Divergenz funktioniert (Smoke: scroll auf Discrepancy, Spalten wandern entgegengesetzt — manueller UAT)
- Reduced-motion: Aurora pausiert (CSS-Guard), motion.div springt sofort in End-State, useTransform liefert konstante 0%
- R1.2 Playwright-Test grün
- CSP-Test grün
- Build grün mit `ƒ /`, keine Tailwind-default-Farben in den zwei Section-Files
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/20-navigation-landing-skeleton/20-03-SUMMARY.md` mit:
- Hero-Komponente: Aurora-Klassen tatsächlich gewählt + Token-Mapping verwendet
- Discrepancy-Komponente: NumberTicker-Props die tatsächlich geklappt haben (falls Props-Signatur abwich), useScroll-Offset, gewählte Brand-Töne
- Falls AuroraBackground oder NumberTicker andere Export-Namen hatten als angenommen → dokumentieren
- Build-Output `ƒ /` Snippet
- Manuelle Sichtprüfung-Notiz (Wow-Wahrnehmung subjektiv) für Lucas-Review in Plan 06
</output>
