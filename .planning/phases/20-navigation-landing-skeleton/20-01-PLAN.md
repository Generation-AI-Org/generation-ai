---
phase: 20
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/package.json
  - pnpm-lock.yaml
  - apps/website/components/ui/dropdown-menu.tsx
  - apps/website/components/ui/sheet.tsx
  - apps/website/components/ui/aurora-background.tsx
  - apps/website/components/ui/bento-grid.tsx
  - apps/website/components/ui/infinite-moving-cards.tsx
  - apps/website/components/ui/lamp.tsx
  - apps/website/components/ui/number-ticker.tsx
  - apps/website/components/ui/marquee.tsx
  - apps/website/lib/utils.ts
  - apps/website/app/globals.css
  - apps/website/components/hero.tsx
  - apps/website/components/sections/features.tsx
  - apps/website/components/sections/target-audience.tsx
  - apps/website/components/sections/signup.tsx
  - apps/website/components/home-client.tsx
  - lighthouserc.json
  - packages/e2e-tools/tests/landing.spec.ts
autonomous: true
requirements: [R1.1, R1.2, R1.3, R1.5, R1.8]
must_haves:
  truths:
    - "motion package is installed in @genai/website with version 12.x"
    - "shadcn dropdown-menu and sheet primitives are installed locally"
    - "Aceternity + MagicUI source files copied locally with framer-motion imports rewritten to motion/react"
    - "Aurora and infinite-scroll keyframes exist in apps/website/app/globals.css"
    - "Old hero/features/target-audience/signup section files are deleted from the repo"
    - "home-client.tsx no longer imports the deleted sections and compiles cleanly"
    - "lighthouserc.json exists at repo root with >=0.9 thresholds for all four categories"
    - "landing.spec.ts skeleton exists in packages/e2e-tools/tests with placeholder tests for R1.1/R1.2/R1.5/R1.8"
    - "pnpm build of @genai/website is green and / route is marked ƒ (dynamic)"
  artifacts:
    - path: "apps/website/package.json"
      provides: "motion dependency entry"
      contains: "\"motion\":"
    - path: "apps/website/components/ui/dropdown-menu.tsx"
      provides: "shadcn dropdown primitive"
      exports: ["DropdownMenu", "DropdownMenuTrigger", "DropdownMenuContent", "DropdownMenuItem"]
    - path: "apps/website/components/ui/sheet.tsx"
      provides: "shadcn sheet primitive (mobile nav overlay)"
      exports: ["Sheet", "SheetContent", "SheetTrigger"]
    - path: "apps/website/components/ui/aurora-background.tsx"
      provides: "Aceternity Aurora Background, brand-rebranded"
    - path: "apps/website/components/ui/bento-grid.tsx"
      provides: "Aceternity Bento Grid"
    - path: "apps/website/components/ui/infinite-moving-cards.tsx"
      provides: "Aceternity Infinite Moving Cards"
    - path: "apps/website/components/ui/lamp.tsx"
      provides: "Aceternity Lamp Effect"
    - path: "apps/website/components/ui/number-ticker.tsx"
      provides: "MagicUI Number Ticker"
    - path: "apps/website/components/ui/marquee.tsx"
      provides: "MagicUI Marquee"
    - path: "apps/website/lib/utils.ts"
      provides: "cn() helper for shadcn"
      contains: "export function cn"
    - path: "apps/website/app/globals.css"
      provides: "Aurora keyframes + infinite-scroll keyframes + reduced-motion guards"
      contains: "@keyframes aurora"
    - path: "lighthouserc.json"
      provides: "Lighthouse-CI gate config"
      contains: "minScore"
    - path: "packages/e2e-tools/tests/landing.spec.ts"
      provides: "Landing E2E test skeleton (Wave 0 gap closure for nyquist)"
      contains: "test.describe"
  key_links:
    - from: "apps/website/components/ui/*.tsx (Aceternity/MagicUI copies)"
      to: "motion/react"
      via: "ES imports"
      pattern: "from ['\"]motion/react['\"]"
    - from: "apps/website/app/globals.css"
      to: "@theme inline animation tokens"
      via: "CSS @theme block"
      pattern: "@keyframes (aurora|scroll)"
    - from: "apps/website/components/home-client.tsx"
      to: "remaining components after deletion"
      via: "ES imports"
      pattern: "import.*from"
---

<objective>
Wave-1 Setup für Phase 20: Stack-Additions (motion, shadcn-Primitives, Aceternity/MagicUI Copy-in) installieren, Animation-Keyframes in globals.css verankern, Lighthouse-CI Gate-Konfig anlegen, Playwright-Landing-Skeleton anlegen, und alle vier Alt-Sections (D-21) löschen ohne den Build zu brechen.

Purpose: Saubere Foundation für Wave 2 (Layout-Shell) und Wave 3 (Sections). Ohne diesen Plan müsste jede Section ihre Dependencies einzeln einziehen und es gäbe keine Animation-Keyframes — das würde drei parallele Plans gegenseitig blockieren. Außerdem schließt dieser Plan alle Wave-0 Gaps aus 20-VALIDATION.md (lighthouserc.json + landing.spec.ts + globals.css keyframes).

Output: Installations + UI-Primitives in `apps/website/components/ui/`, alte Section-Files weg, lauffähiger Build mit `ƒ` für `/`, lighthouserc.json + landing.spec.ts Skeleton.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-navigation-landing-skeleton/20-CONTEXT.md
@.planning/phases/20-navigation-landing-skeleton/20-RESEARCH.md
@.planning/phases/20-navigation-landing-skeleton/20-VALIDATION.md
@LEARNINGS.md
@apps/website/AGENTS.md
@apps/website/components.json
@apps/website/lib/csp.ts
@apps/website/proxy.ts
@apps/website/app/layout.tsx
@apps/website/app/globals.css
@apps/website/components/home-client.tsx
@packages/config/tailwind/base.css

<interfaces>
<!-- Brand-Tokens als Tailwind-Utilities (aus packages/config/tailwind/base.css) -->
<!-- Verfügbar: bg-brand-{neon|blue|pink|red}-{1-12}, text-…, border-… -->
<!-- Plus: bg-bg, bg-bg-card, bg-bg-elevated, bg-bg-header, text-text, text-text-muted, border-border, accent via [var(--accent)] -->

<!-- shadcn-Mapping (aus apps/website/app/globals.css, bestehend): -->
<!-- --color-primary → var(--accent), --color-background → var(--bg), --color-card → var(--bg-card), -->
<!-- --color-muted-foreground → var(--text-muted), --color-input → var(--border), --color-ring → var(--text) -->

<!-- CSP (aus apps/website/lib/csp.ts): -->
<!-- script-src 'self' 'nonce-${nonce}' 'strict-dynamic' https://va.vercel-scripts.com -->
<!-- style-src 'self' 'unsafe-inline' (motion inline-styles sind erlaubt) -->

<!-- Existing components.json registry: "@aceternity": "https://ui.aceternity.com/registry/{name}.json" -->
<!-- aliases: components=@/components, utils=@/lib/utils, ui=@/components/ui -->
<!-- WICHTIG: aktuell existiert KEIN apps/website/lib/utils.ts — muss vom shadcn-init mit cn()-Helper erzeugt werden -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install motion + shadcn primitives + cn-helper</name>
  <files>
    apps/website/package.json,
    pnpm-lock.yaml,
    apps/website/lib/utils.ts,
    apps/website/components/ui/dropdown-menu.tsx,
    apps/website/components/ui/sheet.tsx
  </files>
  <read_first>
    - apps/website/package.json (verstehen welche Deps schon da sind: clsx ✓, tailwind-merge ✓, lucide-react ✓)
    - apps/website/components.json (shadcn config — aliases verifizieren, Aceternity-Registry vorhanden)
    - .planning/phases/20-navigation-landing-skeleton/20-RESEARCH.md § Stack-Setup (Install-Commands wörtlich)
    - .planning/phases/20-navigation-landing-skeleton/20-CONTEXT.md (D-01 Motion, D-02 shadcn locked)
    - LEARNINGS.md (Build-Output `ƒ`-Check ist Pflicht — gilt auch nach Install)
  </read_first>
  <action>
    Im Repo-Root ausführen (in dieser Reihenfolge):

    1. `pnpm add motion --filter @genai/website` — installiert motion@^12 (latest verifiziert in RESEARCH § Stack-Setup, Version 12.38.0).

    2. shadcn cn-helper anlegen (apps/website/lib/utils.ts) — exakter Inhalt:
    ```ts
    import { clsx, type ClassValue } from "clsx"
    import { twMerge } from "tailwind-merge"

    export function cn(...inputs: ClassValue[]) {
      return twMerge(clsx(inputs))
    }
    ```
    (clsx + tailwind-merge sind bereits in package.json, kein Install nötig.)

    3. shadcn Dropdown-Menu installieren: im Repo-Root `pnpm --filter @genai/website dlx shadcn@latest add dropdown-menu --yes`. Falls das wegen Workspace-Pfaden hakt: alternativ `cd apps/website && pnpm dlx shadcn@latest add dropdown-menu --yes`. Erwarteter Output: neue Datei `apps/website/components/ui/dropdown-menu.tsx`.

    4. shadcn Sheet installieren: `pnpm --filter @genai/website dlx shadcn@latest add sheet --yes` (analog Fallback). Erwarteter Output: `apps/website/components/ui/sheet.tsx`.

    5. Falls shadcn weitere Peer-Deps hinzufügt (`@radix-ui/react-dropdown-menu`, `@radix-ui/react-dialog` für Sheet) — das ist erwünscht, einfach installieren lassen.

    6. Build-Verify: `pnpm --filter @genai/website build`. Build muss grün sein, `/` route muss `ƒ` zeigen (NICHT `○`). Falls nicht — STOPP, LEARNINGS.md re-readen, debuggen.

    NICHT die alten Sections jetzt schon löschen (passiert in Task 3). NICHT home-client.tsx jetzt anfassen (passiert in Task 3).
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && grep -q '"motion":' apps/website/package.json && test -f apps/website/lib/utils.ts && grep -q "twMerge" apps/website/lib/utils.ts && test -f apps/website/components/ui/dropdown-menu.tsx && test -f apps/website/components/ui/sheet.tsx && pnpm --filter @genai/website build 2>&1 | tee /tmp/build-20-01-t1.log | grep -E "ƒ\\s+/\\s" && echo "TASK 1 PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"motion":' apps/website/package.json` → exit 0
    - `apps/website/lib/utils.ts` existiert und enthält Token `twMerge` und `export function cn`
    - `apps/website/components/ui/dropdown-menu.tsx` existiert
    - `apps/website/components/ui/sheet.tsx` existiert
    - `pnpm --filter @genai/website build` exit code 0
    - Build-Output enthält Zeile mit `ƒ /` (force-dynamic auf Homepage), kein `○ /`
    - `pnpm-lock.yaml` enthält `motion` Eintrag (changed since baseline)
  </acceptance_criteria>
  <done>
    motion installiert, shadcn dropdown-menu + sheet als lokale Files vorhanden, cn-helper unter @/lib/utils ansprechbar, Build grün mit `ƒ /`. Bereit für Aceternity/MagicUI Copy-in in Task 2.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Copy-in Aceternity + MagicUI components, rewrite imports to motion/react, brand-rebrand</name>
  <files>
    apps/website/components/ui/aurora-background.tsx,
    apps/website/components/ui/bento-grid.tsx,
    apps/website/components/ui/infinite-moving-cards.tsx,
    apps/website/components/ui/lamp.tsx,
    apps/website/components/ui/number-ticker.tsx,
    apps/website/components/ui/marquee.tsx,
    apps/website/app/globals.css
  </files>
  <read_first>
    - apps/website/components.json (Aceternity-Registry-Entry verifizieren)
    - .planning/phases/20-navigation-landing-skeleton/20-RESEARCH.md § Component Recipes (alle Token-Mappings, Keyframes, Imports)
    - .planning/phases/20-navigation-landing-skeleton/20-RESEARCH.md § Open Questions OQ-1 + OQ-3 (CLI-Fallback + framer-motion-Rewrite)
    - apps/website/app/globals.css (verstehen wo @theme inline + @keyframes hinkommen)
    - packages/config/tailwind/base.css (Brand-Token-Klassen verifizieren: brand-neon-3 etc. vorhanden)
    - LEARNINGS.md (CSP — keine framer-motion-Imports erlaubt, motion/react inline-styles sind via style-src 'unsafe-inline' OK)
  </read_first>
  <action>
    Pro Komponente: erst CLI versuchen, bei Fehler manueller Copy-Paste von der jeweiligen Doku-URL aus 20-RESEARCH.md § Component Recipes. Nach jedem Copy SOFORT zwei Rewrites: (a) jeden Import `from "framer-motion"` ersetzen durch `from "motion/react"` (Pflicht — siehe OQ-3, framer-motion ist nicht installiert und wird nicht installiert), (b) Brand-Tokens umfärben gemäß Mapping aus 20-RESEARCH.md.

    **Reihenfolge:**

    1. **Aurora Background** — `cd apps/website && pnpm dlx shadcn@latest add @aceternity/aurora-background --yes`. URL-Fallback: https://ui.aceternity.com/components/aurora-background. Token-Rewrite (RESEARCH § D-08): jede `from-[#ff1493]`/`via-[#7b2ff7]`/`from-blue-500`/`via-purple-500`/`via-cyan-500`-Klasse ersetzen durch Brand-Klassen — Dark-Default: `from-brand-blue-5/30 via-brand-neon-4/20 to-transparent`, `.light`-Variante: `from-brand-pink-5/30 via-brand-red-4/20 to-transparent`. Dafür ggf. zwei Aurora-Layer übereinander mit `dark:block hidden` / `dark:hidden block` Klassen (Theme-Switching ohne JS-Abhängigkeit auf useTheme — Aurora ist BG, Theme-Wechsel via .light Class auf <html> reicht).

    2. **Bento Grid** — `cd apps/website && pnpm dlx shadcn@latest add @aceternity/bento-grid --yes`. URL-Fallback: https://ui.aceternity.com/components/bento-grid. Token-Rewrite: card-bg → `bg-bg-card`, border → `border-border`, hover-border → `hover:border-brand-neon-6`, hover-shadow → `hover:shadow-[0_0_20px_var(--accent-glow)]`.

    3. **Infinite Moving Cards** — `cd apps/website && pnpm dlx shadcn@latest add @aceternity/infinite-moving-cards --yes`. URL-Fallback: https://ui.aceternity.com/components/infinite-moving-cards. Token-Rewrite: gradient-from → `from-bg`, card-bg → `bg-bg-card border-border`. Komponente nutzt CSS `@keyframes scroll` — kommt in globals.css (siehe Punkt 7).

    4. **Lamp Effect** — `cd apps/website && pnpm dlx shadcn@latest add @aceternity/lamp --yes`. URL-Fallback: https://ui.aceternity.com/components/lamp-effect. Diese Komponente NUTZT motion/react (whileInView). KRITISCH: Import-Rewrite `from "framer-motion"` → `from "motion/react"`. Token-Rewrite: cyan/blue conic-gradient → `from-[var(--neon-9)]` für Dark, `from-[var(--red-9)]` für Light (theme-aware via dark:/.light: prefixes). Blur-Bg: `bg-brand-blue-5/60 dark:bg-brand-blue-5/60` und light-Variant analog mit `bg-brand-pink-5/60` (Light-Mode-Selector via `.light` auf <html> — die Komponente bekommt das automatisch durch Tailwind-Dark-Variant-Inversion). Hinweis: bei uns ist DARK default, daher dark: ist der Default-State, light: müssen wir nicht setzen — stattdessen die `.light` Class via Tailwind Custom-Variant ansprechen falls nötig. Pragmatisch: einfach beide Layer mit `dark:opacity-100 opacity-0` und umgekehrt stapeln.

    5. **Number Ticker (MagicUI)** — `cd apps/website && pnpm dlx shadcn@latest add @magicui/number-ticker --yes`. Falls Package-Name nicht klappt: `pnpm dlx shadcn@latest add "https://magicui.design/r/number-ticker.json"` ausprobieren. URL-Fallback: https://magicui.design/docs/components/number-ticker — manuell aus Doku kopieren. Import-Rewrite: framer-motion → motion/react. Keine Token-Änderung nötig (color via `text-*` der parent-klasse).

    6. **Marquee (MagicUI)** — `cd apps/website && pnpm dlx shadcn@latest add @magicui/marquee --yes` (Fallback analog). URL: https://magicui.design/docs/components/marquee. Reines CSS, keine framer-motion-Pflicht.

    7. **globals.css Keyframes ergänzen** (Pflicht, sonst funktionieren Aurora + Marquee + Infinite-Cards nicht). In `apps/website/app/globals.css` AM ENDE (nach allen bestehenden @theme Blöcken) anfügen:

    ```css
    /* ─── Phase 20: Aceternity/MagicUI Animation Keyframes ───────────────── */

    @theme inline {
      --animate-aurora: aurora 60s linear infinite;
      --animate-scroll: scroll var(--scroll-duration, 40s) linear infinite;
      --animate-marquee: marquee var(--duration, 40s) linear infinite;
      --animate-marquee-vertical: marquee-vertical var(--duration, 40s) linear infinite;
    }

    @keyframes aurora {
      from { background-position: 50% 50%, 50% 50%; }
      to { background-position: 350% 50%, 350% 50%; }
    }

    @keyframes scroll {
      to { transform: translate(calc(-50% - 0.5rem)); }
    }

    @keyframes marquee {
      from { transform: translateX(0); }
      to { transform: translateX(calc(-100% - var(--gap, 1rem))); }
    }

    @keyframes marquee-vertical {
      from { transform: translateY(0); }
      to { transform: translateY(calc(-100% - var(--gap, 1rem))); }
    }

    /* Reduced-Motion: alle Phase-20 Animationen pausieren (D-06 PFLICHT) */
    @media (prefers-reduced-motion: reduce) {
      .animate-aurora,
      .animate-scroll,
      .animate-marquee,
      .animate-marquee-vertical {
        animation-play-state: paused !important;
      }
    }
    ```

    8. **Verify**: `grep -r "framer-motion" apps/website/components/ui/ apps/website/components/sections/ 2>/dev/null` muss 0 Ergebnisse liefern. `grep -rE "from-(blue|cyan|purple|pink|violet)-[0-9]+" apps/website/components/ui/aurora-background.tsx apps/website/components/ui/lamp.tsx 2>/dev/null` muss 0 liefern (alle Tailwind-default-Farben rausgefärbt — nur `brand-*` oder `var(--…)`-Klassen erlaubt). Build: `pnpm --filter @genai/website build` grün, `/` muss `ƒ` sein.

    9. **CSP-Smoke** (lokaler Prod-Check, Pflicht laut D-28): `pnpm --filter @genai/website build && cd apps/website && NODE_ENV=production timeout 8 pnpm start &` dann `curl -sI http://localhost:3000 | grep -i "content-security-policy" | head -1` — CSP-Header muss `nonce-` enthalten. Background-Server killen.
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && test -f apps/website/components/ui/aurora-background.tsx && test -f apps/website/components/ui/bento-grid.tsx && test -f apps/website/components/ui/infinite-moving-cards.tsx && test -f apps/website/components/ui/lamp.tsx && test -f apps/website/components/ui/number-ticker.tsx && test -f apps/website/components/ui/marquee.tsx && ! grep -r "framer-motion" apps/website/components/ui/ apps/website/components/sections/ 2>/dev/null && grep -q "@keyframes aurora" apps/website/app/globals.css && grep -q "@keyframes scroll" apps/website/app/globals.css && grep -q "@keyframes marquee" apps/website/app/globals.css && grep -q "prefers-reduced-motion" apps/website/app/globals.css && pnpm --filter @genai/website build 2>&1 | tee /tmp/build-20-01-t2.log | grep -E "ƒ\\s+/\\s" && echo "TASK 2 PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Alle 6 UI-Files existieren in `apps/website/components/ui/`
    - `grep -r "framer-motion" apps/website/components/ui/ apps/website/components/sections/` returns 0 matches
    - `grep "@keyframes aurora" apps/website/app/globals.css` exit 0
    - `grep "@keyframes scroll" apps/website/app/globals.css` exit 0
    - `grep "@keyframes marquee" apps/website/app/globals.css` exit 0
    - `grep "prefers-reduced-motion" apps/website/app/globals.css` exit 0
    - Aurora + Lamp enthalten KEINE Tailwind-default Farb-Klassen wie `from-cyan-500`, `from-purple-500`, `via-blue-500`, `text-cyan-300` (grep -E "(from|via|to|text|bg|border)-(cyan|purple|violet|fuchsia|indigo)-[0-9]+" liefert 0)
    - Aurora + Lamp + Bento + InfiniteCards verwenden mindestens eine `brand-{neon|blue|pink|red}-` Klasse oder `var(--accent|--neon-9|--red-9)` Custom-Property
    - `pnpm --filter @genai/website build` exit 0, Output enthält `ƒ /`
    - Lokaler Prod-Server liefert CSP-Header der `nonce-` enthält
  </acceptance_criteria>
  <done>
    Alle externen Komponenten lokal in `apps/website/components/ui/`, alle motion-Imports sauber auf `motion/react`, alle Brand-Tokens umgefärbt, Animation-Keyframes in globals.css inkl. reduced-motion-Guard, Build grün und CSP-konform.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Delete legacy sections (D-21) + provisorisch home-client.tsx clean + create lighthouserc.json + landing.spec.ts skeleton</name>
  <files>
    apps/website/components/hero.tsx,
    apps/website/components/sections/features.tsx,
    apps/website/components/sections/target-audience.tsx,
    apps/website/components/sections/signup.tsx,
    apps/website/components/home-client.tsx,
    lighthouserc.json,
    packages/e2e-tools/tests/landing.spec.ts
  </files>
  <read_first>
    - apps/website/components/home-client.tsx (current imports — alle 4 zu löschen identifizieren)
    - apps/website/components/hero.tsx, sections/features.tsx, sections/target-audience.tsx, sections/signup.tsx (kurz scannen ob exports/types woanders konsumiert werden — falls ja: STOPP)
    - apps/website/tests/smoke.spec.ts (existierender CSP-Smoke-Pattern als Vorlage für landing.spec.ts)
    - .planning/phases/20-navigation-landing-skeleton/20-RESEARCH.md § Validation Architecture + § Testing Strategy (Test-Cases + lighthouserc-Vorschlag wörtlich)
    - .planning/phases/20-navigation-landing-skeleton/20-VALIDATION.md (Wave 0 Requirements Liste)
    - .planning/phases/20-navigation-landing-skeleton/20-CONTEXT.md (D-21 Liste der zu löschenden Files)
  </read_first>
  <action>
    1. **Cross-Reference-Check** (Sicherheits-Sweep): `grep -rE "from ['\"]@?/(components/hero|components/sections/(features|target-audience|signup))" apps/website/ packages/ 2>/dev/null` — sollte NUR Treffer in `apps/website/components/home-client.tsx` liefern. Falls Treffer woanders → STOPP, an Lucas eskalieren.

    2. **Sections löschen** (D-21):
    ```bash
    rm apps/website/components/hero.tsx
    rm apps/website/components/sections/features.tsx
    rm apps/website/components/sections/target-audience.tsx
    rm apps/website/components/sections/signup.tsx
    ```

    3. **home-client.tsx provisorisch cleanen** (vollständig ersetzen — minimal lauffähig, ohne neue Section-Imports). Wave 2 (Plan 02) baut die finale Struktur. Inhalt:
    ```tsx
    'use client'

    import { useState } from 'react'
    import { Header } from "@/components/layout/header"
    import { Footer } from "@/components/layout/footer"
    import { TerminalSplash } from "@/components/terminal-splash"

    export function HomeClient() {
      const [showContent, setShowContent] = useState(false)
      const [splashDone, setSplashDone] = useState(false)

      const handleSplashComplete = () => {
        setSplashDone(true)
        setTimeout(() => setShowContent(true), 50)
      }

      return (
        <>
          {!splashDone && (
            <TerminalSplash onComplete={handleSplashComplete} skipIfSeen={true} />
          )}

          <div
            className={`transition-all duration-700 ${
              showContent ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-8'
            }`}
          >
            <Header />
            <main id="main-content" className="min-h-screen pt-16">
              {/* Phase 20 Wave 2 (Plan 02) wires Header + sections here. */}
              {/* Phase 20 Wave 3 (Plans 03-05) fills in the section components. */}
            </main>
            <Footer />
          </div>
        </>
      )
    }
    ```

    4. **lighthouserc.json** im Repo-Root anlegen (gegen lokal gestarteten Prod-Build, Plan 06 startet den Server vor lhci-Run):
    ```json
    {
      "ci": {
        "collect": {
          "url": ["http://localhost:3000"],
          "numberOfRuns": 3,
          "settings": {
            "preset": "desktop",
            "chromeFlags": "--no-sandbox --headless"
          }
        },
        "assert": {
          "assertions": {
            "categories:performance": ["error", { "minScore": 0.9 }],
            "categories:accessibility": ["error", { "minScore": 0.9 }],
            "categories:best-practices": ["error", { "minScore": 0.9 }],
            "categories:seo": ["error", { "minScore": 0.9 }],
            "cumulative-layout-shift": ["error", { "maxNumericValue": 0.1 }]
          }
        },
        "upload": { "target": "filesystem", "outputDir": "./.lighthouseci" }
      }
    }
    ```

    5. **packages/e2e-tools/tests/landing.spec.ts** anlegen — Skeleton mit allen Tests die später in Plans 02-05 grün werden müssen. Tests dürfen jetzt skipped sein (`.skip`) oder failen — aber Datei + Test-Namen müssen existieren, damit Nyquist-Sampling pro Wave 0 erfüllt ist:

    ```ts
    import { test, expect, type ConsoleMessage } from "@playwright/test"

    const LANDING_URL = process.env.E2E_BASE_URL ?? "http://localhost:3000"

    test.describe("Phase 20 — Landing", () => {
      test("R1.1 — Nav-Dropdown 'Für Partner' opens on click", async ({ page }) => {
        await page.goto(LANDING_URL)
        await page.getByRole("button", { name: /Für Partner/i }).click()
        await expect(page.getByRole("menu")).toBeVisible()
      })

      test("R1.1 — Nav-Dropdown opens via keyboard (Enter) and closes via Escape", async ({ page }) => {
        await page.goto(LANDING_URL)
        const trigger = page.getByRole("button", { name: /Für Partner/i })
        await trigger.focus()
        await page.keyboard.press("Enter")
        await expect(page.getByRole("menu")).toBeVisible()
        await page.keyboard.press("Escape")
        await expect(page.getByRole("menu")).not.toBeVisible()
      })

      test("R1.1 — Mobile-Nav: Hamburger opens overlay, X closes", async ({ page }) => {
        await page.setViewportSize({ width: 375, height: 800 })
        await page.goto(LANDING_URL)
        await page.getByRole("button", { name: /Menü öffnen|Open menu/i }).click()
        await expect(page.getByRole("dialog")).toBeVisible()
        await page.getByRole("button", { name: /Menü schließen|Close/i }).click()
        await expect(page.getByRole("dialog")).not.toBeVisible()
      })

      test("R1.2 — Hero CTA links to /join", async ({ page }) => {
        await page.goto(LANDING_URL)
        const cta = page.getByRole("link", { name: /Jetzt beitreten/i }).first()
        await expect(cta).toHaveAttribute("href", /\/join/)
      })

      test("R1.5 — Tool-Showcase: 'Beispiel'-Badge sichtbar", async ({ page }) => {
        await page.goto(LANDING_URL)
        // Mindestens ein "Beispiel"-Badge muss in Tool-Showcase sichtbar sein
        const badges = page.getByText("Beispiel", { exact: true })
        await expect(badges.first()).toBeVisible()
      })

      test("R1.6 — Community-Preview: 'Beispiel'-Badge sichtbar", async ({ page }) => {
        await page.goto(LANDING_URL)
        // Plan 04 garantiert mindestens 2 Beispiel-Badges (Tool-Showcase + Community-Preview)
        const badges = page.getByText("Beispiel", { exact: true })
        await expect(await badges.count()).toBeGreaterThanOrEqual(2)
      })

      test("R1.8 — Trust marquee pauses on prefers-reduced-motion", async ({ browser }) => {
        const context = await browser.newContext({ reducedMotion: "reduce" })
        const page = await context.newPage()
        await page.goto(LANDING_URL)
        const marquee = page.locator(".animate-marquee").first()
        // Bei reduced-motion: animation-play-state = paused (durch globals.css Guard)
        const playState = await marquee.evaluate((el) => getComputedStyle(el).animationPlayState)
        expect(playState).toBe("paused")
        await context.close()
      })

      test("CSP — keine Console-Errors mit 'Content Security Policy' auf Landing", async ({ page }) => {
        const cspErrors: string[] = []
        page.on("console", (msg: ConsoleMessage) => {
          if (msg.type() === "error" && /Content Security Policy/i.test(msg.text())) {
            cspErrors.push(msg.text())
          }
        })
        await page.goto(LANDING_URL, { waitUntil: "networkidle" })
        expect(cspErrors).toHaveLength(0)
      })
    })
    ```

    6. **Build-Verify**: `pnpm --filter @genai/website build` muss grün sein (kein Section-Import mehr broken). `/` muss `ƒ` zeigen.

    7. **Skeleton-Test-Run** (Skip ist OK, Setup-Errors NICHT): `cd packages/e2e-tools && pnpm exec playwright test landing.spec.ts --list` muss 8 Tests listen (kein Parse-Error). Tests selbst werden gegen lokalen Stub failen — das ist erwartet, sie werden in Plans 02/04/05 grün.
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && test ! -f apps/website/components/hero.tsx && test ! -f apps/website/components/sections/features.tsx && test ! -f apps/website/components/sections/target-audience.tsx && test ! -f apps/website/components/sections/signup.tsx && test -f lighthouserc.json && test -f packages/e2e-tools/tests/landing.spec.ts && grep -q "minScore" lighthouserc.json && grep -q "test.describe" packages/e2e-tools/tests/landing.spec.ts && ! grep -E "from ['\"]@?/(components/hero|components/sections/(features|target-audience|signup))" apps/website/components/home-client.tsx && pnpm --filter @genai/website build 2>&1 | tee /tmp/build-20-01-t3.log | grep -E "ƒ\\s+/\\s" && cd packages/e2e-tools && pnpm exec playwright test landing.spec.ts --list 2>&1 | grep -E "Total: [0-9]+ tests" && echo "TASK 3 PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f apps/website/components/hero.tsx` exit 0 (gelöscht)
    - `test ! -f apps/website/components/sections/features.tsx` exit 0
    - `test ! -f apps/website/components/sections/target-audience.tsx` exit 0
    - `test ! -f apps/website/components/sections/signup.tsx` exit 0
    - `grep -rE "from ['\"]@?/(components/hero|components/sections/(features|target-audience|signup))" apps/ packages/` returns 0 matches (keine Orphan-Imports irgendwo)
    - `lighthouserc.json` existiert im Repo-Root und enthält `"minScore": 0.9` mind. 4×
    - `lighthouserc.json` enthält `"maxNumericValue": 0.1` für `cumulative-layout-shift`
    - `packages/e2e-tools/tests/landing.spec.ts` existiert und enthält `test.describe("Phase 20`
    - `landing.spec.ts` enthält Tests für R1.1 (3×), R1.2, R1.5, R1.6, R1.8, CSP (insgesamt ≥7 tests)
    - `pnpm --filter @genai/website build` exit 0
    - Build-Output enthält `ƒ /`, kein `○ /`
    - Playwright `--list` listet ≥7 tests in landing.spec.ts ohne Parse-Error
  </acceptance_criteria>
  <done>
    Alle vier Alt-Sections gelöscht, home-client.tsx kompiliert ohne Section-Imports, Build grün mit `ƒ /`, lighthouserc.json + landing.spec.ts Skeleton im Repo. Wave-0-Gaps aus 20-VALIDATION.md geschlossen. Nyquist-Validation kann landing.spec.ts referenzieren.
  </done>
</task>

</tasks>

<verification>
**Vor Plan-Abschluss prüfen:**

1. `pnpm --filter @genai/website build` → exit 0, Output enthält `ƒ /` (KEIN `○ /`)
2. `grep -r "framer-motion" apps/website/ 2>/dev/null` → 0 Treffer
3. `grep -rE "from ['\"]@?/(components/hero|components/sections/(features|target-audience|signup))" apps/ packages/` → 0 Treffer (alle Orphan-Imports weg)
4. Alle 6 Aceternity/MagicUI Files + cn-helper + dropdown-menu + sheet existieren in `apps/website/components/ui/` bzw. `apps/website/lib/`
5. `globals.css` enthält `@keyframes aurora`, `@keyframes scroll`, `@keyframes marquee`, `prefers-reduced-motion`-Guard
6. `lighthouserc.json` + `packages/e2e-tools/tests/landing.spec.ts` existieren
7. Lokaler Prod-Smoke: Server starten, `curl -I localhost:3000` zeigt CSP-Header mit `nonce-`

**Update VALIDATION.md:** Per-Task Verification Map um diese 3 Tasks ergänzen (Plan 20-01, Wave 1, Test-Type "Build/Grep/Playwright-list", Status nach Execution updaten).
</verification>

<success_criteria>
- motion@^12 in `apps/website/package.json`, lock-file aktualisiert
- 8 neue Files in `apps/website/components/ui/`: dropdown-menu, sheet, aurora-background, bento-grid, infinite-moving-cards, lamp, number-ticker, marquee
- 1 neue Datei `apps/website/lib/utils.ts` mit cn()-Helper
- 4 alte Section-Files gelöscht ohne Compile-Fehler
- globals.css um Phase-20 Animation-Keyframes + reduced-motion-Guard erweitert
- lighthouserc.json + landing.spec.ts angelegt (Wave-0-Gaps geschlossen)
- Build grün mit `ƒ /`
- Plans 02-06 können parallel/sequenziell aufsetzen
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/20-navigation-landing-skeleton/20-01-SUMMARY.md` mit:
- Liste aller installierten Packages + Versionen
- Liste aller kopierten Components + URL-Quelle
- Token-Mapping-Notizen pro Komponente (was wurde von was zu was umbenannt)
- Build-Output-Snippet (`ƒ /` als Beweis)
- CSP-Smoke-Curl-Output
- Hinweise/Issues für Plan 02 (z.B. falls shadcn-CLI hakte und manueller Copy-Paste verwendet wurde)
</output>
