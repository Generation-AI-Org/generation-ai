---
phase: 20
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - apps/website/components/layout/header.tsx
  - apps/website/components/layout/footer.tsx
  - apps/website/components/home-client.tsx
  - apps/website/components/sections/hero-section.tsx
  - apps/website/components/sections/discrepancy-section.tsx
  - apps/website/components/sections/offering-section.tsx
  - apps/website/components/sections/tool-showcase-section.tsx
  - apps/website/components/sections/community-preview-section.tsx
  - apps/website/components/sections/audience-split-section.tsx
  - apps/website/components/sections/trust-section.tsx
  - apps/website/components/sections/final-cta-section.tsx
  - apps/website/app/layout.tsx
autonomous: true
requirements: [R1.1, R1.10]
must_haves:
  truths:
    - "Header has Desktop-Nav with Tools (external), Community (external), Für Partner (dropdown), Über uns (/about), [Jetzt beitreten] (primary CTA → /join)"
    - "Header dropdown 'Für Partner' opens via click and via keyboard (Enter), closes via Escape"
    - "Header has Mobile-Hamburger that opens a Sheet overlay containing all nav items + accordion-style Partner sub-items"
    - "Theme-Toggle remains in Header in a logical position relative to new nav density (D-20)"
    - "Footer has Logo + Sitemap (Über uns, Tools, Community, Für Partner, Jetzt beitreten) + Legal (Impressum, Datenschutz) + Social (LinkedIn) + Kontakt-Mail (admin@generation-ai.org) + Copyright"
    - "MotionConfig with request nonce wraps the landing tree (CSP-clean Motion styles)"
    - "All 8 section files exist as empty stubs that export their named function (compile-clean)"
    - "home-client.tsx imports all 8 section stubs in correct order and renders them inside <main>"
    - "Build is green and / route is ƒ (dynamic)"
    - "landing.spec.ts tests for R1.1 (Desktop dropdown click + keyboard, Mobile-Nav hamburger) pass"
  artifacts:
    - path: "apps/website/components/layout/header.tsx"
      provides: "Restructured header with new nav, dropdown, mobile sheet, theme toggle"
      contains: "DropdownMenu"
    - path: "apps/website/components/layout/footer.tsx"
      provides: "Extended footer with sitemap + social + contact"
      contains: "LinkedIn"
    - path: "apps/website/components/home-client.tsx"
      provides: "Landing root with all section stubs mounted in correct order"
      contains: "HeroSection"
    - path: "apps/website/components/sections/hero-section.tsx"
      provides: "Empty stub for Plan 03"
      exports: ["HeroSection"]
    - path: "apps/website/components/sections/discrepancy-section.tsx"
      provides: "Empty stub for Plan 03"
      exports: ["DiscrepancySection"]
    - path: "apps/website/components/sections/offering-section.tsx"
      provides: "Empty stub for Plan 04"
      exports: ["OfferingSection"]
    - path: "apps/website/components/sections/tool-showcase-section.tsx"
      provides: "Empty stub for Plan 04"
      exports: ["ToolShowcaseSection"]
    - path: "apps/website/components/sections/community-preview-section.tsx"
      provides: "Empty stub for Plan 04"
      exports: ["CommunityPreviewSection"]
    - path: "apps/website/components/sections/audience-split-section.tsx"
      provides: "Empty stub for Plan 05"
      exports: ["AudienceSplitSection"]
    - path: "apps/website/components/sections/trust-section.tsx"
      provides: "Empty stub for Plan 05"
      exports: ["TrustSection"]
    - path: "apps/website/components/sections/final-cta-section.tsx"
      provides: "Empty stub for Plan 05"
      exports: ["FinalCTASection"]
    - path: "apps/website/app/layout.tsx"
      provides: "MotionConfig wrapper with request nonce"
      contains: "MotionConfig"
  key_links:
    - from: "apps/website/components/layout/header.tsx"
      to: "@/components/ui/dropdown-menu + @/components/ui/sheet"
      via: "ES imports"
      pattern: "from ['\"]@/components/ui/(dropdown-menu|sheet)['\"]"
    - from: "apps/website/components/home-client.tsx"
      to: "all 8 section stubs"
      via: "ES imports + JSX"
      pattern: "from ['\"]@/components/sections/"
    - from: "apps/website/app/layout.tsx"
      to: "x-nonce request header from proxy.ts"
      via: "headers() server API"
      pattern: "headers\\(\\)"
---

<objective>
Wave-2 Layout-Shell für Phase 20: Bestehender Header wird zu einer vollwertigen Top-Nav mit "Für Partner"-Dropdown und Mobile-Sheet umgebaut (D-17/D-18/D-19). Footer wird zu einer ausführlichen Variante mit Sitemap + Social + Kontakt erweitert (R1.10). home-client.tsx wird mit allen 8 leeren Section-Stubs verkabelt — die Sections werden in Wave 3 (Plans 03-05) inhaltlich gefüllt, aber die Mount-Reihenfolge und die Boundary für `files_modified` werden hier final festgelegt. MotionConfig mit Request-Nonce wird im Root-Layout verankert (CSP-Sauberkeit).

Purpose: Saubere Wave-3-Boundary. Plans 03/04/05 können parallel Sections befüllen ohne sich um home-client.tsx, Mount-Order oder shadcn-Wiring zu streiten. Die zwei R1.1-Anforderungen (Dropdown + Mobile-Nav A11y) werden hier vollständig erfüllt — Wave 3 muss Nav nicht mehr anfassen.

Output: Lauffähiges Layout-Gerüst mit funktionierender Navigation, sichtbarem (aber leerem) Section-Skeleton und CSP-konformer Motion-Integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-navigation-landing-skeleton/20-CONTEXT.md
@.planning/phases/20-navigation-landing-skeleton/20-RESEARCH.md
@.planning/phases/20-navigation-landing-skeleton/20-VALIDATION.md
@.planning/phases/20-navigation-landing-skeleton/20-01-PLAN.md
@LEARNINGS.md
@apps/website/AGENTS.md
@apps/website/components/layout/header.tsx
@apps/website/components/layout/footer.tsx
@apps/website/components/home-client.tsx
@apps/website/components/terminal-splash.tsx
@apps/website/components/ThemeProvider.tsx
@apps/website/app/layout.tsx
@apps/website/proxy.ts
@apps/website/lib/csp.ts
@packages/config/tailwind/base.css
@packages/e2e-tools/tests/landing.spec.ts

<interfaces>
<!-- aus Plan 01 jetzt verfügbar: -->
<!-- @/components/ui/dropdown-menu — exports: DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem, DropdownMenuLabel -->
<!-- @/components/ui/sheet — exports: Sheet, SheetTrigger, SheetContent, SheetHeader, SheetTitle, SheetClose -->
<!-- @/lib/utils — exports: cn (twMerge + clsx) -->

<!-- Bestehend (NICHT modifizieren): -->
<!-- @genai/ui — exports: Logo (props: context, theme, size, colorway, className) -->
<!-- @/components/ThemeProvider — exports: useTheme() returning { theme, toggleTheme } -->
<!-- @/components/terminal-splash — exports: TerminalSplash (props: onComplete, skipIfSeen) -->

<!-- Nav-Struktur (D-18, locked): -->
<!-- Tools (extern, target=_blank, rel=noopener noreferrer) → https://tools.generation-ai.org -->
<!-- Community (extern, target=_blank, rel=noopener noreferrer) → https://community.generation-ai.org -->
<!-- Für Partner ▾ (Dropdown D-17) — items: Unternehmen → /partner#unternehmen, Stiftungen → /partner#stiftungen, Hochschulen → /partner#hochschulen -->
<!-- Über uns → /about -->
<!-- [Jetzt beitreten] (Primary-CTA-Button) → /join -->

<!-- KEIN Login-Button (D-18 explicit) -->

<!-- Mobile-Nav-Pattern (D-19): -->
<!-- Hamburger-Icon (lucide Menu) → opens Sheet (side="right" oder "top", full-screen) -->
<!-- Sheet-Inhalt: gleiche Items, "Für Partner" als Accordion-Section mit den 3 Sub-Items -->
<!-- Stagger-Animation via motion (initial=opacity-0+y, animate=opacity-1+y-0, transition.delay=index*0.05) -->
<!-- Reduced-motion: animations skip via useReducedMotion (D-06) -->

<!-- MotionConfig nonce-Pattern (RESEARCH § CSP & Performance Gates): -->
<!-- import { headers } from "next/headers" -->
<!-- import { MotionConfig } from "motion/react" -->
<!-- const nonce = (await headers()).get("x-nonce") ?? "" -->
<!-- <MotionConfig nonce={nonce}>{children}</MotionConfig> -->
<!-- WICHTIG: layout.tsx ist Server-Component, MotionConfig braucht client — pragmatisch: nonce in layout.tsx lesen, dann an einen ClientWrapper als prop reichen, ODER MotionConfig direkt in home-client.tsx einsetzen und nonce via Server-Action/headers vom Server-Page übergeben. EMPFEHLUNG: nonce in app/page.tsx (server) lesen, an HomeClient als prop übergeben, dort <MotionConfig nonce={nonce}> einbauen. Das ist der saubere Weg ohne app/layout.tsx zu zerlegen. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Restructure header.tsx — Desktop nav with shadcn DropdownMenu + Mobile Sheet + brand-token CTA</name>
  <files>apps/website/components/layout/header.tsx</files>
  <read_first>
    - apps/website/components/layout/header.tsx (current implementation — Theme-Toggle, Logo, einfacher CTA-Link)
    - apps/website/components/ui/dropdown-menu.tsx (exports + composition pattern aus shadcn)
    - apps/website/components/ui/sheet.tsx (Sheet + SheetTrigger + SheetContent API)
    - .planning/phases/20-navigation-landing-skeleton/20-CONTEXT.md (D-17, D-18, D-19, D-20 — Nav-Items locked)
    - .planning/phases/20-navigation-landing-skeleton/20-RESEARCH.md § Architecture Patterns (Reduced-Motion-Pattern für Mobile-Stagger)
    - packages/config/tailwind/base.css § Component Standards (Button-Pattern: primary = `bg-[var(--accent)] text-[var(--text-on-accent)] rounded-full px-4 py-2.5`)
    - packages/e2e-tools/tests/landing.spec.ts (welche role-Attribute + accessible-names die Tests erwarten — Button name "Für Partner", Button name "Menü öffnen"|"Open menu", Button name "Menü schließen"|"Close")
  </read_first>
  <action>
    Datei `apps/website/components/layout/header.tsx` vollständig ersetzen. Ziel: ein Header der auf Desktop (md+) die volle Nav anzeigt und auf Mobile (< md) auf Hamburger + Sheet zurückfällt.

    **Spezifikation (locked aus CONTEXT D-17/D-18/D-19/D-20):**

    Datei beginnt mit `'use client'`. Imports: `Link` (next/link), `useState`, `useTheme` (@/components/ThemeProvider), `Logo` (@genai/ui), `Menu` + `X` + `ChevronDown` (lucide-react), `motion` + `useReducedMotion` (motion/react), `DropdownMenu`, `DropdownMenuTrigger`, `DropdownMenuContent`, `DropdownMenuItem`, `DropdownMenuLabel` (@/components/ui/dropdown-menu), `Sheet`, `SheetTrigger`, `SheetContent`, `SheetClose`, `SheetTitle`, `SheetHeader` (@/components/ui/sheet), `cn` (@/lib/utils).

    **Nav-Items als Konstante** (Single Source of Truth — Desktop + Mobile teilen sie):
    ```ts
    const navLinks = [
      { label: "Tools", href: "https://tools.generation-ai.org", external: true },
      { label: "Community", href: "https://community.generation-ai.org", external: true },
    ] as const

    const partnerSubItems = [
      { label: "Unternehmen", href: "/partner#unternehmen" },
      { label: "Stiftungen", href: "/partner#stiftungen" },
      { label: "Hochschulen", href: "/partner#hochschulen" },
    ] as const

    const aboutLink = { label: "Über uns", href: "/about" } as const
    ```

    **Header-Struktur:**

    ```tsx
    <header className="fixed top-0 left-0 right-0 z-50 bg-bg-header border-b border-white/10">
      <a href="#main-content" className="skip-link">Zum Inhalt springen</a>

      <nav aria-label="Hauptnavigation" className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex items-center justify-between h-16">

          {/* Logo */}
          <Link href="/" className="flex items-center" aria-label="Generation AI - Startseite">
            <Logo context="header" theme={theme} size="md" className="hover:opacity-90 transition-opacity" />
          </Link>

          {/* Desktop Nav (md+) */}
          <div className="hidden md:flex items-center gap-1">
            {/* Tools, Community */}
            {navLinks.map((link) => (
              <a key={link.href} href={link.href} target="_blank" rel="noopener noreferrer"
                 className="px-3 py-2 text-sm font-mono text-[var(--text-on-header)] hover:text-[var(--text-on-header-hover)] transition-colors">
                {link.label}
              </a>
            ))}

            {/* Für Partner Dropdown */}
            <DropdownMenu>
              <DropdownMenuTrigger
                className="px-3 py-2 text-sm font-mono text-[var(--text-on-header)] hover:text-[var(--text-on-header-hover)] inline-flex items-center gap-1 transition-colors"
                aria-label="Für Partner Untermenü öffnen"
              >
                Für Partner
                <ChevronDown className="w-3.5 h-3.5" aria-hidden="true" />
              </DropdownMenuTrigger>
              <DropdownMenuContent align="end" className="bg-bg-card border-border">
                <DropdownMenuLabel className="text-text-muted text-xs font-mono uppercase tracking-wider">
                  Für Partner
                </DropdownMenuLabel>
                {partnerSubItems.map((item) => (
                  <DropdownMenuItem key={item.href} asChild>
                    <Link href={item.href} className="font-mono text-sm">
                      {item.label}
                    </Link>
                  </DropdownMenuItem>
                ))}
              </DropdownMenuContent>
            </DropdownMenu>

            {/* Über uns */}
            <Link href={aboutLink.href}
                  className="px-3 py-2 text-sm font-mono text-[var(--text-on-header)] hover:text-[var(--text-on-header-hover)] transition-colors">
              {aboutLink.label}
            </Link>
          </div>

          {/* Right side: Theme Toggle + CTA + Mobile Hamburger */}
          <div className="flex items-center gap-2">
            {/* Theme Toggle (existing pattern preserved, D-20) */}
            <button
              onClick={toggleTheme}
              className={cn(
                "p-2.5 rounded-lg transition-colors min-w-[44px] min-h-[44px] flex items-center justify-center cursor-pointer",
                theme === 'dark' ? 'bg-white/10 hover:bg-white/20' : 'bg-black/20 hover:bg-black/30'
              )}
              aria-label={theme === 'dark' ? 'Light Mode aktivieren' : 'Dark Mode aktivieren'}
            >
              {/* SVGs aus aktueller header.tsx übernehmen — sun/moon */}
            </button>

            {/* Primary CTA (Desktop only, on Mobile it's in the Sheet) */}
            <Link
              href="/join"
              className="hidden md:inline-flex bg-[var(--accent)] text-[var(--text-on-accent)] px-4 py-2 rounded-full text-sm font-mono font-bold hover:opacity-90 transition-opacity"
            >
              Jetzt beitreten
            </Link>

            {/* Mobile Hamburger */}
            <div className="md:hidden">
              <Sheet>
                <SheetTrigger asChild>
                  <button
                    aria-label="Menü öffnen"
                    className="p-2.5 rounded-lg min-w-[44px] min-h-[44px] flex items-center justify-center text-[var(--text-on-header)] hover:bg-white/10"
                  >
                    <Menu className="w-6 h-6" aria-hidden="true" />
                  </button>
                </SheetTrigger>
                <SheetContent side="right" className="bg-bg-card border-border w-full sm:max-w-sm">
                  <SheetHeader>
                    <SheetTitle className="font-mono">Navigation</SheetTitle>
                  </SheetHeader>
                  <MobileNavList prefersReducedMotion={prefersReducedMotion} />
                </SheetContent>
              </Sheet>
            </div>
          </div>
        </div>
      </nav>
    </header>
    ```

    **MobileNavList** als interne Komponente (gleiche Datei):
    - Rendert die navLinks mit Stagger-Motion (initial: opacity 0, y: 10; animate: opacity 1, y: 0; transition: delay = index * 0.05). Bei `prefersReducedMotion === true`: motion.div initial={false} (keine Animation).
    - Partner-Section als Accordion: Button "Für Partner" toggelt einen kollabierbaren Block mit den 3 Sub-Items (`useState` für `isPartnerOpen`).
    - Schluss: SheetClose als großer Primary-Button "Jetzt beitreten" → /join (gleiche Optik wie Desktop-CTA, aber width-full).
    - Close-Button: SheetContent rendert standardmäßig ein X mit `aria-label` — falls die shadcn-Default-Variante "Close" heißt, das per `<SheetClose aria-label="Menü schließen">` überschreiben (damit Test ihn findet).

    **Top-Level-State im Header:**
    ```ts
    const { theme, toggleTheme } = useTheme()
    const prefersReducedMotion = useReducedMotion()
    ```

    **Wichtige Don'ts:**
    - KEINE inline `<svg>` für Theme-Toggle ändern — bestehende SVG-Pfade wörtlich übernehmen, nur die Wrapper-Klassen anpassen.
    - KEIN Login-Button irgendwo (D-18 explicit).
    - KEIN `<a href="#signup">` mehr (alte Section ist gelöscht — Plan 01).
    - KEIN Zugriff auf bestehende Header-State außer `theme`/`toggleTheme` (kein Mobile-Menu-State auf Header-Level — Sheet managed sich selbst).

    **Verification (im Task selbst):**
    - `pnpm --filter @genai/website build` → grün, `ƒ /`
    - `pnpm --filter @genai/website lint` → 0 Errors auf header.tsx
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && grep -q "DropdownMenu" apps/website/components/layout/header.tsx && grep -q "Sheet" apps/website/components/layout/header.tsx && grep -q "Menü öffnen" apps/website/components/layout/header.tsx && grep -q "Für Partner" apps/website/components/layout/header.tsx && grep -q 'href="/join"' apps/website/components/layout/header.tsx && grep -q 'href="https://tools.generation-ai.org"' apps/website/components/layout/header.tsx && grep -q 'href="https://community.generation-ai.org"' apps/website/components/layout/header.tsx && grep -q 'href="/about"' apps/website/components/layout/header.tsx && grep -q 'href="/partner#unternehmen"' apps/website/components/layout/header.tsx && ! grep -qi "login\|sign in\|anmelden" apps/website/components/layout/header.tsx && grep -q "useReducedMotion" apps/website/components/layout/header.tsx && pnpm --filter @genai/website build 2>&1 | tee /tmp/build-20-02-t1.log | grep -E "ƒ\\s+/\\s" && echo "TASK 1 PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "DropdownMenu" header.tsx` exit 0 (D-17 erfüllt)
    - `grep "Sheet" header.tsx` exit 0 (D-19 erfüllt)
    - `grep "Menü öffnen" header.tsx` exit 0 (passt zum Test in landing.spec.ts)
    - Header-Datei enthält alle 5 Nav-Links: `https://tools.generation-ai.org`, `https://community.generation-ai.org`, `/partner#unternehmen`, `/partner#stiftungen`, `/partner#hochschulen`, `/about`, `/join` (D-18)
    - Externe Links haben `target="_blank"` UND `rel="noopener noreferrer"` (grep beides → exit 0)
    - KEIN Treffer für `Login`, `Sign in`, `Anmelden` in header.tsx (D-18 negativer Check)
    - `useReducedMotion` wird importiert (D-06)
    - Build grün, `ƒ /` im Output
    - `pnpm --filter @genai/website lint` exit 0
  </acceptance_criteria>
  <done>
    Header rendert vollständige Desktop-Nav mit funktionalem Dropdown und Mobile-Hamburger mit Sheet-Overlay. Alle Nav-Links zeigen auf die laut D-18 spezifizierten Routes. Theme-Toggle bleibt erhalten. Build + Lint grün.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend footer.tsx — Sitemap + Social (LinkedIn) + Kontakt-Mail + Copyright</name>
  <files>apps/website/components/layout/footer.tsx</files>
  <read_first>
    - apps/website/components/layout/footer.tsx (current — minimal Footer mit nur Impressum/Datenschutz/Kontakt)
    - .planning/phases/20-navigation-landing-skeleton/20-CONTEXT.md (D-16: klassisch, keine Animation)
    - .planning/REQUIREMENTS.md § R1.10 (Footer — Legal, Sitemap, Social (LinkedIn), Kontakt-Mail, Copyright)
    - packages/config/tailwind/base.css § Component Standards (Footer-Typo: font-mono, Brand-Tokens)
  </read_first>
  <action>
    Datei `apps/website/components/layout/footer.tsx` erweitern. Ziel: 4 Spalten auf Desktop (Logo+Tagline | Sitemap | Legal | Kontakt+Social), 1 Spalte stacked auf Mobile.

    Bestehende Imports beibehalten (`Link`, `useTheme`, `Logo`). Hinzufügen: `Linkedin` (lucide-react).

    **Struktur:**

    ```tsx
    <footer className="bg-bg-header border-t border-white/10 pt-10 pb-6">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="grid grid-cols-1 md:grid-cols-4 gap-8 mb-8">

          {/* Col 1: Logo + Tagline */}
          <div>
            <Link href="/" aria-label="Generation AI - Startseite">
              <Logo context="footer" theme={theme} colorway={theme === "dark" ? "neon" : "auto"} size="md" />
            </Link>
            <p className="mt-4 text-sm font-mono text-[var(--text-on-header)] max-w-xs">
              Die KI-Community für Studierende im DACH-Raum.
            </p>
          </div>

          {/* Col 2: Sitemap */}
          <nav aria-label="Sitemap">
            <h3 className="text-xs font-mono font-bold uppercase tracking-wider text-[var(--text-on-header)] mb-4">
              Entdecken
            </h3>
            <ul className="space-y-2 text-sm font-mono">
              <li><Link href="/about" className="text-[var(--accent)] hover:text-[var(--accent-hover)] transition-colors">Über uns</Link></li>
              <li><a href="https://tools.generation-ai.org" target="_blank" rel="noopener noreferrer" className="text-[var(--accent)] hover:text-[var(--accent-hover)] transition-colors">Tools</a></li>
              <li><a href="https://community.generation-ai.org" target="_blank" rel="noopener noreferrer" className="text-[var(--accent)] hover:text-[var(--accent-hover)] transition-colors">Community</a></li>
              <li><Link href="/partner" className="text-[var(--accent)] hover:text-[var(--accent-hover)] transition-colors">Für Partner</Link></li>
              <li><Link href="/join" className="text-[var(--accent)] hover:text-[var(--accent-hover)] transition-colors">Jetzt beitreten</Link></li>
            </ul>
          </nav>

          {/* Col 3: Legal */}
          <nav aria-label="Rechtliches">
            <h3 className="text-xs font-mono font-bold uppercase tracking-wider text-[var(--text-on-header)] mb-4">
              Rechtliches
            </h3>
            <ul className="space-y-2 text-sm font-mono">
              <li><Link href="/impressum" className="text-[var(--accent)] hover:text-[var(--accent-hover)] transition-colors">Impressum</Link></li>
              <li><Link href="/datenschutz" className="text-[var(--accent)] hover:text-[var(--accent-hover)] transition-colors">Datenschutz</Link></li>
            </ul>
          </nav>

          {/* Col 4: Kontakt + Social */}
          <div>
            <h3 className="text-xs font-mono font-bold uppercase tracking-wider text-[var(--text-on-header)] mb-4">
              Kontakt
            </h3>
            <ul className="space-y-2 text-sm font-mono">
              <li>
                <a href="mailto:admin@generation-ai.org" className="text-[var(--accent)] hover:text-[var(--accent-hover)] transition-colors">
                  admin@generation-ai.org
                </a>
              </li>
              <li>
                <a href="https://www.linkedin.com/company/generation-ai-org/" target="_blank" rel="noopener noreferrer"
                   className="inline-flex items-center gap-2 text-[var(--accent)] hover:text-[var(--accent-hover)] transition-colors"
                   aria-label="Generation AI auf LinkedIn">
                  <Linkedin className="w-4 h-4" aria-hidden="true" />
                  LinkedIn
                </a>
              </li>
            </ul>
          </div>

        </div>

        {/* Bottom bar: Copyright */}
        <div className="border-t border-white/10 pt-6 flex flex-col sm:flex-row items-center justify-between gap-3">
          <p className="text-xs font-mono text-[var(--text-on-header)]">
            © {new Date().getFullYear()} Generation AI e.V. (i.G.). Alle Rechte vorbehalten.
          </p>
          <p className="text-xs font-mono text-[var(--text-on-header)]">
            Made with care in Berlin & Hamburg.
          </p>
        </div>

      </div>
    </footer>
    ```

    **Pflicht-Verifications:**
    - `pnpm --filter @genai/website build` → grün, `ƒ /`
    - LinkedIn-URL muss target=_blank + rel=noopener noreferrer haben
    - Mailto-Link muss `admin@generation-ai.org` sein (nicht `info@`)

    Hinweis: Falls die finale LinkedIn-URL noch nicht stimmt — Lucas finalisiert sie später. Wir nehmen `https://www.linkedin.com/company/generation-ai-org/` als plausiblen Default. Im SUMMARY.md flaggen.
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && grep -q "Sitemap" apps/website/components/layout/footer.tsx && grep -q "linkedin.com" apps/website/components/layout/footer.tsx && grep -q "admin@generation-ai.org" apps/website/components/layout/footer.tsx && grep -q "Impressum" apps/website/components/layout/footer.tsx && grep -q "Datenschutz" apps/website/components/layout/footer.tsx && grep -q 'href="/join"' apps/website/components/layout/footer.tsx && grep -q "Generation AI e.V" apps/website/components/layout/footer.tsx && pnpm --filter @genai/website build 2>&1 | grep -E "ƒ\\s+/\\s" && echo "TASK 2 PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Footer enthält 4-Spalten-Grid (`md:grid-cols-4`) — grep `md:grid-cols-4` exit 0
    - LinkedIn-Link mit target=_blank + rel=noopener noreferrer
    - Mailto-Link `admin@generation-ai.org` vorhanden
    - Sitemap-Links: /about, /partner, /join, tools.generation-ai.org, community.generation-ai.org (alle 5 grep-Treffer)
    - Legal-Links: /impressum, /datenschutz
    - Copyright-Zeile mit `Generation AI e.V` und Jahreszahl
    - Build grün, `ƒ /` im Output
  </acceptance_criteria>
  <done>
    Footer erfüllt R1.10 vollständig: Sitemap (5 Links), Legal (2 Links), Kontakt (Mail + LinkedIn-Social), Copyright mit Vereinsnennung. Mobile stacked, Desktop 4-spaltig.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create 8 section stubs + wire home-client.tsx + add MotionConfig with request nonce in Server-Page</name>
  <files>
    apps/website/components/sections/hero-section.tsx,
    apps/website/components/sections/discrepancy-section.tsx,
    apps/website/components/sections/offering-section.tsx,
    apps/website/components/sections/tool-showcase-section.tsx,
    apps/website/components/sections/community-preview-section.tsx,
    apps/website/components/sections/audience-split-section.tsx,
    apps/website/components/sections/trust-section.tsx,
    apps/website/components/sections/final-cta-section.tsx,
    apps/website/components/home-client.tsx,
    apps/website/app/page.tsx
  </files>
  <read_first>
    - apps/website/components/home-client.tsx (current minimaler Stub aus Plan 01 Task 3)
    - apps/website/app/page.tsx (current — `<HomeClient />` Server-Wrapper)
    - apps/website/proxy.ts (request.headers.set("x-nonce", nonce) — woher nonce kommt)
    - apps/website/lib/csp.ts (style-src 'unsafe-inline' bestätigen — MotionConfig nonce ist optional aber sauber)
    - .planning/phases/20-navigation-landing-skeleton/20-RESEARCH.md § Architecture Patterns + § CSP & Performance Gates (Motion + CSP Nonce Pattern)
    - .planning/phases/20-navigation-landing-skeleton/20-CONTEXT.md (Section-Reihenfolge aus D-08 bis D-15)
    - LEARNINGS.md (force-dynamic Pflicht — `await headers()` erzwingt dynamic, das ist gewünscht da Root-Layout bereits force-dynamic ist)
  </read_first>
  <action>
    **Section-Reihenfolge** (locked aus REQUIREMENTS R1.2-R1.9):
    1. Hero (R1.2)
    2. Discrepancy (R1.3)
    3. Offering / 4-Card (R1.4)
    4. Tool Showcase (R1.5)
    5. Community Preview (R1.6)
    6. Audience Split (R1.7)
    7. Trust (R1.8)
    8. Final CTA (R1.9)

    **Schritt 1: 8 Stub-Files** in `apps/website/components/sections/` anlegen. Pro Datei minimaler Inhalt (sichtbarer Platzhalter, damit Build durchläuft und Reihenfolge im DOM verifizierbar ist):

    Beispiel `hero-section.tsx`:
    ```tsx
    'use client'

    export function HeroSection() {
      return (
        <section
          aria-labelledby="hero-stub-heading"
          data-section="hero"
          className="min-h-[60vh] flex items-center justify-center bg-bg border-b border-border"
        >
          <h2 id="hero-stub-heading" className="font-mono text-text-muted">
            [Hero — Plan 03 Task 1]
          </h2>
        </section>
      )
    }
    ```

    Analog für die anderen 7 mit jeweils:
    - `data-section`: `discrepancy`, `offering`, `tool-showcase`, `community-preview`, `audience-split`, `trust`, `final-cta`
    - `aria-labelledby` mit eindeutiger ID (`{section}-stub-heading`)
    - `[Plan 03 Task 2]` für Discrepancy, `[Plan 04 Task 1/2/3]` für Offering/ToolShowcase/CommunityPreview, `[Plan 05 Task 1/2/3]` für AudienceSplit/Trust/FinalCTA — als Hinweis im Code wer die Section füllen soll.

    **Schritt 2: home-client.tsx** vollständig ersetzen. Importiert alle 8 Sections + MotionConfig + nimmt nonce als prop entgegen:

    ```tsx
    'use client'

    import { useState } from 'react'
    import { MotionConfig } from "motion/react"
    import { Header } from "@/components/layout/header"
    import { Footer } from "@/components/layout/footer"
    import { TerminalSplash } from "@/components/terminal-splash"
    import { HeroSection } from "@/components/sections/hero-section"
    import { DiscrepancySection } from "@/components/sections/discrepancy-section"
    import { OfferingSection } from "@/components/sections/offering-section"
    import { ToolShowcaseSection } from "@/components/sections/tool-showcase-section"
    import { CommunityPreviewSection } from "@/components/sections/community-preview-section"
    import { AudienceSplitSection } from "@/components/sections/audience-split-section"
    import { TrustSection } from "@/components/sections/trust-section"
    import { FinalCTASection } from "@/components/sections/final-cta-section"

    type HomeClientProps = {
      nonce: string
    }

    export function HomeClient({ nonce }: HomeClientProps) {
      const [showContent, setShowContent] = useState(false)
      const [splashDone, setSplashDone] = useState(false)

      const handleSplashComplete = () => {
        setSplashDone(true)
        setTimeout(() => setShowContent(true), 50)
      }

      return (
        <MotionConfig nonce={nonce}>
          {!splashDone && (
            <TerminalSplash onComplete={handleSplashComplete} skipIfSeen={true} />
          )}
          <div
            className={`transition-all duration-700 ${
              showContent ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-8'
            }`}
          >
            <Header />
            <main id="main-content" className="min-h-screen pt-16">
              <HeroSection />
              <DiscrepancySection />
              <OfferingSection />
              <ToolShowcaseSection />
              <CommunityPreviewSection />
              <AudienceSplitSection />
              <TrustSection />
              <FinalCTASection />
            </main>
            <Footer />
          </div>
        </MotionConfig>
      )
    }
    ```

    **Schritt 3: app/page.tsx** anpassen — als Server-Component nonce vom Request-Header lesen und an HomeClient übergeben:

    ```tsx
    import { headers } from "next/headers"
    import { HomeClient } from "@/components/home-client"

    export default async function Home() {
      const nonce = (await headers()).get("x-nonce") ?? ""
      return <HomeClient nonce={nonce} />
    }
    ```

    `await headers()` macht die Page automatisch dynamic — passt zu `export const dynamic = "force-dynamic"` im Root-Layout, kein Konflikt.

    **Schritt 4: app/layout.tsx NICHT modifizieren** — MotionConfig läuft auf Client-Layer im HomeClient, nicht im Root-Layout. Damit bleibt Root-Layout sauber Server-Side (kein 'use client'-Bruch der layout-Konvention). Der `files_modified` Eintrag für `apps/website/app/layout.tsx` ist defensiv enthalten, wird aber nur falls absolut nötig benutzt.

    **Verifications (hart):**
    - `pnpm --filter @genai/website build` grün, `/` ist `ƒ`
    - `grep -c "Section" apps/website/components/home-client.tsx` ≥ 8 (8 Section-Imports + 8 JSX-Mounts)
    - DOM-Smoke (lokal): Server starten, `curl -s localhost:3000 | grep -oE 'data-section="[a-z-]+"' | sort -u | wc -l` muss `8` sein.
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && for s in hero discrepancy offering tool-showcase community-preview audience-split trust final-cta; do test -f "apps/website/components/sections/${s}-section.tsx" || { echo "MISSING ${s}-section.tsx"; exit 1; }; done && grep -q "MotionConfig" apps/website/components/home-client.tsx && grep -q "HeroSection" apps/website/components/home-client.tsx && grep -q "FinalCTASection" apps/website/components/home-client.tsx && grep -q "headers()" apps/website/app/page.tsx && grep -q "nonce" apps/website/app/page.tsx && pnpm --filter @genai/website build 2>&1 | tee /tmp/build-20-02-t3.log | grep -E "ƒ\\s+/\\s" && echo "TASK 3 PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Alle 8 Section-Stub-Files existieren in `apps/website/components/sections/`
    - Jede Stub-Datei exportiert eine named-function (`grep "export function .*Section" apps/website/components/sections/*.tsx | wc -l` = 8)
    - Jede Stub-Datei hat ein `data-section` Attribut (grep `data-section=` über alle 8 Files = 8 Treffer)
    - `home-client.tsx` importiert alle 8 Sections + MotionConfig (grep `import.*Section` ≥ 8 Treffer)
    - `home-client.tsx` rendert alle 8 Sections im JSX (grep `<HeroSection />`, `<DiscrepancySection />` … `<FinalCTASection />` — 8 separate Treffer)
    - `app/page.tsx` enthält `await headers()` und übergibt `nonce` als prop
    - `pnpm --filter @genai/website build` exit 0
    - Build-Output: `ƒ /` (KEIN `○ /`)
    - `pnpm --filter @genai/website lint` exit 0
    - Lokaler Prod-curl gegen `/` enthält 8 verschiedene `data-section=` Werte
  </acceptance_criteria>
  <done>
    8 Section-Stubs vorhanden und korrekt mounted in home-client.tsx. MotionConfig wrappt das Tree mit Request-Nonce. Build grün, alle Sections im DOM unterscheidbar via data-section. Plans 03/04/05 können nun völlig unabhängig je 2-3 Sections befüllen ohne home-client.tsx jemals wieder anzufassen.
  </done>
</task>

</tasks>

<verification>
**Wave-Verify (Plan-Abschluss):**

1. `pnpm --filter @genai/website build` grün mit `ƒ /`
2. Lokaler Prod-Smoke:
   - `pnpm --filter @genai/website build && cd apps/website && NODE_ENV=production pnpm start &`
   - `sleep 3 && curl -sI http://localhost:3000 | grep -i "content-security-policy"` → CSP-Header mit `nonce-` enthalten
   - Background-Server killen
3. Playwright-Subset gegen lokalen Prod-Server:
   - `cd packages/e2e-tools && E2E_BASE_URL=http://localhost:3000 pnpm exec playwright test landing.spec.ts -g "R1.1" --reporter=line`
   - Erwartet: 3 R1.1-Tests grün (Desktop click, Desktop keyboard, Mobile hamburger). R1.2/R1.5/R1.6/R1.8 dürfen weiterhin failen (Sections sind Stubs).
4. CSP-Console-Test:
   - `pnpm exec playwright test landing.spec.ts -g "CSP" --reporter=line` → grün (keine CSP-Errors)
5. VALIDATION.md Per-Task Map updaten: Plan 02 Task 1/2/3 mit Status nach Run.
</verification>

<success_criteria>
- Header hat funktionalen Dropdown + Mobile-Sheet, alle 5 Top-Level-Items + 3 Partner-Sub-Items
- Footer hat 4-Spalten-Grid mit Sitemap + Legal + Kontakt + LinkedIn + Copyright
- 8 Section-Stubs vorhanden, in korrekter Reihenfolge gemountet
- MotionConfig mit Request-Nonce aktiv
- R1.1 Playwright-Tests (Dropdown click + keyboard + Mobile hamburger) grün
- CSP-Test grün
- Build grün mit `ƒ /`
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/20-navigation-landing-skeleton/20-02-SUMMARY.md` mit:
- Header-Diff Highlights (was sich geändert hat)
- Footer-Sitemap-Liste
- Section-Stub-Inventar (8 Files mit data-section-Werten)
- MotionConfig-Wiring-Doku (page.tsx → home-client.tsx via prop)
- Playwright-Result-Snippet (R1.1 + CSP grün)
- Hinweise/Issues für Plans 03/04/05 (z.B. Section-Container-Pattern erinnert: jede Section MUSS ihr `data-section` Attribut behalten weil Tests danach suchen können; bei DiscrepancySection als Custom-Component bewusst auf Section-Container achten)
</output>
