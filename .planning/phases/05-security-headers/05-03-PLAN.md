---
phase: 05-security-headers
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - apps/website/next.config.ts
  - apps/tools-app/app/proxy.ts
autonomous: false
requirements:
  - R1.5

must_haves:
  truths:
    - "CSP ist enforcing (nicht mehr Report-Only)"
    - "securityheaders.com zeigt A+ fuer beide Domains"
    - "Keine CSP-Violations in Production"
    - "Auth funktioniert in Production"
  artifacts:
    - path: "apps/website/next.config.ts"
      provides: "Enforcing CSP"
      contains: "Content-Security-Policy"
    - path: "apps/tools-app/app/proxy.ts"
      provides: "Enforcing CSP"
      contains: "Content-Security-Policy"
  key_links:
    - from: "Both apps"
      to: "securityheaders.com"
      via: "HTTP Response Headers"
      pattern: "A\\+ Rating"
---

<objective>
CSP von Report-Only auf Enforcing umstellen und finale Verifikation durchfuehren.

Purpose: Nach erfolgreichem Test im Report-Only Mode wird CSP scharf geschaltet. Die Verifikation mit securityheaders.com bestaetigt A+ Rating.

Output:
- CSP enforcing in beiden Apps
- A+ Rating auf securityheaders.com
- Dokumentierte Verifikation
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/security-headers.md
@.planning/phases/05-security-headers/05-01-SUMMARY.md
@.planning/phases/05-security-headers/05-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Security Headers in Report-Only Mode fuer beide Apps (Plan 01 + 02)</what-built>
  <how-to-verify>
1. Pruefe Website (generation-ai.org):
   - Oeffne DevTools > Console
   - Lade Seite neu
   - Suche nach CSP-Violations (lila Warnings)
   - Sind welche da? Wenn ja, welche?

2. Pruefe tools-app (tools.generation-ai.org):
   - Oeffne DevTools > Console
   - Teste Login-Flow
   - Teste Chat-Funktion (falls eingeloggt)
   - Suche nach CSP-Violations
   - Sind welche da? Wenn ja, welche?

3. Report:
   - Website: [X Violations / keine Violations]
   - tools-app: [X Violations / keine Violations]
   - Details zu Violations (falls vorhanden)
  </how-to-verify>
  <resume-signal>
Moegliche Antworten:
- "approved" — Keine kritischen Violations, weiter mit Enforcing
- "violations: [beschreibung]" — Violations gefunden, muessen gefixt werden
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: CSP auf Enforcing umstellen - Website</name>
  <files>apps/website/next.config.ts</files>
  <action>
Aendere in next.config.ts den Header-Key von Report-Only auf Enforcing:

Von:
```typescript
{
  key: 'Content-Security-Policy-Report-Only',
  value: cspHeader,
}
```

Zu:
```typescript
{
  key: 'Content-Security-Policy',
  value: cspHeader,
}
```

NUR diese eine Aenderung. Keine anderen Modifikationen.
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && grep -n "Content-Security-Policy" apps/website/next.config.ts | head -5</automated>
  </verify>
  <done>
    - Header-Key ist "Content-Security-Policy" (nicht Report-Only)
    - Keine anderen Aenderungen
  </done>
</task>

<task type="auto">
  <name>Task 3: CSP auf Enforcing umstellen - tools-app</name>
  <files>apps/tools-app/app/proxy.ts</files>
  <action>
Aendere in proxy.ts den Header-Key von Report-Only auf Enforcing:

Von:
```typescript
response.headers.set('Content-Security-Policy-Report-Only', cspHeader)
```

Zu:
```typescript
response.headers.set('Content-Security-Policy', cspHeader)
```

NUR diese eine Aenderung. Keine anderen Modifikationen.
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && grep -n "Content-Security-Policy" apps/tools-app/app/proxy.ts | head -5</automated>
  </verify>
  <done>
    - Header-Key ist "Content-Security-Policy" (nicht Report-Only)
    - Keine anderen Aenderungen
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>CSP auf Enforcing umgestellt, bereit fuer Deploy</what-built>
  <how-to-verify>
1. Deploy beide Apps zu Vercel (oder warte auf naechsten Auto-Deploy nach Push)

2. Nach Deploy - pruefe securityheaders.com:
   - https://securityheaders.com/?q=https://generation-ai.org
   - https://securityheaders.com/?q=https://tools.generation-ai.org
   - Beide sollten A+ zeigen

3. Funktionstest auf Production:
   - Website: Startseite laedt, Sign-up-Flow funktioniert
   - tools-app: Login funktioniert, Chat funktioniert

4. Browser Console auf Production:
   - Keine roten CSP-Errors (blockiert)
   - Violations wuerden jetzt blockieren, nicht nur warnen
  </how-to-verify>
  <resume-signal>
Moegliche Antworten:
- "approved" — A+ auf beiden, alles funktioniert
- "website issue: [beschreibung]" — Website hat Problem
- "tools-app issue: [beschreibung]" — tools-app hat Problem
- "rating issue: [beschreibung]" — Kein A+, was fehlt?
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production → User Browser | Enforcing CSP aktiv |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Spoofing | XSS attempts | mitigate | Enforcing CSP blockiert unauthorized scripts |
| T-05-12 | Denial of Service | False positive blocking | accept | Report-Only Test minimiert Risiko, Rollback moeglich |
</threat_model>

<verification>
- [ ] Checkpoint 1: Report-Only Test bestanden (keine kritischen Violations)
- [ ] CSP Header in Website ist "Content-Security-Policy" (enforcing)
- [ ] CSP Header in tools-app ist "Content-Security-Policy" (enforcing)
- [ ] Checkpoint 2: Production Deploy erfolgreich
- [ ] securityheaders.com zeigt A+ fuer generation-ai.org
- [ ] securityheaders.com zeigt A+ fuer tools.generation-ai.org
- [ ] Auth funktioniert auf Production
</verification>

<success_criteria>
Beide Domains haben A+ Rating auf securityheaders.com. CSP ist enforcing. Alle kritischen Funktionen (Auth, Chat) funktionieren.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/05-security-headers/05-03-SUMMARY.md`
</output>
