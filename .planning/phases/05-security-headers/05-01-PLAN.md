---
phase: 05-security-headers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/next.config.ts
autonomous: true
requirements:
  - R1.1
  - R1.2
  - R1.4
  - R1.5

must_haves:
  truths:
    - "Website hat HSTS Header mit preload"
    - "Website hat alle Standard Security Headers"
    - "Website hat CSP in Report-Only Mode"
    - "Statische Seiten rendern weiterhin korrekt"
  artifacts:
    - path: "apps/website/next.config.ts"
      provides: "Security Headers + CSP Konfiguration"
      contains: "Strict-Transport-Security"
  key_links:
    - from: "apps/website/next.config.ts"
      to: "Vercel Response Headers"
      via: "Next.js headers() config"
      pattern: "async headers\\(\\)"
---

<objective>
Security Headers und CSP fuer die Website (generation-ai.org) implementieren.

Purpose: Die statische Website braucht vollstaendige Security Headers inkl. HSTS und CSP. Da die Website primär statisch rendert, nutzen wir `unsafe-inline` CSP via next.config.ts statt Nonces.

Output: 
- HSTS Header aktiv
- Alle Standard Security Headers aktiv
- CSP in Report-Only Mode (zum Testen)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/security-headers.md

<interfaces>
<!-- Aktuelle next.config.ts ist minimal -->
From apps/website/next.config.ts:
```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
```

<!-- Supabase URL aus Research -->
Supabase Project URL: https://wbohulnuwqrhystaamjc.supabase.co
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Security Headers + CSP-Report-Only in Website next.config.ts</name>
  <files>apps/website/next.config.ts</files>
  <action>
Erweitere next.config.ts mit vollstaendiger headers() Konfiguration:

1. HSTS Header:
   - `Strict-Transport-Security: max-age=63072000; includeSubDomains; preload`
   
2. Standard Security Headers:
   - `X-Content-Type-Options: nosniff`
   - `X-Frame-Options: DENY`
   - `Referrer-Policy: strict-origin-when-cross-origin`
   - `Permissions-Policy: camera=(), microphone=(), geolocation=()`

3. CSP in Report-Only Mode (WICHTIG: erst testen, dann enforcing):
   ```
   Content-Security-Policy-Report-Only:
     default-src 'self';
     script-src 'self' 'unsafe-inline' [+ 'unsafe-eval' nur in dev];
     style-src 'self' 'unsafe-inline';
     img-src 'self' blob: data:;
     font-src 'self';
     connect-src 'self' https://wbohulnuwqrhystaamjc.supabase.co;
     object-src 'none';
     base-uri 'self';
     form-action 'self';
     frame-ancestors 'none';
     upgrade-insecure-requests;
   ```

4. Behalte den statischen Asset Cache-Header (falls vorhanden).

Implementiere mit isDev check fuer Development-spezifische Anpassungen (`'unsafe-eval'` fuer HMR).
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && pnpm build --filter=website 2>&1 | head -50</automated>
  </verify>
  <done>
    - next.config.ts hat headers() mit allen Security Headers
    - CSP ist als Report-Only konfiguriert
    - Build laeuft ohne Fehler
  </done>
</task>

<task type="auto">
  <name>Task 2: Lokaler Test der Website Security Headers</name>
  <files>apps/website/next.config.ts</files>
  <action>
1. Starte Website im Production-Build Mode lokal:
   ```bash
   cd apps/website && pnpm build && pnpm start
   ```

2. Pruefe Response Headers mit curl:
   ```bash
   curl -I http://localhost:3000
   ```

3. Verifiziere dass alle Header vorhanden sind:
   - Strict-Transport-Security
   - X-Content-Type-Options
   - X-Frame-Options
   - Referrer-Policy
   - Permissions-Policy
   - Content-Security-Policy-Report-Only

4. Falls Header fehlen: next.config.ts korrigieren.
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai/apps/website && pnpm build && timeout 10 sh -c 'pnpm start &amp; sleep 5 &amp; curl -sI http://localhost:3000 | grep -E "(Strict-Transport|X-Content-Type|X-Frame|Referrer-Policy|Content-Security)" &amp; kill %1' 2>/dev/null || echo "Headers check requires manual verification"</automated>
  </verify>
  <done>
    - Alle Security Headers erscheinen in Response
    - CSP-Report-Only Header ist vorhanden
    - Keine Build-Fehler
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Website | Statische Seiten, minimaler dynamischer Content |
| Website → Supabase | Auth-Flows (Sign-up, Magic Link) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | HTTP downgrade | mitigate | HSTS mit preload, upgrade-insecure-requests in CSP |
| T-05-02 | Information Disclosure | Clickjacking | mitigate | X-Frame-Options: DENY + frame-ancestors 'none' |
| T-05-03 | Information Disclosure | MIME sniffing | mitigate | X-Content-Type-Options: nosniff |
| T-05-04 | Spoofing | XSS via inline scripts | accept | unsafe-inline noetig fuer static build, aber kein kritischer State auf Website |
| T-05-05 | Information Disclosure | Referrer leakage | mitigate | Referrer-Policy: strict-origin-when-cross-origin |
</threat_model>

<verification>
- [ ] `pnpm build --filter=website` erfolgreich
- [ ] Alle 6 Security Headers in Response vorhanden
- [ ] CSP ist Report-Only (nicht enforcing)
- [ ] Seiten laden korrekt (keine CSP-Violations in Console)
</verification>

<success_criteria>
Website hat vollstaendige Security Headers. CSP ist im Report-Only Mode zum Testen. Keine Funktionalitaet beeintraechtigt.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/05-security-headers/05-01-SUMMARY.md`
</output>
