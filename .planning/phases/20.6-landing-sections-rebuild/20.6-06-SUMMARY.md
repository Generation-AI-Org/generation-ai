---
phase: 20.6-landing-sections-rebuild
plan: 06
subsystem: landing-trust
tags: [trust, polish, marquee, sparringspartner, microproof, design-system, motion-react, reduced-motion]

requires:
  - phase: 20-navigation-landing-skeleton
    provides: Trust-Section mount + MagicUI Marquee mit 6 Stub-Pills + Double-Reduced-Motion-Guard-Pattern (Plan 20-05)
  - phase: 20.6-landing-sections-rebuild/20.6-03
    provides: Marquee-Pattern auf .animate-scroll (tool-showcase-section.tsx) — seamless-loop via useEffect-Clone, mask-linear-gradient fade, hover:pause
  - phase: 20.6-landing-sections-rebuild/20.6-04
    provides: Section-Header-Pattern slash-prefix mit Eyebrow-Dot (community-preview-section), zentriert

provides:
  - Trust-Section auf Marquee-Pattern (Grid → horizontal infinite-scroll) per CONTEXT.md D-12 + Simon §4.8
  - Hero-level H2 (Geist Mono clamp 32-52 tracking -0.025em) "Keine zufälligen Bekannten."
  - 12 Partner-Text-Pills (6 Unis + 6 Firmen/Initiativen) in Geist Mono, monochrome Styling
  - Double reduced-motion Guard aktiv (CSS .animate-scroll @media + JS useReducedMotion fadeIn-gate)
  - motion/react fadeIn entry (viewport once, margin -10%) konsistent mit tool-showcase + community-preview
  - R1.8 Playwright-Selector updated auf data-section="trust" .animate-scroll (Pattern-Migration)

affects:
  - 20.6-07 Final-CTA polish (Marquee rhythm jetzt zwischen Community-Preview und Final-CTA einheitlich)
  - 20.6-08 Inter-section transitions (Trust-Entry-Rhythm ist jetzt settable, Marquee hat soft-edge mask für nahtlose Transitions)
  - Phase 22 /partner (Partner-Liste wird dort konkret, Text-Pills werden durch echte Logos ersetzt sobald Kooperationen gesigned)

tech-stack:
  added: []
  patterns:
    - motion/react fadeIn (viewport once, margin -10%, duration 0.6, ease [0.16,1,0.3,1]) — konsistent zu tool-showcase/offering/community-preview
    - .animate-scroll Marquee mit seamless-loop Clones via useEffect (identisches Pattern wie ToolMarquee)
    - Double reduced-motion Guard: globals.css @media pausiert animation-play-state + JS useReducedMotion gated fadeIn
    - Pill-Styling rounded-2xl border bg-bg-card, hover:border-accent + text-muted→text (monochrome, kein accent-glow)
    - minWidth statt fixed width auf Pills (Labels unterschiedlich lang — "KIT" vs "KI-Bundesverband")

key-files:
  created: []
  modified:
    - apps/website/components/sections/trust-section.tsx — komplett rewrite Grid → Marquee
    - packages/e2e-tools/tests/landing.spec.ts — R1.8 Selector-Migration auf .animate-scroll

key-decisions:
  - "Logo-Strategie: Text-Pills (Stubs) — keine echten Partner-SVGs in apps/website/brand/partners/ oder packages/ui/src/brand-assets/ (beide Pfade existieren nicht, Stand 2026-04-23). Plan 20.6-06 erlaubt Text-Pills als Stubs explizit. Echte SVG-Assets kommen später nach apps/website/brand/partners/ und ersetzen die Pills ohne API-Change (Plan erlaubt 1:1 Swap)."
  - "Monochrome statt Brand-Farben: Trust bleibt understated. text-muted → text on hover, subtle hover:border-[var(--border-accent)]. Kein Accent-Glow auf den Pills, damit Header-Hierarchie stabil bleibt und Section nicht mit der Tool-Bibliothek (die neon-akzentuiert ist) konkurriert."
  - "Marquee-Speed 60s scroll-duration (statt 80s wie Tool-Bibliothek): weniger Pills (12 vs 12 Tools), kürzere Labels — 60s lässt Auge folgen, ohne hektisch zu wirken. Pause-on-hover + reduced-motion-Guard decken Accessibility ab."
  - "Section-Header zentriert wie community-preview — nicht split wie tool-showcase. Trust hat keine sekundäre Action (keine CTA, kein Badge) → zentriertes Layout ist funktional richtig und wirkt ruhiger."
  - "Playwright R1.8-Selector auf data-section='trust' scoped, damit er nicht versehentlich Tool-Showcase-Marquee trifft (beide nutzen jetzt .animate-scroll). Test bleibt describe.skip bis Phase 27 Copy-Pass, Selector-Anpassung ist Vorbereitung."

patterns-established:
  - "Marquee-Pattern-Konvergenz: Trust und Tool-Showcase nutzen jetzt identisches .animate-scroll + useEffect-Clone + mask-gradient Pattern. Eventuelle Trust-/Tool-Marquee-Refactors werden gemeinsam laufen."
  - "Stub-Data in Section-Components: Konstanten (partners, tools, stubArticles, stubEvents) werden direkt im Component-File deklariert statt extrahiert. Rechtfertigung: eng an Component gekoppelt, keine Reuse, erleichtert lokales Editing während UAT. Wird erst bei echten APIs (Phase 25/26) extrahiert."

requirements-completed: [R1.8]

duration: ~25min
completed: 2026-04-23
---

# Phase 20.6 Plan 06: Trust Polish Summary

**Trust-Section von 6-col Partners-Grid (Handout-Rebuild) zurück auf horizontal infinite-scroll Marquee mit 12 Partner-Text-Pills, Hero-level H2 per Simon §4.8 "Keine zufälligen Bekannten.", Double-Reduced-Motion-Guard, R1.8 Playwright-Selector auf neue .animate-scroll Pattern migriert**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-04-23T20:48:00Z (PLAN_START_TIME)
- **Completed:** 2026-04-23T21:12:00Z
- **Tasks:** 2 atomic commits
- **Files modified:** 2

## Accomplishments

- **Grid → Marquee Umbau** (CONTEXT.md D-12): 6-col Partners-Grid aus Handout-Rebuild (Commit `e8a8860`) zurückgebaut auf horizontal infinite-scrolling Marquee analog Tool-Bibliothek Pattern (Plan 20.6-03, `f36e6f0`).
- **Header auf Hero-Quality-Bar gezogen**: Eyebrow "// wer mit uns baut" (slash-prefix Mono-Label mit Dot), H2 Geist Mono clamp 32-52 tracking -0.025em "Keine zufälligen Bekannten." (exakter Simon §4.8 Wortlaut), Lede erklärt Stub-Charakter.
- **12 Partner-Text-Pills** in Geist Mono: 6 Unis (TU Berlin, LMU München, Uni Mannheim, ETH Zürich, WU Wien, KIT) + 6 Firmen/Initiativen (Anthropic, Make, Perplexity, ElevenLabs, Bitkom, KI-Bundesverband). Rounded-2xl border-card, monochrome Styling (text-muted → text hover), minWidth 180px für unterschiedlich lange Labels.
- **Double reduced-motion Guard aktiv**:
  - CSS `@media (prefers-reduced-motion: reduce)` in `globals.css` pausiert `.animate-scroll` (Zeile 230) → getestet via Playwright, play-state: `paused` verifiziert.
  - JS `useReducedMotion` → `fadeIn = {}` bei reduce → motion/react entry skippt komplett.
- **DS-Tokens durchgängig**: `--dur-normal`, `--ease-out`, `--accent`, `--accent-glow`, `--text*`, `--border*`, `--bg-card`. Keine stray hex-Werte.
- **Playwright-Selector-Migration**: R1.8 Test jetzt auf `[data-section="trust"] .animate-scroll` statt alter `.animate-marquee` (MagicUI-Variante aus Plan 20-05 ist nicht mehr im Code). Scoped via data-section, damit nicht die Tool-Showcase-Marquee getroffen wird.
- **Microproof "Beispiel-Auswahl · Gespräche laufen"** bleibt unter dem Marquee, Typography aligned mit Eyebrow-Pattern (Geist Mono text-muted uppercase tracking-[0.2em]). Plan-Microproof "N=109 · März 2026" war bereits in Plan 20-05 entfernt, Ersatz-Text ist konsistenter mit Trust-Framing (Unis/Firmen statt Nutzerzahl).

## Task Commits

1. **Task 1: Grid → Marquee Refactor** — `c372751` (refactor)
   - trust-section.tsx komplett neu: motion/react fadeIn + Marquee-Pattern mit useEffect-Clone + DS-Tokens + Hero-H2 + Lede
   - 143 insertions, 26 deletions (Grid-Code raus, Marquee + Header-Polish rein)

2. **Task 2: R1.8 Playwright-Selector Update** — `a26b3f5` (test)
   - landing.spec.ts R1.8 auf `[data-section="trust"] .animate-scroll` migriert
   - Comment dokumentiert Pattern-Change von .animate-marquee (MagicUI, Plan 20-05) auf .animate-scroll (tool-showcase-Pattern, Plan 20.6-03)
   - Test bleibt describe.skip (ganze Suite deferred bis Phase 27 Copy-Pass)

## Files Created/Modified

- `apps/website/components/sections/trust-section.tsx` — Grid → Marquee rewrite, 12 Partner-Text-Pills, Double-Reduced-Motion-Guard, DS-Tokens
- `packages/e2e-tools/tests/landing.spec.ts` — R1.8 Selector auf .animate-scroll

## Decisions Made

Siehe frontmatter `key-decisions`. Die beiden im Plan explizit als "in Plan treffen" markierten Entscheidungen:

1. **Logo-Strategie: Text-Pills (Stubs)** — bestätigt, da weder `apps/website/brand/partners/` noch `packages/ui/src/brand-assets/` existieren (geprüft 2026-04-23). Plan 20.6-06 erlaubt Text-Pills explizit. Echte SVGs kommen in späterer Phase (vermutlich Phase 22 /partner oder 27 Copy-Pass sobald Kooperationen gesigned).
2. **Monochrome statt Brand-Farben** — empfohlene Default-Wahl aus Spawn-Prompt übernommen: Trust bleibt understated, text-muted → text on hover, kein Accent-Glow auf Pills. Tool-Bibliothek bleibt damit die neon-akzentuierte Marquee, Trust ist die ruhige.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] R1.8 Playwright-Selector auf neues Marquee-Pattern migriert**
- **Found during:** Task 1 (Grid → Marquee Refactor)
- **Issue:** Plan 20.6-06 PLAN.md listet R1.8 in `requirements` und Success-Criteria "Playwright R1.8 bleibt grün". Der R1.8 Test suchte aber `.animate-marquee` (MagicUI-Klasse aus Plan 20-05). Mit dem Refactor auf `.animate-scroll` (tool-showcase-Pattern) würde der Test später beim unskip fehlschlagen.
- **Fix:** Selector auf `[data-section="trust"] .animate-scroll` migriert, scoped via data-section damit nicht Tool-Showcase-Marquee getroffen wird. Comment dokumentiert Pattern-Change mit Plan-Referenzen.
- **Files modified:** packages/e2e-tools/tests/landing.spec.ts
- **Verification:** Lokaler Smoke-Test via Playwright gegen `NODE_ENV=production pnpm start` auf PORT 3030 — 3/3 grün (render, reduced-motion-paused, no-CSP-errors).
- **Committed in:** a26b3f5 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Notwendig für Success-Criteria "Playwright R1.8 bleibt grün". Kein Scope-Creep — ganze Testsuite bleibt describe.skip bis Phase 27.

## Issues Encountered

- Initial Playwright-Smoke-Test via `pnpm exec playwright test /tmp/smoke-trust.spec.ts` schlug fehl ("No tests found") — `playwright.config` scoped auf `packages/e2e-tools/tests/`. Fix: temp-File nach `tests/` kopiert, Test lief 3/3 grün, dann entfernt. Kein Code-Impact.

## Verification Evidence

**Build:** `pnpm --filter @genai/website build` grün, `ƒ /` dynamic intakt (CSP-Nonce-Flow erhalten per LEARNINGS.md).

**Playwright-Smoke** (lokaler Prod-Server PORT 3030):
- `trust-section renders marquee with pills` — 24 pills im DOM (12 originals + 12 clones), H2 "Keine zufälligen Bekannten." visible
- `R1.8 — trust marquee paused on prefers-reduced-motion` — `.animate-scroll` play-state: `paused` ✓
- `CSP — no Content Security Policy errors on landing` — 0 errors

**DOM-Smoke (curl):** HTTP 200, 102KB HTML, 1× `data-section="trust"`, alle 6 sampled Pill-Texts (TU Berlin, LMU München, Anthropic, ElevenLabs, KI-Bundesverband) + "Keine zufälligen" im gerenderten HTML.

## Next Phase Readiness

- Trust-Section ist Launch-ready modulo echter Partner-Logos.
- Logo-Swap ist triviale Code-Änderung (Pills ersetzen durch `<img>` oder Inline-SVG), sobald Assets in `apps/website/brand/partners/` liegen. Marquee-Layout/Motion bleibt unverändert.
- Marquee-Pattern ist jetzt in zwei Sections identisch implementiert (Trust + Tool-Showcase) — künftige Refactors laufen gemeinsam.
- R1.8 Test-Selector ist auf das neue Pattern aligned, aber Testsuite bleibt describe.skip bis Phase 27.

---

## Self-Check

**Created files:**
- None (only modifications)

**Modified files:**
```
FOUND: apps/website/components/sections/trust-section.tsx
FOUND: packages/e2e-tools/tests/landing.spec.ts
```

**Commits:**
```
FOUND: c372751 refactor(20.6-06): trust grid → marquee, DS-polished text-pills
FOUND: a26b3f5 test(20.6-06): R1.8 trust-marquee selector on .animate-scroll
```

**Self-Check: PASSED**

---
*Phase: 20.6-landing-sections-rebuild*
*Completed: 2026-04-23*
