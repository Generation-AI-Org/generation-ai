---
phase: 20.6-landing-sections-rebuild
plan: 03
subsystem: landing-tool-showcase
tags: [tool-showcase, polish, motion-react, marquee, design-system, focus-visible, theme-aware]

requires:
  - phase: 20-navigation-landing-skeleton
    provides: tool-showcase-section mount + BeispielBadge theme-aware pattern (Plan 20-04)
  - phase: 20.6-landing-sections-rebuild/20.6-02
    provides: DS-polish baseline (motion/react + DS-Tokens + focus-visible Muster in offering-section.tsx)

provides:
  - Tool-Showcase section auf DS-Quality-Bar
  - Hero-level H2 (Geist Mono clamp 32-52 tracking -0.025em)
  - motion/react Entry-Fade mit useReducedMotion-Guard
  - Card-Links mit DS-Tokens (--ease-out, --dur-normal) + focus-visible outline
  - Theme-aware Icon-BG (stray hex raus, --bg-elevated + --border)
  - Marquee pausiert bei prefers-reduced-motion (via globals.css @media-Rule, verifiziert)

affects:
  - 20.6-06 Trust (Marquee-Pattern wird in Trust-Umbau reused)
  - 20.6-08 Inter-section transitions (Tool-Showcase Entry-Rhythm ist jetzt settable)
  - 26 Community-Preview + Subdomain-Integration (Featured-Tools-API ersetzt Stub-Liste, Slug-Matching-Pattern bleibt)

tech-stack:
  added: []
  patterns:
    - motion/react fadeIn (viewport once, margin -10%, duration 0.6, ease [0.16,1,0.3,1]) matches offering/problem-block
    - CSS-Token Transitions via inline style (transitionDuration + transitionTimingFunction via var(--dur-normal) / var(--ease-out))
    - focus-visible auf Card-Links mit outlineColor var(--text) (DS section C neutral ring)
    - Stub-Markierung BeispielBadge bleibt export-kompatibel zu community-preview-section

key-files:
  created: []
  modified:
    - apps/website/components/sections/tool-showcase-section.tsx — Header + Card polish

key-decisions:
  - "Header-Layout bleibt split (H2 links, BeispielBadge + CTA rechts) statt zentriert wie offering — funktional gerechtfertigt: BeispielBadge muss nah am H2 sein (Stub-Kennzeichnung), CTA leitet zur Subdomain (Funktion)"
  - "Icon-Container bekommt DS-Tokens (--bg-elevated + --border) statt stray hex Gradient — dark-only-Design wäre auf Light-Theme schwarzer Kasten gewesen"
  - "Marquee-Speed (80s scroll-duration) bleibt unverändert — wurde in 20-04 auf 'ruhig genug um lesbar zu sein' gesetzt"
  - "Kein Austausch der 12 Tools — Slug-Matching mit tools-app ist PASS, Content-Iteration ist Phase 26"

patterns-established:
  - "DS-Polish-Rezept für bestehende Sections: motion/react entry + CSS-Token transitions + focus-visible + theme-aware colors"
  - "Stray-hex-Audit: Bei jeder DS-Polish nach inline Hex-Werten grep, auf Tokens migrieren oder als Mask-Layer dokumentieren"

requirements-completed: [R1.5]

duration: ~12min
completed: 2026-04-23
---

# Phase 20.6 Plan 03: Tool-Showcase Polish Summary

**Tool-Showcase auf DS-Quality-Bar: Hero-H2 + motion/react entry + DS-Token-Transitions + theme-aware Icon-BG — Copy (Simon §4.6) und Slug-Matching waren bereits grün, Polish war rein visuell**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-04-23
- **Completed:** 2026-04-23
- **Tasks:** 2 atomic commits
- **Files modified:** 1

## Accomplishments

- Header typography auf Hero-Level gezogen (Geist Mono clamp 32-52, tracking -0.025em) — matches offering-section neighbor (commit bb4b586) und problem-block-section
- motion/react fadeIn entry mit useReducedMotion-Guard — Header und Marquee-Block haben jetzt kohärenten Entry-Rhythm
- DS-Tokens durchgängig: alle `duration-300` + `cubic-bezier(0.16,1,0.3,1)` raus, CSS-Vars `--dur-normal` + `--ease-out` rein
- Stray hex Icon-BG gefixt: `linear-gradient(135deg, #2a2a2a, #1a1a1a)` war dark-only → jetzt `var(--bg-elevated)` + `var(--border)`, funktioniert in beiden Themes
- focus-visible outline auf Card-Links mit neutral ring (`outlineColor: var(--text)`) per DS §C
- Subtle hover lift (-2px) mit motion-reduce override statt nur border-color-change
- BeispielBadge theme-aware (.light → brand-red, dark → brand-neon) und export-Kompatibilität für community-preview-section bleibt erhalten

## Task Commits

1. **Task 1: Header polish** — `f36e6f0` (refactor)
   - motion/react fadeIn + useReducedMotion guard
   - Hero-level H2 typography (Mono, clamp, tight tracking)
   - "Alle Tools"-CTA: mono uppercase tracked, DS-Token transitions, focus-visible
   - Lede auf 18/20px rhythm
2. **Task 2: Card polish** — `cb7e652` (refactor)
   - Card-Link: focus-visible outline via `var(--text)`
   - Transitions auf DS-Tokens
   - Icon-BG: stray hex raus, `--bg-elevated` + `--border` rein
   - Subtle hover lift mit motion-reduce guard

## Files Created/Modified

- `apps/website/components/sections/tool-showcase-section.tsx` — Header + ToolCard polish (65 Zeilen netto geändert)

## Decisions Made

- **Header-Layout bleibt split** (H2 links, CTA-Row rechts) — funktional gerechtfertigt, nicht zentriert gemacht um keine Konvention-Inkonsistenz zur offering-section zu bauen. offering hat keine inline-CTA im Header, tool-showcase braucht sie funktional
- **Marquee-Speed unverändert** (80s) — in 20-04 bewusst "ruhig genug zum Lesen" kalibriert
- **Kein Austausch der 12 Tools** — Plan-Objective war Polish, nicht Content-Change. Slug-Matching PASS (siehe Notes)

## Deviations from Plan

Minor — Plan-Skeleton listete "5 Stub-Tools (ChatPDF Pro / Notion AI / Perplexity / ElevenLabs / Gamma) + BeispielBadge" weil es aus Phase 20-04-Zeit war (vor commit `13d6641`). CONTEXT.md-Scope ist aktuell ("12 echte Tool-Logos + Kachel-Links auf tools.generation-ai.org/{slug}") — executor hat CONTEXT-Scope gefolgt. Plan-Skeleton-Wording ist veraltet, Implementierung ist aktuell.

Keine Rule-1/2/3 Auto-Fixes nötig — Code-Qualität der Baseline war solide, nur DS-Alignment fehlte.

## Issues Encountered

Keine.

## Slug-Matching Cross-Check (PASS)

Aus Objective: "Cross-check slug-matching with tools-app content-package". Die 12 slugs in `tool-showcase-section.tsx` wurden gegen `apps/tools-app/supabase/seed.sql` + `seed-v2.sql` + `update-april-2026.sql` geprüft:

| slug | in tools-app seed | Ziel-URL |
|---|---|---|
| `claude` | ✅ seed.sql L14, seed-v2.sql UPDATE L12, update-april L20 | `tools.generation-ai.org/claude` |
| `chatgpt` | ✅ seed.sql L6, seed-v2.sql UPDATE L9, update-april L14 | `tools.generation-ai.org/chatgpt` |
| `gemini` | ✅ seed-v2.sql INSERT L19, update-april L27 | `tools.generation-ai.org/gemini` |
| `perplexity` | ✅ seed.sql L22, update-april L120 | `tools.generation-ai.org/perplexity` |
| `notebooklm` | ✅ seed-v2.sql INSERT L27, update-april L126 | `tools.generation-ai.org/notebooklm` |
| `midjourney` | ✅ seed.sql L70, update-april L182 | `tools.generation-ai.org/midjourney` |
| `elevenlabs` | ✅ seed-v2.sql INSERT L51, update-april L88 | `tools.generation-ai.org/elevenlabs` |
| `gamma` | ✅ seed.sql L62, update-april L176 | `tools.generation-ai.org/gamma` |
| `runway` | ✅ seed-v2.sql INSERT L147, update-april L188 | `tools.generation-ai.org/runway` |
| `github-copilot` | ✅ seed.sql L38, update-april L57 | `tools.generation-ai.org/github-copilot` |
| `notion-ai` | ✅ seed.sql L46, update-april L138 | `tools.generation-ai.org/notion-ai` |
| `make` | ✅ seed.sql L54, update-april L154 | `tools.generation-ai.org/make` |

Alle 12 slugs sind 1:1 in der tools-app Content-Table vorhanden. Keine Kachel linkt ins Leere.

## Copy Cross-Check — Simon §4.6 (PASS)

Simon §4.6 (Generation_AI_Website_Konzept.md L170-174):
> **4.6 Tool-Bibliothek (bereits live)**
> "Über 100 KI-Tools, kuratiert"
> 12 Beispiel-Kacheln, Link auf `tools.generation-ai.org`.

Implementiert:
- H2: "Über 100 KI-Tools, kuratiert." (Punkt am Ende für DS-H2-Gravitas)
- 12 Kacheln ✅
- Link `tools.generation-ai.org` in "Alle Tools ansehen" ✅
- Lede ergänzt ("Eine wachsende Bibliothek mit Anleitungen — sortiert nach Anwendungsfall. Diese Auswahl ist exemplarisch.") — CONTEXT.md listet keinen Lede-Zwang, passt thematisch zu Simon §4.6

## Build Verification

```
pnpm --filter @genai/website build
✓ Compiled successfully in 3.0s
✓ Generating static pages (8/8) in 192ms

Route (app)
┌ ƒ /                        ← dynamic (CSP-Nonce PFLICHT erfüllt)
├ ƒ /_not-found
├ ƒ /api/auth/signup
├ ƒ /datenschutz
├ ○ /icon.svg
├ ƒ /impressum
├ ○ /opengraph-image
├ ○ /robots.txt
├ ○ /sitemap.xml
└ ○ /twitter-image
```

## Self-Check Audits

- **Stray hex im geänderten File:** `grep '#[0-9a-fA-F]{3,6}'` → nur `#000` im `mask-image`-gradient (semantisch korrekt: Alpha-Mask-Layer, keine Design-Farbe)
- **Hardcoded easings:** `grep 'cubic-bezier(0.16|duration-300'` → **0 Matches**
- **Marquee reduced-motion:** `.animate-scroll` ist in `apps/website/app/globals.css` L230-234 in `@media (prefers-reduced-motion: reduce)` → `animation-play-state: paused !important`. Verifiziert.
- **Slug-Matching:** 12/12 PASS (siehe Table oben)

## Success Criteria Check

- [x] Section auf DS-Quality-Bar (Hero-gleichrangig) — H2 ist jetzt Mono clamp 32-52 wie offering + problem-block
- [x] BeispielBadge theme-aware erhalten — `.light` → brand-red, dark → brand-neon
- [x] Marquee pausiert bei reduced-motion — via globals.css @media (verifiziert, kein Code-Change nötig)
- [x] DS-Tokens durchgängig, keine stray hex — audit above
- [x] Hero-Line per Simon §4.6 gesetzt — "Über 100 KI-Tools, kuratiert." (unverändert, war bereits korrekt)
- [x] Build `ƒ /` grün — verifiziert
- [x] Slug-Matching mit tools-app notiert — PASS (12/12)
- [x] Each task committed atomically — 2 commits
- [x] SUMMARY.md at `.planning/phases/20.6-landing-sections-rebuild/20.6-03-SUMMARY.md`

## Next Plan Readiness

- **20.6-04 Community-Preview Polish:** Kann starten. Nutzt gleiches DS-Polish-Rezept (motion entry + DS-Tokens + focus-visible) + Header-Fix `// aus der community` nach Simon §4.7. BeispielBadge-Export aus dieser Datei bleibt stabil.
- **20.6-06 Trust Marquee-Umbau:** Marquee-Pattern ist jetzt zweifach validiert (tool-showcase live, offering in Preview-Mini). Direktübertragbar.
- **26 Featured-Tools-API-Integration:** Slug-Matching-Pattern dokumentiert, Array-Struktur `{ name, slug, cat, desc }` ist API-Shape-kompatibel.

## Self-Check: PASSED

Verifizierte Artefakte:
- `apps/website/components/sections/tool-showcase-section.tsx` — FOUND (modified)
- commit `f36e6f0` — FOUND (Task 1)
- commit `cb7e652` — FOUND (Task 2)
- Build `ƒ /` — FOUND (dynamic route, CSP-konform)

---
*Phase: 20.6-landing-sections-rebuild*
*Plan: 03*
*Completed: 2026-04-23*
