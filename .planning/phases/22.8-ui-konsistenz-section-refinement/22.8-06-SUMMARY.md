---
phase: 22.8
plan: 06
type: summary
status: done
created: 2026-04-26
---

# Plan 22.8-06 — Summary

**Status:** ✅ DONE

## What shipped

- `/join` hat jetzt einen Status-Switch: Studierend / Berufstätig / Alumni / Sonstiges.
- Hochschule bleibt required für Studierende und Alumni, wird für Berufstätige ausgeblendet und bei Sonstiges als optionaler Kontext genutzt.
- Studienfeld ist ein strukturierter Select und wird zusätzlich in `study_program` gespiegelt.
- Waitlist-V1 und Signup-V2 nutzen dieselben Statuswerte.
- Neue Migration `20260426000001_waitlist_status_study_field.sql` erweitert `waitlist` additiv.
- Supabase-Migration wurde angewendet; Schema zeigt `status` als ENUM, `study_field` als nullable text und `university` nullable.

## Verification

- `pnpm --filter @genai/website exec tsc --noEmit` ✅
- `pnpm build` ✅
- Join Playwright gegen localhost: 12/12 ✅

## Next

Plan 22.8-07 — Join Hero-Layout-Refinement.
