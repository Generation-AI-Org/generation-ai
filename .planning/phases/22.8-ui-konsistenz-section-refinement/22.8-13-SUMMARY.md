---
phase: 22.8
plan: 13
title: Theme-Toggle Konsistenz
status: done
completed: 2026-04-26
---

# 22.8-13 Summary — Theme-Toggle Konsistenz

## Delivered

- Website-Header-Toggle nutzt jetzt dieselbe visuelle Sprache wie tools-app: `rounded-full`, 44×44px, Background-Hover, `hover:scale-105`, `active:scale-[0.96]`, Sonne `rotate-45`, Mond `rotate-[-20deg]`, Icon-Scale.
- Website-Toggle ergänzt `type="button"` und `aria-pressed`.
- Tools-app Toggle behält sein Verhalten, wurde aber von `transition-all` auf `transition-[background-color,transform]` und denselben `active:scale-[0.96]` Press-State gebracht.
- `packages/ui`-Extraktion bewusst nicht gemacht; Copy-with-sync-comment ist für Launch kleiner und konfliktärmer.

## Validation

- `pnpm --filter @genai/tools-app exec tsc --noEmit` ✅
- `pnpm build` ✅
- Visual-Smoke via Playwright gegen Production-Server:
  - Website `http://localhost:3001/`: Toggle 44×44, Pill-Radius, Light-Flip, Screenshot 366172 bytes ✅
  - Tools `http://localhost:3002/`: Toggle 44×44, Pill-Radius, Light-Flip, Screenshot 124983 bytes ✅
