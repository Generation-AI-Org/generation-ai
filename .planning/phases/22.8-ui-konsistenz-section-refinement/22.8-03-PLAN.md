---
phase: 22.8
plan: 03
slug: favicon-konsolidierung
subsystem: design-system-foundation
type: execute
status: ready
autonomous: true
wave: 1
depends_on: []
requirements:
  - F.5
tags: [branding, favicon, opengraph, metadata, cross-domain]
created: 2026-04-25
last-updated: 2026-04-25
files_modified:
  # Website — convention-based icon sources (replace existing app/* + delete public/* duplicates)
  - apps/website/app/icon.svg
  - apps/website/app/favicon.ico
  - apps/website/app/apple-icon.png
  - apps/website/public/favicon.png       # DELETED (legacy duplicate)
  - apps/website/public/apple-touch-icon.png  # DELETED (legacy duplicate)
  - apps/website/public/icon.svg          # DELETED (legacy duplicate)
  - apps/website/public/icon-512.png      # DELETED (legacy duplicate)
  - apps/website/public/og-image.jpg      # DELETED (replaced by dynamic app/opengraph-image.tsx since 22.6)
  # Tools-app — convert to convention-based, drop explicit metadata.icons block
  - apps/tools-app/app/icon.svg
  - apps/tools-app/app/favicon.ico
  - apps/tools-app/app/apple-icon.png
  - apps/tools-app/app/layout.tsx          # delete metadata.icons block, update og-image url
  - apps/tools-app/public/favicon.png      # DELETED (replaced by app/icon.svg convention)
  - apps/tools-app/public/apple-touch-icon.png  # DELETED
  - apps/tools-app/public/icon.svg         # DELETED
  - apps/tools-app/public/icon-512.png     # DELETED
  - apps/tools-app/public/og-image-v2.png  # REPLACED by /og-image-default.png from brand source

must_haves:
  truths:
    - "Tab-Favicon auf generation-ai.org und tools.generation-ai.org rendert dasselbe Asset (visueller Vergleich beider Tabs)"
    - "Apple-Touch-Icon auf iOS Home Screen rendert dasselbe Asset für beide Apps"
    - "OG-Image-Preview in Slack/WhatsApp/iMessage zeigt auf beiden Domains konsistentes Branding"
    - "Beide Apps nutzen Next.js App Router Icon-File-Convention (app/icon.svg + app/favicon.ico + app/apple-icon.png) — keine explizite metadata.icons-Wiring mehr nötig"
    - "brand/logos/favicons/ bleibt single source of truth — beide Apps haben byte-identische Kopien der Source-Files"
  artifacts:
    - path: "apps/website/app/icon.svg"
      provides: "Website SVG favicon (Next.js auto-detected)"
      source: "brand/logos/favicons/favicon.svg"
    - path: "apps/website/app/favicon.ico"
      provides: "Website ICO fallback (legacy browsers)"
      source: "brand/logos/favicons/favicon.ico"
    - path: "apps/website/app/apple-icon.png"
      provides: "Website Apple Touch Icon 180×180 (Next.js auto-detected)"
      source: "brand/logos/favicons/apple-touch-icon.png"
    - path: "apps/tools-app/app/icon.svg"
      provides: "Tools-app SVG favicon (Next.js auto-detected)"
      source: "brand/logos/favicons/favicon.svg"
    - path: "apps/tools-app/app/favicon.ico"
      provides: "Tools-app ICO fallback"
      source: "brand/logos/favicons/favicon.ico"
    - path: "apps/tools-app/app/apple-icon.png"
      provides: "Tools-app Apple Touch Icon (Next.js auto-detected)"
      source: "brand/logos/favicons/apple-touch-icon.png"
    - path: "apps/tools-app/public/og-image-default.png"
      provides: "Tools-app OG-Image (replaces og-image-v2.png)"
      source: "brand/logos/og-image-default.png"
    - path: "apps/tools-app/app/layout.tsx"
      provides: "Tools-app metadata mit gestrippter icons-Section + aktualisierter og-image-URL"
      contains: "openGraph: { ... images: [{ url: '/og-image-default.png' }] }"
  key_links:
    - from: "Browser tab on generation-ai.org"
      to: "apps/website/app/icon.svg"
      via: "Next.js automatic <link rel='icon'> emission via app/icon.svg file convention"
    - from: "Browser tab on tools.generation-ai.org"
      to: "apps/tools-app/app/icon.svg"
      via: "Next.js automatic <link rel='icon'> emission via app/icon.svg file convention"
    - from: "Slack/WhatsApp/iMessage link preview for tools.generation-ai.org"
      to: "apps/tools-app/public/og-image-default.png"
      via: "metadata.openGraph.images[0].url in apps/tools-app/app/layout.tsx"
    - from: "iOS Home Screen on either domain"
      to: "{app}/app/apple-icon.png"
      via: "Next.js automatic <link rel='apple-touch-icon'> emission via app/apple-icon.png file convention"
---

<objective>
Cross-domain Favicon- und OG-Image-Parität herstellen. `brand/logos/favicons/` ist die single source of truth — beide Apps konsumieren von dort über Next.js App Router File Conventions (`app/icon.svg`, `app/favicon.ico`, `app/apple-icon.png`). OG-Image für tools-app wechselt von `og-image-v2.png` auf das neue `og-image-default.png` aus `brand/logos/`.

Purpose: Aktuell rendern Website und tools-app **unterschiedliche Favicons** — Website nutzt das alte blaue/sand-Branding (`apps/website/app/icon.svg`), tools-app nutzt sein eigenes `apps/tools-app/public/icon.svg`. Beide sollen das neue konsolidierte Asset aus `brand/logos/favicons/favicon.svg` nutzen. D-05 macht das explizit verbindlich.

Output: Beide Apps zeigen denselben Favicon im Tab + iOS-Home-Screen + Link-Previews. Source-of-truth-Pfad ist klar dokumentiert (`brand/logos/favicons/`). Keine doppelten/widersprüchlichen Icon-Files mehr in `public/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22.8-ui-konsistenz-section-refinement/22.8-CONTEXT.md
@brand/logos/favicons/
@apps/website/app/layout.tsx
@apps/tools-app/app/layout.tsx

<background>
**Decision Source:** D-05 in CONTEXT.md — *"Favicon = was tools-app aktuell rendert (Pfade `/icon.svg` + `/icon-512.png` + `/favicon.png`). Asset wird in beiden Apps unter denselben Konventionen verlinkt (Next.js App-Router-Konvention `app/icon.svg` etc.). Brand-Asset-Source: `brand/logos/favicons/favicon.svg`."*

Inventur aktuell (vom Orchestrator vorrecherchiert):

**`brand/logos/favicons/` (single source of truth — bereits committed):**
- `favicon.svg` — primary SVG, transparenter Background, blau/lime-grün AI-Logo
- `favicon.ico` — multi-size ICO fallback
- `favicon-32.png`, `favicon-512.png` — PNG fallbacks
- `apple-touch-icon.png` — 180×180 für iOS Home Screen
- (rote Varianten + padded Varianten existieren als Alternative-Branches, aber NICHT in Scope dieses Plans)

**Aktuell in `apps/website/`:**
- `app/icon.svg` (commit-tracked, von Next.js auto-detected) — ALTES Asset (blau/sand AI-Motiv)
- `public/{favicon.png, apple-touch-icon.png, icon.svg, icon-512.png}` — alte Duplikate, vermutlich Phase-21-Setup vor dem Brand-Refresh
- `app/favicon.ico` — existiert, vermutlich Default
- `app/opengraph-image.tsx` — generiert OG-Image dynamisch aus `public/brand/logos/logo-wide-neon-on-blue.svg` (Phase 22.6 Track B). Bleibt unverändert.
- `app/twitter-image.tsx` — analog. Bleibt.
- `public/og-image.jpg` — VERWAIST seit Phase 22.6 (dynamic ersetzt static). Wird gelöscht.

**Aktuell in `apps/tools-app/`:**
- `app/favicon.ico` — existiert
- `public/{favicon.png, apple-touch-icon.png, icon.svg, icon-512.png}` — wird via expliziter `metadata.icons`-Block in `app/layout.tsx` gewired
- `public/og-image-v2.png` — wird via `metadata.openGraph.images[0]` gewired
- KEIN `app/icon.svg`, KEIN `app/apple-icon.png` — nutzt `public/`-Pfade statt File-Convention

**Lücke:** Beide Apps zeigen JETZT unterschiedliche Favicons. Website's `app/icon.svg` ist nicht das neue `brand/logos/favicons/favicon.svg`. Tools-app nutzt `public/icon.svg`, das ebenfalls nicht das neue Asset ist.

**Lösung:** Beide Apps auf Next.js App Router Convention vereinheitlichen (`app/icon.svg` + `app/favicon.ico` + `app/apple-icon.png` — kein expliziter metadata.icons-Block mehr). Source-Files alle aus `brand/logos/favicons/` kopiert. Tools-app's `metadata.icons`-Block wird gestrippt — Convention-Detection übernimmt.

**Cross-domain consistency:** OG-Image für tools-app wechselt parallel auf das neue `brand/logos/og-image-default.png`. Website's dynamic OG (`app/opengraph-image.tsx`) bleibt — produziert bereits ein konsistentes Brand-Image aus `logo-wide-neon-on-blue.svg`.
</background>

<interfaces>
**Next.js App Router Icon File Conventions (Next 16):**

Auto-detected by Next.js when present in `app/`:
- `app/icon.svg` (oder `.png`/`.jpg`) — emittiert `<link rel="icon" type="image/svg+xml" href="/icon.svg?...">`
- `app/favicon.ico` — emittiert `<link rel="shortcut icon" href="/favicon.ico">`
- `app/apple-icon.png` — emittiert `<link rel="apple-touch-icon" href="/apple-icon.png?...">`
- `app/opengraph-image.{png,jpg}` ODER `app/opengraph-image.tsx` — emittiert OG-Meta-Tags
- `app/twitter-image.{png,jpg}` ODER `app/twitter-image.tsx` — analog

Reference docs: `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/01-metadata/app-icons.mdx` (oder Next.js-Doku falls Pfad differiert).

Wenn diese Files in `app/` liegen, ist KEINE explizite `metadata.icons`-Konfiguration in `layout.tsx` nötig. Next.js generiert die `<link>`-Tags automatisch + addet Cache-Buster zur URL.

**Currently in `apps/tools-app/app/layout.tsx` (lines 35-44):**
```ts
icons: {
  icon: [
    { url: "/icon.svg", type: "image/svg+xml" },
    { url: "/favicon.png", sizes: "32x32", type: "image/png" },
    { url: "/icon-512.png", sizes: "512x512", type: "image/png" },
  ],
  apple: [
    { url: "/apple-touch-icon.png", sizes: "180x180", type: "image/png" },
  ],
},
```
**Wird komplett entfernt** — Convention-Detection übernimmt nach Asset-Move zu `app/`.

**Currently in `apps/website/app/layout.tsx` (lines 36-37):**
```ts
// Icons werden automatisch über app/icon.svg (Next.js file convention) bereitgestellt.
```
**Bleibt unverändert** — Website nutzt die Convention bereits korrekt. Nur das Asset selbst wird ersetzt.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Asset-Swap Website — neue Favicons aus brand/logos/favicons/ + Cleanup public/</name>
  <files>
    apps/website/app/icon.svg,
    apps/website/app/favicon.ico,
    apps/website/app/apple-icon.png,
    apps/website/public/favicon.png (DELETE),
    apps/website/public/apple-touch-icon.png (DELETE),
    apps/website/public/icon.svg (DELETE),
    apps/website/public/icon-512.png (DELETE),
    apps/website/public/og-image.jpg (DELETE)
  </files>
  <action>
    **Source: `brand/logos/favicons/` ist single source of truth (D-05).**

    Schritte (alle vom Repo-Root ausführen):

    1. **Replace App Router Convention Files** (überschreibt existierende):
       ```bash
       cp brand/logos/favicons/favicon.svg          apps/website/app/icon.svg
       cp brand/logos/favicons/favicon.ico          apps/website/app/favicon.ico
       cp brand/logos/favicons/apple-touch-icon.png apps/website/app/apple-icon.png
       ```
       Hinweis: `app/apple-icon.png` ist NEU für die Website (existiert bisher nicht). Convention-Naming ist `apple-icon.{png}` (nicht `apple-touch-icon.png`!) — Next.js auto-detected nur diesen Filenamen.

    2. **Delete legacy duplicates aus `public/`**:
       ```bash
       git rm apps/website/public/favicon.png
       git rm apps/website/public/apple-touch-icon.png
       git rm apps/website/public/icon.svg
       git rm apps/website/public/icon-512.png
       git rm apps/website/public/og-image.jpg
       ```
       Begründung pro File:
       - `public/icon.svg` — duplikat zu `app/icon.svg`, Next.js würde Convention bevorzugen aber Konflikt-Risiko (gleicher öffentlicher Pfad).
       - `public/favicon.png` — alter PNG-Fallback, durch `app/icon.svg` + `app/favicon.ico` ersetzt.
       - `public/icon-512.png` — alter Hi-DPI-Fallback, SVG ist resolution-independent.
       - `public/apple-touch-icon.png` — durch `app/apple-icon.png` Convention ersetzt.
       - `public/og-image.jpg` — verwaist seit Phase 22.6 Track B (`app/opengraph-image.tsx` generiert dynamisch). Keine Referenzen mehr in metadata oder Code.

    3. **Verify keine Code-Referenzen zu gelöschten Files**:
       ```bash
       grep -rn "favicon.png\|apple-touch-icon\|og-image.jpg\|icon-512" apps/website --include="*.ts" --include="*.tsx" --include="*.json"
       ```
       Erwartetes Ergebnis: KEINE Treffer (außer ggf. das bereits-vorhandene `app/opengraph-image.tsx`, das aber `logo-wide-neon-on-blue.svg` nutzt, nicht `og-image.jpg`).

       Falls Treffer: stoppen, Referenz-Pfad anpassen, dann erst löschen.

    **Out-of-scope:**
    - `apps/website/app/opengraph-image.tsx` + `app/twitter-image.tsx` bleiben unverändert (Phase 22.6 Track B Pattern).
    - `public/brand/` Ordner bleibt komplett unverändert (Marketing-Logos für OG-Generation).
    - Keine Manifest-Files (`site.webmanifest`) — Next.js Convention regelt das via `metadata.manifest` falls je nötig.
  </action>
  <verify>
    <automated>
    cd /Users/lucaschweigmann/projects/generation-ai &amp;&amp;
    test -f apps/website/app/icon.svg &amp;&amp;
    test -f apps/website/app/favicon.ico &amp;&amp;
    test -f apps/website/app/apple-icon.png &amp;&amp;
    test ! -f apps/website/public/favicon.png &amp;&amp;
    test ! -f apps/website/public/apple-touch-icon.png &amp;&amp;
    test ! -f apps/website/public/icon.svg &amp;&amp;
    test ! -f apps/website/public/icon-512.png &amp;&amp;
    test ! -f apps/website/public/og-image.jpg &amp;&amp;
    diff -q brand/logos/favicons/favicon.svg apps/website/app/icon.svg &amp;&amp;
    diff -q brand/logos/favicons/favicon.ico apps/website/app/favicon.ico &amp;&amp;
    diff -q brand/logos/favicons/apple-touch-icon.png apps/website/app/apple-icon.png &amp;&amp;
    ! grep -rn "favicon.png\|apple-touch-icon\|og-image.jpg\|icon-512" apps/website --include="*.ts" --include="*.tsx" --include="*.json"
    </automated>
  </verify>
  <done>
    Website nutzt `app/icon.svg` + `app/favicon.ico` + `app/apple-icon.png` aus `brand/logos/favicons/`. Legacy `public/`-Files gelöscht. Keine verwaisten Referenzen in Code.
  </done>
</task>

<task type="auto">
  <name>Task 2: Asset-Swap Tools-App + Strip metadata.icons + OG-Image-Update</name>
  <files>
    apps/tools-app/app/icon.svg,
    apps/tools-app/app/favicon.ico,
    apps/tools-app/app/apple-icon.png,
    apps/tools-app/app/layout.tsx,
    apps/tools-app/public/og-image-default.png,
    apps/tools-app/public/favicon.png (DELETE),
    apps/tools-app/public/apple-touch-icon.png (DELETE),
    apps/tools-app/public/icon.svg (DELETE),
    apps/tools-app/public/icon-512.png (DELETE),
    apps/tools-app/public/og-image-v2.png (DELETE)
  </files>
  <action>
    Tools-app von `public/` + expliziter `metadata.icons` auf Next.js File Convention umstellen.

    Schritte (alle vom Repo-Root):

    1. **Copy new icon assets to App Router convention paths** (`app/icon.svg` etc. existiert bisher NICHT in tools-app):
       ```bash
       cp brand/logos/favicons/favicon.svg          apps/tools-app/app/icon.svg
       cp brand/logos/favicons/favicon.ico          apps/tools-app/app/favicon.ico
       cp brand/logos/favicons/apple-touch-icon.png apps/tools-app/app/apple-icon.png
       ```
       Note: `app/favicon.ico` existiert bereits in tools-app (Phase-15-Setup) — wird durch das neue Asset überschrieben.

    2. **Copy new OG-image to public/**:
       ```bash
       cp brand/logos/og-image-default.png apps/tools-app/public/og-image-default.png
       ```

    3. **Edit `apps/tools-app/app/layout.tsx`**:

       **a) Delete the `icons` block (lines 34-44):**
       ```diff
       -  // Favicon &amp; Icons
       -  icons: {
       -    icon: [
       -      { url: "/icon.svg", type: "image/svg+xml" },
       -      { url: "/favicon.png", sizes: "32x32", type: "image/png" },
       -      { url: "/icon-512.png", sizes: "512x512", type: "image/png" },
       -    ],
       -    apple: [
       -      { url: "/apple-touch-icon.png", sizes: "180x180", type: "image/png" },
       -    ],
       -  },
       -
          // Open Graph (Facebook, WhatsApp, etc.)
       ```
       Inline-Kommentar (analog Website-Pattern) oberhalb des entfernten Blocks adden:
       ```ts
       // Icons werden automatisch über app/icon.svg + app/favicon.ico + app/apple-icon.png
       // (Next.js file convention) bereitgestellt — kein expliziter metadata.icons-Block mehr.
       ```

       **b) Update OG-image URLs (lines 54-61 + 69)**:
       ```diff
            images: [
              {
       -        url: "/og-image-v2.png",
       +        url: "/og-image-default.png",
                width: 1200,
                height: 630,
                alt: "Generation AI - KI-Tools für Studierende",
              },
            ],
        ...
            description: "Die kuratierte KI-Tool-Bibliothek für Studierende im DACH-Raum.",
       -    images: ["/og-image-v2.png"],
       +    images: ["/og-image-default.png"],
       ```

    4. **Delete legacy duplicates in `public/`**:
       ```bash
       git rm apps/tools-app/public/favicon.png
       git rm apps/tools-app/public/apple-touch-icon.png
       git rm apps/tools-app/public/icon.svg
       git rm apps/tools-app/public/icon-512.png
       git rm apps/tools-app/public/og-image-v2.png
       ```

    5. **Verify keine Code-Referenzen zu gelöschten Files**:
       ```bash
       grep -rn "favicon.png\|apple-touch-icon\|og-image-v2\|icon-512\|/icon.svg" apps/tools-app --include="*.ts" --include="*.tsx" --include="*.json"
       ```
       Erwartetes Ergebnis: KEINE Treffer. Falls `/icon.svg` Treffer kommen aus `node_modules` oder Tests — die sind ok. Eigene Source-Files: 0.

    **Out-of-scope:**
    - `apps/tools-app/public/dot-grid-neon.png`, `logo-blue-neon-new.jpg`, `logo-pink-red.jpg`, `logo-blue-neon.png` bleiben — sind Content-Assets (Hero-Backgrounds, Logo-Renders), keine Browser-Icons.
    - `apps/tools-app/public/brand/` falls existiert: bleibt.
    - Twitter-Image: tools-app hat keinen separaten `app/twitter-image.tsx` — `metadata.twitter.images` referenziert dieselbe URL, mitupgedated in Step 3b.
  </action>
  <verify>
    <automated>
    cd /Users/lucaschweigmann/projects/generation-ai &amp;&amp;
    test -f apps/tools-app/app/icon.svg &amp;&amp;
    test -f apps/tools-app/app/favicon.ico &amp;&amp;
    test -f apps/tools-app/app/apple-icon.png &amp;&amp;
    test -f apps/tools-app/public/og-image-default.png &amp;&amp;
    test ! -f apps/tools-app/public/favicon.png &amp;&amp;
    test ! -f apps/tools-app/public/apple-touch-icon.png &amp;&amp;
    test ! -f apps/tools-app/public/icon.svg &amp;&amp;
    test ! -f apps/tools-app/public/icon-512.png &amp;&amp;
    test ! -f apps/tools-app/public/og-image-v2.png &amp;&amp;
    diff -q brand/logos/favicons/favicon.svg apps/tools-app/app/icon.svg &amp;&amp;
    diff -q brand/logos/favicons/favicon.ico apps/tools-app/app/favicon.ico &amp;&amp;
    diff -q brand/logos/favicons/apple-touch-icon.png apps/tools-app/app/apple-icon.png &amp;&amp;
    diff -q brand/logos/og-image-default.png apps/tools-app/public/og-image-default.png &amp;&amp;
    ! grep -n "icons:\s*{" apps/tools-app/app/layout.tsx &amp;&amp;
    grep -q "og-image-default.png" apps/tools-app/app/layout.tsx &amp;&amp;
    ! grep -q "og-image-v2" apps/tools-app/app/layout.tsx
    </automated>
  </verify>
  <done>
    Tools-app nutzt App Router File Convention für Icons (kein expliziter `metadata.icons`-Block mehr). OG-Image-Pfad zeigt auf neues `og-image-default.png`. Legacy `public/`-Files gelöscht. Source-of-truth-Files byte-identisch zu `brand/logos/favicons/`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build-Verify beide Apps + visueller Cross-Domain-Check</name>
  <files>
    (kein File-Change — Verifikation only)
  </files>
  <action>
    Sicherstellen dass beide Apps grün bauen, keine 404er auf Icon/OG-Pfaden, und dass Next.js die Convention-basierten Icons korrekt emittiert.

    **A) Build-Check (beide Apps)**:
    ```bash
    cd /Users/lucaschweigmann/projects/generation-ai
    pnpm --filter @genai/website build
    pnpm --filter @genai/tools-app build
    ```
    Erwartung: Beide grün, keine "Module not found" Errors für gelöschte Icon/OG-Pfade.

    **B) Lokaler Prod-Smoke (Website)**:
    ```bash
    cd apps/website &amp;&amp; NODE_ENV=production pnpm start &amp;
    # warten bis "Ready", dann:
    curl -sI http://localhost:3000/icon.svg     | head -1   # erwartet 200
    curl -sI http://localhost:3000/favicon.ico  | head -1   # erwartet 200
    curl -sI http://localhost:3000/apple-icon.png | head -1 # erwartet 200 (Next.js convention path)
    curl -s  http://localhost:3000/ | grep -oE 'rel="icon"[^>]*' | head -3  # erwartet rel="icon" mit href auf /icon.svg
    curl -s  http://localhost:3000/ | grep -oE 'rel="apple-touch-icon"[^>]*' | head -1  # erwartet 1 Match
    # Cleanup:
    pkill -f "next start" 2>/dev/null
    ```

    **C) Lokaler Prod-Smoke (Tools-app)**:
    ```bash
    cd apps/tools-app &amp;&amp; NODE_ENV=production pnpm start &amp;
    # warten, dann analog:
    curl -sI http://localhost:3000/icon.svg                | head -1   # erwartet 200
    curl -sI http://localhost:3000/favicon.ico             | head -1   # erwartet 200
    curl -sI http://localhost:3000/apple-icon.png          | head -1   # erwartet 200
    curl -sI http://localhost:3000/og-image-default.png    | head -1   # erwartet 200
    curl -sI http://localhost:3000/og-image-v2.png         | head -1   # erwartet 404 (gelöscht)
    curl -s  http://localhost:3000/ | grep -oE 'og:image[^>]*content="[^"]*"' | head -1  # erwartet og-image-default.png
    pkill -f "next start" 2>/dev/null
    ```

    **D) Visueller Cross-Domain-Tab-Check (manuell, im Browser)**:
    Da das hier ein autonomer Plan ist und Luca die Phase pre-approved hat, KEIN Stop-Gate. Stattdessen Screenshot via Playwright als Beweis:
    ```bash
    # Im Repo-Root, mit lokalen prod-Servern beider Apps auf 3000 + 3001:
    cd apps/website &amp;&amp; PORT=3000 NODE_ENV=production pnpm start &amp;
    cd apps/tools-app &amp;&amp; PORT=3001 NODE_ENV=production pnpm start &amp;
    # Playwright-Snippet (in packages/e2e-tools oder inline):
    npx playwright screenshot --browser=chromium http://localhost:3000/icon.svg /tmp/website-icon.png
    npx playwright screenshot --browser=chromium http://localhost:3001/icon.svg /tmp/tools-icon.png
    diff /tmp/website-icon.png /tmp/tools-icon.png &amp;&amp; echo "IDENTICAL FAVICON ✓" || echo "FAVICON MISMATCH ✗"
    pkill -f "next start" 2>/dev/null
    ```

    Falls `playwright screenshot` nicht verfügbar oder Pfad fragwürdig: Fallback auf direkten Asset-Vergleich:
    ```bash
    diff -q apps/website/app/icon.svg apps/tools-app/app/icon.svg &amp;&amp; echo "BYTE-IDENTICAL ✓"
    ```

    **E) ROADMAP-Status-Update**: Plan 22.8-03 als done markieren in `.planning/ROADMAP.md`.

    **Out-of-scope (post-deploy-Checks für Luca selbst nach Vercel-Promotion)**:
    - Browser-Tab-Favicon visuell auf `generation-ai.org` vs. `tools.generation-ai.org` (nach Deploy via gewohntem Vercel-Preview-Workflow)
    - iOS Home Screen Icon-Test (manuell, Luca's Device, nach Deploy)
    - Slack/WhatsApp/iMessage Link-Preview-Test (manuell, post-deploy)
    - Browser-Cache-Bust für returning Users (advise: hard-refresh)
  </action>
  <verify>
    <automated>
    cd /Users/lucaschweigmann/projects/generation-ai &amp;&amp;
    pnpm --filter @genai/website build &amp;&amp;
    pnpm --filter @genai/tools-app build &amp;&amp;
    diff -q apps/website/app/icon.svg apps/tools-app/app/icon.svg &amp;&amp;
    diff -q apps/website/app/favicon.ico apps/tools-app/app/favicon.ico &amp;&amp;
    diff -q apps/website/app/apple-icon.png apps/tools-app/app/apple-icon.png
    </automated>
  </verify>
  <done>
    Beide Apps bauen grün. Source-Files in beiden Apps sind byte-identisch (= identical favicon rendering). OG-Image für tools-app zeigt auf das neue Asset. Manuelle Cross-Domain-Verifikation post-deploy macht Luca selbst (out-of-scope dieses Plans, nicht Stop-Gate-würdig per D-14).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Static Asset | Public Icon/OG-Files werden als unauth Static-Assets ausgeliefert. Kein User-Input, keine Server-Logik. |
| Build-Pipeline → Source Asset | `brand/logos/favicons/` Files werden zur Build-Zeit kopiert (Git-tracked) — keine Build-Zeit-Resolution externer Sources. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22.8.03-01 | Information Disclosure | OG-Image-Tag | accept | OG-Images sind public-by-design (Twitter/Facebook crawlen sie). Asset enthält nur Brand-Logo + Tagline, keine PII. |
| T-22.8.03-02 | Tampering | Stale Browser Cache | accept | Returning Users sehen evtl. alten Favicon bis Hard-Refresh. Next.js addet automatisch Cache-Buster-Query an Convention-URLs (`?...`) für neue Builds. Nicht-deterministisch für bereits gecachte alte URLs — Mitigation: Vercel-Deploy invalidiert CDN, returning Users hard-refreshen. |
| T-22.8.03-03 | Denial of Service | 404 auf Legacy-Asset-Pfad | accept | Externe Links/Bookmarks die `/og-image-v2.png` oder `/favicon.png` referenzieren, brechen post-deploy. Risiko niedrig: Es gibt keine bekannten externen Konsumenten dieser Pfade (interne Apps nutzen Convention-Path). Sentry-Alert auf 404-Spike würde es fangen falls relevant. |
| T-22.8.03-04 | Spoofing | Favicon-Inkonsistenz | mitigate | Plan-Mission: identical Favicon auf beiden Domains. Mitigation: byte-Level-diff in Verify-Schritt + single source of truth `brand/logos/favicons/`. Phishing-Risiko reduziert weil Generation-AI-Subdomains alle gleich aussehen. |
</threat_model>

<verification>
**Automated:**
- Beide Apps bauen grün (Tasks 1-2 → Task 3)
- Source-Files in `app/icon.{svg,ico}` + `app/apple-icon.png` byte-identisch zu `brand/logos/favicons/` (Task 1+2 verify)
- Beide Apps haben byte-identische Icon-Files (Task 3 verify cross-app)
- Keine verwaisten Referenzen zu gelöschten Files (Task 1+2 grep)
- `metadata.icons`-Block aus tools-app/layout.tsx entfernt (Task 2 verify)
- OG-Image-URL in tools-app updated (Task 2 verify)
- Lokaler Prod-Smoke beide Apps: HTTP 200 auf Convention-Pfaden, `<link rel="icon">` Tags emittiert (Task 3 manual)

**Out-of-scope (post-deploy für Luca):**
- Visueller Browser-Tab-Vergleich auf `generation-ai.org` vs. `tools.generation-ai.org`
- iOS Home Screen Icon-Test
- Slack/WhatsApp/iMessage Link-Preview Cross-Check
</verification>

<success_criteria>
- [ ] `apps/website/app/icon.svg`, `app/favicon.ico`, `app/apple-icon.png` existieren und sind byte-identisch zu `brand/logos/favicons/{favicon.svg, favicon.ico, apple-touch-icon.png}`
- [ ] `apps/tools-app/app/icon.svg`, `app/favicon.ico`, `app/apple-icon.png` existieren und sind byte-identisch zu denselben Source-Files
- [ ] `apps/website/public/{favicon.png, apple-touch-icon.png, icon.svg, icon-512.png, og-image.jpg}` GELÖSCHT
- [ ] `apps/tools-app/public/{favicon.png, apple-touch-icon.png, icon.svg, icon-512.png, og-image-v2.png}` GELÖSCHT
- [ ] `apps/tools-app/public/og-image-default.png` existiert (kopiert aus `brand/logos/og-image-default.png`)
- [ ] `apps/tools-app/app/layout.tsx`: kein `icons:`-Block mehr, OG-Image-URL = `/og-image-default.png` (sowohl in `openGraph.images` als auch `twitter.images`)
- [ ] `apps/website/app/layout.tsx` unverändert bezüglich `metadata` (Convention war schon korrekt, nur das Asset getauscht)
- [ ] `pnpm --filter @genai/website build` grün
- [ ] `pnpm --filter @genai/tools-app build` grün
- [ ] Keine verwaisten Code-Referenzen zu `favicon.png`, `apple-touch-icon`, `og-image.jpg`, `og-image-v2`, `icon-512` in beiden Apps
- [ ] Alle Routes weiterhin `ƒ` (dynamic) im Build-Output (CSP-Force-Dynamic-Regel aus LEARNINGS.md unverletzt)
</success_criteria>

<output>
After completion, create `.planning/phases/22.8-ui-konsistenz-section-refinement/22.8-03-SUMMARY.md` with:
- Asset-Inventur vorher/nachher (welche Files in `app/`, welche in `public/`)
- Convention-Migration-Notes (tools-app: explicit metadata → Convention)
- Build-Output-Excerpt (beide Apps grün, dynamic-Routes erhalten)
- Cross-app byte-diff Bestätigung (identisches Favicon-Asset)
- Out-of-scope post-deploy checks für Luca dokumentiert (Tab-Vergleich, iOS Home Screen, Link-Preview)
</output>
