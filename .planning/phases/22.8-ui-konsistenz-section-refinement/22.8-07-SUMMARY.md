---
phase: 22.8
plan: 07
type: summary
status: done
created: 2026-04-26
wave: 1
---

# Plan 22.8-07 — Summary

**Status:** ✅ DONE

## What shipped

- `/join` renders the form inside the hero: desktop has copy left and form right; mobile stacks copy then form.
- The embedded form uses compact spacing so the desktop CTA fits in a 1440×900 viewport.
- Mobile spacing was tightened so the form starts high in the first viewport while preserving 40px+ touch targets.
- Status helper text now uses Sans, while the status labels remain Mono.
- Hochschule input now has 61 deterministic DACH Hochschule/Uni options plus `Andere`, live type-to-filter behavior, and a detail input when `Andere` is selected.
- Studienfeld `Sonstiges` now reveals a detail input.
- Analytics remain categorical: `university` stays `Andere` and `study_field` stays `Sonstiges`; free-text detail is carried via existing `study_program` in V1 and metadata in Signup V2. No new DB migration.

## Verification

- `pnpm --filter @genai/website exec tsc --noEmit` ✅
- `pnpm --filter @genai/e2e-tools exec playwright test tests/join.spec.ts` ✅ 14/14
- `pnpm build` ✅
- Visual viewport smoke:
  - Desktop 1440×900: form top 118px, bottom 828px, submit bottom 807px.
  - Mobile 390×844: form starts at 396px; first viewport shows copy, form start, status block, and education-field entry.

## Notes

- Sources checked for university coverage: CHE Hochschuldaten and Studis Online confirm IU/FOM/FernUni as high-volume institutions; IU and FOM were added to the fixed list.
- Next wave: 22.8-08 + 22.8-09 About form and timeline.
