---
phase: 20.5-landing-wow-pass-signal-grid
plan: 03
type: execute
wave: 3
depends_on: [20.5-01, 20.5-02]
files_modified:
  - apps/website/components/sections/hero-section.tsx
  - apps/website/components/ui/grid-background.tsx
autonomous: true
requirements:
  - HERO-WOW-01
  - HERO-WOW-02

must_haves:
  truths:
    - "Hero-Background ist SignalGrid, nicht mehr GridBackground"
    - "H1 rendert in Geist Mono (DS-Spec)"
    - "Primary-CTA hat Pill-Radius + Neon-Glow-Hover (scale 1.03 + box-shadow accent-glow)"
    - "Alte GridBackground als deprecated markiert, nicht gelöscht"
  artifacts:
    - path: "apps/website/components/sections/hero-section.tsx"
      provides: "Hero mit SignalGrid-Background"
    - path: "apps/website/components/ui/grid-background.tsx"
      provides: "Deprecated-Comment-Header"
  key_links:
    - from: "hero-section.tsx"
      to: "signal-grid.tsx"
      via: "import + JSX-Wrap"
      pattern: "import.*SignalGrid.*ui/signal-grid"
---

<objective>
Hero-Section verwendet jetzt `<SignalGrid>` statt `<GridBackground>`. Typography und Button-States sind DS-konform.

Purpose: D-01 (Hero = Signal-Grid) umsetzen. D-03 (DS als Nordstern) für Hero anwenden.
Output: Rewritten hero-section.tsx, alte grid-background.tsx bleibt aber deprecated markiert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20.5-landing-wow-pass-signal-grid/20.5-CONTEXT.md
@brand/Generation AI Design System/README.md
@brand/Generation AI Design System/DESIGN.md
@apps/website/components/sections/hero-section.tsx
@apps/website/components/ui/grid-background.tsx
@apps/website/components/ui/signal-grid.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero Rewrite — SignalGrid + DS-Typography + DS-Button-States</name>
  <files>apps/website/components/sections/hero-section.tsx</files>
  <action>
    Öffne `apps/website/components/sections/hero-section.tsx`. Ersetze:

    1. **Background swap:** `<GridBackground>…</GridBackground>` → `<SignalGrid>…</SignalGrid>`. Import anpassen: `import { SignalGrid } from "@/components/ui/signal-grid";`. Props min-height/className analog übernehmen. Falls Hero aktuell `min-h-[80vh]` o.ä. hatte, gleich lassen.

    2. **H1 Typography:** `<h1>` bekommt Tailwind-Klasse `font-mono` (Geist Mono per DS). Bestehende size-Klassen (`text-4xl md:text-6xl` o.ä.) bleiben, nur Font-Familie wechselt. Tracking ebenfalls prüfen: DS-Spec sagt `-0.02em` für H1 — via Tailwind `tracking-tight` oder custom `[letter-spacing:-0.02em]`.

    3. **Primary-CTA DS-Button-States:** der "Jetzt beitreten" / primary-CTA-Button:
       - Radius: `rounded-full` (Pill per DS)
       - Hover: `hover:scale-[1.03] hover:shadow-[0_0_20px_var(--accent-glow)] transition-all duration-[var(--dur-normal)] ease-[var(--ease-out)]`
       - Active/Press: `active:scale-[0.98]`
       - Icon innerhalb Button (falls vorhanden): `group-hover:translate-x-0.5` oder `group-hover:scale-110`
       - Falls ein Secondary-CTA (z.B. "Was wir machen") existiert: per DS `border-accent-soft` hover, text-heller.

    4. **Claim-Placeholder:** bleibt wie er ist (finale Wording ist phase-später). Nicht neu schreiben.

    5. **Reduced-Motion-Guard:** useReducedMotion aus motion/react bleibt — wenn schon drin, lassen. SignalGrid handled intern auch Reduced-Motion.

    **Strikt:** keine neuen hardcodierten Hex-Colors. Alles via DS-CSS-Vars oder Tailwind-Utilities. Wenn Hintergrund-Bereich außerhalb von SignalGrid eine Farbe braucht → `var(--bg)`.
  </action>
  <verify>
    <automated>cd apps/website && pnpm build 2>&1 | tail -20 && grep -c "SignalGrid" apps/website/components/sections/hero-section.tsx</automated>
  </verify>
  <done>
    - Hero importiert + verwendet `SignalGrid` (grep count >= 2: import + JSX)
    - `GridBackground`-Import + -Verwendung entfernt aus hero-section.tsx
    - H1 hat `font-mono` Klasse
    - Primary-CTA hat `rounded-full` + Hover-Glow-State
    - Build + Lint grün
    - Localhost-Smoke: Hero rendert mit SignalGrid, H1 in Mono, CTA-Hover zeigt Neon-Glow
  </done>
</task>

<task type="auto">
  <name>Task 2: Deprecate grid-background.tsx</name>
  <files>apps/website/components/ui/grid-background.tsx</files>
  <action>
    Datei nicht löschen (könnte später für andere Contexts nützlich sein). Am Top der Datei ein Deprecation-Header-Kommentar:

    ```tsx
    /**
     * @deprecated Since Phase 20.5 (2026-04-21)
     *
     * Ersetzt durch <SignalGrid> in apps/website/components/ui/signal-grid.tsx,
     * welches der Design-System-Spezifikation entspricht
     * (brand/Generation AI Design System/README.md §Visual Foundations → Backgrounds).
     *
     * Nicht mehr in Hero verwendet. Behalten für evtl. zukünftige Contexts,
     * wo ein dezenterer Grid-Background gewünscht ist. Bei endgültigem Nicht-Gebrauch löschbar.
     */
    ```

    Kein Code-Change sonst. Keine Imports anpassen (Hero importiert es nicht mehr, andere Stellen prüfen: `grep -r "grid-background" apps/website` — falls noch wo referenziert, Kommentar dran lassen aber nicht löschen).
  </action>
  <verify>
    <automated>cd apps/website && pnpm build 2>&1 | tail -10 && grep -c "@deprecated" apps/website/components/ui/grid-background.tsx</automated>
  </verify>
  <done>
    - Datei enthält `@deprecated`-Tag
    - Keine Imports von `grid-background` mehr in hero-section (verifiziert via grep)
    - Build grün
  </done>
</task>

</tasks>

<verification>
- `pnpm build` in apps/website grün
- `pnpm lint` grün
- Playwright Landing-Test (R1.2 + CSP) bleibt grün — kein Test-Break durch Component-Swap
- Localhost-Smoke: Hero mit SignalGrid sichtbar, H1 in Mono, CTA-Hover = Neon-Glow
</verification>

<success_criteria>
- Hero nutzt SignalGrid
- Typography + Button-States DS-konform
- GridBackground behalten aber deprecated markiert
- Keine Playwright-Regression
</success_criteria>

<output>
`.planning/phases/20.5-landing-wow-pass-signal-grid/20.5-03-SUMMARY.md` mit:
- Vor/Nach-Diff des Hero (prose)
- Button-State-Details
- Deprecation-Note für GridBackground
- Commit-Hash
</output>
