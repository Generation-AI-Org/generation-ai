---
phase: 20.5-landing-wow-pass-signal-grid
plan: 05
type: execute
wave: 4
depends_on: [20.5-03, 20.5-04]
files_modified:
  - apps/website/components/sections/hero-section.tsx
  - apps/website/components/sections/discrepancy-section.tsx
  - apps/website/components/home-client.tsx
autonomous: true
requirements:
  - TRANSITION-01

must_haves:
  truths:
    - "Beim Scroll von Hero-End zu Discrepancy-Start passiert eine bewusste Transition"
    - "Transition fühlt sich intentional an, nicht zufällig"
    - "Reduced-Motion-Pfad: keine Transition, harter Cut, beide Sections statisch"
  artifacts:
    - path: "apps/website/components/sections/hero-section.tsx"
      provides: "Hero-End-Transition-Hook (oder shared Transition-Component)"
    - path: "apps/website/components/sections/discrepancy-section.tsx"
      provides: "Discrepancy-Start-Transition-Hook"
  key_links:
    - from: "hero-section.tsx OR home-client.tsx"
      to: "discrepancy-section.tsx"
      via: "shared scroll-progress oder Transition-Wrapper-Component"
      pattern: "useScroll|motion\\.div.*transition"
---

<objective>
Transition-Animation zwischen Hero (end) und Discrepancy (start). Luca hat das als bewussten Design-Moment markiert — **Spec wird live mit Luca festgelegt**, nicht upfront.

Purpose: D-06 — Transition ist Design-Moment, not afterthought. Mid-execution co-designed.
Output: 1 Transition-Variante implementiert, die zwischen den 4 Kandidaten gewählt wurde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20.5-landing-wow-pass-signal-grid/20.5-CONTEXT.md
@brand/Generation AI Design System/README.md
@apps/website/components/sections/hero-section.tsx
@apps/website/components/sections/discrepancy-section.tsx
@apps/website/components/home-client.tsx
@apps/website/components/ui/signal-grid.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kandidaten präsentieren + Luca-Pick</name>
  <files>apps/website/components/sections/hero-section.tsx, apps/website/components/sections/discrepancy-section.tsx, apps/website/components/home-client.tsx</files>
  <action>
    **Dieses Task ist interaktiv** — Luca schaut live zu.

    4 Kandidaten-Richtungen für die Hero → Discrepancy Transition (alle reduzierbar auf prefers-reduced-motion):

    **Kandidat A — "Nodes reisen ab":**
    - Am Ende des Hero-Scrolls migrieren die SignalGrid-Nodes nach unten aus dem Viewport, morphen ihre Opacity/Size auf die späteren Chart-Datenpunkte. Übergang fühlt sich an wie "das Signal wird zum Datenpunkt".

    **Kandidat B — "Signal-Line shoots":**
    - Eine einzelne Linie startet vom Zentrum des Hero-Ends, schießt diagonal nach unten ins Discrepancy-Panel und entrollt sich dort in die obere Chart-Kurve. Kurze, filmische Line-Draw-Animation — "red-thread"-Vibe.

    **Kandidat C — "Radial Wipe":**
    - Am Ende des Hero-Scrolls: Radial-Gradient-Mask expandiert vom Center-Hero und revealed darunter den Discrepancy-Title + initial Chart-Frame. Fast wie ein Tinten-Bleed.

    **Kandidat D — "Fade-Leak":**
    - Hero's Radial-Fade-Overlay wird beim Scroll-out weiter, "leakt" hinunter in die Discrepancy-Section. Die erste Chart-Kurve startet bei fade-opacity 0.3 und wird mit weiterem Scroll bis 1.0 geführt. Ambient, subtil.

    **Entscheidungs-Mode:**
    - Zeige Luca die 4 Optionen (als Text-Beschreibung im Chat, mit kurzen Animations-Metaphern)
    - Falls time allows: baue 1-2 Quick-Previews (~10 Min jeweils) auf localhost um sie zu fühlen
    - Luca picks — dann den gewählten Kandidaten ausbauen

    **Commit-Strategie:**
    - Quick-Previews als WIP-Commits: `wip(20.5-05): transition candidate A preview` — können später gesquasht werden
    - Finale Version: `feat(20.5-05): hero → discrepancy transition animation ({kandidat})`
  </action>
  <verify>
    <automated>cd apps/website && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Luca hat einen Kandidaten gewählt (notiert im SUMMARY)
    - Quick-Previews (falls erstellt) als WIP-Commits oder verworfen
    - Build nach Picks grün
  </done>
</task>

<task type="auto">
  <name>Task 2: Chosen Transition — Final Build + Reduced-Motion-Guard</name>
  <files>apps/website/components/sections/hero-section.tsx, apps/website/components/sections/discrepancy-section.tsx, apps/website/components/home-client.tsx</files>
  <action>
    Implementiere den gewählten Kandidaten finalisiert:

    **Allgemeine Constraints (gelten für jeden Kandidaten):**
    - `useScroll` mit `offset` zwischen Hero-End und Discrepancy-Start (entweder in home-client.tsx als shared wrapper, oder via Intersection-Observer pro Section)
    - Motion-Durations aus DS: `--dur-normal` (300ms) oder `--dur-slow` (500ms) je nach Feeling
    - Easing: `--ease-out` für "stößt raus" Feel, `--ease-in-out` wenn mehr cinematic
    - CSS-Vars nur — keine hardcodierten Colors
    - Performance: wenn neue rAF-Loops nötig → IntersectionObserver-pause-Pattern wie in SignalGrid
    - CSP-safe: keine inline-styles die gegen nonce-CSP verstoßen (motion/react via framer injizes nonce selbst über MotionConfig — schon in Phase 20 geregelt)

    **Reduced-Motion-Guard:**
    - `const prefersReducedMotion = useReducedMotion();`
    - Wenn `true`: Transition-Element rendert **nicht** (oder in final-state sichtbar), kein useScroll-Listener wird registriert. Harter Cut zwischen Hero und Discrepancy — beide Sections rendern ihre eigenen Reduced-Motion-Pfade (Plan 02/03/04 haben das bereits).

    **Luca-Feedback-Loop:**
    - Commit initial Version
    - Luca scrollt, gibt Feedback
    - 1-2 Tuning-Iterationen (speed, timing, opacity-curves)
    - Endversion committen

    **Akzeptanz:** Luca sagt "das fühlt sich richtig an". Nicht mehr, nicht weniger.
  </action>
  <verify>
    <automated>cd apps/website && pnpm build 2>&1 | tail -20 && pnpm --filter @genai/e2e-tools exec playwright test tests/landing.spec.ts --reporter=line 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Transition sichtbar + smooth auf localhost
    - Reduced-Motion-Browser-Setting → keine Transition, beide Sections statisch OK
    - Build + Lint grün
    - Playwright Landing-Test bleibt grün (keine neue Regression)
    - Luca-"OK" notiert im SUMMARY
  </done>
</task>

</tasks>

<verification>
- `pnpm build` grün
- Playwright `landing.spec.ts` grün (R1.1, R1.2, R1.3, CSP)
- Localhost-Live-Check: Transition wirkt intentional, Reduced-Motion OK
</verification>

<success_criteria>
- 1 Transition-Variante implementiert, Luca-approved
- Reduced-Motion respektiert
- Keine Regression in Phase-20-Skeleton-Tests
</success_criteria>

<output>
`.planning/phases/20.5-landing-wow-pass-signal-grid/20.5-05-SUMMARY.md` mit:
- Gewählter Kandidat (A/B/C/D) + Rationale von Luca
- Final Spring/Motion-Parameter
- Luca-Quote "das fühlt sich richtig an"
- Commit-Hashes (inkl. WIP-Previews falls behalten)
</output>
