---
phase: 20.5-landing-wow-pass-signal-grid
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/globals.css
  - packages/config/tailwind/base.css
autonomous: true
requirements:
  - DS-ALIGN-01
  - DS-ALIGN-02
  - DS-ALIGN-03

must_haves:
  truths:
    - "CSS-Tokens in Website + base stimmen mit Design-System überein"
    - "Motion-Easings + Durations aus DS verfügbar als CSS-Vars"
    - "H1 rendert in Geist Mono, Body in Geist Sans (per DS)"
    - "Button-Pill-Radius + Card-rounded-2xl gematcht"
  artifacts:
    - path: "apps/website/app/globals.css"
      provides: "DS-aligned CSS-Variables"
    - path: "packages/config/tailwind/base.css"
      provides: "Neutrals + Accent + Semantic-Status aligned mit colors_and_type.css"
  key_links:
    - from: "apps/website/app/globals.css"
      to: "brand/Generation AI Design System/colors_and_type.css"
      via: "manual alignment (variable names + values)"
      pattern: "--accent|--neon|--ease|--dur"
---

<objective>
Reconcile die CSS-Variablen in der Website + im geteilten Tailwind-Base mit dem Design-System (`brand/Generation AI Design System/colors_and_type.css` + `tokens.json`). Danach sind Plan 02-05 darauf gebaut — Signal-Grid, Hero-Polish und Discrepancy-Polish referenzieren nur noch DS-konforme Tokens.

Purpose: Design-System ist Nordstern (D-03). Ohne stimmige Tokens entstehen in 02-05 wieder Abweichungen.
Output: Abgeglichene globals.css + base.css, dokumentierter Diff in Commit-Message.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20.5-landing-wow-pass-signal-grid/20.5-CONTEXT.md
@brand/Generation AI Design System/README.md
@brand/Generation AI Design System/DESIGN.md
@brand/Generation AI Design System/colors_and_type.css
@brand/Generation AI Design System/tokens.json
@apps/website/app/globals.css
@packages/config/tailwind/base.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diff + Alignment-Report</name>
  <files>apps/website/app/globals.css, packages/config/tailwind/base.css</files>
  <action>
    Lies `brand/Generation AI Design System/colors_and_type.css` vollständig. Lies `apps/website/app/globals.css` + `packages/config/tailwind/base.css`. Erstelle im Kopf einen Diff-Bericht:
    - Welche Accent/Neon/Brand-Skalen-Vars fehlen oder weichen ab?
    - Welche Motion-Tokens fehlen (`--ease-out`, `--ease-in-out`, `--dur-fast|normal|slow`)?
    - Welche Semantic-Status-Tokens fehlen oder weichen ab?
    - Welche Spacing/Radius-Tokens fehlen?

    Dann: update `globals.css` + `base.css` **minimal-invasiv**, damit sie mit DS übereinstimmen. Kein komplettes Replace — nur die Delta-Zeilen. Bestehende Radix-slate-Imports bleiben (Phase 16). Brand-Akzente (neon/red/pink/blue 1-12) aus DS übernehmen falls abweichend.

    Neu hinzufügen (falls nicht vorhanden) als globale CSS-Vars im `:root`:
    ```
    --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
    --ease-in-out: cubic-bezier(0.65, 0, 0.35, 1);
    --dur-fast: 150ms;
    --dur-normal: 300ms;
    --dur-slow: 500ms;
    ```

    Accent-Glow-Var verifizieren (für Hover-Glow auf Primary-Button): `--accent-glow` sollte im Dark-Mode neon-glow (`rgba(206, 255, 50, 0.4)` o.ä.), im Light-Mode red-glow sein. Falls fehlend → hinzufügen per DS-Spec.

    Kein neues File anlegen. Keine Tailwind-Config umschreiben. Nur CSS-Var-Deltas.
  </action>
  <verify>
    <automated>cd apps/website && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `grep -E "--(ease-out|ease-in-out|dur-fast|dur-normal|dur-slow|accent-glow)" apps/website/app/globals.css packages/config/tailwind/base.css` findet alle 6 Vars
    - `pnpm build` in apps/website grün
    - Visual-Smoke: Localhost-Home sieht identisch aus wie vorher (keine Farb-Regression, keine Layout-Verschiebung)
  </done>
</task>

<task type="auto">
  <name>Task 2: Typography + Radius Verification</name>
  <files>apps/website/app/globals.css, packages/config/tailwind/base.css</files>
  <action>
    Per DS-Spec (`README.md` §Type + `DESIGN.md`):
    - **H1** in Geist Mono, `clamp(32px, 5vw, 48px)` / 700 / -0.02em
    - **H2/H3** in Geist Sans
    - **Body** 16/400/1.65 Geist Sans
    - **Buttons/Labels/Tags/Section-Marker** in Geist Mono
    - **UI-Text:** Ligaturen aus (`font-feature-settings: "liga" 0, "calt" 0`)

    Prüfen ob Phase-16-Setup das bereits so hat. Falls ja → nichts tun. Falls H1 noch Geist Sans ist → CSS-Regel oder Tailwind-Utility ergänzen, sodass `<h1>` im Landing-Kontext per Default Geist Mono kriegt. Nicht global per `h1 { font-family }` erzwingen wenn das in tools-app Kollateral-Schaden verursacht — lieber gezielt via Tailwind-Klasse `font-mono` auf den H1-Call-Sites dokumentieren (Plan 03 wendet das an).

    Radius-Tokens verifizieren:
    - `rounded-full` für Buttons (Pill) — Tailwind-Default, passt
    - `rounded-2xl` (16px) für Cards/Modals/Inputs — Tailwind-Default, passt
    - `rounded-full` für Tags — passt

    Falls irgendwo ein nicht-DS-Radius (z.B. `rounded-lg` auf Buttons) hardcoded ist im base-layer → in Plan 03 markieren, hier nichts fixen.
  </action>
  <verify>
    <automated>cd apps/website && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Typography-Verification dokumentiert in Commit-Message: was war schon richtig, was wurde ergänzt
    - Build grün
    - Keine Regression im Visual (Localhost-Home, Header, Footer unverändert)
  </done>
</task>

</tasks>

<verification>
- `pnpm build` in apps/website grün
- `pnpm lint` in apps/website grün
- Localhost http://localhost:3000 rendert visuell identisch zu vor dem Alignment (keine Farb/Layout-Regression)
- 6 neue Motion-CSS-Vars sind auffindbar via grep
</verification>

<success_criteria>
- DS-Tokens ↔ Codebase-Tokens aligned, Delta dokumentiert
- Motion-Easings + Durations verfügbar als CSS-Vars für Plan 02-05
- Keine visuelle Regression
</success_criteria>

<output>
`.planning/phases/20.5-landing-wow-pass-signal-grid/20.5-01-SUMMARY.md` mit:
- Liste der Delta-Zeilen
- Bestätigung: Motion-Tokens verfügbar
- Typography-Status (was war schon DS-konform)
- Commit-Hash
</output>
