---
phase: 20.5-landing-wow-pass-signal-grid
plan: 04
type: execute
wave: 3
depends_on: [20.5-01]
files_modified:
  - apps/website/components/sections/discrepancy-section.tsx
autonomous: true
requirements:
  - DISCREPANCY-POLISH-01
  - DISCREPANCY-POLISH-02
  - DISCREPANCY-POLISH-03

must_haves:
  truths:
    - "Scroll-Animationen des Charts fühlen sich smooth an (useSpring gut getuned)"
    - "Achsen-Labels sind honest — keine semantisch strapazierten Gruppierungen"
    - "Mobile readable: Labels kollidieren nicht, Legend bleibt lesbar"
    - "Reduced-Motion-Path zeigt Chart sofort vollständig, keine Transforms"
  artifacts:
    - path: "apps/website/components/sections/discrepancy-section.tsx"
      provides: "Discrepancy-Section mit smoother Springs + honest Achsen"
  key_links:
    - from: "discrepancy-section.tsx"
      to: "DS Motion Tokens"
      via: "CSS-Var Referenz --ease-out / --dur-normal"
      pattern: "--ease|--dur"
---

<objective>
Polish-Pass für das Discrepancy-Sticky-Scroll-Chart. Smoother Springs, clearer X/Y-Achsen, clearer Legend/Subtitle, Mobile-Readability, Reduced-Motion-Default zeigt kompletten Chart statisch.

Purpose: D-02 umsetzen — Chart ist auf dem richtigen Weg, braucht nur Polish.
Output: Überarbeitete discrepancy-section.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20.5-landing-wow-pass-signal-grid/20.5-CONTEXT.md
@brand/Generation AI Design System/DESIGN.md
@apps/website/components/sections/discrepancy-section.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoother Springs + Reduced-Motion-Sanity</name>
  <files>apps/website/components/sections/discrepancy-section.tsx</files>
  <action>
    Öffne `discrepancy-section.tsx`. Finde alle `useSpring`-Aufrufe. Tune Parameter für smootheres Scroll-Feeling:

    - Starting-Point: probiere `stiffness: 100, damping: 30, mass: 1` als smooth-default (vs. aggressiver stiff Config)
    - Falls aktuell `restDelta` / `restSpeed` nicht gesetzt → nicht setzen (Defaults sind fine)
    - Falls `useScroll` mit `offset: ["start end", "end start"]` — gut, lassen
    - Bei scrollYProgress → useTransform → useSpring chain: die Transforms sollten ge-springt werden, nicht der raw scrollYProgress (das fühlt sich oft besser an)

    **Reduced-Motion:**
    - `const prefersReducedMotion = useReducedMotion();` am Top (aus `motion/react`, schon importiert)
    - Wenn `prefersReducedMotion === true`: alle `useTransform`-Output-Values werden auf ihren "Final"-Wert gesetzt (Chart-Divergenz voll ausgefahren), kein useSpring, keine Animation. Die 6 NumberTicker zeigen ihre finalen Zahlen ohne Count-Up-Animation.
    - Das bedeutet: 2 Code-Pfade — motion-Pfad (Scroll-getriggered) + static-Pfad (sofort final). Cleanster Approach: `prefersReducedMotion ? finalValue : useTransform(...)` pro animierter Wert, oder ein conditional wrapper.

    **Chart-Line-Smoothness:**
    - Falls das Chart SVG-Paths generiert deren `d`-Attribut durch useTransform animiert wird: sicherstellen dass Path-Interpolation zwischen Keyframes continuous ist (kein Jump-Cut).

    **Fine-Tune-Step (Luca live):**
    - Commit mit smooth-springs-V1
    - Luca scrollt auf localhost
    - Wenn Luca "zu träge" oder "zu schnell" sagt → stiffness/damping-Delta pushen (meist stiffness 120-180 oder damping 25-40 Range)
    - Expected: 1-3 Tuning-Iterationen, kleine Commits jeweils
  </action>
  <verify>
    <automated>cd apps/website && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Scroll auf localhost fühlt sich smooth an (Luca subjektiv OK)
    - Reduced-Motion (Browser-Setting oder OS-Setting) → Chart rendert komplett, keine Transitions
    - Build + Lint grün
  </done>
</task>

<task type="auto">
  <name>Task 2: Honest X/Y-Axes + Legend + Mobile-Readability</name>
  <files>apps/website/components/sections/discrepancy-section.tsx</files>
  <action>
    **Problem-Analyse zuerst:** Schau dir die aktuellen X-Achsen-Labels an. Sie waren "Nachfrage / Vergütung / Ausbildung" für 3 Gruppen à 2 Stats.

    Die Daten sind aktuell:
    - Group 1 (Nachfrage): 7× Stellen-Zuwachs vs. 56% Lohnaufschlag
    - Group 2 (etwas mit Einkommen/Vergütung): 73% KI-Nutzung?  vs.  83.5% irgendwas
    - Group 3 (Ausbildung): 75% "Studium bereitet nicht vor" vs. 6.4% andere

    Lies die aktuellen Zahlen + Labels aus dem Code und verifiziere, welche Paar-Gruppierung wirklich honest ist. Drei Options:

    **Option A — Honest-Labels-Fit:** Falls eine honest Gruppierung existiert, X-Achse labeln mit realer Kategorie. Beispiel "Jobmarkt-Signal / Einkommens-Gap / Ausbildungs-Gap". Subtitle klärt "Links = Marktsignal (KI-Wachstum), rechts = Arbeitskräfte-Defizit".

    **Option B — Pair-IDs statt Kategorien:** X-Achse zeigt keine Wort-Labels, nur "Pair 1 / Pair 2 / Pair 3" oder sogar nur Index-Marker "01 · 02 · 03" in Geist Mono (passt zur editorial-tech Signatur).

    **Option C — Axis-Labels-drop:** Axis-Linien bleiben, aber keine Kategorien-Worte. Jedes Stat-Paar hat seinen eigenen Title direkt am Datenpunkt ("7× Stellen-Zuwachs" / "56% Lohn+") — Axis ist rein visueller Anker ohne Kategorie-Narrative.

    **Entscheidung:** Start mit **Option A**, wenn ehrlich möglich. Fallback **Option C** wenn das Pairing semantisch nicht trägt. Option B als Kompromiss, wenn Luca sagt "weder noch".

    **Y-Achse:**
    - "Level" (hoch/mittel/niedrig) bleibt als abstract label, OK
    - Füge Subtitle/Helper-Text irgendwo unter Chart hinzu: ca. "Zwei Kurven, eine Wirklichkeit: Wo KI-Relevanz steigt, bleibt die Ausbildung stehen. Daten aus N=109 Studis, März 2026" (finale Wording mit Luca feinjustieren — Placeholder erstmal OK)

    **Legend:**
    - Zwei Farb-Dots + Labels: z.B. ● Markt-Signal (Neon) / ● Ausbildung (Rot-Akzent oder Muted)
    - Position: over-chart oder unter-chart, je nach Layout — priorität ist Mobile-Readability
    - Mobile: Legend unter Chart statt darüber wenn Platzproblem

    **Mobile-Readability-Check:**
    - X-Labels: bei schmalem Viewport entweder truncieren oder auf Index-Marker (Option B-style) fallbacken
    - Y-Labels: abkürzen falls nötig ("Hoch/Mittel/Niedrig" → "H/M/N" bei <400px? Lieber nicht, wird kryptisch — lieber kleinere Font-Size)
    - Legend darf nicht Chart-Lines überlappen — Padding/Margins entsprechend
    - Subtitle: `text-sm md:text-base` max — nicht Body-Voll-Größe

    Test-Viewports: 360px, 768px, 1440px. Keine Label-Kollision, keine overflow-x.
  </action>
  <verify>
    <automated>cd apps/website && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Axis-Labels sind ehrlich (Luca-OK auf localhost)
    - Legend vorhanden + sauber positioniert
    - Subtitle klärt den Chart-Meta-Kontext
    - Mobile-Viewport (360px, 768px): keine Label-Kollision, keine horizontale Scrollbar
    - Build + Lint grün
    - Playwright-Test R1.3 bleibt grün (oder wird minimal angepasst falls Label-Strings harcoded im Test — dann selectors relaxen)
  </done>
</task>

</tasks>

<verification>
- `pnpm build` in apps/website grün
- `pnpm lint` grün
- Playwright R1.3 + CSP grün (keine Regression)
- Mobile-Viewports (360/768/1440) keine Überlappung
- Reduced-Motion-Setting im Browser → Chart statisch vollständig
</verification>

<success_criteria>
- Scroll-Feeling ist smooth (subjektiv Luca-OK)
- Achsen-Labels sind honest, keine Strapaze
- Mobile-Readability gewährleistet
- Reduced-Motion-Pfad sauber
</success_criteria>

<output>
`.planning/phases/20.5-landing-wow-pass-signal-grid/20.5-04-SUMMARY.md` mit:
- Axis-Option (A/B/C) gewählt + Rationale
- Spring-Parameter-Final
- Mobile-Layout-Notes
- Commit-Hash(es) — mehrere wenn Tuning-Iterationen
</output>
