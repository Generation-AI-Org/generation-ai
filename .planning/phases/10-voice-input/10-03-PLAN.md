---
phase: 10-voice-input
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - apps/tools-app/components/chat/FloatingChat.tsx
  - apps/tools-app/hooks/useVoiceInput.ts
autonomous: false
requirements:
  - VOICE-07
  - VOICE-08

must_haves:
  truths:
    - "Voice Button zeigt Interim-Text waehrend Aufnahme"
    - "Finaler Text landet im Textarea nach Stopp"
    - "User kann Text vor Senden editieren"
    - "Voice funktioniert in Chrome, Safari, Firefox, Edge"
    - "Voice funktioniert auf iOS Safari und Chrome Android"
  artifacts:
    - path: "apps/tools-app/components/chat/FloatingChat.tsx"
      provides: "Integration von useDeepgramVoice mit Live-Preview"
      contains: "useDeepgramVoice"
    - path: "apps/tools-app/hooks/useVoiceInput.ts"
      provides: "Re-Export oder Redirect zu useDeepgramVoice"
      exports: ["useVoiceInput"]
  key_links:
    - from: "FloatingChat"
      to: "useDeepgramVoice"
      via: "React Hook"
      pattern: "useDeepgramVoice"
    - from: "FloatingChat"
      to: "VoiceInputButton"
      via: "audioLevels prop"
      pattern: "audioLevels=.*audioLevels"
---

<objective>
Integration in FloatingChat mit Live-Preview und Cross-Browser Polish.

Purpose: Per D-05 soll der transkribierte Text ins Textarea eingefuegt werden, User kann editieren vor dem Senden. Interim-Text zeigt Live-Fortschritt.

Output:
- FloatingChat nutzt useDeepgramVoice statt useVoiceInput
- Interim-Preview waehrend Aufnahme
- Finaler Text im Textarea nach Stopp
- Cross-Browser Verifikation
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-voice-input/10-CONTEXT.md
@.planning/phases/10-voice-input/10-RESEARCH.md
@.planning/phases/10-voice-input/10-01-SUMMARY.md
@.planning/phases/10-voice-input/10-02-SUMMARY.md

# Zu modifizierende Dateien
@apps/tools-app/components/chat/FloatingChat.tsx
@apps/tools-app/hooks/useVoiceInput.ts
</context>

<interfaces>
<!-- useDeepgramVoice (nach Plan 01+02) -->
```typescript
interface UseDeepgramVoiceReturn {
  isRecording: boolean
  isConnecting: boolean
  isSupported: boolean
  transcript: string
  interimTranscript: string
  audioLevels: number[]
  error: string | null
  startRecording: () => Promise<void>
  stopRecording: () => void
  clearError: () => void
}
```

<!-- VoiceInputButton (nach Plan 02) -->
```typescript
interface VoiceInputButtonProps {
  isRecording: boolean
  isSupported: boolean
  onToggle: () => void
  className?: string
  audioLevels?: number[]
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: FloatingChat auf useDeepgramVoice umstellen</name>
  <files>apps/tools-app/components/chat/FloatingChat.tsx</files>
  <action>
Ersetze useVoiceInput durch useDeepgramVoice mit erweiterter Integration:

**Import aendern:**
```typescript
// ALT:
import { useVoiceInput } from '@/hooks/useVoiceInput'

// NEU:
import { useDeepgramVoice } from '@/hooks/useDeepgramVoice'
```

**Hook-Aufruf aendern:**
```typescript
// ALT:
const { isRecording, isSupported: isVoiceSupported, toggleRecording, transcript, error: voiceError, clearError } = useVoiceInput()

// NEU:
const { 
  isRecording, 
  isConnecting,
  isSupported: isVoiceSupported, 
  startRecording,
  stopRecording,
  transcript, 
  interimTranscript,
  audioLevels,
  error: voiceError, 
  clearError 
} = useDeepgramVoice()

// Toggle-Funktion erstellen
const toggleRecording = useCallback(() => {
  if (isRecording) {
    stopRecording()
  } else {
    startRecording()
  }
}, [isRecording, startRecording, stopRecording])
```

**VoiceInputButton Props erweitern:**
```tsx
<VoiceInputButton
  isRecording={isRecording}
  isSupported={isVoiceSupported}
  onToggle={toggleRecording}
  audioLevels={audioLevels}  // NEU
/>
```

**Interim-Preview anzeigen:**
Zeige interimTranscript waehrend Recording als temporaeren Text.

Option A - Inline im Textarea (dezent):
```tsx
// Textarea value berechnen
const displayMessage = isRecording && interimTranscript 
  ? `${message}${message ? ' ' : ''}${interimTranscript}` 
  : message

<textarea
  value={displayMessage}
  // ... rest
/>
```

Option B - Separates Preview-Element (empfohlen fuer Klarheit):
```tsx
{isRecording && interimTranscript && (
  <div className="px-4 py-2 text-sm text-[var(--text-muted)] italic border-b border-[var(--border)]/30">
    <span className="animate-pulse mr-2">...</span>
    {interimTranscript}
  </div>
)}
```

**Transcript in Textarea einfuegen (per D-05):**
Der existierende useEffect funktioniert bereits:
```typescript
useEffect(() => {
  if (transcript) {
    setMessage((prev) => {
      const newMessage = prev ? `${prev} ${transcript}` : transcript
      setCharCount(newMessage.length)
      return newMessage
    })
    if (textareaRef.current) {
      textareaRef.current.focus()
    }
  }
}, [transcript])
```

**Connecting-State anzeigen (optional):**
```tsx
<VoiceInputButton
  isRecording={isRecording || isConnecting}
  // ...
/>
```

Oder eigenes Loading-Feedback wenn isConnecting.
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && pnpm tsc --noEmit 2>&1 | grep -E "FloatingChat|useDeepgramVoice|error" | head -5</automated>
  </verify>
  <done>
- FloatingChat importiert useDeepgramVoice
- VoiceInputButton bekommt audioLevels prop
- Interim-Text wird waehrend Recording angezeigt
- Finaler Text landet im Textarea
- Keine TypeScript-Errors
  </done>
</task>

<task type="auto">
  <name>Task 2: useVoiceInput als Redirect/Deprecation</name>
  <files>apps/tools-app/hooks/useVoiceInput.ts</files>
  <action>
Zwei Optionen:

**Option A - Redirect (empfohlen):**
Behalte useVoiceInput.ts als Re-Export fuer Backwards-Compatibility:

```typescript
'use client'

// DEPRECATED: Use useDeepgramVoice directly
// This file is kept for backwards compatibility

export { useDeepgramVoice as useVoiceInput } from './useDeepgramVoice'

// Re-export types for consumers
export type { UseDeepgramVoiceReturn as UseVoiceInputReturn } from './useDeepgramVoice'
```

**Option B - Behalten als Fallback:**
Behalte den alten Code als Browser Speech API Fallback.
Dann in useDeepgramVoice:
```typescript
// Wenn Deepgram fehlschlaegt, automatisch zu Web Speech API wechseln?
// Per CONTEXT.md ist das "CLAUDE'S DISCRETION"
```

**Empfehlung:** Option A (Redirect).
- Deepgram ist der gewaelte Provider
- Silent Fallback waere verwirrend fuer User
- Error + Retry ist transparenter (per RESEARCH.md Assumption A1)

Falls du Option B waehlst (Fallback zu Web Speech API):
- Nur bei Deepgram-Netzwerkfehler
- User-Feedback: "Wechsle zu Browser-Spracherkennung..."
- Qualitaet ist schlechter, aber funktioniert offline
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && grep -l "useVoiceInput\|useDeepgramVoice" apps/tools-app/hooks/*.ts</automated>
  </verify>
  <done>
- useVoiceInput.ts existiert noch (Backwards-Compat)
- Import von useVoiceInput funktioniert weiterhin
- Neuer Code sollte useDeepgramVoice direkt nutzen
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Cross-Browser Verifikation</name>
  <what-built>
Vollstaendige Voice Input Integration mit Deepgram:
- Live-Transkription waehrend Sprechen
- Echte Audio-Visualisierung in Bars
- Text im Textarea nach Stopp
  </what-built>
  <how-to-verify>
**Vorbereitung:**
1. Stelle sicher dass `DEEPGRAM_API_KEY` in `.env.local` gesetzt ist
2. Starte Dev-Server: `pnpm dev:tools`
3. Oeffne https://localhost:3001 (oder deine lokale URL)

**Test 1 - Desktop Chrome:**
1. Oeffne Chat (Kiwi-Button)
2. Klicke Mic-Button
3. Erlaube Mikrofon wenn gefragt
4. Sprich: "Hallo, das ist ein Test auf Deutsch"
5. Erwarte: Bars bewegen sich mit Lautstaerke
6. Erwarte: Interim-Text erscheint live
7. Klicke Stopp
8. Erwarte: Text erscheint im Textarea
9. Erwarte: Text kann editiert werden vor Senden

**Test 2 - Desktop Safari:**
- Gleicher Flow wie Test 1
- Besonders pruefen: AudioContext funktioniert (Bars nicht nur Nullen)

**Test 3 - Desktop Firefox:**
- Gleicher Flow wie Test 1
- Besonders pruefen: audio/webm wird unterstuetzt

**Test 4 - Mobile iOS Safari (falls verfuegbar):**
1. Oeffne Seite auf iPhone/iPad
2. Gleicher Flow
3. Besonders pruefen: AudioContext.resume() nach User-Gesture

**Test 5 - Mobile Chrome Android (falls verfuegbar):**
1. Oeffne Seite auf Android-Geraet
2. Gleicher Flow

**Fehler-Test:**
1. Deaktiviere Internet
2. Starte Voice
3. Erwarte: Fehler-Toast "Verbindung fehlgeschlagen..."
  </how-to-verify>
  <resume-signal>
Antworte mit:
- "approved" wenn alle Tests bestanden
- Oder beschreibe welche Browser/Tests fehlgeschlagen sind
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Input -> Textarea | Transkribierter Text wird als User-Input behandelt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Injection | Transcript -> Textarea | accept | Text wird nur lokal angezeigt, Server-side Validation bei Chat-API |
| T-10-08 | Denial of Service | Lange Aufnahmen | mitigate | Bestehende maxChars Limitierung (2000) |
</threat_model>

<verification>
1. Chrome: Voice startet, Bars animieren, Text erscheint
2. Safari: Kein AudioContext-Fehler, Bars zeigen echte Pegel
3. Firefox: audio/webm funktioniert
4. iOS Safari: AudioContext nicht suspended
5. Android Chrome: Alles funktioniert
6. Error-Handling: Fehler-Toast bei Netzwerkproblem
</verification>

<success_criteria>
- [ ] Chrome Desktop: Voice funktioniert vollstaendig
- [ ] Safari Desktop: Voice funktioniert vollstaendig
- [ ] Firefox Desktop: Voice funktioniert vollstaendig
- [ ] iOS Safari: Voice funktioniert (mit AudioContext Workaround)
- [ ] Chrome Android: Voice funktioniert
- [ ] Interim-Text erscheint waehrend Sprechen
- [ ] Finaler Text im Textarea nach Stopp
- [ ] User kann Text vor Senden editieren
- [ ] Fehler-Toast bei Verbindungsproblemen
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/10-voice-input/10-03-SUMMARY.md`
</output>
