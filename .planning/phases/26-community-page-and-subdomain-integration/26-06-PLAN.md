---
phase: 26
plan: 06
type: execute
wave: 4
depends_on:
  - 26-02
  - 26-03
  - 26-05
files_modified:
  - apps/website/components/layout/header.tsx
  - .planning/phases/26-community-page-and-subdomain-integration/26-VALIDATION.md
  - .planning/phases/26-community-page-and-subdomain-integration/26-UAT.md
  - .changeset/phase-26-nav-update.md
autonomous: false
requirements:
  - R7.6
  - R7.7
must_haves:
  truths:
    - "Header navigation „Community" link goes to /community (internal Link, not external Circle URL) — D-18"
    - "Desktop-Nav-Renderer respects link.external: internal links render as next/link <Link>, not as <a target=_blank>"
    - "Mobile-Nav-Renderer (already external-aware) continues to work for Community internal link"
    - "/community Hero CTA „Direkt zur Community →" remains the explicit gateway for members to Circle"
    - "Lighthouse mobile audit on /community ≥ 90 across Performance, A11y, BP, SEO"
    - "Lighthouse audit on /community/artikel/{any-slug} ≥ 90 same metrics"
    - "Final UAT spreadsheet documents pass/fail for all Block A + Block B success-criteria"
    - "Phase summary file documents observed vs planned outcomes"
  artifacts:
    - path: "apps/website/components/layout/header.tsx"
      provides: "Updated nav: 'Community' link points to '/community' (internal), Desktop-Renderer rewritten to conditional Link/<a> based on external flag"
    - path: ".planning/phases/26-community-page-and-subdomain-integration/26-VALIDATION.md"
      provides: "Lighthouse + automated test results captured"
    - path: ".planning/phases/26-community-page-and-subdomain-integration/26-UAT.md"
      provides: "Manual UAT checklist signed off by Luca"
  key_links:
    - from: "header.tsx (Desktop nav-renderer)"
      to: "next/link Link component"
      via: "conditional render: link.external ? <a target=_blank> : <Link href>"
      pattern: "link\\.external \\?"
    - from: "header.tsx Community entry"
      to: "/community"
      via: "next/link Link href, NOT external `<a>`"
      pattern: "href: \"/community\""
---

<objective>
Phase-end consolidation: ship the header-nav rewire (D-18 — internal route now that `/community` exists), run Lighthouse on the two new public surfaces, and conduct the manual UAT against the merged Block A + Block B success criteria from `26-CONTEXT.md`.

Purpose: Last mile. The header nav was deferred to this plan because (a) we needed `/community` to actually exist before pointing the nav at it (Plan 26-02 ships first), and (b) it's a focused renderer + 2-entry change that pairs naturally with Lighthouse + UAT in a final QA gate.

**Important context (PLAN-CHECK Warning #3):** The current Desktop-Nav-Renderer in `header.tsx` (lines ~60-71) hardcodes `<a target="_blank" rel="noopener noreferrer">` for ALL nav links — it does NOT respect the `external: true|false` field on the link config. So just flipping the Community entry to `external: false` would still produce a new-tab full-page-load to `/community`. We must update the renderer at the same time. The Mobile-Nav-Renderer (lines ~200-213) already respects `external` correctly — no change needed there.

Output: Header link updated AND Desktop-Renderer rewritten to honor `link.external`. Two Lighthouse runs documented. UAT checklist filled in by Luca. Phase declared shipped or specific gaps logged for `/gsd-execute-phase 26 --gaps-only`.

Implements: D-18 (Header → /community internal). Closes Block A + Block B Success Criteria from CONTEXT.md.

**Has `checkpoint:human-verify` for Lighthouse + UAT — these are user-driven gates by definition.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-community-page-and-subdomain-integration/26-CONTEXT.md
@.planning/phases/26-community-page-and-subdomain-integration/RESEARCH.md
@.planning/phases/26-community-page-and-subdomain-integration/26-02-SUMMARY.md
@.planning/phases/26-community-page-and-subdomain-integration/26-03-SUMMARY.md
@.planning/phases/26-community-page-and-subdomain-integration/26-05-SUMMARY.md
@apps/website/components/layout/header.tsx

<interfaces>
Existing header.tsx has TWO references to `community.generation-ai.org`:
- Line 30 (approx): desktop nav config entry
- Line 182 (approx): mobile sheet/dropdown variant

Both must become `/community` internal Links (D-18).

Pattern in header.tsx for internal links (read first, mimic):
```typescript
{ label: "About", href: "/about", external: false }
```

After change:
```typescript
{ label: "Community", href: "/community", external: false }
```

(`external: false` means the existing nav-renderer should use `next/link` Link, not `<a target=_blank>`.)

**Current Desktop-Nav-Renderer state (verified by PLAN-CHECK):**
```tsx
// header.tsx ~line 60-71 — Desktop nav-Renderer
{navLinks.map((link) => (
  <a
    key={link.href}
    href={link.href}
    target="_blank"          // ← HARDCODED, ignoriert link.external
    rel="noopener noreferrer"
    ...
  >
    {link.label}
  </a>
))}
```

This must be replaced with a conditional renderer that respects `link.external` (see Step 2 below).

**Mobile-Nav-Renderer (~line 200-213)** already does the right thing — it branches on the link type/external flag and emits Link or `<a>` accordingly. No code change there, just confirm the Community entry update flows through.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update header navigation — Community config to /community + Desktop-Renderer conditional Link/&lt;a&gt; on external flag</name>
  <files>apps/website/components/layout/header.tsx, .changeset/phase-26-nav-update.md</files>
  <action>
1. Read `apps/website/components/layout/header.tsx` in full first. Confirm the assertions in `<interfaces>` block above (renderer at ~line 60-71 hardcodes target=_blank; mobile renderer at ~line 200-213 already branches on external).

2. **Update Desktop-Nav-Renderer (PLAN-CHECK Warning #3 — REQUIRED, not optional):**
   The current renderer at lines ~60-71 emits `<a target="_blank" rel="noopener noreferrer">` for every link regardless of `link.external`. Rewrite to conditional rendering:

   ```tsx
   import Link from "next/link"; // confirm import exists at top of file (it should — header.tsx already uses Link elsewhere)

   {navLinks.map((link) =>
     link.external ? (
       <a
         key={link.href}
         href={link.href}
         target="_blank"
         rel="noopener noreferrer"
         className="<existing classes>"
       >
         {link.label}
       </a>
     ) : (
       <Link
         key={link.href}
         href={link.href}
         className="<existing classes>"
       >
         {link.label}
       </Link>
     )
   )}
   ```

   Carry over the EXACT className string + any other props (data-attrs, onClick, etc.) from the current `<a>` element to BOTH branches verbatim. The only thing changing is: external → keep target=_blank/rel; internal → use next/link Link, drop target+rel.

   **Verify Link import is present:** `grep -q '^import Link from "next/link"' components/layout/header.tsx`. If missing, add at top of file. (Existing file likely already imports Link for the logo or other internal links.)

3. **Update Desktop nav-config entry** (around line 30):
   - From: `{ label: "Community", href: "https://community.generation-ai.org", external: true }`
   - To: `{ label: "Community", href: "/community", external: false }`

4. **Update Mobile sheet entry** (around line 182):
   - From: `{ type: "link", label: "Community", href: "https://community.generation-ai.org", external: true }`
   - To: `{ type: "link", label: "Community", href: "/community", external: false }`

   Mobile-Renderer already respects `external` flag — no renderer rewrite needed for mobile.

5. **DO NOT remove the Circle direct-link** anywhere else in the codebase. The /community Hero (Plan 26-02) explicitly contains the „Direkt zur Community →" link to community.generation-ai.org for members. That is the gateway. The Header-Nav change ONLY affects the global navigation entry — the Hero CTA on /community continues to live independently.

6. Create changeset:
```
.changeset/phase-26-nav-update.md:
---
"@genai/website": patch
---

phase 26 — header navigation „Community" now points to internal /community page (D-18); Desktop-Renderer rewritten to honor link.external flag (conditional next/link <Link> for internal, <a target=_blank> for external). Direct Circle link remains as Hero CTA on /community for members.
```

7. Verify:
   - `pnpm tsc --noEmit` clean
   - `pnpm dev` — click „Community" in header. Browser navigates **without full reload** (next/link client-side navigation). URL becomes `localhost:3000/community`. **Same tab, not a new tab.**
   - Verify other internal-nav links (About, etc.) still work the same way (no regression — they were affected by the renderer change too).
   - Verify external links (if any in nav, e.g. blog/docs on a different domain) still open in new tab via `<a target=_blank>`.
   - Mobile menu (open the burger): „Community" entry also goes to `/community` in the same tab.
   - Confirm `/community` Hero still has the „Direkt zur Community →" link to Circle in a new tab.
  </action>
  <verify>
    <automated>cd apps/website && pnpm tsc --noEmit && grep -c 'href: "/community"' components/layout/header.tsx | grep -q "^[2-9]" && ! grep -q 'href: "https://community.generation-ai.org"' components/layout/header.tsx && grep -q "link.external ?" components/layout/header.tsx && grep -q '^import Link from "next/link"' components/layout/header.tsx</automated>
  </verify>
  <done>header.tsx has both Community-nav entries pointing to /community (internal), Desktop-Renderer rewritten to conditional `link.external ? <a> : <Link>`, no remaining external Circle hrefs in header, dev nav works (client-side routing, same tab), changeset committed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Lighthouse audit on /community + /community/artikel/[slug]</name>
  <what-built>
- Phase 26 Block A complete (own page + 4 articles + sitemap + Schema.org)
- Phase 26 Block B complete (live API + MDX-driven preview)
- Header nav rewired to /community + Desktop-Renderer fixed to respect link.external (D-18)
- All 23 decisions from CONTEXT.md implemented
  </what-built>
  <how-to-verify>
1. **Build production:**
   ```bash
   pnpm --filter @genai/website build
   pnpm --filter @genai/website start &
   ```
   (Or use a Vercel preview deploy if pushed.)

2. **Lighthouse — /community:**
   ```bash
   npx lighthouse http://localhost:3000/community --preset=desktop --output=html --output-path=./lh-community-desktop.html
   npx lighthouse http://localhost:3000/community --preset=mobile --output=html --output-path=./lh-community-mobile.html
   ```
   Open both reports. Target ≥ 90 for Performance, Accessibility, Best Practices, SEO. Capture scores in 26-VALIDATION.md.

3. **Lighthouse — Article page (pick one slug):**
   ```bash
   npx lighthouse "http://localhost:3000/community/artikel/bachelorarbeit-mit-claude" --preset=desktop --output-path=./lh-article-desktop.html
   npx lighthouse "http://localhost:3000/community/artikel/bachelorarbeit-mit-claude" --preset=mobile --output-path=./lh-article-mobile.html
   ```
   Target ≥ 90 same metrics. Capture in 26-VALIDATION.md.

4. **If any metric < 90:**
   - Performance: check images (any large bg images? optimize), JS-bundle-size (likely fine since most are server-rendered), CLS (Pitfall 7 mitigation)
   - A11y: usually missing labels, contrast, or focus rings
   - SEO: missing meta tags (shouldn't happen — Plan 26-02 + 26-03 set them), missing canonical
   - BP: usually deprecated APIs or insecure resources

5. **Document findings** in `.planning/phases/26-community-page-and-subdomain-integration/26-VALIDATION.md` with this template:

```markdown
# Phase 26 — Validation Report

## Lighthouse Results

### /community

| Preset | Performance | A11y | BP | SEO |
|--------|-------------|------|----|-----|
| Desktop | XX | XX | XX | XX |
| Mobile  | XX | XX | XX | XX |

### /community/artikel/bachelorarbeit-mit-claude

| Preset | Performance | A11y | BP | SEO |
|--------|-------------|------|----|-----|
| Desktop | XX | XX | XX | XX |
| Mobile  | XX | XX | XX | XX |

## Automated Tests

- [x] `pnpm --filter @genai/website test --run` — N tests pass (lib/mdx, schema, sitemap)
- [x] `pnpm --filter @genai/tools-app test app/api/public/featured-tools --run` — 4 tests pass
- [x] `pnpm --filter @genai/website build` — clean, no errors

## Open Issues

(List any Lighthouse < 90 or test failures + decision: fix-now vs defer-to-gap-closure)
```
  </how-to-verify>
  <resume-signal>
- Type "approved" if all Lighthouse metrics ≥ 90 and tests are green
- Type "revise: <specific issues>" to send back to planner with gap list (use `/gsd-plan-phase 26 --gaps`)
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final UAT — walk Block A + Block B success criteria</name>
  <what-built>
Final Phase 26 sign-off. Walks the merged success-criteria checklist from CONTEXT.md.
  </what-built>
  <how-to-verify>
Open `.planning/phases/26-community-page-and-subdomain-integration/26-UAT.md` (create if missing) and tick each item with screenshots/evidence as needed.

Block A — /community page:
- [ ] Visit `https://generation-ai.org/community` (or local equivalent). All 4 sections render in correct order: Hero / Pillars / Carousel / Final-CTA
- [ ] Hero: Section-Header „// generation ai · community", H1 „Mehr als eine Community.", Sub-Copy correct, „Direkt zur Community →" opens new tab to Circle
- [ ] 4 Pillar-Kacheln present: Austausch / Lernpfade & Kurse / News & Insights / Exklusive Inhalte. Lucide-Icons: Users / BookOpen / Newspaper / Lock. NO Circle screenshots (D-12).
- [ ] Carousel shows 4 placeholder articles, chronologisch (newest left). KI-News-card has „KI-News" pill badge.
- [ ] Mobile (375px): carousel scrolls horizontally with finger drag, snaps to each card edge.
- [ ] Click any card → opens `/community/artikel/[slug]` (real subpage, not modal). URL changes correctly.
- [ ] Article-Page renders: H1 + date+readingTime + MDX-body + „Weiterlesen in der Community →" CTA at bottom
- [ ] Article-Page „Weiterlesen" link opens new tab to circleUrl
- [ ] KI-News-Article (`/community/artikel/ki-news-kw17-2026`): badge „KI-generiert · vom Team kuratiert" visible at top
- [ ] OG-Tags: view-source on article — `og:title`, `og:description`, `og:type=article`, `og:url`, `twitter:card=summary_large_image`. No `og:image` override (D-22 — root inherits).
- [ ] Schema.org: view-source — `<script type="application/ld+json">{"@type":"Article",...}</script>` present. `<` chars are `\u003c` (escape verified).
- [ ] Sitemap: visit `/sitemap.xml` — contains entries for `/`, `/community`, and all 4 articles.
- [ ] Robots.txt: visit `/robots.txt` — allow all, sitemap URL present. (No code change in this phase — `apps/website/app/robots.ts` was already correct, see Plan 26-03 T3 info note.)
- [ ] Final-CTA: H2 „Wir sehen uns drinnen.", body, button „Kostenlos beitreten" → /join (internal)
- [ ] Lighthouse `/community` mobile + desktop ≥ 90 (from VALIDATION.md)
- [ ] Lighthouse article page mobile + desktop ≥ 90

Block B — Subdomain integration:
- [ ] curl `https://tools.generation-ai.org/api/public/featured-tools` (no auth) → 200 + JSON shape `{ tools: [...], generated_at: ... }` with 5 tools
- [ ] curl `-I` same URL → `Cache-Control: public, s-maxage=300, stale-while-revalidate=1800`
- [ ] Landing Tool-Showcase: 5 tools render (chatgpt, claude, lovable, cursor, perplexity in that order)
- [ ] Tool-Showcase fallback (manually break env-var → reload): falls back to ALL 12 hardcoded stubs without crash (verify count = 12, not 5)
- [ ] Landing Community-Preview Article-Spalte: 3 latest MDX articles, KI-News-Badge on KI-News, „Alle Artikel ansehen →" → /community
- [ ] Landing Community-Preview Events-Spalte: stub events with „Beispiel"-Badges (D-21 — Phase 22.5 territory)
- [ ] No layout shift on landing during data swap (Pitfall 7 mitigation)

Header navigation:
- [ ] Click „Community" in header → goes to /community (no full page reload — next/link, same tab)
- [ ] Mobile menu „Community" → same /community route, same tab
- [ ] Other internal nav links (About, etc.) also use client-side routing (no regression from Desktop-Renderer rewrite)
- [ ] „Direkt zur Community →" CTA on /community Hero still goes to Circle (members gateway, new tab)

Sign-off line at the bottom:
```
Phase 26 UAT signed off by Luca on YYYY-MM-DD.
Open issues for follow-up: <list or "none">
```
  </how-to-verify>
  <resume-signal>
- Type "approved" with sign-off date if all checks pass
- Type "issues: <list>" to log gaps for `/gsd-plan-phase 26 --gaps`
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal nav state vs external link | Header nav is editor-controlled config, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-06-01 | Spoofing | nav link mistake routes users to wrong domain | mitigate | Internal `/community` cannot accidentally bridge to attacker-controlled domain. The intentional Circle link in /community Hero uses target=_blank rel=noopener (Plan 26-02). |
| T-26-06-02 | Tampering | external-flag bypass causing internal hrefs to open as `<a target=_blank>` | mitigate | Desktop-Renderer rewrite explicitly branches on `link.external`. Internal links use `<Link>` (no target=_blank), external use `<a target=_blank rel=noopener noreferrer>`. Tested on Community + About + any external entries. |
</threat_model>

<verification>
- header.tsx grep returns exactly 0 matches for `community.generation-ai.org` (no leftover external links in nav)
- header.tsx grep returns ≥ 2 matches for `href: "/community"` (desktop + mobile entries)
- header.tsx grep returns 1+ match for `link.external ?` (Desktop-Renderer conditional)
- header.tsx imports `next/link` Link (regression-safe)
- Lighthouse reports captured in 26-VALIDATION.md
- 26-UAT.md fully ticked OR explicit gap list noted
- All checkpoints passed
</verification>

<success_criteria>
- [ ] Header „Community" link goes to `/community` (D-18)
- [ ] Desktop-Nav-Renderer rewritten to honor `link.external` flag (PLAN-CHECK Warning #3)
- [ ] Mobile-Nav-Renderer continues to honor `external` flag (no regression)
- [ ] Lighthouse `/community` ≥ 90 (mobile + desktop) all 4 categories
- [ ] Lighthouse `/community/artikel/{slug}` ≥ 90 (mobile + desktop) all 4 categories
- [ ] All Block A success-criteria items checked off in 26-UAT.md
- [ ] All Block B success-criteria items checked off in 26-UAT.md
- [ ] 26-VALIDATION.md saved with metrics + automated test status
- [ ] Phase signed off OR gaps logged for `--gaps-only` re-run
- [ ] Changeset committed
</success_criteria>

<output>
After completion, create `.planning/phases/26-community-page-and-subdomain-integration/26-06-SUMMARY.md` and update `.planning/STATE.md` with Phase 26 done.
</output>
