---
phase: 06-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/layout.tsx
  - apps/website/package.json
  - apps/tools-app/app/layout.tsx
  - apps/tools-app/app/api/health/route.ts
  - apps/tools-app/package.json
autonomous: true
requirements: [R3.5, R3.6]

must_haves:
  truths:
    - "CWV-Daten werden in Vercel Dashboard erfasst"
    - "/api/health gibt 200 OK zurueck"
  artifacts:
    - path: "apps/website/app/layout.tsx"
      provides: "SpeedInsights Component"
      contains: "SpeedInsights"
    - path: "apps/tools-app/app/layout.tsx"
      provides: "SpeedInsights Component"
      contains: "SpeedInsights"
    - path: "apps/tools-app/app/api/health/route.ts"
      provides: "Health Check Endpoint"
      exports: ["GET"]
  key_links:
    - from: "apps/website/app/layout.tsx"
      to: "@vercel/speed-insights"
      via: "import"
      pattern: "SpeedInsights"
    - from: "apps/tools-app/app/layout.tsx"
      to: "@vercel/speed-insights"
      via: "import"
      pattern: "SpeedInsights"
---

<objective>
Vercel Speed Insights und Health Endpoint einrichten.

Purpose: Basis-Monitoring ohne externe Accounts — sofort ausfuehrbar.
Output: SpeedInsights in beiden Apps, /api/health Endpoint in tools-app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/monitoring.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Speed Insights in beide Apps installieren</name>
  <files>
    apps/website/package.json
    apps/website/app/layout.tsx
    apps/tools-app/package.json
    apps/tools-app/app/layout.tsx
  </files>
  <action>
1. In apps/website/:
   - `pnpm add @vercel/speed-insights`
   - In layout.tsx: Import `import { SpeedInsights } from "@vercel/speed-insights/next"`
   - SpeedInsights Component direkt vor `</body>` einfuegen: `<SpeedInsights />`

2. In apps/tools-app/:
   - `pnpm add @vercel/speed-insights`
   - In layout.tsx: Import `import { SpeedInsights } from "@vercel/speed-insights/next"`
   - SpeedInsights Component direkt vor `</body>` einfuegen: `<SpeedInsights />`

Kein Config noetig — funktioniert automatisch auf Vercel.
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && pnpm build</automated>
  </verify>
  <done>
    - @vercel/speed-insights in beiden Apps installiert
    - SpeedInsights Component in beiden layout.tsx eingefuegt
    - Build erfolgreich
  </done>
</task>

<task type="auto">
  <name>Task 2: Health Check Endpoint in tools-app</name>
  <files>apps/tools-app/app/api/health/route.ts</files>
  <action>
Erstelle Health Check Endpoint:

```typescript
// apps/tools-app/app/api/health/route.ts
export async function GET() {
  return Response.json({ 
    status: "ok", 
    timestamp: new Date().toISOString() 
  });
}
```

Einfacher Endpoint ohne DB-Check — reicht fuer Uptime Monitoring.
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/generation-ai && pnpm build && curl -s http://localhost:3001/api/health 2>/dev/null || echo "Build OK, curl nach Deploy testen"</automated>
  </verify>
  <done>
    - /api/health Endpoint existiert
    - Gibt JSON mit status: "ok" zurueck
    - Build erfolgreich
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /api/health | Oeffentlicher Endpoint, kein Auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | I (Information Disclosure) | /api/health | accept | Gibt nur status + timestamp zurueck, keine sensiblen Daten |
| T-06-02 | D (Denial of Service) | /api/health | accept | Vercel Rate Limiting schuetzt, Endpoint ist minimal |
</threat_model>

<verification>
- `pnpm build` erfolgreich (keine TypeScript-Fehler)
- SpeedInsights Component in beiden layout.tsx vorhanden
- /api/health Route-Datei existiert
</verification>

<success_criteria>
- Build laeuft durch ohne Fehler
- Nach Deploy: CWV-Tab in Vercel Dashboard sichtbar
- Nach Deploy: tools.generation-ai.org/api/health → 200 OK
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/06-monitoring/06-01-SUMMARY.md`
</output>
