---
phase: 21
plan: 04
slug: values-verein-sections
type: plan
status: skeleton
created: 2026-04-24
last-updated: 2026-04-24
autonomous: true
wave: 2
depends_on: []
files_modified:
  - apps/website/components/about/about-values-section.tsx
  - apps/website/components/about/about-verein-section.tsx
requirements: []
---

# Plan 21-04 — Values + Verein Sections

> Werte-Section (4 Bold-Claim-Blöcke) und Verein-Section (Hervorgehobene Card, Anker `#verein`). Beide typografie-getrieben, Card-Pattern aus 20.6-Baseline. `#verein`-Anker ist load-bearing (Phase 22 Partner-Page verlinkt direkt).

---

<objective>
Zwei Client-Components (wegen `useReducedMotion`-Gate), die Werte + Verein-Section rendern. Copy 1:1 aus UI-SPEC. Werte-Grid 2×2 auf Desktop, vertikaler Stack Mobile. Verein-Card hat dezente Accent-Hairline oben (wie Final-CTA-Pattern) plus `id="verein"`-Anker. Section-Anker `#werte` auf Werte-Section.
</objective>

<scope>
**In-scope:**
- `about-values-section.tsx` — 4 Werte-Blöcke, 2×2 Grid Desktop, Stack Mobile, Anker `#werte`
- `about-verein-section.tsx` — Hervorgehobene Card mit 3 Absätzen, Anker `#verein`, Accent-Hairline oben

**Out-of-scope:**
- Andere Sections
- Werte-Visualisierungen über reine Typo hinaus (Roadmap / Phase 27)
- Verein-Finanzreport-PDF (deferred bis e.V. offiziell eingetragen)
</scope>

<context>
**Read:**
- `.planning/phases/21-about-page/21-CONTEXT.md` — §4 Werte (Zeile 49-55), §5 Verein (Zeile 57-61), D-09 (`#verein`-Anker kritisch für Phase 22)
- `.planning/phases/21-about-page/21-UI-SPEC.md` — Werte-Copy (Zeile 175-183), Verein-Copy (Zeile 187-195), Color Zeile 123 (Verein-Card-Accent-Hairline), Responsive Contract Zeile 387 (Werte-Layout: 2×2 Default)
- `apps/website/components/sections/final-cta-section.tsx` — Pattern für Accent-Hairline (horizontaler Akzent-Strich oben auf der Verein-Card)
- `apps/website/components/sections/kurz-faq-section.tsx` — Motion-Pattern
</context>

<tasks>

### Task 1 — AboutValuesSection

**Read first:**
- `.planning/phases/21-about-page/21-UI-SPEC.md` Zeile 175-183 (Werte-Tabelle mit exakter Copy)
- `apps/website/components/sections/kurz-faq-section.tsx` (Motion-Pattern)

**Action:**
Erstelle `apps/website/components/about/about-values-section.tsx`:

```tsx
'use client'

import { motion, useReducedMotion } from "motion/react"

type ValueBlock = {
  claim: string
  body: string
}

const values: ValueBlock[] = [
  {
    claim: "Offen für alle.",
    body: "KI-Kompetenz ist keine Frage des Studiengangs. Von Medizin bis Maschinenbau, von Pädagogik bis Politik — jede Disziplin wird durch KI verändert, und jede Disziplin braucht Leute, die verstehen, wie sie funktioniert.",
  },
  {
    claim: "Anwenden statt auswendig lernen.",
    body: "Theorie alleine bringt nichts. Bei uns baust du in Workshops echte Dinge — vom ersten Chat-Agenten bis zur Mini-Automatisierung. Erfolg heißt: du hast am Ende einen Output, den du nutzen kannst.",
  },
  {
    claim: "Signal statt Noise.",
    body: "Das KI-Feld ist voll mit Gurus, Hype-Tools und Tutorial-Flut. Wir filtern. Was wir zeigen, haben wir selbst geprüft — oder wir sagen ehrlich, wenn wir's noch nicht wissen.",
  },
  {
    claim: "Voneinander lernen, zusammen wachsen.",
    body: "Community ist unser Kernangebot, nicht Content. Die besten Einsichten kommen aus der Gruppe — nicht von einem Dozenten vorne. Wer aktiv teilt, lernt am schnellsten.",
  },
]

export function AboutValuesSection() {
  const prefersReducedMotion = useReducedMotion()

  const fadeIn = prefersReducedMotion
    ? {}
    : {
        initial: { opacity: 0, y: 16 },
        whileInView: { opacity: 1, y: 0 },
        viewport: { once: true, margin: "-10% 0px" },
        transition: { duration: 0.6, ease: [0.16, 1, 0.3, 1] as const },
      }

  return (
    <section
      id="werte"
      aria-labelledby="about-values-heading"
      data-section="about-values"
      className="relative bg-bg py-24 sm:py-32 border-b border-border"
    >
      <div className="mx-auto max-w-3xl px-6">
        {/* Eyebrow */}
        <motion.div
          {...fadeIn}
          className="mx-auto mb-5 flex items-center justify-center gap-2 font-mono text-[11px] font-bold uppercase tracking-[0.2em] text-text-muted"
        >
          <span
            aria-hidden="true"
            className="h-1.5 w-1.5 rounded-full"
            style={{ background: "var(--accent)", boxShadow: "0 0 8px var(--accent-glow)" }}
          />
          {"// was uns antreibt"}
        </motion.div>

        {/* H2 */}
        <motion.h2
          {...fadeIn}
          id="about-values-heading"
          className="text-center font-sans font-bold text-text text-balance"
          style={{ fontSize: "var(--fs-h3)", lineHeight: "var(--lh-sub)" }}
        >
          Worauf wir Wert legen.
        </motion.h2>

        {/* Werte-Grid: 2×2 Desktop, 1-col Mobile */}
        <motion.ul
          {...fadeIn}
          className="mt-14 grid grid-cols-1 sm:grid-cols-2 gap-8"
        >
          {values.map((value) => (
            <li key={value.claim} className="flex flex-col gap-2">
              <h3
                className="font-sans font-bold text-text text-pretty"
                style={{ fontSize: "var(--fs-body)", lineHeight: 1.3 }}
              >
                {value.claim}
              </h3>
              <p
                className="text-text-secondary text-pretty"
                style={{ fontSize: "var(--fs-body)", lineHeight: "var(--lh-body)" }}
              >
                {value.body}
              </p>
            </li>
          ))}
        </motion.ul>
      </div>
    </section>
  )
}
```

**Concrete values — verbatim aus UI-SPEC Werte-Tabelle:**
- Section-Anker: `id="werte"`
- Eyebrow: `// was uns antreibt`
- H2: `Worauf wir Wert legen.`
- 4 Werte-Blöcke mit exakter Copy (siehe UI-SPEC Zeile 178-181):
  - Block 1: claim `"Offen für alle."`, body starts with `"KI-Kompetenz ist keine Frage des Studiengangs."`
  - Block 2: claim `"Anwenden statt auswendig lernen."`, body starts with `"Theorie alleine bringt nichts."`
  - Block 3: claim `"Signal statt Noise."`, body starts with `"Das KI-Feld ist voll mit Gurus"`
  - Block 4: claim `"Voneinander lernen, zusammen wachsen."`, body starts with `"Community ist unser Kernangebot"`
- Heading inside Werte-Block: `<h3>` semantisch OK hier, weil innerhalb einer H2-Section als Sub-Heading (H2 → H3 ist valide Hierarchie)

**Grid-Entscheidung:** UI-SPEC Responsive Contract (Zeile 387) gibt 2×2 als Default. Umgesetzt: `grid-cols-1 sm:grid-cols-2`.

**Heading-Korrektur zur Plan-21-02-Decision:** Innerhalb einer `<h2>`-Section können `<h3>` als Sub-Headings genutzt werden — das ist die STANDARD-Hierarchie. Die Regel "kein H3 auf der Page" aus Plan 21-02 bezog sich auf SECTION-HEADS (die alle H2 sein sollen). Innerhalb einer Section sind H3 für Sub-Items (wie Werte-Claims) KORREKT.

**Acceptance criteria:**
- [ ] Datei `apps/website/components/about/about-values-section.tsx` existiert
- [ ] Anker `id="werte"`: `grep -n 'id="werte"' apps/website/components/about/about-values-section.tsx` matched
- [ ] Eyebrow: `grep -F '// was uns antreibt' apps/website/components/about/about-values-section.tsx` matched
- [ ] H2 Copy: `grep -F 'Worauf wir Wert legen.' apps/website/components/about/about-values-section.tsx` matched
- [ ] 4 Werte-Claims vollständig: `grep -cE '^    claim: "(Offen für alle\.|Anwenden statt auswendig lernen\.|Signal statt Noise\.|Voneinander lernen, zusammen wachsen\.)"' apps/website/components/about/about-values-section.tsx` liefert 4
- [ ] Body-Snippets vorhanden (echte Umlaute):
  - `grep -F 'KI-Kompetenz ist keine Frage des Studiengangs' apps/website/components/about/about-values-section.tsx` matched
  - `grep -F 'Theorie alleine bringt nichts' apps/website/components/about/about-values-section.tsx` matched
  - `grep -F 'Das KI-Feld ist voll mit Gurus' apps/website/components/about/about-values-section.tsx` matched
  - `grep -F 'Community ist unser Kernangebot' apps/website/components/about/about-values-section.tsx` matched
- [ ] Grid 2-col: `grep -n 'sm:grid-cols-2' apps/website/components/about/about-values-section.tsx` matched
- [ ] `<h2>` Section-Head: `grep -n 'motion\.h2' apps/website/components/about/about-values-section.tsx` matched
- [ ] `<h3>` für Value-Claims: `grep -c '<h3' apps/website/components/about/about-values-section.tsx` liefert ≥1

---

### Task 2 — AboutVereinSection

**Read first:**
- `.planning/phases/21-about-page/21-UI-SPEC.md` Zeile 187-195 (Verein-Copy + Visual-Spec)
- `apps/website/components/sections/final-cta-section.tsx` (Accent-Hairline-Pattern)
- `.planning/phases/21-about-page/21-CONTEXT.md` D-09 (`#verein`-Anker kritisch)

**Action:**
Erstelle `apps/website/components/about/about-verein-section.tsx`:

```tsx
'use client'

import { motion, useReducedMotion } from "motion/react"

export function AboutVereinSection() {
  const prefersReducedMotion = useReducedMotion()

  const fadeIn = prefersReducedMotion
    ? {}
    : {
        initial: { opacity: 0, y: 16 },
        whileInView: { opacity: 1, y: 0 },
        viewport: { once: true, margin: "-10% 0px" },
        transition: { duration: 0.6, ease: [0.16, 1, 0.3, 1] as const },
      }

  return (
    <section
      id="verein"
      aria-labelledby="about-verein-heading"
      data-section="about-verein"
      className="relative bg-bg py-24 sm:py-32 border-b border-border"
    >
      <div className="mx-auto max-w-3xl px-6">
        {/* Eyebrow */}
        <motion.div
          {...fadeIn}
          className="mx-auto mb-5 flex items-center justify-center gap-2 font-mono text-[11px] font-bold uppercase tracking-[0.2em] text-text-muted"
        >
          <span
            aria-hidden="true"
            className="h-1.5 w-1.5 rounded-full"
            style={{ background: "var(--accent)", boxShadow: "0 0 8px var(--accent-glow)" }}
          />
          {"// verein"}
        </motion.div>

        {/* H2 */}
        <motion.h2
          {...fadeIn}
          id="about-verein-heading"
          className="text-center font-sans font-bold text-text text-balance"
          style={{ fontSize: "var(--fs-h3)", lineHeight: "var(--lh-sub)" }}
        >
          Gemeinnützig. Transparent. Offen.
        </motion.h2>

        {/* Verein-Card mit Accent-Hairline oben */}
        <motion.div
          {...fadeIn}
          className="relative mt-12 rounded-2xl border border-border bg-bg-card px-8 py-10 overflow-hidden"
        >
          {/* Accent-Hairline oben — echo zu final-cta-section.tsx */}
          <div
            aria-hidden="true"
            className="absolute top-0 left-0 right-0 h-[2px]"
            style={{
              background: "var(--accent)",
              opacity: 0.6,
              boxShadow: "0 0 8px var(--accent-glow)",
            }}
          />

          <div
            className="flex flex-col gap-6 text-text-secondary text-pretty"
            style={{ fontSize: "var(--fs-body)", lineHeight: "var(--lh-body)" }}
          >
            <p>
              Generation AI ist ein gemeinnütziger Verein in Gründung (e.V. i.G.). Gewinnorientiert sind wir nicht — alles, was reinkommt, geht in die Community zurück.
            </p>
            <p>
              Finanziert durch Fördermittel, Sachleistungen und Partnerschaften mit Unternehmen, Stiftungen und Hochschulen. Keine Paywall, keine versteckten Kosten für Mitglieder.
            </p>
            <p>
              Mitgliedschaft ist kostenlos. Aktives Mitmachen — Events organisieren, Content beitragen, Strategie mitbauen — ist möglich und willkommen.
            </p>
          </div>
        </motion.div>
      </div>
    </section>
  )
}
```

**Concrete values — verbatim aus UI-SPEC:**
- Section-Anker: `id="verein"` (load-bearing, D-09)
- Eyebrow: `// verein`
- H2: `Gemeinnützig. Transparent. Offen.`
- Absatz 1: `Generation AI ist ein gemeinnütziger Verein in Gründung (e.V. i.G.). Gewinnorientiert sind wir nicht — alles, was reinkommt, geht in die Community zurück.`
- Absatz 2: `Finanziert durch Fördermittel, Sachleistungen und Partnerschaften mit Unternehmen, Stiftungen und Hochschulen. Keine Paywall, keine versteckten Kosten für Mitglieder.`
- Absatz 3: `Mitgliedschaft ist kostenlos. Aktives Mitmachen — Events organisieren, Content beitragen, Strategie mitbauen — ist möglich und willkommen.`
- Card-Pattern: `rounded-2xl border border-border bg-bg-card px-8 py-10` (exact aus UI-SPEC Zeile 195)
- Accent-Hairline oben: 2px, `var(--accent)`, `opacity: 0.6`, `boxShadow: 0 0 8px var(--accent-glow)` (echo zu final-cta-section.tsx Hairline-Pattern)

**Acceptance criteria:**
- [ ] Datei `apps/website/components/about/about-verein-section.tsx` existiert
- [ ] Anker `id="verein"` (load-bearing, D-09): `grep -n 'id="verein"' apps/website/components/about/about-verein-section.tsx` matched
- [ ] Eyebrow: `grep -F '// verein' apps/website/components/about/about-verein-section.tsx` matched
- [ ] H2 Copy: `grep -F 'Gemeinnützig. Transparent. Offen.' apps/website/components/about/about-verein-section.tsx` matched
- [ ] Absatz 1: `grep -F 'gemeinnütziger Verein in Gründung (e.V. i.G.)' apps/website/components/about/about-verein-section.tsx` matched
- [ ] Absatz 2: `grep -F 'Fördermittel, Sachleistungen und Partnerschaften' apps/website/components/about/about-verein-section.tsx` matched
- [ ] Absatz 3: `grep -F 'Events organisieren, Content beitragen' apps/website/components/about/about-verein-section.tsx` matched
- [ ] Card-Classes: `grep -nE 'rounded-2xl|bg-bg-card|border border-border' apps/website/components/about/about-verein-section.tsx` liefert mehrere Treffer
- [ ] Accent-Hairline mit var(--accent): `grep -n 'var(--accent)' apps/website/components/about/about-verein-section.tsx` matched
- [ ] Card-Padding `px-8 py-10`: `grep -n 'px-8 py-10' apps/website/components/about/about-verein-section.tsx` matched

---

### Task 3 — Verify Build

**Read first:**
- `apps/website/package.json`

**Action:**
```bash
cd apps/website
pnpm tsc --noEmit 2>&1 | head -20
```

**Acceptance criteria:**
- [ ] `pnpm --filter @genai/website tsc --noEmit` exit code 0
- [ ] Dateien exist: `ls apps/website/components/about/about-values-section.tsx apps/website/components/about/about-verein-section.tsx`

</tasks>

<threat_model>

| Threat | Surface | Mitigation |
|--------|---------|-----------|
| Verein-Anker `#verein` bricht bei Refactor → Phase 22 Partner-Page-Link tot | `<section id="verein">` | `id="verein"` ist load-bearing (D-09). Acceptance-Criterion grep-verifiziert. In Integration-Smoke-Test (Plan 21-08) wird `/about#verein` gefolgt. |
| Werte-Copy-Drift (VOICE-Konsistenz) | Hardcoded copy | UI-SPEC liefert exakte Copy, Plan-Checker grep-verifiziert. Phase-27-Copy-Pass refined wenn nötig. |
| Accent-Hairline zu prominent → überfrachtet Design | Verein-Card visual | Opacity 0.6 + schmaler 2px-Strich mit Glow-Shadow — subtiler Signal-Akzent, nicht Bruch. Review in UAT. |

**Severity: LOW**

</threat_model>

<verification_commands>

```bash
# Files exist
ls apps/website/components/about/about-values-section.tsx
ls apps/website/components/about/about-verein-section.tsx

# Type-check
pnpm --filter @genai/website tsc --noEmit

# Critical anchors present
grep -q 'id="werte"' apps/website/components/about/about-values-section.tsx
grep -q 'id="verein"' apps/website/components/about/about-verein-section.tsx

# Section-heads are h2, not h3
grep -q 'motion\.h2' apps/website/components/about/about-values-section.tsx
grep -q 'motion\.h2' apps/website/components/about/about-verein-section.tsx

# 4 values defined
grep -cE '  claim:' apps/website/components/about/about-values-section.tsx  # expect 4
```

</verification_commands>

<must_haves>

- [ ] Both section files exist under `apps/website/components/about/`
- [ ] Copy 1:1 aus UI-SPEC mit echten Umlauten (ü/ö/ä/ß)
- [ ] 4 Werte-Blöcke vollständig, in-order
- [ ] Verein-Card mit Accent-Hairline oben (2px, var(--accent), opacity 0.6)
- [ ] Anker `id="werte"` und `id="verein"` (D-09 load-bearing)
- [ ] Heading-Struktur: H2 Section-Head, H3 für Werte-Block-Claims (valide Hierarchie)
- [ ] TypeScript + ESLint grün

</must_haves>

<notes>

- Werte-Block-Heading ist `<h3>` (Sub-Heading innerhalb H2-Section) — Korrektur zur Plan-21-02-Regel: H3 ist VERBOTEN für Section-Heads, aber ERLAUBT für Sub-Items innerhalb Sections.
- `#verein`-Anker ist kritischer Integration-Punkt für Phase 22 (Partner-Page). Plan 21-08 Smoke-Test wird `/about#verein` explizit testen.
- Werte-Layout-Alternativen (UI-SPEC Open Issue 4): 4-Spalten-Row oder vertical stack — der Plan wählt 2×2 (Default-Empfehlung aus UI-SPEC).

</notes>
