---
phase: 21
plan: 08
slug: route-metadata-smoke
type: plan
status: skeleton
created: 2026-04-24
last-updated: 2026-04-24
autonomous: true
wave: 3
depends_on:
  - 21-01 (Team Sub-Components)
  - 21-02 (Hero + Story Sections)
  - 21-03 (Team Section)
  - 21-04 (Values + Verein Sections)
  - 21-05 (Mitmach + Abschluss CTAs)
  - 21-06 (FAQ Section)
  - 21-07 (Kontakt Section)
files_modified:
  - apps/website/app/about/page.tsx
  - packages/e2e-tools/tests/about.spec.ts
requirements: []
---

# Plan 21-08 — Route, Metadata, Playwright-Smoke-Test

> Finaler Assembly-Plan: `app/about/page.tsx` mountet alle 9 Sections in korrekter Reihenfolge, setzt Metadata (Title, Description, OG). Playwright-Smoke-Test verifiziert Route + alle Anker + FAQ-Accordion-Klick + Deep-Link `/about#faq` + Build-Integrität. Nav-Active-State-Check (informativ, nicht-blockierend).

---

<objective>
Route `/about` liveschalten, indem Sections aus Plans 21-02 bis 21-07 in einer `app/about/page.tsx` komponiert werden. Metadata für SEO setzen (Title, Meta-Description, OG). Playwright-Smoke-Test als e2e-Regression, der Route-Load + 9-Section-Mount + 3 Anker + FAQ-Accordion-Toggle + Deep-Link-Navigation prüft. Build-Verify dass Route `ƒ` (dynamic) bleibt (LEARNINGS.md CSP-Regel).
</objective>

<scope>
**In-scope:**
- `apps/website/app/about/page.tsx` — Server Component, rendert alle 9 Section-Components in Reihenfolge
- Metadata export (Title, Description, OG-Title, OG-Description — OG-Image Reuse vom Landing bis Phase 27)
- Playwright-Smoke-Test `packages/e2e-tools/tests/about.spec.ts`
- Build-Verification: Route ist dynamic (`ƒ`), keine CSP-Regressionen

**Out-of-scope:**
- `apps/website/app/about/layout.tsx` — NICHT nötig, Root-Layout reicht (UI-SPEC Zeile 334)
- Dediziertes OG-Image für `/about` (Phase 27, UI-SPEC Zeile 280)
- Header-Active-State-Enhancement für `/about` (UI-SPEC Open Issue 8) — Header zeigt aktuell Link, aktives Highlighting ist Phase-27-Polish
- Nav-Item "Über uns" Verifikation (Header hat Link bereits, nur Smoke-Test-Assertion)
- Sitemap-Update (separate minor task, kann in Phase-Verification nachgezogen werden — siehe Notes)
</scope>

<context>
**Read:**
- `.planning/phases/21-about-page/21-CONTEXT.md` — Success Criteria (Zeile 126-144)
- `.planning/phases/21-about-page/21-UI-SPEC.md` — Meta-Tags (Zeile 273-280), A11y-Kontrakt (Zeile 360-376)
- `apps/website/app/page.tsx` — Homepage-Route als Referenz (nur 11 Zeilen, einfach)
- `apps/website/app/layout.tsx` — Root-Layout (`dynamic = "force-dynamic"` bereits gesetzt)
- `LEARNINGS.md` — CSP/Proxy-Regeln (load-bearing!)
- `apps/website/AGENTS.md` + `apps/website/CLAUDE.md` — Next.js-Konventionen
- `packages/e2e-tools/tests/landing.spec.ts` — Playwright-Test-Pattern
- `packages/e2e-tools/tests/smoke.spec.ts` — Smoke-Test-Pattern
- `packages/e2e-tools/playwright.config.ts` — E2E-Config
- `apps/website/components/layout/header.tsx` — Nav-Link "Über uns" bereits vorhanden (grep-verified)
</context>

<tasks>

### Task 1 — Create `/about` Route

**Read first:**
- `apps/website/app/page.tsx` (Homepage als Vorlage)
- `apps/website/app/layout.tsx` (Metadata-Pattern)
- `.planning/phases/21-about-page/21-UI-SPEC.md` Zeile 273-280 (Meta-Tags)
- `apps/website/app/about/page.tsx` (prüfen ob Datei bereits existiert — falls nicht, neu anlegen)
- Alle 9 Section-Components aus Plans 21-02 bis 21-07 (import paths verifizieren)
- `LEARNINGS.md` (CSP-Regeln — Route muss dynamic bleiben)

**Action:**
Erstelle `apps/website/app/about/page.tsx`:

```tsx
import type { Metadata } from "next"

import { AboutHeroSection } from "@/components/about/about-hero-section"
import { AboutStorySection } from "@/components/about/about-story-section"
import { AboutTeamSection } from "@/components/about/about-team-section"
import { AboutValuesSection } from "@/components/about/about-values-section"
import { AboutVereinSection } from "@/components/about/about-verein-section"
import { AboutMitmachCTASection } from "@/components/about/about-mitmach-cta-section"
import { AboutFaqSection } from "@/components/about/about-faq-section"
import { AboutFinalCTASection } from "@/components/about/about-final-cta-section"
import { AboutKontaktSection } from "@/components/about/about-kontakt-section"

// Route `/about` — Credibility-Anker-Seite (Phase 21).
// Single-Page-Layout mit 9 Sections in fester Reihenfolge.
// Route ist dynamic via Root-Layout (`export const dynamic = "force-dynamic"` in app/layout.tsx)
// — CSP-Nonce-Flow bleibt intakt (siehe LEARNINGS.md).

export const metadata: Metadata = {
  title: {
    absolute: "Über uns · Generation AI",
  },
  description:
    "Warum es Generation AI gibt, wer dahintersteckt und wie wir uns organisieren. Gemeinnütziger Verein in Gründung, von und für Studierende im DACH-Raum.",
  openGraph: {
    title: "Über uns · Generation AI",
    description:
      "Warum es Generation AI gibt, wer dahintersteckt und wie wir uns organisieren. Gemeinnütziger Verein in Gründung, von und für Studierende im DACH-Raum.",
    url: "https://generation-ai.org/about",
    // OG-Image Reuse vom Root (Landing-OG) — dediziertes /about-OG ist Phase 27 Scope (UI-SPEC Zeile 280).
  },
  twitter: {
    title: "Über uns · Generation AI",
    description:
      "Warum es Generation AI gibt, wer dahintersteckt und wie wir uns organisieren. Gemeinnütziger Verein in Gründung, von und für Studierende im DACH-Raum.",
  },
  alternates: {
    canonical: "https://generation-ai.org/about",
  },
}

export default function AboutPage() {
  return (
    <main id="main-content">
      <AboutHeroSection />
      <AboutStorySection />
      <AboutTeamSection />
      <AboutValuesSection />
      <AboutVereinSection />
      <AboutMitmachCTASection />
      <AboutFaqSection />
      <AboutFinalCTASection />
      <AboutKontaktSection />
    </main>
  )
}
```

**Concrete values:**
- Title: `title: { absolute: "Über uns · Generation AI" }` — `absolute` unterdrückt das Root-Template `"%s | Generation AI"` (sonst würde es zu "Über uns · Generation AI | Generation AI" werden)
- Meta-Description (exakt aus UI-SPEC Zeile 277): `Warum es Generation AI gibt, wer dahintersteckt und wie wir uns organisieren. Gemeinnütziger Verein in Gründung, von und für Studierende im DACH-Raum.`
- OG-Title + Description: identisch zum Meta
- Canonical URL: `https://generation-ai.org/about`
- `<main id="main-content">` — Skip-Link-Target (konsistent mit Header, UI-SPEC Zeile 363)
- Section-Reihenfolge FIX (9 Sections, siehe UI-SPEC §Mission + Section-Inventory):
  1. Hero
  2. Story
  3. Team
  4. Values
  5. Verein
  6. Mitmach-CTA
  7. FAQ
  8. Final-CTA (Abschluss)
  9. Kontakt

**CSP/Proxy-Safety:** Route benötigt KEINE eigene `dynamic = "force-dynamic"` Direktive, weil Root-Layout (`app/layout.tsx` Zeile 7) es bereits global setzt. Verifikation in Task 4.

**Acceptance criteria:**
- [ ] Datei `apps/website/app/about/page.tsx` existiert
- [ ] 9 Imports für die 9 Section-Components vorhanden: `grep -cE 'import \{ About(Hero|Story|Team|Values|Verein|MitmachCTA|Faq|FinalCTA|Kontakt)Section' apps/website/app/about/page.tsx` liefert 9
- [ ] `<main id="main-content">` vorhanden: `grep -n 'id="main-content"' apps/website/app/about/page.tsx` matched
- [ ] Metadata-Export: `grep -n 'export const metadata' apps/website/app/about/page.tsx` matched
- [ ] Title mit `absolute`: `grep -F 'absolute: "Über uns · Generation AI"' apps/website/app/about/page.tsx` matched
- [ ] Meta-Description exakt: `grep -F 'Warum es Generation AI gibt, wer dahintersteckt und wie wir uns organisieren.' apps/website/app/about/page.tsx` matched
- [ ] Alle 9 Components gemountet in korrekter Reihenfolge: manuelle Code-Review via `sed -n '/return (/,/)/p' apps/website/app/about/page.tsx` zeigt Hero → Story → Team → Values → Verein → MitmachCTA → Faq → FinalCTA → Kontakt (in dieser Reihenfolge)
- [ ] Canonical URL: `grep -F 'https://generation-ai.org/about' apps/website/app/about/page.tsx` matched

---

### Task 2 — Playwright Smoke Test

**Read first:**
- `packages/e2e-tools/tests/landing.spec.ts` (Pattern für test.describe, page.goto, expect-Patterns)
- `packages/e2e-tools/tests/smoke.spec.ts` (einfacher Smoke-Test als Template)
- `packages/e2e-tools/playwright.config.ts` (baseURL, webServer-Config)

**Action:**
Erstelle `packages/e2e-tools/tests/about.spec.ts`:

```ts
import { test, expect } from "@playwright/test"

const BASE_URL = process.env.E2E_BASE_URL ?? "http://localhost:3000"

test.describe("Phase 21 — /about", () => {
  test.beforeEach(async ({ page }) => {
    // Skip Terminal-Splash (siehe landing.spec.ts Pattern).
    await page.addInitScript(() => {
      try {
        sessionStorage.setItem("terminal-splash-seen", "true")
      } catch {
        // sessionStorage may be unavailable in some contexts — safe to ignore.
      }
    })
  })

  test("Route /about lädt mit HTTP 200 und rendert alle 9 Sections", async ({ page }) => {
    const response = await page.goto(`${BASE_URL}/about`)
    expect(response?.status()).toBe(200)

    // All 9 sections mounted via data-section attribute.
    const expectedSections = [
      "about-hero",
      "about-story",
      "about-team",
      "about-values",
      "about-verein",
      "about-mitmach",
      "about-faq",
      "about-final-cta",
      "about-kontakt",
    ]
    for (const name of expectedSections) {
      await expect(page.locator(`section[data-section="${name}"]`)).toBeVisible()
    }
  })

  test("Meta-Tags korrekt gesetzt", async ({ page }) => {
    await page.goto(`${BASE_URL}/about`)
    await expect(page).toHaveTitle(/Über uns · Generation AI/)

    const description = await page.locator('meta[name="description"]').getAttribute("content")
    expect(description).toContain("Warum es Generation AI gibt")
  })

  test("Anker #team, #verein, #mitmach, #faq, #kontakt existieren", async ({ page }) => {
    await page.goto(`${BASE_URL}/about`)
    for (const anchor of ["team", "verein", "mitmach", "faq", "kontakt"]) {
      await expect(page.locator(`#${anchor}`)).toBeAttached()
    }
  })

  test("Story-Anker #story und Werte-Anker #werte existieren", async ({ page }) => {
    await page.goto(`${BASE_URL}/about`)
    await expect(page.locator('#story')).toBeAttached()
    await expect(page.locator('#werte')).toBeAttached()
  })

  test("FAQ-Accordion öffnet bei Klick auf Trigger", async ({ page }) => {
    await page.goto(`${BASE_URL}/about#faq`)

    // Find first FAQ trigger button — first question "Was ist Generation AI?".
    const firstTrigger = page.getByRole("button", { name: /Was ist Generation AI/i })
    await expect(firstTrigger).toBeVisible()

    // Initially closed.
    await expect(firstTrigger).toHaveAttribute("aria-expanded", "false")

    // Click to open.
    await firstTrigger.click()
    await expect(firstTrigger).toHaveAttribute("aria-expanded", "true")

    // Answer panel visible (first sentence of Frage 1 answer).
    await expect(
      page.getByText(/kostenlose KI-Community für Studierende im DACH-Raum/i)
    ).toBeVisible()
  })

  test("FAQ-Multi-Open: 2 Panels gleichzeitig geöffnet", async ({ page }) => {
    await page.goto(`${BASE_URL}/about#faq`)

    const trigger1 = page.getByRole("button", { name: /Was ist Generation AI/i })
    const trigger2 = page.getByRole("button", { name: /Wer kann Mitglied werden/i })

    await trigger1.click()
    await trigger2.click()

    // Both should be expanded simultaneously (Multi-Open, D-03).
    await expect(trigger1).toHaveAttribute("aria-expanded", "true")
    await expect(trigger2).toHaveAttribute("aria-expanded", "true")
  })

  test("Deep-Link /about#faq scrollt zur FAQ-Section", async ({ page }) => {
    await page.goto(`${BASE_URL}/about#faq`)

    const faqSection = page.locator('#faq')
    await expect(faqSection).toBeInViewport({ timeout: 2000 })
  })

  test("Nav-Link 'Über uns' im Header führt zu /about", async ({ page }) => {
    await page.goto(BASE_URL)
    // Multiple "Über uns" links may exist (desktop + mobile menu) — pick the
    // desktop nav one, which is visible at default viewport (md+).
    const aboutLink = page.getByRole("link", { name: "Über uns" }).first()
    await expect(aboutLink).toBeVisible()
    await aboutLink.click()
    await page.waitForURL("**/about")
    expect(page.url()).toContain("/about")
  })

  test("Skip-Link 'Zum Inhalt springen' funktioniert auf /about", async ({ page }) => {
    await page.goto(`${BASE_URL}/about`)
    // Skip-link is the first focusable element in Header.
    await page.keyboard.press("Tab")
    const skipLink = page.locator('.skip-link')
    await expect(skipLink).toBeFocused()
    await page.keyboard.press("Enter")
    // Main content should be focused/in-view.
    await expect(page.locator('#main-content')).toBeInViewport()
  })

  test("Keine Console-Errors auf /about (CSP-Regression-Check)", async ({ page }) => {
    const errors: string[] = []
    page.on("console", (msg) => {
      if (msg.type() === "error") errors.push(msg.text())
    })

    await page.goto(`${BASE_URL}/about`)
    await page.waitForLoadState("networkidle")

    // Filter out known benign errors (e.g., 404 on /partner-route if Phase 22 not yet live —
    // /partner link is in Footer + Abschluss-CTA + Kontakt, but they're not followed in this test).
    const fatalErrors = errors.filter(
      (e) => !e.includes("Speed Insights") && !e.includes("vitals")
    )
    expect(fatalErrors).toEqual([])
  })
})
```

**Concrete test assertions:**
- 9 Sections via `data-section` locator
- Meta-Title contains `"Über uns · Generation AI"`
- Meta-Description contains `"Warum es Generation AI gibt"`
- 7 Anker: `#story`, `#team`, `#werte`, `#verein`, `#mitmach`, `#faq`, `#kontakt`
- FAQ-Accordion: erst Trigger existiert, `aria-expanded="false"` → Click → `"true"` → Answer-Text sichtbar
- FAQ-Multi-Open: 2 Panels können gleichzeitig offen sein (D-03)
- Deep-Link `/about#faq` → FAQ-Section in Viewport
- Nav-Link "Über uns" führt zu `/about`
- Skip-Link funktioniert (A11y-Kontrakt UI-SPEC Zeile 362)
- Keine Console-Errors (CSP-Regression-Check)

**Acceptance criteria:**
- [ ] Datei `packages/e2e-tools/tests/about.spec.ts` existiert
- [ ] Test `Route /about lädt mit HTTP 200` definiert: `grep -n 'Route /about lädt' packages/e2e-tools/tests/about.spec.ts` matched
- [ ] Alle 9 data-section checks: `grep -c 'data-section="about-' packages/e2e-tools/tests/about.spec.ts` liefert ≥9
- [ ] FAQ-Multi-Open Test: `grep -n 'FAQ-Multi-Open' packages/e2e-tools/tests/about.spec.ts` matched
- [ ] Deep-Link Test: `grep -n '#faq' packages/e2e-tools/tests/about.spec.ts` matched
- [ ] CSP-Regression-Check Test: `grep -n 'Console-Errors' packages/e2e-tools/tests/about.spec.ts` matched

---

### Task 3 — Build & CSP Verification

**Read first:**
- `LEARNINGS.md` (Regel 3: nach Build `ƒ`/`○`-Check)
- `apps/website/package.json` (Build-Script)

**Action:**
```bash
cd apps/website

# Type-check
pnpm tsc --noEmit

# Build
pnpm build 2>&1 | tee /tmp/about-build.log

# Verifikation: /about Route ist dynamic (ƒ)
grep -E "ƒ.*(about|/about)" /tmp/about-build.log
# Alternative if grep via symbol doesn't match:
grep -E "(about|/about)" /tmp/about-build.log | grep -v "○"
```

Falls `/about` als `○` (static) erscheint → BROKEN, Root-Layout `force-dynamic` greift nicht auf das Sub-Route. Fix: `export const dynamic = "force-dynamic"` explizit in `app/about/page.tsx` ergänzen.

**Acceptance criteria:**
- [ ] `pnpm --filter @genai/website tsc --noEmit` exit code 0
- [ ] `pnpm --filter @genai/website build` exit code 0
- [ ] Build-Log zeigt `/about` als `ƒ` (dynamic) NICHT als `○` (static): visuell verifizieren
- [ ] Keine neuen CSP-Warnings in Build-Log

---

### Task 4 — Run Playwright Test (Prod-Parity)

**Read first:**
- `packages/e2e-tools/playwright.config.ts` (webServer-Config, ob Tests lokal gegen `pnpm dev` oder built server laufen)

**Action:**
```bash
cd /Users/lucaschweigmann/projects/generation-ai

# Start prod-build locally (CSP-parity check, LEARNINGS.md Regel 4)
cd apps/website
NODE_ENV=production pnpm start &
SERVER_PID=$!

# Wait for server ready
sleep 5

# Run the about.spec.ts tests
cd ../../packages/e2e-tools
E2E_BASE_URL=http://localhost:3000 pnpm playwright test tests/about.spec.ts 2>&1 | tee /tmp/about-e2e.log

# Capture result
TEST_EXIT=$?

# Cleanup
kill $SERVER_PID || true

# Fail the task if tests didn't pass
exit $TEST_EXIT
```

Falls Tests fehlschlagen:
- Console-Errors-Test: prüfe tatsächliche Errors → CSP-Regression? Dann CRITICAL, mit Plan-Checker eskalieren.
- Andere Assertion-Failures: meist Copy-Mismatch (grep vs. tatsächlicher Render). Fix per Copy-Abgleich.

**Acceptance criteria:**
- [ ] `pnpm playwright test tests/about.spec.ts` exit code 0
- [ ] Alle 10 definierten Tests (Task 2) gehen grün
- [ ] Keine CSP-Console-Errors im Test-Log

---

### Task 5 — Sitemap Update (Minor)

**Read first:**
- `apps/website/app/sitemap.ts`

**Action:**
Erweitere `apps/website/app/sitemap.ts` um `/about`-Route:

```ts
// Innerhalb des sitemap array, füge hinzu:
{
  url: `${baseUrl}/about`,
  lastModified: new Date(),
  changeFrequency: "monthly",
  priority: 0.8,
},
```

Exakte Struktur siehe existierende Einträge in `sitemap.ts`. `priority: 0.8` (hoch, weil Credibility-Anker-Seite — aber niedriger als Landing `1.0`).

**Acceptance criteria:**
- [ ] `/about` in `apps/website/app/sitemap.ts` enthalten: `grep -F '/about' apps/website/app/sitemap.ts` matched
- [ ] `priority: 0.8` oder vergleichbarer Wert gesetzt
- [ ] Build nach Änderung weiterhin grün: `pnpm --filter @genai/website build` exit 0

</tasks>

<threat_model>

| Threat | Surface | Mitigation |
|--------|---------|-----------|
| **CSP-Regression — Route wird static statt dynamic** | `app/about/page.tsx` ohne explicit force-dynamic | Root-Layout `force-dynamic` propagiert zu child routes in Next.js 16 App-Router. Build-Check in Task 3 verifiziert `ƒ`-Symbol. Falls nicht, explicit `export const dynamic = "force-dynamic"` als Fallback (siehe Task 3 Fix). CRITICAL-SEVERITY: Bricht CSP-Nonce → schwarze Seite (LEARNINGS.md Incident). |
| **Missing Anker bricht Landing-Kurz-FAQ-Link** | `/about#faq` target | Plan 21-06 setzt `id="faq"`. Task 2 Test verifiziert via Deep-Link-Navigation. Acceptance-Criterion grep + runtime Playwright. |
| **Broken section order → User-Confusion** | 9 Components gemountet in wrong order | Explicit order in code + Acceptance-Criterion-Code-Review. Keine dynamische Mount-Logic. |
| **Meta-Tag-Konflikt mit Root-Template** | Title ohne `absolute` | `title: { absolute: "..." }` unterdrückt Root `template: "%s | Generation AI"` — sonst würde "Über uns · Generation AI | Generation AI" erscheinen. Verified via Playwright `toHaveTitle(/Über uns · Generation AI/)`. |
| **XSS via Metadata (if content from CMS)** | Hardcoded strings | Alle Meta-Strings sind hardcoded in TSX, React/Next.js escape-safe. Kein User-Input. |
| **`/partner` Link 404 bis Phase 22** | Abschluss-CTA + Kontakt Secondary | Acceptable short-term (Phase 22 ist direkt danach geplant). User-Impact: broken Link für ~Tage bis Phase 22 live. Wenn Phase 22 länger dauert: Fallback-Copy erwägen. |
| **Sitemap-Drift (Phase 21 Route nicht indexiert)** | SEO | Task 5 ergänzt `/about` in sitemap. Fallback wenn Task 5 skipped: Google findet Route trotzdem via Header-Link-Crawl. |
| **Playwright-Test flaky via Terminal-Splash overlay** | Page-load | `page.addInitScript(sessionStorage.setItem("terminal-splash-seen", "true"))` Pattern (copied from landing.spec.ts) umgeht Splash. |

**Severity: MEDIUM** (CSP-Regression wäre CRITICAL, aber durch Build-Check mitigated).

</threat_model>

<verification_commands>

```bash
# Files exist
ls apps/website/app/about/page.tsx
ls packages/e2e-tools/tests/about.spec.ts

# Type-check + Build
pnpm --filter @genai/website tsc --noEmit
pnpm --filter @genai/website build

# CSP: Route must be dynamic (ƒ), not static (○)
pnpm --filter @genai/website build 2>&1 | grep -E "/about" | grep -qv "○"

# Metadata verification (visual grep)
grep -F 'absolute: "Über uns · Generation AI"' apps/website/app/about/page.tsx
grep -F 'Warum es Generation AI gibt, wer dahintersteckt' apps/website/app/about/page.tsx

# Section order (9 components in correct order)
sed -n '/return (/,/)/p' apps/website/app/about/page.tsx | grep -c 'About'  # expect ≥9

# Playwright smoke (requires dev-server running)
cd packages/e2e-tools && pnpm playwright test tests/about.spec.ts

# Sitemap includes /about
grep -F '/about' apps/website/app/sitemap.ts
```

</verification_commands>

<must_haves>

- [ ] `apps/website/app/about/page.tsx` exists, rendert 9 Sections in fester Reihenfolge
- [ ] Metadata: Title `"Über uns · Generation AI"` mit `absolute: true`, Description exakt aus UI-SPEC, Canonical URL
- [ ] `<main id="main-content">` für Skip-Link-Target
- [ ] Route wird dynamic gerendert (`ƒ`-Symbol im Build-Log), CSP-Nonce-Flow intakt
- [ ] `packages/e2e-tools/tests/about.spec.ts` mit mindestens 10 Tests (Route-Load, Meta, Anker, Accordion, Multi-Open, Deep-Link, Nav, Skip-Link, Console-Errors)
- [ ] Alle Playwright-Tests gehen grün gegen lokalen Prod-Build
- [ ] Sitemap enthält `/about` mit priority 0.8
- [ ] TypeScript + Build + Lint + E2E alle grün

</must_haves>

<notes>

- **Nav-Active-State für `/about`** (UI-SPEC Open Issue 8): NICHT in diesem Plan. Header zeigt den Link bereits, aktives Highlighting ist Phase-27-Polish-Scope. Smoke-Test prüft nur: Link existiert + führt nach `/about`.
- **OG-Image Reuse** (UI-SPEC Open Issue 7): Root-Layout serviert `opengraph-image.tsx` global für alle Routen ohne eigenes OG — Reuse automatisch aktiv. Dediziertes `/about`-OG ist Phase-27-Scope.
- **`/partner` Link:** Führt bis Phase 22 live ist zu einem 404. Acceptable, da Phase 22 direkt nach 21 geplant ist (ROADMAP). Falls Phase 22 deutlich verzögert wird, kann Plan 21-05 (Abschluss-CTA) + 21-07 (Kontakt) kurzfristig "→ Partner werden" auf `mailto:partner@generation-ai.org` umstellen.
- **LEARNINGS.md CSP-Regel** ist LOAD-BEARING für dieses Plan. Task 3 Build-Check ist der primäre Schutz gegen Regression. Falls Build `/about` als `○` markiert, SOFORT Explicit-dynamic-Fallback in `page.tsx` einsetzen.
- **Playwright-Snapshot/Visual-Regression:** Nicht in diesem Plan. UAT durch Luca (manuell) + potential Phase-27-Visual-Baseline-Spec.
- **STATE.md Update:** Nach erfolgreichem Execute wird `.planning/STATE.md` automatisch vom `/gsd-execute-phase` Workflow aktualisiert — nicht manuell hier.

</notes>
