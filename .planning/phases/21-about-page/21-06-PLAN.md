---
phase: 21
plan: 06
slug: faq-section
type: plan
status: skeleton
created: 2026-04-24
last-updated: 2026-04-24
autonomous: true
wave: 2
depends_on: []
files_modified:
  - apps/website/components/about/about-faq-section.tsx
  - apps/website/components/about/faq-data.ts
requirements: []
---

# Plan 21-06 — 10-Fragen FAQ Accordion (`/about#faq`)

> KERN-FEATURE. 10-Fragen-Accordion mit Multi-Open, Anker `#faq` als Target vom Landing-Kurz-FAQ-Link. Reuse 1:1 der Accordion-Mechanik aus `components/sections/kurz-faq-section.tsx` (Plan 20.6-09). Sync-Regel: 4 Fragen sind 1:1 identisch zum Kurz-FAQ. Inline-Anker-Links in Antworten 8/9/10.

---

<objective>
Client-Component `AboutFaqSection` rendern mit 10-Fragen-Accordion. Accordion-Mechanik (Multi-Open-Set, `useId`-basierte aria-controls, Plus→Cross-Rotation, AnimatePresence height:0→auto, Reduced-Motion-Gate) ist 1:1 aus `kurz-faq-section.tsx` übernommen. Fragen in separatem `faq-data.ts`-Modul. Copy 1:1 aus UI-SPEC. Anker `id="faq"` load-bearing (Landing-Link-Target).
</objective>

<scope>
**In-scope:**
- `faq-data.ts` — 10 Fragen mit Antworten als strukturierte Daten, Inline-Anker-Refs in 3 Antworten
- `about-faq-section.tsx` — Accordion-Section mit `id="faq"`, Eyebrow `// fragen & antworten`, H2 "Was du wissen solltest."
- Inline-Link-Support in Antworten (für Fragen 8/9/10): Anker `#team`, `#verein`, `#mitmach`
- Multi-Open-State (User kann mehrere Panels offen haben)
- A11y: `aria-expanded`, `aria-controls`, `role="region"`, Keyboard-Support

**Out-of-scope:**
- Extraktion der Accordion-Mechanik nach `packages/ui` oder `components/ui/` (UI-SPEC-Default: lokale Kopie, Extraction ist Phase 27 Cleanup wenn Muster stabil)
- Analytics-Events pro Panel (Roadmap)
- Such/Filter-UI (nicht nötig bei 10 Fragen)
- Kurz-FAQ-Copy-Drift-Fix (UI-SPEC Sync-Regel sagt: Kurz-FAQ ist SoT für die 4 überlappenden Fragen — hier 1:1 kopieren)
</scope>

<context>
**Read:**
- `apps/website/components/sections/kurz-faq-section.tsx` — **Blueprint für Accordion-Mechanik (ganze Datei lesen, 227 Zeilen)**
- `.planning/phases/21-about-page/21-UI-SPEC.md` — FAQ-Section (Zeile 207-238), A11y-Pattern (Zeile 231-238), Sync-Regel (Zeile 213)
- `.planning/phases/21-about-page/21-CONTEXT.md` — §7 FAQ (Zeile 69-84), D-03 (multi-open Pflicht), D-10 (Antworten Simon-gescripted)
- Plan 20.6-09 und dessen SUMMARY: `.planning/phases/20.6-landing-sections-rebuild/20.6-09-SUMMARY.md` (falls existiert, für Rückblick was exakt geliefert wurde)
</context>

<tasks>

### Task 1 — faq-data.ts

**Read first:**
- `.planning/phases/21-about-page/21-UI-SPEC.md` Zeile 215-228 (10-Fragen-Liste mit Antworten)
- `apps/website/components/sections/kurz-faq-section.tsx` Zeile 38-58 (Kurz-FAQ-Data-Struktur — identische Props für Fragen 3, 5, 6, 7 erforderlich)

**Action:**
Erstelle `apps/website/components/about/faq-data.ts`:

```ts
// 10-Fragen-FAQ für /about#faq (UI-SPEC §FAQ, Zeile 215-228).
//
// Sync-Regel (UI-SPEC Zeile 213): Fragen 3, 5, 6, 7 sind 1:1 identisch zur Copy
// in components/sections/kurz-faq-section.tsx. Copy-Änderungen müssen in
// BEIDEN Files gemacht werden (Phase 27 Copy-Pass wird dies validieren).
//
// Inline-Anker-Links in Antworten 8, 9, 10 → Plan erlaubt HTML-like Markup
// durch segmented-text-nodes (siehe FaqItem.answerNodes unten).

export type FaqLink = {
  kind: "link"
  text: string
  href: string   // Anker same-page: #team, #verein, #mitmach
}

export type FaqTextNode = {
  kind: "text"
  text: string
}

export type FaqAnswerNode = FaqTextNode | FaqLink

export type FaqItem = {
  q: string
  // answer is either a plain string OR an array of nodes (for inline links).
  // For A11y: when answerNodes present, render as inline <a> within <p>.
  answerNodes: FaqAnswerNode[]
}

// Helper for pure-text answers (most of the 10 are pure text).
const t = (text: string): FaqTextNode => ({ kind: "text", text })
const a = (text: string, href: string): FaqLink => ({ kind: "link", text, href })

export const faqs: FaqItem[] = [
  {
    q: "Was ist Generation AI?",
    answerNodes: [
      t("Wir sind die erste kostenlose KI-Community für Studierende im DACH-Raum. Gemeinnütziger Verein in Gründung. Wir bringen praktische KI-Skills in Workshops, Community-Austausch und kuratierte Wissensplattform zusammen — für alle Fachrichtungen, nicht nur Informatik."),
    ],
  },
  {
    q: "Wer kann Mitglied werden?",
    answerNodes: [
      t("Studierende und Early-Career-Professionals aus dem DACH-Raum (Deutschland, Österreich, Schweiz) — unabhängig von Hochschule, Fachrichtung oder Vorerfahrung."),
    ],
  },
  {
    // SYNC with kurz-faq-section.tsx #1 "Kostet das was?" — exact answer text.
    q: "Kostet die Mitgliedschaft etwas?",
    answerNodes: [
      t("Nein. Mitgliedschaft, Community-Zugang, Wissensplattform und Events sind kostenlos. Generation AI ist als gemeinnütziger Verein aufgestellt und finanziert sich über Fördermitglieder und Partner."),
    ],
  },
  {
    q: "Wie melde ich mich an?",
    answerNodes: [
      t("Über den Button oben (`Jetzt beitreten`) — kurzer Fragebogen, Bestätigung per Mail, dann direkter Zugang zur Community. Dauert keine 2 Minuten."),
    ],
  },
  {
    // SYNC with kurz-faq-section.tsx #2 — exact answer text.
    q: "Brauche ich technisches Vorwissen?",
    answerNodes: [
      t("Nein. Wir starten beim Alltagsnutzen und führen schrittweise zu Agenten und Automatisierung. Alle Fachrichtungen willkommen — du musst weder programmieren können noch Informatik studieren."),
    ],
  },
  {
    // SYNC with kurz-faq-section.tsx #4 — exact answer text.
    q: "Wie viel Zeit muss ich investieren?",
    answerNodes: [
      t("So viel oder so wenig du willst. Kein Pflichtprogramm. Die Community läuft asynchron, Events sind optional. Viele schauen monatlich rein, andere bauen aktiv mit — beides passt."),
    ],
  },
  {
    // SYNC with kurz-faq-section.tsx #3 "Muss ich an einer bestimmten Uni studieren?"
    // Note: q wording slightly different ("Uni sein" vs "Uni studieren"), answer exact.
    q: "Muss ich an einer bestimmten Uni sein?",
    answerNodes: [
      t("Nein. Offen für Studierende und Early-Career aus dem gesamten DACH-Raum, unabhängig von Hochschule oder Fachrichtung. Wir arbeiten perspektivisch mit Hochschulen zusammen, aber eine Mitgliedschaft ist daran nicht gebunden."),
    ],
  },
  {
    q: "Wer steckt hinter Generation AI?",
    answerNodes: [
      t("Gegründet im Februar 2026 von Janna und Simon. Inzwischen ein zehnköpfiges Team aus Studierenden und Early-Careers, alle ehrenamtlich. "),
      a("→ Mehr zum Team.", "#team"),
    ],
  },
  {
    q: "Wie finanziert ihr euch?",
    answerNodes: [
      t("Über Fördermittel, Partnerschaften mit Unternehmen, Stiftungen, Hochschulen — und durch Sachleistungen (z.B. Tool-Zugänge, Event-Räume). Gewinnorientiert sind wir nicht. "),
      a("→ Details im Verein-Abschnitt.", "#verein"),
    ],
  },
  {
    q: "Kann ich aktiv im Verein mitarbeiten?",
    answerNodes: [
      t("Ja — und wir suchen aktiv Leute. Events, Content, Strategie, Tech, Partnerschaften. "),
      a("→ Mitmachen.", "#mitmach"),
    ],
  },
]
```

**Concrete values — verbatim aus UI-SPEC Zeile 215-228:**
- 10 Fragen in exakter Reihenfolge (1-10, siehe SPEC-Tabelle)
- Fragen 3, 5, 6, 7 haben Sync-Markierung im Kommentar (Source-of-Truth für Copy ist `kurz-faq-section.tsx`)
- Frage 8 hat Inline-Link `→ Mehr zum Team.` → `#team`
- Frage 9 hat Inline-Link `→ Details im Verein-Abschnitt.` → `#verein`
- Frage 10 hat Inline-Link `→ Mitmachen.` → `#mitmach`

**Acceptance criteria:**
- [ ] Datei `apps/website/components/about/faq-data.ts` existiert
- [ ] Export `faqs: FaqItem[]` vorhanden: `grep -n 'export const faqs' apps/website/components/about/faq-data.ts` matched
- [ ] Exakt 10 Fragen: `grep -cE '^  \{$' apps/website/components/about/faq-data.ts` liefert 10
- [ ] Frage 1 exakt: `grep -F 'Was ist Generation AI?' apps/website/components/about/faq-data.ts` matched
- [ ] Frage 2 exakt: `grep -F 'Wer kann Mitglied werden?' apps/website/components/about/faq-data.ts` matched
- [ ] Frage 3 exakt: `grep -F 'Kostet die Mitgliedschaft etwas?' apps/website/components/about/faq-data.ts` matched
- [ ] Frage 8 hat Inline-Link auf `#team`: `grep -nE 'a\(.*#team' apps/website/components/about/faq-data.ts` matched
- [ ] Frage 9 hat Inline-Link auf `#verein`: `grep -nE 'a\(.*#verein' apps/website/components/about/faq-data.ts` matched
- [ ] Frage 10 hat Inline-Link auf `#mitmach`: `grep -nE 'a\(.*#mitmach' apps/website/components/about/faq-data.ts` matched
- [ ] Sync-Copy Frage 3 identisch zu `kurz-faq-section.tsx`: `grep -F 'Nein. Mitgliedschaft, Community-Zugang' apps/website/components/sections/kurz-faq-section.tsx` matched UND `grep -F 'Nein. Mitgliedschaft, Community-Zugang' apps/website/components/about/faq-data.ts` matched
- [ ] Sync-Copy Frage 5 identisch: beide Files haben "`Nein. Wir starten beim Alltagsnutzen`"
- [ ] Sync-Copy Frage 6 identisch: beide Files haben "`So viel oder so wenig du willst. Kein Pflichtprogramm.`"
- [ ] Sync-Copy Frage 7 identisch (answer): beide Files haben "`Nein. Offen für Studierende und Early-Career aus dem gesamten DACH-Raum`"

---

### Task 2 — AboutFaqSection (Accordion-Komposition)

**Read first:**
- `apps/website/components/sections/kurz-faq-section.tsx` — **ganze Datei (227 Zeilen)** — blueprint für exakte Mechanik-Reuse
- `apps/website/components/about/faq-data.ts` (Task 1)
- `.planning/phases/21-about-page/21-UI-SPEC.md` Zeile 207-238 (FAQ-Section-Spec)

**Action:**
Erstelle `apps/website/components/about/about-faq-section.tsx`. Die Struktur ist 1:1 `kurz-faq-section.tsx` mit folgenden **spezifischen Abweichungen**:

1. **Eyebrow-Text:** `// fragen & antworten` (statt `// häufige fragen`)
2. **H2-Text:** `Was du wissen solltest.` (statt `Kurz & knapp.`)
3. **H2-Size:** `var(--fs-h3)` visuell (sub-section der Page, nicht Peak) — aber semantisch `<h2>` bleiben, weil Section-Head
   - **Entscheidung-Korrektur:** UI-SPEC Zeile 84 definiert "FAQ-Trigger-Label 15-16px" und H3 für "Was du wissen solltest." — hier muss visuelle Size `--fs-h3` (20px) sein, NICHT die große 3xl/4xl/5xl aus Kurz-FAQ. Grund: Kurz-FAQ ist Closing-Peak auf Landing, FAQ hier ist Mid-Page-Section.
4. **Daten-Source:** `import { faqs } from "./faq-data"` statt inline-Array
5. **Section-ID:** `id="faq"` (load-bearing!) statt kein Anker
6. **Kein Footer-Link** ("Mehr Fragen? → Über uns") — wir SIND `/about`, kein self-referential Link
7. **Answer-Rendering:** Iteriere über `answerNodes`-Array, render `{kind: "text"} → <>text</>` und `{kind: "link"} → <Link href={href}>text</Link>` mit DS-styled Hover

```tsx
'use client'

import { useId, useState } from "react"
import Link from "next/link"
import { AnimatePresence, motion, useReducedMotion } from "motion/react"
import { faqs, type FaqAnswerNode } from "./faq-data"

// 10-Fragen FAQ-Section für /about#faq.
// Mechanik: 1:1 Reuse von components/sections/kurz-faq-section.tsx (20.6-09 Blueprint).
// Abweichungen:
//   - Eyebrow: "// fragen & antworten"
//   - H2: "Was du wissen solltest." (visuell --fs-h3, semantisch <h2>)
//   - 10 Items statt 5, aus faq-data.ts
//   - id="faq" (Anker-Target vom Kurz-FAQ-Footer-Link, Zeile 207 UI-SPEC)
//   - Kein Footer-Link
//   - Inline-Links in Antwort 8/9/10 (#team, #verein, #mitmach)

function AnswerNode({ node }: { node: FaqAnswerNode }) {
  if (node.kind === "text") {
    return <>{node.text}</>
  }
  return (
    <Link
      href={node.href}
      className="text-[var(--accent)] underline-offset-4 hover:underline transition-colors duration-[var(--dur-fast)] ease-[var(--ease-out)] focus-visible:outline-none focus-visible:underline"
    >
      {node.text}
    </Link>
  )
}

export function AboutFaqSection() {
  const [openIndices, setOpenIndices] = useState<Set<number>>(new Set())
  const prefersReducedMotion = useReducedMotion()
  const baseId = useId()

  const toggle = (i: number) => {
    setOpenIndices((prev) => {
      const next = new Set(prev)
      if (next.has(i)) {
        next.delete(i)
      } else {
        next.add(i)
      }
      return next
    })
  }

  const fadeIn = prefersReducedMotion
    ? {}
    : {
        initial: { opacity: 0, y: 16 },
        whileInView: { opacity: 1, y: 0 },
        viewport: { once: true, margin: "-10% 0px" },
        transition: { duration: 0.6, ease: [0.16, 1, 0.3, 1] as const },
      }

  return (
    <section
      id="faq"
      aria-labelledby="about-faq-heading"
      data-section="about-faq"
      className="relative bg-bg py-24 sm:py-32 border-b border-border"
    >
      <div className="mx-auto max-w-3xl px-6">
        {/* Eyebrow */}
        <motion.div
          {...fadeIn}
          className="mx-auto mb-5 flex items-center justify-center gap-2 font-mono text-[11px] font-bold uppercase tracking-[0.2em] text-text-muted"
        >
          <span
            aria-hidden="true"
            className="h-1.5 w-1.5 rounded-full"
            style={{ background: "var(--accent)", boxShadow: "0 0 8px var(--accent-glow)" }}
          />
          {"// fragen & antworten"}
        </motion.div>

        {/* H2 — visuell --fs-h3 */}
        <motion.h2
          {...fadeIn}
          id="about-faq-heading"
          className="text-center font-sans font-bold text-text text-balance"
          style={{ fontSize: "var(--fs-h3)", lineHeight: "var(--lh-sub)" }}
        >
          Was du wissen solltest.
        </motion.h2>

        {/* Accordion-List */}
        <motion.ul {...fadeIn} className="mt-14 flex flex-col gap-3">
          {faqs.map((item, i) => {
            const isOpen = openIndices.has(i)
            const triggerId = `${baseId}-about-faq-trigger-${i}`
            const panelId = `${baseId}-about-faq-panel-${i}`
            const indexLabel = i + 1 < 10 ? `// 0${i + 1}` : `// ${i + 1}`

            return (
              <li
                key={item.q}
                className="overflow-hidden rounded-[16px] border border-border bg-bg-card transition-colors duration-[var(--dur-normal)] ease-[var(--ease-out)] hover:border-[var(--border-accent)]"
              >
                <h3 className="m-0">
                  <button
                    type="button"
                    id={triggerId}
                    aria-expanded={isOpen}
                    aria-controls={panelId}
                    onClick={() => toggle(i)}
                    className="group flex w-full items-center justify-between gap-4 px-6 py-5 text-left font-mono text-[15px] sm:text-base font-bold leading-[1.35] tracking-[-0.01em] text-text transition-colors duration-[var(--dur-fast)] ease-[var(--ease-out)] hover:text-[var(--accent)] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--accent)] focus-visible:ring-offset-0"
                  >
                    <span className="flex items-start gap-3">
                      <span
                        aria-hidden="true"
                        className="font-mono text-[11px] font-bold uppercase tracking-[0.12em] text-[var(--accent)] pt-1 shrink-0"
                      >
                        {indexLabel}
                      </span>
                      <span className="text-pretty">{item.q}</span>
                    </span>
                    <span
                      aria-hidden="true"
                      className="shrink-0 font-mono text-xl text-text-muted transition-transform duration-[var(--dur-normal)] ease-[var(--ease-out)] group-hover:text-[var(--accent)]"
                      style={{
                        transform: isOpen ? "rotate(45deg)" : "rotate(0deg)",
                      }}
                    >
                      +
                    </span>
                  </button>
                </h3>

                <AnimatePresence initial={false}>
                  {isOpen && (
                    <motion.div
                      key="content"
                      id={panelId}
                      role="region"
                      aria-labelledby={triggerId}
                      initial={prefersReducedMotion ? { height: "auto", opacity: 1 } : { height: 0, opacity: 0 }}
                      animate={{ height: "auto", opacity: 1 }}
                      exit={prefersReducedMotion ? { height: "auto", opacity: 1 } : { height: 0, opacity: 0 }}
                      transition={prefersReducedMotion ? { duration: 0 } : { duration: 0.3, ease: [0.16, 1, 0.3, 1] as const }}
                      className="overflow-hidden"
                    >
                      <div className="px-6 pb-6 pt-0">
                        <div
                          aria-hidden="true"
                          className="mb-4 h-px w-full"
                          style={{ background: "var(--border)" }}
                        />
                        <p className="text-[15px] leading-[1.6] text-text-secondary text-pretty">
                          {item.answerNodes.map((node, nodeIdx) => (
                            <AnswerNode key={nodeIdx} node={node} />
                          ))}
                        </p>
                      </div>
                    </motion.div>
                  )}
                </AnimatePresence>
              </li>
            )
          })}
        </motion.ul>
      </div>
    </section>
  )
}
```

**Concrete values — exact spec:**
- Section-Anker: `id="faq"` (LOAD-BEARING — `/about#faq` Target vom Landing-Kurz-FAQ-Link)
- Eyebrow: `// fragen & antworten`
- H2 Copy: `Was du wissen solltest.` (visuell `--fs-h3`)
- Index-Label pro Panel: `// 01` bis `// 10` (siehe `indexLabel`-Logic — zweistellig für 1-9, einstellig für 10)
- Accordion-Item-Classes: EXAKT wie in `kurz-faq-section.tsx` (copy Zeile 131-200)
- Inline-Link-Styling in Answer: `text-[var(--accent)]`, `hover:underline`, `underline-offset-4`
- Keine Footer-Link-Section

**Acceptance criteria:**
- [ ] Datei `apps/website/components/about/about-faq-section.tsx` existiert
- [ ] Anker `id="faq"` (LOAD-BEARING): `grep -n 'id="faq"' apps/website/components/about/about-faq-section.tsx` matched
- [ ] Eyebrow: `grep -F '// fragen & antworten' apps/website/components/about/about-faq-section.tsx` matched
- [ ] H2 Copy: `grep -F 'Was du wissen solltest.' apps/website/components/about/about-faq-section.tsx` matched
- [ ] Import `faqs` from `./faq-data`: `grep -n "from \"./faq-data\"" apps/website/components/about/about-faq-section.tsx` matched
- [ ] `useState<Set<number>>` Multi-Open-Pattern: `grep -nE 'useState<Set<number>>' apps/website/components/about/about-faq-section.tsx` matched
- [ ] `aria-expanded` + `aria-controls` Pairing: `grep -cE 'aria-expanded|aria-controls' apps/website/components/about/about-faq-section.tsx` liefert ≥2
- [ ] `role="region"` auf Panel: `grep -n 'role="region"' apps/website/components/about/about-faq-section.tsx` matched
- [ ] `AnimatePresence` + `useReducedMotion` beide importiert: `grep -cE 'AnimatePresence|useReducedMotion' apps/website/components/about/about-faq-section.tsx` liefert ≥2
- [ ] Plus-Icon rotate logic: `grep -nE 'rotate\(45deg\)' apps/website/components/about/about-faq-section.tsx` matched
- [ ] Index-Label-Logic für 10+: `grep -nE 'i \+ 1 < 10' apps/website/components/about/about-faq-section.tsx` matched
- [ ] KEIN Footer-Link "Mehr Fragen": `grep -F 'Mehr Fragen' apps/website/components/about/about-faq-section.tsx` leer
- [ ] AnswerNode-Rendering vorhanden: `grep -n 'AnswerNode' apps/website/components/about/about-faq-section.tsx` matched mehrfach
- [ ] H2 nutzt `--fs-h3`: `grep -n 'var(--fs-h3)' apps/website/components/about/about-faq-section.tsx` matched

---

### Task 3 — Verify Build + Sync-Check

**Read first:**
- `apps/website/components/sections/kurz-faq-section.tsx` (für Sync-Verification)

**Action:**
```bash
cd apps/website
pnpm tsc --noEmit 2>&1 | head -20

# Sync-Verification (Plan-21-SPEC Sync-Rule)
echo "=== Sync-Check: Frage 3 (Kosten) ==="
grep -c 'Nein. Mitgliedschaft, Community-Zugang, Wissensplattform und Events sind kostenlos' components/sections/kurz-faq-section.tsx
grep -c 'Nein. Mitgliedschaft, Community-Zugang, Wissensplattform und Events sind kostenlos' components/about/faq-data.ts

echo "=== Sync-Check: Frage 5 (Vorwissen) ==="
grep -c 'Nein. Wir starten beim Alltagsnutzen' components/sections/kurz-faq-section.tsx
grep -c 'Nein. Wir starten beim Alltagsnutzen' components/about/faq-data.ts

echo "=== Sync-Check: Frage 6 (Zeit) ==="
grep -c 'So viel oder so wenig du willst. Kein Pflichtprogramm' components/sections/kurz-faq-section.tsx
grep -c 'So viel oder so wenig du willst. Kein Pflichtprogramm' components/about/faq-data.ts
```

**Acceptance criteria:**
- [ ] `pnpm --filter @genai/website tsc --noEmit` exit code 0
- [ ] Sync-Check Frage 3: beide greps liefern 1 (exact match)
- [ ] Sync-Check Frage 5: beide greps liefern 1
- [ ] Sync-Check Frage 6: beide greps liefern 1
- [ ] Dateien exist: `ls apps/website/components/about/about-faq-section.tsx apps/website/components/about/faq-data.ts`

</tasks>

<threat_model>

| Threat | Surface | Mitigation |
|--------|---------|-----------|
| Copy-Drift zwischen Kurz-FAQ und /about-FAQ (UI-SPEC Sync-Rule Zeile 213) | Duplicated copy across 2 files | Sync-Check-Grep in Task 3 verifiziert. Phase 27 Copy-Pass wird das als Regression-Check aufnehmen. Dokumentation in `faq-data.ts` header-Kommentar. |
| XSS via Inline-Link-Text | `FaqLink.text` rendered directly | Text is hardcoded, React escapes. Kein `dangerouslySetInnerHTML`. Kein User-Input. |
| Missing `#faq`-Anker → Landing-Kurz-FAQ-Link tot | `<section id="faq">` | Anker ist load-bearing, grep-verified. Plan 21-08 Smoke-Test navigiert explizit `/about#faq` und prüft Scroll. |
| Accordion-State-Leak beim Route-Change (stale open panels) | `useState<Set<number>>` | State ist Client-only, komponenten-lokal. Bei Route-Change wird Component unmounted → State verloren → korrektes Verhalten für User-Expectation. |
| Keyboard-Trap wenn AnimatePresence height:0 Element fokussiert | Accordion Panel Exit | Panel-Content wird nur gerendert wenn `isOpen`, sonst `null` (nicht `display:none` mit focusable Children). Fokus kann nicht in closed panel wandern. |
| A11y-Regression: `role="region"` ohne `aria-labelledby` | Panel role + labelledby | `role="region"` + `aria-labelledby={triggerId}` — Pairing vorhanden, grep-verified. |

**Severity: MEDIUM** — Copy-Sync ist ein Maintenance-Risk, rest ist LOW.

</threat_model>

<verification_commands>

```bash
# Files exist
ls apps/website/components/about/about-faq-section.tsx
ls apps/website/components/about/faq-data.ts

# Type-check
pnpm --filter @genai/website tsc --noEmit

# Critical anchor (load-bearing)
grep -q 'id="faq"' apps/website/components/about/about-faq-section.tsx

# 10 questions exist
grep -cE '^    q: "' apps/website/components/about/faq-data.ts  # expect 10

# Inline links in questions 8, 9, 10
grep -q "#team" apps/website/components/about/faq-data.ts
grep -q "#verein" apps/website/components/about/faq-data.ts
grep -q "#mitmach" apps/website/components/about/faq-data.ts

# Sync-Check (4 overlapping answers must be identical to kurz-faq-section.tsx)
for phrase in \
  "Nein. Mitgliedschaft, Community-Zugang, Wissensplattform und Events sind kostenlos" \
  "Nein. Wir starten beim Alltagsnutzen" \
  "So viel oder so wenig du willst. Kein Pflichtprogramm" \
  "Nein. Offen für Studierende und Early-Career aus dem gesamten DACH-Raum"; do
  grep -qF "$phrase" apps/website/components/sections/kurz-faq-section.tsx \
    && grep -qF "$phrase" apps/website/components/about/faq-data.ts \
    && echo "SYNC OK: $phrase" || echo "SYNC FAIL: $phrase"
done

# A11y essentials
grep -q 'aria-expanded' apps/website/components/about/about-faq-section.tsx
grep -q 'aria-controls' apps/website/components/about/about-faq-section.tsx
grep -q 'role="region"' apps/website/components/about/about-faq-section.tsx

# Multi-open Set-based state
grep -q 'useState<Set<number>>' apps/website/components/about/about-faq-section.tsx
```

</verification_commands>

<must_haves>

- [ ] `faq-data.ts` mit 10 Fragen + Antworten (pure-text oder gemischt text+link)
- [ ] 4 Fragen (3, 5, 6, 7) sind Sync-gelockt mit `kurz-faq-section.tsx` — grep-verified beide Richtungen
- [ ] `about-faq-section.tsx` implementiert 1:1 Accordion-Mechanik aus 20.6-09 (Multi-Open-Set, AnimatePresence, Plus→Cross-Rotation, Reduced-Motion-Gate, aria-expanded/controls/labelledby Pairing)
- [ ] Anker `id="faq"` load-bearing
- [ ] Inline-Links in Answers 8/9/10 (Targets: `#team`, `#verein`, `#mitmach`)
- [ ] Keine Footer-Link "Mehr Fragen" (self-referential auf /about/#faq wäre sinnlos)
- [ ] Index-Label `// 01` bis `// 10` (zweistellig für 1-9, einstellig für 10 — oder alternativ `// 10` — Decision in code)
- [ ] H2 semantisch, `--fs-h3` visuell (Mid-Page Sub-Section)
- [ ] TypeScript + ESLint grün

</must_haves>

<notes>

- **Sync-Regel ist ein dauerhaftes Wartungs-Invariante.** Phase 27 Copy-Pass soll einen automatisierten Sync-Check einbauen (z.B. CI-Lint-Rule). Für V1 reicht Plan-Checker-Grep.
- Die Entscheidung, Accordion-Mechanik zu kopieren statt zu extrahieren (UI-SPEC Open Issue 5), ist bewusst: zu früh für `packages/ui`-Extract, Muster muss sich erst stabilisieren. Phase 27 Cleanup kann extrahieren.
- Index-Label für Frage 10: `// 10` (einstellig). Frage 1-9: `// 01` bis `// 09` (zweistellig via String-Padding). Logic: `i + 1 < 10 ? '// 0' + (i + 1) : '// ' + (i + 1)`.
- FAQ-Question 7-Titel ist leicht anders: UI-SPEC hat "Muss ich an einer bestimmten Uni sein?" vs. kurz-faq-section "Muss ich an einer bestimmten Uni studieren?" — Answer ist identisch. Question-Wording-Diff ist acceptable (Luca kann Phase 27 vereinheitlichen).

</notes>
