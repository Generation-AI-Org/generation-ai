---
phase: 21
plan: 03
slug: team-section
type: plan
status: skeleton
created: 2026-04-24
last-updated: 2026-04-24
autonomous: true
wave: 2
depends_on:
  - 21-01 (Team Sub-Components — PlaceholderAvatar, TeamMemberCard, FounderCard)
files_modified:
  - apps/website/components/about/about-team-section.tsx
  - apps/website/components/about/team-data.ts
requirements: []
---

# Plan 21-03 — Team Section

> Team-Section der `/about`-Seite. Zwei prominente Gründer-Karten (Janna + Simon) oben, darunter Grid mit aktiven Mitgliedern (Placeholder-Avatars + Namen). Sub-Zeile "Stand: April 2026 · Wir wachsen." Responsive: 1-col Mobile, 2-col Tablet, 4-col Desktop (4×3 Grid für 10 Members). Daten separat in `team-data.ts` für Phase-27-Foto-Swap-Vorbereitung.

---

<objective>
Server Component `AboutTeamSection` rendern, die FounderCard + TeamMemberCard aus Plan 21-01 komponiert. Team-Daten in separatem Modul (`team-data.ts`) für Swap-Ease. 10 Mitgliedernamen als Placeholder (Luca liefert echte Namen in Phase 27). Anker `#team` für Deep-Linking.
</objective>

<scope>
**In-scope:**
- `about-team-section.tsx` — Section mit Eyebrow + H2 + Sub-Zeile + Gründer-Grid + Mitglieder-Grid
- `team-data.ts` — Gründer + Mitglieder als strukturierte Daten
- Responsive Grid-Logic (Gründer: 2-col Desktop / 1-col Mobile; Mitglieder: 4-col Desktop / 2-col Tablet / 1-col Mobile)
- Anker `id="team"`

**Out-of-scope:**
- Echte Team-Fotos (Placeholder bis Phase 27)
- Team-Detailseiten pro Person (Roadmap)
- Micro-Trenner-Eyebrow "// aktive mitglieder" zwischen Gründer und Members (UI-SPEC-Default: nahtlos, kein Trenner)
- LinkedIn-URLs für Janna + Simon (Placeholder-undefined bis Luca liefert — D-06)
</scope>

<context>
**Read:**
- `.planning/phases/21-about-page/21-CONTEXT.md` — §3 Team (Zeile 42-47), D-05 (Placeholder-Avatars), D-06 (LinkedIn disabled)
- `.planning/phases/21-about-page/21-UI-SPEC.md` — Team Copy (Zeile 159-172), Responsive Contract Team-Grid (Zeile 385-388), Open Issue 1 (Team-Grid-Layout 4×3)
- Plan 21-01 — Sub-Components die hier komponiert werden
- `apps/website/components/sections/kurz-faq-section.tsx` — Section-Header-Pattern
</context>

<tasks>

### Task 1 — team-data.ts

**Read first:**
- `.planning/phases/21-about-page/21-UI-SPEC.md` Zeile 159-172 (Team-Copy, Gründer-Bio-Drafts)
- `.planning/phases/21-about-page/21-CONTEXT.md` Zeile 42-47 (Scope Team)

**Action:**
Erstelle `apps/website/components/about/team-data.ts`:

```ts
// Team-Daten für /about-Team-Section.
//
// Quelle: UI-SPEC §Team (Zeile 159-172).
// Namen + Bios sind Placeholder bis Luca finale Copy liefert (CONTEXT Open Q 2).
// LinkedIn-URLs nachgeliefert (D-06) — bis dahin undefined.
// Member-Namen: Aktuelle 10-Leute-Liste noch nicht gelockt (CONTEXT Open Q 1).
// Bei Phase-27-Copy-Pass wird diese Datei mit finalen Namen + Fotos + URLs befüllt.

export type Founder = {
  name: string
  role: string
  bio: string
  linkedinUrl?: string  // undefined → Icon nicht rendern (D-06)
}

export type Member = {
  name: string
}

export const founders: Founder[] = [
  {
    name: "Janna Meister",
    role: "Co-Founder",
    bio: "Gründerin. Macht das strategische Big-Picture und die Partnerschaften.",
    linkedinUrl: undefined,
  },
  {
    name: "Simon Becker",
    role: "Co-Founder",
    bio: "Gründer. Verantwortet Community-Aufbau und Events.",
    linkedinUrl: undefined,
  },
]

// Placeholder-Namen für 10 aktive Mitglieder. Luca ersetzt in Phase 27.
export const members: Member[] = [
  { name: "Mitglied 1" },
  { name: "Mitglied 2" },
  { name: "Mitglied 3" },
  { name: "Mitglied 4" },
  { name: "Mitglied 5" },
  { name: "Mitglied 6" },
  { name: "Mitglied 7" },
  { name: "Mitglied 8" },
  { name: "Mitglied 9" },
  { name: "Mitglied 10" },
]
```

**Concrete values:**
- Founders: 2 Einträge (Janna Meister, Simon Becker — Platzhalter-Nachnamen falls echte Nachnamen fehlen; Luca liefert in Phase 27)
- Founder-Role: beide `"Co-Founder"`
- Founder-Bios: wörtlich aus UI-SPEC Zeile 165-166 (Placeholder-Draft)
- `linkedinUrl: undefined` für beide (D-06)
- Members: 10 Einträge mit Placeholder-Namen `"Mitglied 1"` bis `"Mitglied 10"` (Reflecting Open Question 1 aus CONTEXT: 10 Members zum Launch)

**Acceptance criteria:**
- [ ] Datei `apps/website/components/about/team-data.ts` existiert
- [ ] Exports `founders` und `members`: `grep -nE 'export const (founders|members)' apps/website/components/about/team-data.ts` zeigt 2 Treffer
- [ ] Exports `Founder` und `Member` Types: `grep -nE 'export type (Founder|Member)' apps/website/components/about/team-data.ts` zeigt 2 Treffer
- [ ] `founders` hat genau 2 Einträge: `grep -c '^  {$' apps/website/components/about/team-data.ts` liefert ≥12 (2 founders + 10 members = mind. 12 Objekt-Anfänge)
- [ ] `members` hat 10 Einträge: `grep -c 'name: "Mitglied' apps/website/components/about/team-data.ts` liefert 10
- [ ] Beide Founders haben `linkedinUrl: undefined`: `grep -c 'linkedinUrl: undefined' apps/website/components/about/team-data.ts` liefert 2
- [ ] Bio-Copy "strategische Big-Picture" vorhanden: `grep -F 'strategische Big-Picture' apps/website/components/about/team-data.ts` matched

---

### Task 2 — AboutTeamSection

**Read first:**
- `apps/website/components/about/placeholder-avatar.tsx` (Plan 21-01)
- `apps/website/components/about/team-member-card.tsx` (Plan 21-01)
- `apps/website/components/about/founder-card.tsx` (Plan 21-01)
- `apps/website/components/about/team-data.ts` (Task 1)
- `apps/website/components/about/about-story-section.tsx` (Plan 21-02, Motion-Pattern + Section-Header-Pattern — wenn vorhanden)
- `apps/website/components/sections/kurz-faq-section.tsx` (falls Plan 21-02 noch nicht committet, Fallback-Reference)

**Action:**
Erstelle `apps/website/components/about/about-team-section.tsx`:

```tsx
'use client'

import { motion, useReducedMotion } from "motion/react"
import { FounderCard } from "./founder-card"
import { TeamMemberCard } from "./team-member-card"
import { founders, members } from "./team-data"

export function AboutTeamSection() {
  const prefersReducedMotion = useReducedMotion()

  const fadeIn = prefersReducedMotion
    ? {}
    : {
        initial: { opacity: 0, y: 16 },
        whileInView: { opacity: 1, y: 0 },
        viewport: { once: true, margin: "-10% 0px" },
        transition: { duration: 0.6, ease: [0.16, 1, 0.3, 1] as const },
      }

  return (
    <section
      id="team"
      aria-labelledby="about-team-heading"
      data-section="about-team"
      className="relative bg-bg py-24 sm:py-32 border-b border-border"
    >
      <div className="mx-auto max-w-5xl px-6">
        {/* Eyebrow */}
        <motion.div
          {...fadeIn}
          className="mx-auto mb-5 flex items-center justify-center gap-2 font-mono text-[11px] font-bold uppercase tracking-[0.2em] text-text-muted"
        >
          <span
            aria-hidden="true"
            className="h-1.5 w-1.5 rounded-full"
            style={{
              background: "var(--accent)",
              boxShadow: "0 0 8px var(--accent-glow)",
            }}
          />
          {"// wer dahintersteckt"}
        </motion.div>

        {/* H2 — Wir sind Generation AI. */}
        <motion.h2
          {...fadeIn}
          id="about-team-heading"
          className="text-center font-sans font-bold text-text text-balance"
          style={{
            fontSize: "var(--fs-h3)",
            lineHeight: "var(--lh-sub)",
          }}
        >
          Wir sind Generation AI.
        </motion.h2>

        {/* Sub-Zeile */}
        <motion.div
          {...fadeIn}
          className="mt-4 text-center font-mono text-[11px] font-bold uppercase tracking-[0.2em] text-text-muted tabular-nums"
        >
          Stand: April 2026 · Wir wachsen.
        </motion.div>

        {/* Gründer-Grid: 1 col Mobile, 2 col Desktop */}
        <motion.div
          {...fadeIn}
          className="mt-14 grid grid-cols-1 sm:grid-cols-2 gap-6 max-w-2xl mx-auto"
        >
          {founders.map((founder) => (
            <FounderCard
              key={founder.name}
              name={founder.name}
              role={founder.role}
              bio={founder.bio}
              linkedinUrl={founder.linkedinUrl}
            />
          ))}
        </motion.div>

        {/* Mitglieder-Grid: 1 col Mobile, 2 col Tablet, 4 col Desktop */}
        <motion.div
          {...fadeIn}
          className="mt-10 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4"
        >
          {members.map((member) => (
            <TeamMemberCard key={member.name} name={member.name} />
          ))}
        </motion.div>
      </div>
    </section>
  )
}
```

**Concrete values — verbatim from UI-SPEC:**
- Section-Anker: `id="team"`
- Eyebrow: `// wer dahintersteckt`
- H2 (semantisch, `--fs-h3` visuell): `Wir sind Generation AI.`
- Sub-Zeile: `Stand: April 2026 · Wir wachsen.` (mit echtem · Unicode)
- Container: `max-w-5xl px-6 mx-auto` (Team-Section bekommt mehr Breite laut UI-SPEC Zeile 58-59)
- Founders-Grid: `grid-cols-1 sm:grid-cols-2 gap-6 max-w-2xl mx-auto` (2 Karten auf Desktop zentriert)
- Members-Grid: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4` — 4 Spalten Desktop (10 Items → 4+4+2 = 3 Zeilen, letzte Reihe mit 2 Items linksbündig, UI-SPEC Open Issue 1 Default)
- Section-Padding: `py-24 sm:py-32` (Standard-Padding, NICHT Hero-Padding `py-28 sm:py-36`)
- Heading: semantisch `<motion.h2>`, visuell `--fs-h3`-Size (siehe Plan 21-02 Heading-Hierarchie-Decision)

**Acceptance criteria:**
- [ ] Datei `apps/website/components/about/about-team-section.tsx` existiert
- [ ] Imports aus `./founder-card`, `./team-member-card`, `./team-data`: `grep -cE 'from "./(founder-card|team-member-card|team-data)"' apps/website/components/about/about-team-section.tsx` liefert 3
- [ ] Anker `id="team"`: `grep -n 'id="team"' apps/website/components/about/about-team-section.tsx` matched
- [ ] Eyebrow exakt: `grep -F '// wer dahintersteckt' apps/website/components/about/about-team-section.tsx` matched
- [ ] H2-Copy exakt: `grep -F 'Wir sind Generation AI.' apps/website/components/about/about-team-section.tsx` matched
- [ ] Sub-Zeile exakt: `grep -F 'Stand: April 2026 · Wir wachsen.' apps/website/components/about/about-team-section.tsx` matched
- [ ] Founders-Grid mit 2-col: `grep -n 'sm:grid-cols-2' apps/website/components/about/about-team-section.tsx` matched
- [ ] Members-Grid mit 4-col: `grep -n 'lg:grid-cols-4' apps/website/components/about/about-team-section.tsx` matched
- [ ] `founders.map` + `members.map` beide vorhanden: `grep -cE '(founders|members)\.map' apps/website/components/about/about-team-section.tsx` liefert 2
- [ ] Container `max-w-5xl`: `grep -n 'max-w-5xl' apps/website/components/about/about-team-section.tsx` matched
- [ ] Heading ist `<motion.h2>`, NICHT `<motion.h3>`: `grep -cE 'motion\.h2' apps/website/components/about/about-team-section.tsx` liefert ≥1, `grep -cE 'motion\.h3' apps/website/components/about/about-team-section.tsx` liefert 0

---

### Task 3 — Verify Build

**Read first:**
- `apps/website/package.json`

**Action:**
```bash
cd apps/website
pnpm tsc --noEmit 2>&1 | head -20
```

**Acceptance criteria:**
- [ ] `pnpm --filter @genai/website tsc --noEmit` exit code 0
- [ ] Dateien exist: `ls apps/website/components/about/about-team-section.tsx apps/website/components/about/team-data.ts`

</tasks>

<threat_model>

| Threat | Surface | Mitigation |
|--------|---------|-----------|
| XSS via Team-Name (wenn später aus CMS) | Member/Founder names | Aktuell hardcoded, React-escape. Wenn Phase 27 CMS-Integration → Sanitization-Check zu Plan-27-Time |
| Layout-Shift bei Foto-Swap Phase 27 | Grid-Items | `min-h-[320px]` auf FounderCard (Plan 21-01), `aspect-square` auf PlaceholderAvatar, `next/image` mit explicit w/h Props in Phase 27 |
| A11y-Regression bei leeren Grid-Slots (10 Items in 4-col = 2 leere Slots letzte Reihe) | Members-Grid | Natural grid-fill: Items fallen linksbündig, Screenreader liest Items sequenziell, keine ghost-slots |
| Dead LinkedIn-Link wenn URL undefined | FounderCard | Bereits in Plan 21-01 mitigated: conditional render, kein `#`-Placeholder |

**Severity: LOW** — Hardcoded Präsentations-Komponente, keine dynamischen Daten.

</threat_model>

<verification_commands>

```bash
# Files exist
ls apps/website/components/about/about-team-section.tsx
ls apps/website/components/about/team-data.ts

# Type-check
pnpm --filter @genai/website tsc --noEmit

# Lint
pnpm --filter @genai/website lint -- components/about/

# Anker present
grep -q 'id="team"' apps/website/components/about/about-team-section.tsx

# Heading hierarchy (h2, not h3)
! grep -En 'motion\.h3' apps/website/components/about/about-team-section.tsx
grep -q 'motion\.h2' apps/website/components/about/about-team-section.tsx

# Data structure
grep -c 'name: "Mitglied' apps/website/components/about/team-data.ts  # expect 10
grep -c 'role: "Co-Founder"' apps/website/components/about/team-data.ts  # expect 2
```

</verification_commands>

<must_haves>

- [ ] `about-team-section.tsx` exists, mountet FounderCard + TeamMemberCard + team-data
- [ ] `team-data.ts` exists, 2 Founders + 10 Members, Founders haben `linkedinUrl: undefined`
- [ ] Anker `id="team"` für Deep-Link `/about#team`
- [ ] Responsive Grids: Gründer 1→2 cols, Members 1→2→4 cols
- [ ] Copy 1:1 aus UI-SPEC (Eyebrow, H2, Sub-Zeile)
- [ ] Heading semantisch `<h2>`, visuell `--fs-h3`-Size
- [ ] Container `max-w-5xl` (Team-Section-Exception zur Standard-3xl)
- [ ] TypeScript + ESLint grün

</must_haves>

<notes>

- Die Placeholder-Mitgliedernamen "Mitglied 1–10" sind dokumentiert als Phase-27-Replace-Target. Luca liefert finale Liste.
- Gründer-Nachnamen "Meister" und "Becker" sind Platzhalter — wenn echte Nachnamen gewünscht sind, muss Luca in der UAT-Phase oder Phase 27 korrigieren.
- Micro-Trenner zwischen Gründer- und Member-Grid: UI-SPEC-Default ist **nahtlos** (Open Issue 6) → kein Extra-Header, nur `mt-10` Abstand. Wenn visueller Bruch später stört, einfach einen `<div>` mit `// aktive mitglieder` einschieben.
- Leere Grid-Slots bei 10 Items in 4-col Grid: Natural fill (4+4+2). UI-SPEC Open Issue 1 — kein explicit Placeholder-Fill nötig.

</notes>
